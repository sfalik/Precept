# Fixture: obligations whose WP folds to false — the default violates the
# rule and a write always violates it; no premise respelling applies.
field Total as decimal default 5000.0

rule Total <= 1000 because "cap"

event Reset
on Reset -> set Total = 2000
