Frank and Elaine both traced the priority-queue action-site problem to structure rather than simple wording, but they diverged on the fix: Frank argues enqueue should drop connectors entirely and let `dequeue ... into X Y` capture both values positionally, while Elaine argues Precept still needs an explicit structural cue and recommends keeping the connector shape with `priority` as the clearest action-site word. They agree declaration-site `by` should not be forced onto actions, `=` is misleading, and the three-line peek/priority/dequeue sequence is only a teaching aid. Shane now needs to choose between the connector-free structural model and the explicit connector model.
