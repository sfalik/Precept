# Frank — Precept.Next Pre-TypeChecker Contract Review

**Timestamp:** 2026-04-24T00:00:00Z
**Agent:** Frank (Lead/Architect)
**Task:** Pre-implementation audit of docs.next/ compiler documentation vs src/Precept.Next/Pipeline/ implementation artifacts, scoped to TypeChecker stage readiness
**Mode:** Background

---

## Outcome

**BLOCKED.** TypeChecker implementation is not safe to start. Four contract scaffolding blockers must be resolved first.

---

## Findings Summary

| ID | Severity | Description |
|----|----------|-------------|
| F1 | **BLOCKER** | TypedModel is a one-field stub; all 27 resolved symbol/declaration/type types absent from codebase |
| F2 | **BLOCKER** | `SyntaxTree.Root` typed `PreceptNode?` in code; type-checker doc guarantees always non-null |
| F3 | **BLOCKER** | `DiagnosticCode.cs` missing ~13+ type-stage codes (DuplicateFieldName, DuplicateStateName, DuplicateEventName, DuplicateArgName, UndeclaredState, UndeclaredEvent, MultipleInitialStates, NoInitialState, CaseInsensitiveStringOnNonCollection, InvalidModifierBounds, UnguardedCollectionAccess, UnguardedCollectionMutation, NonOrderableCollectionExtreme, and more) |
| F4 | **BLOCKER** | No SourceSpan→SourceRange bridge defined; TypeChecker receives SyntaxTree with SourceSpan nodes but Diagnostics.Create requires SourceRange; two resolution options documented, one must be chosen |
| (3 GAPs) | GAP | See inbox file for full gap list |
| (2 NOTEs) | NOTE | See inbox file for full notes |

---

## Resolutions Required Before TypeChecker Work Begins

1. **F1:** Define full TypedModel shape and all supporting types in `src/Precept.Next/Pipeline/TypedModel.cs`. Doc is authoritative.
2. **F2:** Change `SyntaxTree.Root` to `PreceptNode` (non-nullable). Parser must synthesize IsMissing PreceptNode on catastrophic failure.
3. **F3:** Audit full type-checker doc, add all missing `DiagnosticCode` values, add `GetMeta()` entries in `Diagnostics.cs`.
4. **F4:** Choose one bridge option: (a) pass source text to `TypeChecker.Check(SyntaxTree, string)` and build line map internally, or (b) add `SourceRange` to `SyntaxNode` alongside `SourceSpan` at parse time.

---

## Inbox File

`.squad/decisions/inbox/frank-precept-next-contract-review.md` — merged into decisions.md this batch
