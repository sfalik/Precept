# Soup Nazi — Precept.Next Stub vs Test Coverage Gap Audit

**Timestamp:** 2026-04-24T00:00:00Z
**Agent:** Soup Nazi (Tester)
**Task:** Stub vs test coverage gap audit for Precept.Next — identifying what cannot be tested until scaffolding is resolved
**Mode:** Background

---

## Outcome

**BLOCKED.** Two compile-time blockers prevent TypeChecker test files from being written. No test files exist for TypeChecker, GraphAnalyzer, ProofEngine, Compiler, or Runtime.

---

## Compile-Time Blockers (Must Resolve Before Tests)

| ID | Severity | Description |
|----|----------|-------------|
| SN-B1 | **BLOCKER** | TypedModel is a stub — `TypeCheckerTests.cs` cannot compile assertions against the current one-field record |
| SN-B2 | **BLOCKER** | DiagnosticCode missing ~20 type-checker-specific codes — test assertions for `DuplicateFieldName`, `DuplicateStateName`, etc. fail at C# build level |

---

## Non-Blocking Coverage Gaps

| ID | Severity | Description |
|----|----------|-------------|
| SN-G1 | GAP | `FaultsTests.cs` absent — `Faults.GetMeta` and `Faults.Create` are real, testable today (~10 tests warranted) |
| SN-G2 | GAP | `CompilerTests.cs` absent — `Compiler.Compile` is real but throws; smoke-test placeholder appropriate |
| SN-G3 | GAP | `~string` type-checker tests — `CaseInsensitiveStringOnNonCollection` and `~string` in contains/guards — belong in TypeCheckerTests.cs, not a separate file |
| SN-G4 | GAP | `GraphAnalyzerTests.cs` and `ProofEngineTests.cs` — not needed until those stubs are implemented |

---

## Recommended TypeChecker Implementation Slice Order

1. Expand `DiagnosticCode.cs` with all type-checker codes from the doc
2. Define TypedModel full record shape (can be data-only initially)
3. Add `Diagnostics.GetMeta` entries for each new code
4. Create `TypeCheckerTests.cs` with stubs covering documented behavioral categories
5. Implement `TypeChecker.Check` to make those tests pass

---

## TypeChecker Test Behavioral Categories Identified

Name resolution, type compatibility, modifier validation, scope rules, collection typing, numeric literal resolution, typed constant resolution, `~string` semantics. Full category breakdown in inbox file.

---

## Inbox File

`.squad/decisions/inbox/soup-nazi-precept-next-coverage-audit.md` — merged into decisions.md this batch
