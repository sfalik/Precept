# George — Precept.Next Source/Docs Consistency Review

**Timestamp:** 2026-04-24T00:00:00Z
**Agent:** George (Runtime Dev)
**Task:** Source code vs docs consistency review for Precept.Next Pipeline and Runtime layers
**Mode:** Background

---

## Outcome

**BLOCKED.** Pipeline has two real implementations and three pure stubs. The semantic model types the TypeChecker must produce don't exist in code.

---

## Pipeline Stage Status

| Stage | Status |
|-------|--------|
| Lexer | Real implementation (~500+ lines) |
| Parser | Real implementation (~600+ lines) |
| TypeChecker | Pure stub — `NotImplementedException` |
| GraphAnalyzer | Pure stub — `NotImplementedException` |
| ProofEngine | Pure stub — `NotImplementedException` |

---

## Findings Summary

| ID | Severity | Description |
|----|----------|-------------|
| G1 | **BLOCKER** | TypedModel is a hollow skeleton — only `Diagnostics`; all symbol tables and resolved declaration types absent |
| G2 | **BLOCKER** | GraphResult and ProofModel are hollow skeletons — only `Diagnostics`; graph structure and proof attribution absent |
| G3 | **BLOCKER** (docs) | `pipeline-artifacts-and-consumer-contracts.md` has stale Version API (`Edit()` / `Version` return type); newer docs (`runtime-api.md`) are correct |
| G4 | NOTE | Runtime layer fully stubbed — expected state, blocked on G1/G2 |
| G5 | GAP | `~string` CaseInsensitive: syntax captured (ScalarTypeRef.CaseInsensitive, TokenKind.Tilde, BinaryOp.CaseInsensitiveEqual); type system behavior unspecified (3 open questions) |
| G6 | NOTE | `Compiler.Compile()` structurally complete but throws immediately — expected, confirms Lexer+Parser tests must call stages individually |

---

## Resolutions Required

1. **G1:** Define TypedModel full shape before TypeChecker implementation. Doc is complete — transcription task.
2. **G2:** Design GraphResult and ProofModel property shapes before implementing those stages.
3. **G3:** Update `pipeline-artifacts-and-consumer-contracts.md` Version section to match `runtime-api.md`.
4. **G5:** Lock 3 `~string` behavior questions in type-checker doc before TypeChecker implements it: (a) Is `~=` valid on plain `string`? (b) Does `set of ~string` make `contains` case-insensitive? (c) Is `~string` assignable from `string`?

---

## Inbox File

`.squad/decisions/inbox/george-precept-next-impl-review.md` — merged into decisions.md this batch
