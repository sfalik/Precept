# Provenance of `docs/Working/what-i-want-2026-07-16.md`

**Status**: Reference — 2026-07-26
**Subject**: `docs/Working/what-i-want-2026-07-16.md` at HEAD (221 lines)

## Why this exists

Phase 1 of `docs/working-reset/compiler-readiness-plan.md` decides which claimed decisions are actually Shane's. Its rule is that only a document Shane wrote himself can produce a **traced** verdict, and the want document is the only such document in the repository.

The rule was applied to the document as it stands at HEAD. But the document at HEAD is not entirely his. `git blame` reports author "shane" on all 221 lines, because every commit in this repository is under his identity — which is the condition phase 1 exists to work around, so blame cannot settle it. Three commits touched the file, and they are not the same kind of thing.

| Commit | Date | Message | What it is | Lines at HEAD |
|---|---|---|---|---|
| `72c2e183` | 2026-07-18 23:31 | "owner want statement — compile-time proof vs runtime governance" | The original, described in its own message as "drafted bit-by-bit in conversation" | **182** |
| `ed57d7fd` | 2026-07-18 23:44 | "edit pass on want statement — flow, precision, owner rulings" | A Fable edit pass, thirteen minutes later, claiming "no substance changes" | **23** |
| `73f4db7d` | 2026-07-19 12:03 | "deep-review + non-linear RepaymentCapPercent amendment" | A Copilot-co-authored amendment, the next day | **16** |

**182 of 221 lines — 82% — are Shane's as first committed.**

## How the line numbers were derived

`git diff -U0` between each adjacent pair, then the v2→v3 line offsets applied to carry the `ed57d7fd` line numbers forward to HEAD. Verified two ways: the three counts sum to 221, and every line number in the tables below was read back out of the file at HEAD and matched against the corresponding commit's diff hunk. The one line touched by both later commits (154) is attributed to the later one.

## The 182 lines that are Shane's

```
1-7, 9-25, 27-35, 37-39, 41-67, 69-70, 73-74, 76-77, 79, 82-103,
107-120, 124-128, 130-134, 137-138, 140, 142, 144, 146-153, 156-161,
163-164, 166-170, 172, 174-176, 178, 180-182, 184-198, 200-216
```

Everything the register's traced verdicts lean on most heavily is in this set: the induction model (:14-:24), symmetric obligations (:20), modifiers-as-sugar (:22), the eleven original mutation-table rows (:110, :114-:120, :124-:128), the certificates section (:200-:216), the structural-integrity bullets, and the philosophy flag (:215).

## Table A — the 16 lines introduced or changed by `73f4db7d`

`73f4db7d` (2026-07-19 12:03, three days after the document's date, `Co-authored-by: Copilot`) is the commit whose own message calls what it did an "amendment". Twelve of its lines are content with no counterpart in the original. Three graft a new clause onto a clause Shane wrote. One is a reword that the same commit made necessary.

### New content — no counterpart in `72c2e183`

| HEAD line | Text at HEAD | Note |
|---|---|---|
| 36 | field RepaymentCapPercent as decimal default 0.25 positive max 1.0 editable | A second field. The original example declares five fields; this is a sixth. |
| 40 | rule MonthlyRepayment <= OverdraftLimit * RepaymentCapPercent when MonthlyRepayment is set because "A planned repayment may not exceed the capped fraction of the overdraft facility" | A second rule. The original example declares exactly one rule, and it is linear. |
| 71 | from Active on PlanRepayment when Balance < 0.0 | A new second `PlanRepayment` row. The row header text is identical to the original's only `PlanRepayment` row (`72c2e183:66`), but it heads a row the original does not have. |
| 72 |   -> reject "A monthly repayment of {-Balance / PlanRepayment.Months} would exceed the capped fraction ({RepaymentCapPercent}) of the overdraft facility (limit {OverdraftLimit})" | A reject message containing a division. **No message in `72c2e183` contains any arithmetic.** |
| 80 | from Active on ReduceLimit | A new third `ReduceLimit` row. The header text matches the original's fallback row (`72c2e183:74`), but that row is now line 78 with a guard on it; this is an additional row below it. |
| 81 |   -> reject "Cannot reduce the overdraft limit to {ReduceLimit.NewLimit}: a planned repayment of {MonthlyRepayment} would exceed the capped fraction ({RepaymentCapPercent}) of the reduced facility" | A reject message reading `{MonthlyRepayment}`, a field declared `optional` at :35, on a row with no guard. **This is the only optional read inside any message anywhere in the document, and it is not Shane's.** |
| 105 | - **`RepaymentCapPercent`** — the non-linear obligation. The added rule `MonthlyRepayment <= OverdraftLimit * RepaymentCapPercent` multiplies two fields, so no single write closes it by substitution the way `Withdraw`'s guard closes the linear rule. `PlanRepayment` sets `MonthlyRepayment` to a *quotient* and the obligation compares it against a *product* — four free variables and nothing to cancel — so premise class (c) has to carry it: the guard restates the post-state condition verbatim (`-Balance / PlanRepayment.Months <= OverdraftLimit * RepaymentCapPercent`) and the proof closes against what the author wrote, not against algebra the compiler won't attempt. And because the rule mentions `OverdraftLimit`, `ReduceLimit` inherits the same obligation from the other side — lowering the limit shrinks the product even though `MonthlyRepayment` is untouched, so it too must restate the condition (`MonthlyRepayment <= ReduceLimit.NewLimit * RepaymentCapPercent`) or be rejected. This is the symmetric write site again, but non-linear: substitution alone never closes it, and the ideal compiler rejects any unguarded non-linear rule rather than reach for a solver it does not carry. | A whole bullet on non-linear obligations. The original's bullet list under the worked example has five entries; this is a sixth. |
| 121 | \| Remove `PlanRepayment`'s cap conjunct (`-Balance / Months <= OverdraftLimit * RepaymentCapPercent`) \| Cannot prove `MonthlyRepayment <= OverdraftLimit * RepaymentCapPercent` after `set MonthlyRepayment = -Balance / Months` — the quotient is unbounded above; restate the post-state condition in the guard \| | A twelfth mutation-table row. The original table has eleven. |
| 122 | \| Remove `ReduceLimit`'s cap conjunct (`MonthlyRepayment <= NewLimit * RepaymentCapPercent`) \| Cannot prove `MonthlyRepayment <= OverdraftLimit * RepaymentCapPercent` — the rule mentions `OverdraftLimit`, so lowering it carries the obligation even though `MonthlyRepayment` is untouched; restate it in the guard \| | A thirteenth mutation-table row. |
| 123 | \| Guard `PlanRepayment` on `Balance < 0.0` alone, dropping the restating conjunct \| Cannot prove the cap rule: the product is non-linear, so substitution can't close it and no post-mutation sweep or solver stands behind it — the guard must state the post-state condition directly, or the rule must be linearized \| | A fourteenth mutation-table row. |
| 135 | On non-linear rules I want the line drawn in the open. A relational rule whose post-state condition is non-linear — a product or quotient of fields, like the repayment cap above — is proven only when a guard or arg constraint makes that condition directly checkable, so the proof closes against what the author wrote. Where no such premise exists, the compiler rejects the rule with a teachable message and names the guard that would close it. It is never governed by a post-mutation sweep, and never deferred to a solver the compiler doesn't carry. This is the same discipline as everywhere else in this document — prove from a declared premise or reject — I'm only saying it out loud for the non-linear case, because substitution closes the linear rules so quietly that it could leave the impression the harder ones come along for free. They do not: the author guards them, linearizes them, or the compiler refuses them. | A first-person paragraph ("On non-linear rules I want the line drawn in the open…"). The original document says nothing about non-linearity anywhere. |
| 136 | *(blank line)* | Blank line following the paragraph above. |

### Modified in place — a new clause grafted onto a clause Shane wrote

| HEAD line | Text at HEAD | `72c2e183` line and text | What changed |
|---|---|---|---|
| 68 | from Active on PlanRepayment when Balance < 0.0 and -Balance / PlanRepayment.Months <= OverdraftLimit * RepaymentCapPercent | 66: from Active on PlanRepayment when Balance < 0.0 | The conjunct `and -Balance / PlanRepayment.Months <= OverdraftLimit * RepaymentCapPercent` is added. `when Balance < 0.0` is Shane's. |
| 75 | from Active on ReduceLimit when Balance >= -ReduceLimit.NewLimit and MonthlyRepayment <= ReduceLimit.NewLimit * RepaymentCapPercent | 71: from Active on ReduceLimit when Balance >= -ReduceLimit.NewLimit | The conjunct `and MonthlyRepayment <= ReduceLimit.NewLimit * RepaymentCapPercent` is added. `when Balance >= -ReduceLimit.NewLimit` is Shane's. |
| 78 | from Active on ReduceLimit when Balance < -ReduceLimit.NewLimit | 74: from Active on ReduceLimit | A guard is added to what was Shane's unguarded fallback row, which is what made room for the new unguarded row at :80. |

### Reworded

| HEAD line | Text at HEAD | `72c2e183` line and text | Assessment |
|---|---|---|---|
| 154 | - **Inapplicability vs. refusal.** The same principle applies at guard level: `PlanRepayment` offers no row on a healthy balance, so there the event is simply inapplicable (Unmatched — the operation is not offered), while `Withdraw` over the limit gets an explained `reject`. Absence expresses *inapplicability*; `reject` is reserved for *resolvable refusals*. Explanations are for things the user can act on, not for things that simply don't apply. | 142: - **Two different silences.** The same principle applies at guard level: `PlanRepayment` has no fallback row, so on a healthy balance the event is simply inapplicable (Unmatched — the affordance doesn't exist), while `Withdraw` over the limit gets an explained `reject`. Absence expresses *inapplicability*; `reject` is reserved for *resolvable refusals*. Explanations are for things the user can act on, not for things that simply don't apply. | Reworded twice — first by `ed57d7fd` (the heading, and "affordance doesn't exist" → "operation is not offered"), then here. The second reword was forced by this same commit: the original claim "`PlanRepayment` has no fallback row" became false once :71-72 added one, so it was restated as "offers no row on a healthy balance". The claim that survives — absence expresses inapplicability, `reject` is reserved for resolvable refusals — is Shane's and is untouched. |

## Table B — the 23 lines whose HEAD wording comes from `ed57d7fd`

`ed57d7fd` (2026-07-18 23:44, thirteen minutes after the original commit, Fable co-authored) describes itself as an edit pass with "no substance changes". On this evidence that holds. Every one of these 23 lines carries a claim that is present in `72c2e183`; none introduces a claim the original does not make. The bulk of the change is one vocabulary sweep — the document's own "door" metaphor replaced by "ingress". Two lines deserve a second look and are marked.

| HEAD line | `72c2e183` line | Assessment | Original wording |
|---|---|---|---|
| 8 | 8 | Reworded — same claim, tightened. | What Precept should *be* is already defined at a high level in `docs/philosophy.md` — this document does not redo that. What it clears up is **how Precept achieves it**: specifically, how far the compiler should go, and where the line sits between **compile-time proof** and **runtime governance**. The proof engine especially, but also the whole compiler. |
| 26 | 26 | Reworded and shortened. The original spelled out *what* the runtime enforces ("it validates args against their declared constraints and evaluates guards against live data"); the reword drops that clause. The claim — premises are what the runtime enforces, that is governance and not deferred proof, the compiler proved those checks sufficient — survives. | The premises are what the runtime enforces: it validates args against their declared constraints and evaluates guards against live data. That is governance, not deferred proof — the compiler has proven that these checks are *sufficient*. |
| 104 | 98 | Reworded — "the arg validation at the door" → "the ingress validation of the arg". Nothing else on the line changed. | - **`PlanRepayment`** — the fault family. Business rules are not the only obligations: faults (division by zero, overflow, out-of-range) get the identical premise-and-certificate treatment. Here the divide-by-zero obligation is closed by the arg constraint (`Months positive` — the divisor is provably non-zero), and the `nonnegative` bound on `MonthlyRepayment` is closed by the guard (`Balance < 0.0` makes `-Balance` positive). At runtime there is no zero-check anywhere — the arg validation at the door is what makes the division safe, and the certificate records that dependency. |
| 106 | 99 | Reworded — "the remaining two doors" → "the remaining premise sources"; "opens the door" → "opens that ingress"; "at the door" → "at the ingress point". | - **`DailyWithdrawalLimit`** — the remaining two doors. Its `default 500.0` is a genuine business value (accounts start with it; no constructor arg sets it), so the base case has a default to evaluate: `500.0` must satisfy `positive` and `max 10000.0`. And it is the sample's editable-field ingress: `in Active modify … editable` opens the door only while `Active` — writes are validated against the field's modifier-rules at the door, and the editing window closes structurally the moment the account freezes or closes. |
| 129 | 119 | Reworded — "the editable door" → "the editable-field ingress"; "that door check" → "that ingress check". | One deliberate omission: there is no deletion row for `DailyWithdrawalLimit`'s modifiers. They *are* proven unviolable — the default is checked statically, the editable door evaluates them on every write (that door check is the declared premise, and the certificate marks it load-bearing), and no other write path exists. But no *other* proof in the file consumes them: `Withdraw`'s guard reads the field's live value, not its bounds. So deleting them breaks no proof — it just weakens the specification to an unconstrained cap, which is the author saying less, not the compiler proving less. The asymmetry in miniature: deleting a consumed premise makes a file unprovable; deleting a rule nobody consumes merely makes it a weaker spec. |
| 139 | 127 | Reworded — "so that the runtime can perform simple validation against those constraints" → "so that ingress validation stays simple". | At ingress, I expect the compiler to **require the author to extend constraints to the entry points**, so that the runtime can perform simple validation against those constraints. I believe this forces authors to be more explicit — and that leads to better precepts. |
| 141 | 129 | **Look twice.** "two doors" → "two points" is vocabulary. But the original parenthesis read "(post-MVP, this expands to include schema evolution)", and *this* was ambiguous — it could mean ingress or restore. The edit pins it to restore ("the restore path itself expands"). The commit message calls that an owner ruling; nothing outside the commit message says so. The edit also drops "Cross-entity data is out of scope for now", a claim that survives independently at HEAD:193. | Ingress is exactly two doors: **event args** (including on initial/construction events) and **editable-field writes**. Restore/rehydration is *not* ingress — data coming back from persistence is trusted and unchecked, because it was governed when it was written (post-MVP, this expands to include schema evolution). Cross-entity data is out of scope for now. |
| 143 | 131 | Reworded — "What validation at the door means" → "What ingress validation means"; "at the door" → "at the ingress point". Drops ", since modifiers are sugar for rules", a claim that survives independently at HEAD:22. | What validation at the door means: the applicable **rules** are evaluated — however they were spelled, as modifiers or as rule statements, since modifiers are sugar for rules. For an arg, that's its modifier-rules; anything relational an event needs lives in its guards. For an editable-field write, it's the field's modifier-rules *and* every rule that mentions the field — an edit is an arbitrary incoming value that no static proof can cover, so evaluating those rules at the door is exactly the premise that closes the editable write sites in the compiler's proof. |
| 145 | 133 | Reworded — same claim. | This largely involves the proof engine, but it is not limited to it. The graph analyzer also plays a key role, and I'm open to additional compiler stages and techniques if needed. |
| 155 | 143 | Reworded — "is unreachable code wearing a business rule's clothes" → "is unreachable". The rider "given the declared constraints" is Shane's and is untouched. | - **Dead rows are structural defects too.** A row whose guard can never be true given the declared constraints is unreachable code wearing a business rule's clothes — and whatever state it leads to may be unreachable in consequence. The ideal compiler rejects it with an explanation (e.g., a guard comparing a 0-to-1 ratio against `75` instead of `0.75` can never fire — and the total-loss path it guards silently never happens). |
| 162 | 150 | Reworded — grammatical. | - respecting the business process defined by the precept |
| 165 | 153 | **Look twice.** Two changes on one line. The lighter-runtime clause is reworded, and the cross-reference "a trust it establishes by verification, once, at load (see Certificates)" is added — a claim Shane already made in the Certificates section he wrote (`72c2e183:197`), pulled forward into this paragraph. The rest of the line, including "fault-prevention checks are omitted at evaluation time. No divide-by-zero tests, no overflow guards, no re-checking of proven bounds on writes — the evaluator just computes", is verbatim Shane. | But the runtime **can be made lighter due to the complete nature of the compiler** — it should trust that the compiler did its job. "Lighter" means concretely: **fault-prevention checks are omitted at evaluation time**. No divide-by-zero tests, no overflow guards, no re-checking of proven bounds on writes — the evaluator just computes. What runs at runtime are the declared premises (ingress validation of args and editable fields, guards) that the compiler's proofs depend on, and nothing beyond them. The division in `PlanRepayment` executes with no zero-check anywhere, because `Months positive` was validated at the door and the certificate records that this is sufficient. |
| 171 | 159 | Reworded — the runtime's duties are named by cross-reference ("the governance duties described above") instead of re-enumerated. The enumeration it points at is Shane's, at HEAD:160-163. The first two sentences are verbatim Shane. | In an ideal world there is **no deferral**. A business rule whose enforcement the compiler cannot prove complete is rejected — never handed to the runtime on the compiler's own initiative. The runtime still has its own governance duties (ingress validation, inspect, process enforcement, strict immutable versioning and write guarantees — not an exhaustive list); what it never does is pick up proof work the compiler couldn't finish. |
| 173 | 161 | Reworded — word order in the first clause only. The rest of the line is verbatim Shane. | I want Precept's guarantees to be **easy to understand and trust for authors** — this is a big part of Precept's value proposition. Authors should not have to understand the difference between things checked at compile time vs things governed at runtime. To be precise: the **guarantee is uniform** — a rule holds, period; there is no author-facing distinction between "compile-time-checked rule" and "runtime-governed rule," no tiers of trustworthiness to reason about. The mechanisms differ and are visible (diagnostics teach them, refusals reveal them) — what the author is spared is not seeing the machinery, but ever having to ask "how strongly is this rule held?" |
| 177 | 165 | Reworded — "and the same applies at runtime" folded into "critical **to it**". | We have always strived for a strong developer experience, and the same applies at runtime. **Clear explanations are critical** — a rejection is not just a verdict. This holds on both sides of the boundary: compile-time rejections and runtime refusals alike must explain themselves clearly. |
| 179 | 167 | Reworded — "deferral wearing an authored costume" → "deferral relabeled as authorship". The rest of the line is verbatim Shane. | When a proof cannot close, the compiler **suggests the missing premise with a teachable message**: it computes the guard or arg constraint that would close the proof and shows it — "cannot prove `Balance >= -NewLimit` after this write; adding `when Balance >= -ReduceLimit.NewLimit` would make it provable" — and explains *why* the premise is needed, so the author learns the model rather than just obeying the tool. It never inserts the premise itself. The author writes it, deliberately: a premise the author never wrote is deferral wearing an authored costume. |
| 183 | 171 | Reworded — grammatical. | We have already shrunk the language once. Whether prove-or-reject is actually feasible is exactly what this document sets up exploring — but for now we are thinking ideal world. |
| 199 | 192 | Reworded — word order. "re-evaluate rules against the post-mutation configuration" → "re-evaluate them against…", and the certificate clause moved to the active voice. Same claim. | Once the compiler has proven every rule, the runtime does **not** re-evaluate rules against the post-mutation configuration. But it does not blindly trust the compiler either. The compiler emits, alongside the compiled definition, a **certificate**: a checkable record of the proof — per obligation, the theorem, the premises used (which guard, which arg constraint, which pre-state rule), and the derivation. |
| 217 | 185 | Moved, text unchanged. The whole Open-questions section was relocated from before the Certificates section to after it. | ## Open questions |
| 218 | 186 | Moved, unchanged (blank). | *(blank line)* |
| 219 | 187 | Moved, and one word changed — "there is no ingress door for it" → "no ingress point for it". | - **Time-referencing rules.** A rule like `ExpiryDate > today` can become false with no event in flight — nothing writes `today`, there is no ingress door for it, and the inductive model has no step where pure passage of time fits. Do rules that reference the current moment exist at all, or does time belong only in guards (evaluated at event time, like any other premise)? Deliberately left open. |
| 220 | 188 | Moved, text unchanged. | - **The promise across precepts.** Whether and how prove-or-reject and no-deferral extend across precepts (message passing, saga precepts) is undefined; the MVP proof boundary is the single precept. Deliberately left open until that capability is designed. |
| 221 | 189 | Moved, unchanged (blank). | *(blank line)* |

## What this changes, in one line each

**`ed57d7fd` is safe to quote through.** It reworded 23 lines and moved one section. No claim in it is absent from `72c2e183`. Where a verdict quotes one of these lines, the right correction is to swap in the original wording, not to withdraw the verdict. Two lines want a second look — :141 (the schema-evolution referent was ambiguous and the edit pinned it one way) and :165 (a cross-reference was added, drawn from a section Shane did write).

**`73f4db7d` is not safe to quote.** It added a second field, a second rule, three transition rows, a bullet, three mutation-table rows and a first-person paragraph, and it put guards on two rows Shane wrote unguarded. A verdict resting on any of :36, :40, :71, :72, :80, :81, :105, :121, :122, :123 or :135 is resting on text an agent wrote inside the document that everything else in this phase measures against.

## The specific check that started this

**Does any message in `72c2e183` contain a read of an `optional` field?** No.

`MonthlyRepayment` is the only field declared `optional` in the original (`72c2e183:35`). In the original it appears exactly four times: the declaration at :35, a `set` at :67, the prose bullet at :98, and a mutation-table row at :113. **It is never read** — not in a message, not in a guard, not anywhere. Nor does any other message in the original read anything optional; the original's four reject messages interpolate `Withdraw.Amount`, `DailyWithdrawalLimit`, `Balance`, `OverdraftLimit` and `ReduceLimit.NewLimit`, all of which are required.

The only optional read inside a message anywhere in this document is `{MonthlyRepayment}` at HEAD:81, on the unguarded row at HEAD:80. Both lines were written by `73f4db7d`.

**Does any message in `72c2e183` contain a fault-prone expression?** No. The original's four messages interpolate bare field and argument reads. The division inside a message at HEAD:72 was written by `73f4db7d`.

Both of those absences are load-bearing for the register, and both are corrected there. See `docs/working-reset/phase-1/decision-register-FINDINGS.md`.
