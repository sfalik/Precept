---
status: Appendix to compiler-readiness-plan-2026-06-11.md — Working-docs triage + obligation register
authored: 2026-06-11
---

# Working-Docs Triage & Obligation Register

> Every pre-existing `docs/Working/` doc dispositioned (verified against code/git, not status fields), and every still-valid unfinished obligation mined out of them so the new readiness plan loses nothing. 4 archive / 28 supersede. Keep set: `bugs.md` + the three `compile-time-niche-*` docs + the new plan.

## Dispositions
- **[SUPERSEDE]** `docs/Working/collection-inner-type-value-modifiers-2026-06-02.md` — Compile-time half fully shipped+promoted, but the design's runtime element-governance semantic (Rule 2/Acceptance 7) is unbuildable today (Evaluator.cs Fire/Update = NotImplementedException) plus three deferred follow-ons ride in the doc.
- **[SUPERSEDE]** `docs/Working/collection-inner-type-value-modifiers-plan-2026-06-02.md` — Both phases landed (4a003b64/fcbaaf71) but carries live forward obligations (sign-set element-band gap, element-entry-path coverage check) and a runtime-gated acceptance criterion.
- **[SUPERSEDE]** `docs/Working/compiler-readiness-plan-2026-05-24.md` — Direct predecessor of compiler-readiness-plan-2026-06-11.md: Phases 1-6 done, Phase 7 (5 open slices), Phase 8 (3 open slices), Phases 9-12 stubs all transfer to the new plan.
- **[SUPERSEDE]** `docs/Working/compiler-readiness-review-2026-05-24.md` — Audit register, not implemented-and-promoted content; closed rows are history, open finding families (Phases 9-11 residue + F-LANG-BIZ-11) transfer; the Stage-2 audit is its replacement.
- **[SUPERSEDE]** `docs/Working/compiler-readiness-review-2026-05-24-appendices/` — Evidence folder traveling with its parent review so cross-links stay resolvable; the 16 Archive promotions it drove were executed but the folder is audit evidence, not promoted content.
- **[SUPERSEDE]** `docs/Working/computed-field-default-folding-2026-06-05.md` — Locked-but-unbuilt design: ProofEngine.Analysis.cs:138 still carries the unconditional computed→unfoldable arm the design replaces (re-verified at HEAD); BUG-028 un-re-scoped.
- **[SUPERSEDE]** `docs/Working/conditional-totality-short-circuit-narrowing-2026-06-07.md` — Approved-in-direction but nothing built, and the witness half is structurally entangled with the deleted verify-don't-trust soundness architecture; must re-ground under the fault-floor framing.
- **[SUPERSEDE]** `docs/Working/conformance-suite-design-2026-06-01.md` — Locked design with ZERO execution (no Precept.Conformance.Tests project — re-verified, no fence annotations, no clock-seam doc note); new plan must re-home it and reconcile against the Stage-2 audit.
- **[SUPERSEDE]** `docs/Working/conformance-suite-plan-2026-06-01.md` — Draft plan, Phase 0 never started; its phasing and five carried open decisions are inputs to the new board, not standing structure.
- **[SUPERSEDE]** `docs/Working/dynamic-modifier-bounds-research-2026-06-01.md` — Conclusions consumed by locked designs (cite-satisfied) but the consuming 2c-ii/2c-iii builds are unbuilt and Q3/Q4 diagnostic-policy residues remain genuinely open.
- **[SUPERSEDE]** `docs/Working/compile-time-guarantee-statement-draft-2026-06-10.md` — Discussion draft never locked; the compile-time-niche decision and the in-flight fault-floor definition run own this territory; its open Points A/B/C transfer.
- **[SUPERSEDE]** `docs/Working/compile-time-prevention-features-proposal-2026-06-06.md` — Unbuilt proposal (no Karr/TVPI code, grep-verified); Part 2's annotation framing is overtaken by the flag-soundness contract; Part 1's solver-free technique ranking transfers as candidate input.
- **[SUPERSEDE]** `docs/Working/compile-time-vs-runtime-contract-clarity-2026-06-01.md` — Point-in-time clarity audit whose central finding (contract scattered across 7+ locations) is exactly what the fault-floor run fixes; several findings shifted under it since 06-01 (Restore partially resolved).
- **[SUPERSEDE]** `docs/Working/field-reference-bound-enforcement-2026-06-04.md` — Locked (re-locked 06-05 with soundness corrections) but NOT built — BUG-020 enforcement half still Active, no band-edge code in src, and its hard prerequisite (BUG-027) has since landed, making it fully unblocked work for the new plan.
- **[SUPERSEDE]** `docs/Working/guarantee-claims-doc-audit-2026-06-02.md` — Claims audit whose recommended corrections were never applied (philosophy.md:49/:53 verified unchanged) and whose subject (the guarantee statement) is being wholesale rewritten by the fault-floor run.
- **[ARCHIVE]** `docs/Working/modifier-name-axis-overlap-plan-2026-06-03.md` — Independently re-verified fully implemented+promoted: Modifiers.cs Notempty StringOnly, Precept0029ModifierAxisDisjoint analyzer + Precept0029Tests, all doc-syncs present in spec/collection-types/type-checker/proof-engine; companion design already archived; flip the stale 'Active' status at archive time.
- **[SUPERSEDE]** `docs/Working/phase7-slice2-design-review-FRANK-2026-05-31.md` — Historical review whose Condition 1 (business-domain-types.md:168 disambiguation) never landed and whose Warning 2 hardened into BUG-030, which now contradicts the locked design — not fully promoted.
- **[SUPERSEDE]** `docs/Working/phase7-slice2-plan-2026-05-31.md` — Built (fe7d1187) but NOT promoted — Stage-5 promotion pending on its own terms (d210b2b6) and the evaluator.md runtime reduction-rule requirement (the slice's 'durable carrier') was never written (grep-verified absent).
- **[SUPERSEDE]** `docs/Working/precept-guarantee-specification-2026-06-05.md` — Guarantee-thesis strawman the strategy moved past twice (06-10 draft, then the niche decision's fault-floor + flag-sound shape); never locked; surviving proof-power items extracted.
- **[SUPERSEDE]** `docs/Working/proven-core-governed-boundary-2026-06-05.md` — Pre-pivot exploration: the niche decision settled the proven/governed split as governance-at-result + flag-sound universal layer, not ingress-refinement; expressiveness candidates extracted as owner-gated stubs.
- **[SUPERSEDE]** `docs/Working/phase8-slice1-emission-inventory-2026-05-31.md` — Investigation whose primary consumer (Slice 3c ownership analyzer) is unbuilt and whose site counts are stale against HEAD (PRE0163/0164/0136 + relational work added sites since).
- **[SUPERSEDE]** `docs/Working/phase8-slice2-dispatch-deepdive-2026-06-01.md` — Half shipped (Slice 2, 59fe2666, archived via 7b8dfa89); the other half (ownership/consistency analyzer family, F-CAT-01) unbuilt — mixed state fails the archive bar.
- **[SUPERSEDE]** `docs/Working/phase8-slice3-emission-ownership-2026-05-31.md` — Locked design whose headline deliverable (D1 single-stage-ownership analyzer, Slice 3c) is not in HEAD (analyzers stop at Precept0029 — re-verified); archive commit 7b8dfa89 itself declined to archive it for this reason.
- **[SUPERSEDE]** `docs/Working/phase8-slices2-3-execution-plan-2026-06-01.md` — Tracking doc whose definition-of-done (ownership analyzer green) is unmet; 3c + the PRE0141 gating decision + re-scoped Slice 4 transfer to the new plan.
- **[SUPERSEDE]** `docs/Working/price-cross-unit-cancellation-2026-05-31.md` — Self-declares superseded by the companion design, but that design cannot archive (promotion pending, factor application unbuilt, BUG-030 contests the policy), so the evidence base travels with it.
- **[SUPERSEDE]** `docs/Working/price-cross-unit-cancellation-design-2026-05-31.md` — Locked and compile-time-shipped (fe7d1187) but fails the archive bar on both axes: runtime factor application unbuilt (BUG-030) and canon never promoted (business-domain-types untouched; proof-engine.md:1790 still says 'undecided' — live drift vs the lock).
- **[ARCHIVE]** `docs/Working/proof-engine-contract-grounding-2026-06-02.md` — Independently re-verified: all three breaches it found are Fixed in bugs.md with commit hashes (BUG-017 9820b8c1, BUG-018 c27a382b, BUG-019 4e4e2961), the contract is single-sourced at spec §0.7, and its one superseded conclusion (Restore re-validates) is corrected in canon (runtime-api.md trusted-hydration note).
- **[ARCHIVE]** `docs/Working/proof-engine-guarantee-contract-plan-2026-06-02.md` — Independently re-verified all 4 phases landed in canon: spec §0.7 exists (line 262 + TOC), philosophy carries-proof insertion live, runtime-doc tightening (9fa6a16c), README §0.7 pointer (README.md:117), completion commit e1c0a959; the one residual (RestoreConstraintsFailed removal) is recorded in canon itself.
- **[SUPERSEDE]** `docs/Working/relational-rules-and-bounds-plan-2026-06-02.md` — Live plan with substantial unfinished owner-prioritized soundness work: 2c-ii (BUG-020 enforcement, now unblocked by BUG-027 fix 210229ff), 2c-iii (BUG-021), promote-stage doc-syncs, and the BUG-025/026 policy bundle.
- **[ARCHIVE]** `docs/Working/review-skill-reshape-2026-06-07.md` — Independently re-verified fully implemented in f2b852ed: .claude/skills/review/SKILL.md (305 lines) carries every designed section (Dispatch, Branches, Result fold, Collision: intentional subsumption, Migration, High/Ultra modes), CONTRIBUTING.md carries the umbrella+subsumption description; dev-tooling design promoted to its canonical homes.
- **[SUPERSEDE]** `docs/Working/sample-corpus-expansion-plan.md` — Corpus goal met by other routes (77 samples) but Phase 5 catalog-showcase samples never authored, and the strategy framing is two pivots old.
- **[SUPERSEDE]** `docs/Working/spec-conformance-audit-2026-05-30.md` — Live finding register with probe-confirmed open rows (exchangerate slash still PRE0076, kg/hour still PRE0075, spec §0.6 items 7-10 still mislabeled) and the only record of the 2026-05-30 slash-canonical owner ruling; the Stage-2 audit replaces it.

## Archive verifications (independently re-verified)

- `docs/Working/modifier-name-axis-overlap-plan-2026-06-03.md` — verified=True: Code: src/Precept/Language/Modifiers.cs:114-117 routes Notempty through the StringOnly TypeTarget array (line 37); src/Precept.Analyzers/Precept0029ModifierAxisDisjoint.cs exists with test/Precept.Analyzers.Tests/Precept0029Tests.cs. Git: c3209ad2 (Slice 1 notempty→string-only + generic element-bound binding), eeb9a9c6 (Slice 2 no-overlap analyzer; commit message says PRECEPT0031 but the shipped/renumbered analyzer is Precept0029 — implementation present either way), cceab68e (design lock). Canon doc-syncs all verified present: precept-language-spec.md:435/:1123/:1150/:1671/:1681/:1683/:1696 (string-only notempty, per-element semantics, mincount-1 cardinality + discharge); collection-types.md:353/:631/:775/:793; type-checker.md:362 (catalog-generic BuildElementValueBounds, satisfaction-DU dispatch, no *Kind switch, notempty string-only routing); proof-engine.md:1257 count-band tracking. Companion design already in Archive/ (modifier-name-axis-overlap-2026-06-03.md). The plan's only forward item (consumer-side *Kind exhaustiveness sweep) is explicitly out-of-scope and independently tracked as BUG-022 in bugs.md. Note: the doc's own Status field still says 'Active' — stale; flip at archive time. ARCHIVE CONFIRMED.
- `docs/Working/proof-engine-contract-grounding-2026-06-02.md` — verified=True: All three breaches it discovered are Fixed with verified commits: BUG-017 status line reads '✅ Fixed 2026-06-02 (9820b8c1, Slice 2a)' (entry physically still under ## Active with an explicit '(Move to § Fixed in a cleanup pass)' note — a filing-position artifact, not an open bug; chased and confirmed); BUG-018 and BUG-019 headings sit inside ## Fixed (bugs.md lines 236/245, section starts 227) with commits c27a382b and 4e4e2961, both verified in git. The contract it grounded is single-sourced at spec §0.7 (precept-language-spec.md:262 + TOC line 33). Its Restore finding was resolved in canon (runtime-api.md:266 trusted hydration, :270 RestoreConstraintsFailed supersession note) — the doc's own contrary recommendation is corrected in canon, which is what reference-only archive is for. The runtime-governance obligation it carries is recorded in spec §0.7 and transfers via the obligation register, not via the doc. ARCHIVE CONFIRMED.
- `docs/Working/proof-engine-guarantee-contract-plan-2026-06-02.md` — verified=True: All cited commits exist and verify: d4367254 (§3.5 clarify), 5af46537 (Phase 1 — spec §0.7 'The Compile-Time and Runtime Guarantee Contract' landed; section verified at precept-language-spec.md:262 with TOC entry), c4a50887 (Phase 2 grounding + bug filings), 9fa6a16c (Phase 3 runtime-doc tightening), 63e08963 (Restore→trusted hydration), e7c1255f (Phase 4 philosophy composition statement + README routing), e1c0a959 (completion mark). Philosophy carries-proof insertion verified live in docs/philosophy.md ('It requires the value to *carry* a constraint sufficient to prove the calculation safe… The compiler proves a structural fact… never the value itself'). README.md:117 carries the §0.7 guarantee-contract pointer. The one residual (remove RestoreConstraintsFailed at Restore-API finalization) is self-recorded in canon (runtime-api.md:270; RestoreOutcome.cs) and transfers via the obligation register. ARCHIVE CONFIRMED.
- `docs/Working/review-skill-reshape-2026-06-07.md` — verified=True: Commit f2b852ed verified ('chore(skills): bare-name lifecycle skills, add high/ultra modes, reshape /review into the umbrella reviewer'). .claude/skills/review/SKILL.md exists, 305 lines, carrying every designed section verified by header grep: ## Dispatch (:28), ## Branches (:61), ## Result fold (:161), ## Read-only contract (:195), ## Collision: intentional subsumption (:201), ## Migration (:209), ## High & Ultra Modes (:220), ## Behavioral guards (:233), ## Anti-patterns to refuse (:257). CONTRIBUTING.md carries the umbrella description + subsumption note ('intentionally subsumes (does not invoke) the built-in', :331). Dev-tooling design fully promoted to its canonical homes (the SKILL.md itself + CONTRIBUTING.md); deferred items are explicitly deferred-by-the-lock dev-tooling backlog, not unfinished promotion. ARCHIVE CONFIRMED.

## Obligation register (feeds the new plan — 43 items)

### canon-drift (4)

- **RECONCILE canon drift: BUG-030 and proof-engine.md:1790 both describe the cancellation policy as 'genuinely undecided' while price-cross-unit-cancellation-design-2026-05-31.md is LOCKED on allow-all-with-surfacing (Decision 2, owner-affirmed 2026-05-31, lock commit 0eb8c4f3). Either promote the lock to canon and narrow BUG-030 to 'factor application unbuilt', or the owner explicitly re-opens the decision under the §0.7 fault-floor lens. Do not let two contradictory records stand. Also restate BUG-030's validating-consumer line — it still references 'the witness-checker' from the deleted architecture.**
  - source: `docs/Working/price-cross-unit-cancellation-design-2026-05-31.md vs bugs.md BUG-030 vs docs/compiler/proof-engine.md:1790` · evidence: Verified contradiction (prior batch grep + this pass: lock commit 0eb8c4f3 exists, BUG-030 Active at bugs.md:37 with the 'undecided' framing). The single biggest drift found in the triage.
- **D-class doc-stale fixes: spec §0.6 lines 241-244 still mark items 7-10 'Specification-only' though PRE0153-0155/PRE0082 are implemented and firing (verified live); D2 collection-types quantifier predicates 'Not yet implemented'; D3 PRE0160-0162 'TBD'; D4 temporal doc phantom code spellings — re-verify each and fix in a doc sweep.**
  - source: `docs/Working/spec-conformance-audit-2026-05-30.md §D` · evidence: precept-language-spec.md:241-244 grep (prior batch, 2026-06-11); bugs.md BUG-026 confirms the codes fire as warnings.
- **Finish the Restore reconciliation: fix spec §3A.2:1946 (still claims 'Restoring an entity in an invalid state is not allowed... constraint failure') to match the locked trusted-hydration decision (spec §0.7:272, runtime-api.md:266); remove RestoreConstraintsFailed from RestoreOutcome at Restore-API finalization (RestoreOutcome may keep parse/state-resolution failures).**
  - source: `docs/Working/compile-time-vs-runtime-contract-clarity-2026-06-01.md + guarantee-claims-doc-audit-2026-06-02.md + proof-engine-guarantee-contract-plan-2026-06-02.md (archived; residual self-recorded in canon)` · evidence: Re-verified this pass: runtime-api.md:266 trusted hydration + :270 'should be removed when the Restore API is finalized'; src/Precept/Runtime/RestoreOutcome.cs still carries it; spec §3A.2:1946 contradiction verified by prior batch.
- **philosophy.md absolute-claims reconciliation (OWNER-GATED — never auto-edit): 'No errors. No bugs.' (:49), 'They are compile-time impossibilities' (:53), and the unqualified 'no unproven evaluation faults' close must be reconciled with the proof-gap/stub/external-bypass reality as part of the fault-floor guarantee-statement promotion; README/evaluator.md headline claims ride the same pass. The 06-02 audit's SPECIFIC wordings are superseded — what survives is the reconciliation obligation itself (= guarantee-spec OD-4).**
  - source: `docs/Working/guarantee-claims-doc-audit-2026-06-02.md + precept-guarantee-specification-2026-06-05.md OD-4` · evidence: Re-verified this pass: philosophy.md still reads 'Prevention, not detection. No errors. No bugs.' and 'They are compile-time impossibilities' verbatim.

### doc-sync-owed (6)

- **2c-ii promote-stage doc-sync package (travels with the build): proof-engine.md §Strategy 8/9/10 + Obligation Generation Contract + default-satisfiability fold; diagnostic-system.md rich-message composition; spec §2.4 no-literal-only-qualifier check; business-domain-types.md:426 field-reference bound-interpretation clause (carried doc-drift from parent D6); spec §3.8 InvalidModifierBounds field-ref-pair semantics; mcp.md + CompileToolDtos.cs projection check; language-server.md hover provenance/go-to-def; flip BUG-020 to Fixed; plus the relational plan's residual R-NARROW-* mechanism lift into proof-engine.md/compiler-and-runtime-design.md beyond what e7faae3a covered.**
  - source: `docs/Working/field-reference-bound-enforcement-2026-06-04.md § Doc-update enumeration + relational-rules-and-bounds-plan-2026-06-02.md § Promote stage` · evidence: business-domain-types.md:426 drift still present; e7faae3a promoted the 2c-i cluster only.
- **Conditional-totality spec + evaluator-contract half (owner-approved Decisions 2/6/7): state short-circuit and/or/not/if reduction semantics in the spec (desugaring anchor 'A and B ≡ if A then B else false', riding locked §0.4.2 'exactly one is evaluated'); lock the strict-boolean BRANCH_FALSE/BRANCH_TRUE + if-lowering opcode contract (resolving evaluator.md:993); fix the &&/||/?: notation drift in evaluator.md AND precept-builder.md (six sites).**
  - source: `docs/Working/conditional-totality-short-circuit-narrowing-2026-06-07.md` · evidence: Owner-approved 2026-06-07; pure doc/spec work independent of the deleted witness architecture; evaluator.md:1006-1008 still carries drifted opcode rows; nothing landed (no spec short-circuit paragraph).
- **D9 clock-seam v1 runtime requirement: now() must resolve through an injectable per-operation clock (sampled once per fire) — record in docs/runtime/evaluator.md (+ runtime-api.md). Independent of whether the conformance suite is built; constrains the runtime build.**
  - source: `docs/Working/conformance-suite-design-2026-06-01.md Decision 9` · evidence: Re-verified: grep IClock/injectable-clock over docs/runtime/evaluator.md returns nothing.
- **Stage-5 promotion of the price cross-unit cancellation design: business-domain-types.md D8 extension + allow-all-with-surfacing decision + absolute-position exclusion + the :168 'explicit' disambiguation ('explicit' = visible-to-inspection/traceable, not manual conversion syntax — FRANK review Condition 1); fix proof-engine.md:1790's 'undecided' note.**
  - source: `docs/Working/phase7-slice2-plan-2026-05-31.md + phase7-slice2-design-review-FRANK-2026-05-31.md + price-cross-unit-cancellation-design-2026-05-31.md § Doc-update enumeration` · evidence: d210b2b6 'Stage-5 promotion pending'; business-domain-types.md:168 verified unchanged; business-domain-types has no auto-convert price text; fe7d1187's doc-sync covered catalog-system/language-server/diagnostic-system only.
- **Write the runtime reduction-rule requirement into docs/runtime/evaluator.md as a pending obligation: on price × quantity with same-dimension different-unit operands, convert by exact-rational k = ScaleToBase(u_q)/ScaleToBase(u_p) in decimal, target-directed to the price denominator unit (D8 rule 1), rounding at maxplaces; k=1 for same-unit. The slice declared this doc 'the durable carrier' of the runtime obligation and it was NEVER WRITTEN — highest-priority doc extraction in the triage.**
  - source: `docs/Working/phase7-slice2-plan-2026-05-31.md § Runtime requirement` · evidence: grep of evaluator.md confirms no reduction-rule/ScaleToBase content; only other carriers are the superseded docs + BUG-030.
- **Record the standing declines in the new plan so the boundary isn't re-litigated: convex polyhedra and predicate abstraction are permanently off the interactive path (data-dependent latency breaks R2/determinism; theorem-prover step breaks R1) — consistent with spec §0.6 #3 and the niche decision's solver-free scope lock.**
  - source: `docs/Working/compile-time-prevention-features-proposal-2026-06-06.md § What we explicitly decline` · evidence: Aligned with locked canon; a one-paragraph record in the new plan.

### future-candidate (2)

- **Proof-power candidate list for the new plan (all solver-free, unbuilt, grep/git-verified absent; fault-floor-serving — more discharges under prove-or-reject = fewer author rejections): (1) Karr affine-equality domain as reduced-product component — TOP candidate; closes the recurring 3-field balance-identity gap ('100/(Total-A)' under 'rule A + B == Total' → PRE0083 today; corpus demand at equipment-lease-agreement.precept:79); gate on a ProofEngine.Intervals.cs single-pass architecture probe. (2) FlowNarrowing→IntervalContainment wiring — a guard/relation should discharge an assignment-range obligation the way it discharges a divisor ('when Balance >= Withdraw.Amount' ⟹ Balance-Amount ≥ 0); probe-verified NOT discharging 2026-06-05, re-probe at plan time. (3) Incremental numeric-aggregate deltas (extend the shipped count-delta walk to monotone 'set X = X + e'). (4) Dominator-based fact propagation (reuse §0.5 dominators). (5) DBM-emptiness guard-feasibility / per-(state,event,guard) semantic completability — technique survives but POSTURE CHANGED: ships in the niche packet's Layer 2 under flag-soundness (warning-default, earned promotion), NOT prove-or-reject. (6) Min-bounded-divisor fold (PRE0083 over-reject on min/nonzero fields; safe-direction completeness). (7) Quantifier-binding sign-set discharge over element bounds (element bounds feed only the interval path today; div-by-zero/index proofs still read field modifiers; safe over-reject). (8) Interval multiplication for bounded products (X·Y ≤ c, both operands bounded — no language surface needed). (9) DBM→octagon two-field-sum extension of the relational record. (10) TVPI-rational + congruence closure — demand-gated stubs only.**
  - source: `docs/Working/compile-time-prevention-features-proposal-2026-06-06.md + precept-guarantee-specification-2026-06-05.md §5/§6 + proven-core-governed-boundary-2026-06-05.md §4/§6.5 + relational-rules-and-bounds-plan-2026-06-02.md § Discovered during execution + collection-inner-type-value-modifiers-plan-2026-06-02.md sign-set caveat` · evidence: No Karr/TVPI/affine code in src/Precept (grep, prior batch); all machinery-reusing per the proposal's probe-grounded verification log; consistent with spec §0.6 #3 no-solver.
- **Expressiveness candidates (ALL owner-gated: Tier-2 conversation + /design before any work; carry as candidate stubs, NOT committed): (1) Maintained aggregates — compiler-owned incremental sum/count fields making 'sum(lines) ≤ budget'-class rules expressible+provable (incremental view maintenance, not general reduce); independently re-validated by the niche packet as 'the most-demanded expressiveness gap'; 'sum' as a function remains a locked rejection (collection-types.md:860, PRE0030). (2) Conserved pools / transfer-only mutation ('A+B+C == Total' by construction) — thinner evidence; SEQUENCE KARR FIRST (addresses the same balance-identity demand with no new surface). (3) Exclusive-interval collections; refinement-typed elements — speculative stubs. (4) Two-axis element bounds on key/ordering parameters (queue of T by P's P; lookup of K to V's K/V) — deferred-not-rejected at the collection design's lock, grammar slot reserved, no sample needs it. (5) Phase 7 Slice 4 absolute measurement positions (the 'instant' analog for units: absolute temperature/dBm/pH point type) — parked, edge-case business-wise.**
  - source: `docs/Working/proven-core-governed-boundary-2026-06-05.md + collection-inner-type-value-modifiers-2026-06-02.md § Scope + price-cross-unit-cancellation-2026-05-31.md / compiler-readiness-plan Phase 7 Slice 4` · evidence: Niche packet re-validation of aggregates; locked rejections quoted in source docs; Pre-Design Owner Consultation gate (CLAUDE.md) applies to every item.

### locked-design-unbuilt (5)

- **Build sub-slice 2c-ii — field-reference modifier bound enforcement (closes BUG-020 enforcement half): desugar each field-reference bound modifier (min Floor, max Cap, length/count field-refs) to a synthetic span-tagged TypedRule at the field stamp; numeric dependent-op discharge via the shipped 2c-i relational narrowing; numeric default-containment riding BUG-027's ScanRulesAgainstDefaults fold; length/count band-edge DU {Scalar|FieldRef|Unbounded} resolved at discharge via ExtractFieldInterval with the S3-soundness edge direction, empty-band contradiction guard, identity-degradation; rich message naming the referenced field; D5/D6/D7 single-emission-site reconciliation; self/mutual-reference termination; full test matrix incl. the cross-domain edge test.**
  - source: `docs/Working/field-reference-bound-enforcement-2026-06-04.md (+ relational-rules-and-bounds-plan-2026-06-02.md row 2c-ii; dynamic-modifier-bounds-research; collection-inner-type design Supporting Decision 4 inherits it for element bounds)` · evidence: Design Locked 2026-06-04, amended/re-locked 2026-06-05 with three soundness corrections (one triage batch recorded the first lock as found-unsound — the 06-05 re-lock IS the correction; confirm the lock's standing at plan time rather than re-designing blind). bugs.md BUG-020 Active: 'Enforcement half … is the merged relational-narrowing slice'. No LengthBandEdge/CountBandEdge/MinLengthRefField in src (grep). Hard prerequisite BUG-027 Fixed (210229ff verified) — fully unblocked. Squarely fault-floor: a declared bound that does nothing is a prove-or-reject soundness hole. Largest single unfinished locked design in the triage.
- **Build the computed-field default fold (Locked design): add the IsComputed arm to BuildDefaultEnvironment (FoldValue over accumulated defaults, declaration-order, cycles rejected upstream), improving rules + ensures + the 2c-ii computed-bound default uniformly; acceptance matrix ACs 1-8 (AC #2 / matrix cell 17b additionally depends on the unbuilt 2c-ii desugar; ACs 1/3/4 standalone); corpus soundness-tax sweep; doc-sync proof-engine.md:483/:1987/:2024; re-scope BUG-028 (strike the computed-unfoldable facet, keep construction-coarseness + general short-circuit). Deferred refinement recorded: dependency-ordered folding via the name binder's topological order (low priority, safe direction).**
  - source: `docs/Working/computed-field-default-folding-2026-06-05.md` · evidence: Re-verified at HEAD: ProofEngine.Analysis.cs:138 still reads 'else if (field.DefaultExpression is not null || field.IsComputed) unfoldable.Add(field.Name)' — the exact arm the design replaces. Prerequisite BUG-027 shipped (210229ff). Never-over-rejects; Principle-11-aligned.
- **Build Phase 8 Slice 3c — the catalog-declared single-stage diagnostic-ownership Roslyn analyzer (locked D1): every Diagnostics.Create site's containing stage equals Diagnostics.GetMeta(code).Stage; covers literal + catalog-mediated + bounded Pattern-2 residue sites; targets one documented carve-out (TypedConditional PRE0141); zero allow-list. Plus its doc items: diagnostic-system.md ownership-invariant section + analyzer-roster entries in docs/compiler/README.md.**
  - source: `docs/Working/phase8-slice3-emission-ownership-2026-05-31.md (Locked 2026-06-01) + phase8-slices2-3-execution-plan-2026-06-01.md + compiler-readiness-plan-2026-05-24.md Phase 8` · evidence: Re-verified: src/Precept.Analyzers/ files stop at Precept0029ModifierAxisDisjoint.cs — no stage-ownership analyzer. Archive commit 7b8dfa89 explicitly held the design in Working pending 3c. Compiler-internal hygiene; nothing in the niche decision invalidates it.
- **Conditional-totality proof-engine half (owner-approved Decisions 1/3/4): path-sensitive intra-expression narrowing via walk-time AssumedConditions + EffectiveGuard substitution feeding the existing GuardInPath/FlowNarrowing/BuildNarrowedIntervals readers (no new ProofStrategy); replace the if/then/else Γ-shared UNION with per-branch narrowing; ship as proof-engine unit tests now, author-visible PRE0083 flip gated behind the evaluator/differential test. CAVEAT: re-derive the build-order/soundness-backstop reasoning against the fault-floor run's outcome — the doc's demote-to-Unresolved backstop cited the now-DELETED ProofChecker; do NOT execute from its stale soundness-arch citations.**
  - source: `docs/Working/conditional-totality-short-circuit-narrowing-2026-06-07.md` · evidence: No ReduceFacts/EffectiveGuard/AssumedConditions anywhere in src/Precept; probe-confirmed gap: rule 'Y != 0 and X / Y' → PRE0083 Unresolved; two red-teams clean on soundness at approval.
- **The conformance-suite program (Locked design, zero execution): own-lane allowed-red Precept.Conformance.Tests; three-edge coverage (catalog↔impl, spec↔impl harvest+census, spec↔catalog reconciliation); [SpecRef]/[Covers] + coverage meta-test; feature-AC-sheet method; hand-asserted PRE####@stage negatives; fence-annotation convention + docs/language sweep; runtime facet authored as Skip'd v1 acceptance spec. Plus the plan's Phase 0 infrastructure (project+lane, attributes, meta-tests, harvester, determinism fixture, contributing doc), area sequencing (BusinessDomainTypes → Temporal → …), and its five carried open decisions (derived-AC triage cadence, existing-7000 relationship, agent-authoring QC, [Covers] exclusion hook, board reconciliation). New plan decides where/whether-whole this lands; reconcile its gap-discovery role against the Stage-2 exhaustive audit — ONE register, not two.**
  - source: `docs/Working/conformance-suite-design-2026-06-01.md + conformance-suite-plan-2026-06-01.md` · evidence: Re-verified: test/ contains only integrationtests + Precept.{Analyzers,LanguageServer,Mcp}.Tests + Precept.Tests — no conformance project; no fence annotations in docs/language; design Locked 2026-06-01 with full four-leg decisions, grounded in research/architecture/spec-conformance-test-suite-survey.md.

### open-bug (6)

- **Build sub-slice 2c-iii — close BUG-021: set-action assignment creates no lower-bound IntervalContainment obligation on a min-only field (provably-below-min compiles clean). Build-time confirm: match PRE0078 vs split PRE0079.**
  - source: `docs/Working/relational-rules-and-bounds-plan-2026-06-02.md § 2c-iii + precept-guarantee-specification-2026-06-05.md §6` · evidence: bugs.md BUG-021 verified still Active (heading line 209 region, no Fixed status). Core Principle-10/11 fault-floor breach.
- **Close BUG-030 in the runtime phase: the evaluator must actually apply the cross-unit conversion factor (un-skip/extend CrossUnit test with the magnitude assertion: 4.00 USD/ft × 36 in → 12.00 USD); flip evaluator.md to Implemented. Includes the count-pair price-path gap: 'USD/each' × quantity in 'box' cancels silently because PRE0137 is gated on both-operands-quantity (pinned by characterization test CrossCountingUnit_PricePath_CurrentlyCancelsSilently_KnownGap) — the design's 'count still rejects' acceptance is unmet on the price path.**
  - source: `docs/Working/price-cross-unit-cancellation-design-2026-05-31.md + spec-conformance-audit-2026-05-30.md E4` · evidence: bugs.md BUG-030 Active (false Proved, SOUNDNESS); Evaluator stubbed (NotImplementedException); fe7d1187 commit message records the E4 silent-cancel.
- **BUG-022 / F-X-03 — extend catalog-exhaustiveness enforcement to consumer-side *Kind switches ([HandlesCatalogExhaustively] coverage / a 'no consumer-side *Kind dispatch' analyzer over DiagnosticCoverageScanner, the deep-dive's Tier-3 'keep adding invariants on that one scanner' line — stage-ownership, [StaticallyPreventable] consistency, emit-or-retire as siblings); needs a small /design for the Precept0019 exemption model (owner-aligned shape 2026-06-03). Includes the modifier-plan's arm-shape coverage forward watch.**
  - source: `bugs.md BUG-022 (canonical tracker) + phase8-slice2-dispatch-deepdive-2026-06-01.md §7 + modifier-name-axis-overlap-plan-2026-06-03.md § Decisions captured` · evidence: BUG-022 verified Active in bugs.md; new plan should pick it up from bugs.md, not the archived/superseded docs.
- **Close obligation-position incompleteness — the ~10 live fail-open positions (guards, messages, defaults, member-call args) where fault obligations are never collected, yielding false Proved: BUG-031 (faulting arithmetic in any 'when' guard collects NO obligation) + BUG-032 (member-call args, coll.at(A/B)) are the filed instances. Fix shape per the surviving post-pivot finding: catalog ChildExpressionPositions + a build-time obligation-position coverage checker = the missing guarantee property. CONVERGES with the collection plan's element-entry-path family coverage watch (Phase-1 reviewer's strongest objection / design Falsifier 4) — fold both into ONE enumerated-family coverage mechanism, not two trackers.**
  - source: `bugs.md BUG-031/BUG-032 + compile-time-guarantee-statement-draft-2026-06-10.md § Built vs promised + collection-inner-type-value-modifiers-plan-2026-06-02.md Phase-1 forward watch` · evidence: BUG-031/032 verified Active at bugs.md lines 3/22 (both 'SOUNDNESS hole', false Proved); MEMORY: this is the finding that survived the verify-don't-trust deletion. Core fault-floor work.
- **BUG-025 + BUG-026 + Q3 + Q4 diagnostic-policy bundle (one /design-sized cluster): BUG-025 relational contradictions invisible to the satisfiability scan (no warning, no rule attribution); BUG-026 Error-vs-Warning severity for provable contradictions (genuinely unspecified in canon); Q3 residue — InvalidModifierBounds semantics for a field-reference min/max pair (fire only on PROVABLE Floor>Ceiling vs never-fire; §0.7 ruled out silent-skip and reject-because-non-literal, residual choice unmade — ride with 2c-ii); Q4 residue — vacuous (min X) and mutual (A min B + B min A) bound warning policy (explicitly out-of-scope in the 2c-ii design as 'the parent's Q4').**
  - source: `bugs.md BUG-025/BUG-026 + dynamic-modifier-bounds-research-2026-06-01.md Q3/Q4 + field-reference-bound-enforcement-2026-06-04.md Decision 6 tradeoff` · evidence: BUG-025/026 verified Active in bugs.md (lines 78/94); Q3/Q4 unaddressed in the locked 2c-ii design.
- **BUG-028 (default-satisfiability fold completeness: construction-handler coarseness + compound short-circuit; safe under-emit — re-scope when the computed-fold lands, striking the computed-unfoldable facet) and BUG-029 (satisfiability diagnostics render raw TypedExpression AST in user-facing messages).**
  - source: `bugs.md BUG-028/BUG-029 (filed by the relational/BUG-027 workstream)` · evidence: Both verified Active in bugs.md (lines 50/65).

### open-decision (5)

- **Settle the PRE0141 bound-expression ownership decision that gates Slice 3c (walk bounds / split the code / accept carve-out / static-only bounds) — and settle it AGAINST the 2c-ii interaction: 2c-ii's bounds-desugar-to-rules will likely resolve it as a side effect; decide the interaction rather than deciding independently.**
  - source: `docs/Working/phase8-slices2-3-execution-plan-2026-06-01.md § Open decisions ('GATES Slice 3c')` · evidence: Relational plan 2c-ii scope (bounds-as-rules per spec §2.4 line 1112); decision still open at HEAD.
- **Phase 7 Slice 6 — log-unit (dB/Np/B) cross-unit policy ruling: UCUM log function stripped from catalog (UcumAtomCatalog.cs:466), no multiplicative factor exists, the field's libraries forbid multiplying log units; needs a small /design ruling (allow-with-what vs restrict) BEFORE execution. ScaleIsRational=false guard shipped as interim soundness floor.**
  - source: `docs/Working/phase7-slice2-plan-2026-05-31.md + compiler-readiness-plan-2026-05-24.md Phase 7 Slice 6 + spec-conformance-audit E2` · evidence: Parked 'design ruling needed' in both plans; never taken up.
- **OD-1 design constraint for the runtime build: the runtime evaluator and the compile-time ConstantFold must share one expression-evaluation core (guarantee drift prevention) — cheap to lose, expensive to retrofit; the Phase-12 build must inherit it.**
  - source: `docs/Working/precept-guarantee-specification-2026-06-05.md §8 OD-1 (+ research/architecture static-vs-runtime-expression-evaluation survey)` · evidence: Runtime still stubbed, so the constraint is still cheap to adopt now.
- **Fault-floor definition inputs (feed the parallel run; make the resulting statement's canonical placement an explicit deliverable): Point A — prove-or-reject vs govern for statically-decidable-but-unproven-as-written rules (e.g. unguarded 'set Balance = Balance - amount'); Point C / clarity-Q6 — map which outcome type owns each class of external-input value violation (InvalidArgs arg-gate vs ConstraintsFailed post-mutation vs Faulted QualifierMismatch/OutOfRange) and reconcile evaluator.md's type/presence-only arg gate with business-domain-types' value/qualifier boundary validation — MUST be specified before the runtime build; Q4/Q5 — state whether 'no runtime faults / runtime checks are only defensive redundancy' (Principles 1/11) is scoped to definition-internal values and excludes externally-sourced inputs; give the compile/runtime division of labor a single identifiable canonical home (today assembled from 7+ locations).**
  - source: `docs/Working/compile-time-guarantee-statement-draft-2026-06-10.md Points A/C + compile-time-vs-runtime-contract-clarity-2026-06-01.md Q4/Q5/Q6` · evidence: Canon still silent on each (philosophy.md:49-55 unchanged; no outcome-mapping anywhere in evaluator.md/runtime-api.md/business-domain-types.md); the niche decision fixed the fault floor but left these lines undrawn.
- **Point B (low priority, needs /design if taken up): whether to adopt certain/possible/impossible as guarantee vocabulary; must reconcile the runtime Prospect enum (src/Precept/Runtime/Inspection.cs:6 — Certain/Possible/Impossible) with compile-time graph/proof rejection — two different soundness regimes sharing words.**
  - source: `docs/Working/compile-time-guarantee-statement-draft-2026-06-10.md § Proposed framing` · evidence: Inspection.cs:6 verified; vocabulary unsettled in canon.

### plan-phase-incomplete (15)

- **Phase 7 Slice 3 — exchangerate slash syntax (OWNER-RULED 2026-05-30, unbuilt): parse 'USD/EUR' into from/to, retire the 'to' form, update the Types.cs UsageExample, sweep business-domain-types:178 / collection-types:602 / to-form tests. This ruling exists ONLY in the superseded register/plan — do not lose it.**
  - source: `docs/Working/spec-conformance-audit-2026-05-30.md A2 + compiler-readiness-plan-2026-05-24.md Phase 7 Slice 3` · evidence: Live probe (prior batch, 2026-06-11): 'USD/EUR' → PRE0076 + PRE0068; src/Precept/Language/Types.cs:684 still ships "exchangerate in 'USD' to 'EUR'".
- **Phase 7 Slice 5 — angle-unit dimension identity: deg/rad/gon currently reject via empty dimension name (UnitDimensionHelper.cs:48, PRE0114 even same-unit), DIVERGING from the locked allow+surface decision; give them a dimension identity and surface approximate via ScaleIsRational.**
  - source: `docs/Working/phase7-slice2-plan-2026-05-31.md § Enumeration findings + compiler-readiness-plan-2026-05-24.md Phase 7 Slice 5 + spec-conformance-audit-2026-05-30.md E3` · evidence: Slice never executed (no later commits); recorded divergence from a locked design = also drift.
- **Phase 7 / spec-conformance register open rows to fold into the Stage-2 audit register (ONE register, not two): A3 date ± '30 days' typed-constant quantity (probe: still PRE0056); A4 time-denominator compound unit 'kg/hour' (probe: still PRE0075, despite business-domain-types:1137 mandating NodaTime denominator vocabulary); B1-B9 spec-says-error-compiler-accepts gaps (B1 re-confirmed open: collection-in-interpolation compiles clean; B4 changed under later work — re-probe every B row); C1-C4 diagnostic-identity gaps (PRE0073 DurationDenominatorMismatch never fires → generic PRE0114; DenominatorUnitMismatch/CompoundPeriodDenominator; possible CollectionOperationOnScalar/ScalarOperationOnCollection swap; ~15 temporal teachable-message drifts); E1 residual (interval proofs read [pi]-approximated scales — decide whether ScaleIsRational surfacing suffices under §0.7); audit self-coverage gaps (spec §1 lexer, §2.7 parser, §3.10 diagnostic catalog, §3A semantics never audited; 162-code emits-from-real-path check); Phase 7's never-reached exit criteria.**
  - source: `docs/Working/spec-conformance-audit-2026-05-30.md + compiler-readiness-plan-2026-05-24.md Phase 7` · evidence: Live probes 2026-06-11 (prior batch) re-confirmed A2/A3/A4/B1 broken; Stage-2 exhaustive audit in flight is the replacement register — dedupe, don't double-track.
- **Phase 8 Slice 4 — counterexample/witness richness on failed proofs ('X could be 0 here'), structured for tooling. Not started; scope/shape decision open. MUST be re-grounded post-pivot: this is diagnostic-inspectability (Principle 4) work, categorically distinct from the DELETED verify-don't-trust witness-CHECKING architecture — the new plan must restate what witness work survives and must not resurrect the ProofChecker design via this item.**
  - source: `docs/Working/compiler-readiness-plan-2026-05-24.md Phase 8 + phase8-slices2-3-execution-plan-2026-06-01.md § Scope gate` · evidence: Gated on the (now-shipped) unified proof emission point; soundness docs deleted in working tree (9d54e958's docs); owner directive: do not resurrect.
- **Phase 8 Slice 5 — maxplaces compile-time precision proof: decimal-scale abstract domain, PrecisionContainmentProofRequirement, flips MaxPlacesExceeded to proof-owned and supersedes the phase8 D6 runtime-gap posture. Needs /research extension + /design; owner-authorized spec evolution. Natural fault-floor candidate (makes maxplaces prevention rather than runtime-author-managed). Until/unless built, the D6 known gap stands: maxplaces enforcement points 2-3 (event-arg ingress + arithmetic-result-at-set) are runtime work for Phase 12.**
  - source: `docs/Working/compiler-readiness-plan-2026-05-24.md Phase 8 + phase8-slice3-emission-ownership-2026-05-31.md D6` · evidence: Stub in both docs; business-domain-types.md:1581-1590 documents the runtime points; runtime is a stub.
- **Phase 9 — diagnostic completeness: every declared DiagnosticCode emits-or-retires (~18 spec-declared codes with no emission path — RE-DERIVE the list, it is stale against HEAD; consult DiagnosticCoverageScanner Patterns 1/2/3, literal grep is incomplete); NullInNonNullableContext wire through the proof-stage Presence channel or retire (verified still unwired: zero hits in src/Precept/Pipeline/); F-LANG-SPEC-06 (~startsWith first-arg), F-LANG-SPEC-09 (ChoiceElementTypeMismatch/ChoiceMissingElementType), F-LANG-SPEC-12 (OutOfRange constant-literal) incl. the OutOfRange consolidate-or-retire vs NumericOverflow/UnprovedModifierRequirement question, F-LANG-SPEC-13 (NonOrderableCollectionExtreme); NonAssociativeComparison category/stage label tension (Diagnostics.cs:103 TypeSystem-category on a Parse-stage code); reconcile the 148-vs-162 code-count discrepancy; bidirectional CI. Verify every [StaticallyPreventable] code is live-not-dead — that is itself a fault-floor property (a dead preventable code = a P11 hole). Merge the three lenses: this phase + the conformance [Covers] meta-test + the Stage-2 audit.**
  - source: `docs/Working/compiler-readiness-plan-2026-05-24.md Phase 9 + phase8-slice1-emission-inventory-2026-05-31.md §6 + phase8-slices2-3-execution-plan-2026-06-01.md § Open decisions` · evidence: Phase 9 is a stub; emission-inventory counts stale; MEMORY: enumerate-the-whole-family discipline applies.
- **Phase 10 — API surface solidity: F-API-01 typed field descriptors (explicit RUNTIME-PHASE PRECURSOR — 'runtime builds against the final API shape'; Runtime API still string-keyed, Evaluator.cs signatures unchanged); F-PAR-04; F-TC-02; F-TC-03.**
  - source: `docs/Working/compiler-readiness-plan-2026-05-24.md Phase 10 + compiler-readiness-review-2026-05-24.md Stage 10` · evidence: Phase 10 stub; typed-descriptor refactor absent at HEAD.
- **Phase 11 — polish burn-down: ~20 P2 findings, F-NB-01, F-PRF-01, F-LS-03, F-CAT-01 re-verification of the 27 catalog-audit violations, and BUILD the /audit lifecycle skill (CLAUDE.md/CONTRIBUTING reference it; .claude/skills/ has design/execute/plan/promote/research/review only).**
  - source: `docs/Working/compiler-readiness-plan-2026-05-24.md Phase 11 + compiler-readiness-review-2026-05-24.md` · evidence: Phase 11 stub; skills directory listing confirms no audit skill.
- **Phase 12 — runtime gate verification + the 'compiler production-ready' exit-criteria checklist (full-suite stability, zero warnings, doc/code reconciliation, catalog sweep, diagnostic scenario matrix, owner sign-off). The review's §6 wave-sequencing and §7 exit criteria are content-level seeds for the new plan's gate definition — the FRAMING is superseded (the old gate predates 'fault floor = prove-or-reject, universal layer = post-runtime flag-sound gravy'); re-derive the gate under the niche decision.**
  - source: `docs/Working/compiler-readiness-plan-2026-05-24.md Phase 12 + § Definition of compiler production-ready + compiler-readiness-review-2026-05-24.md §6/§7` · evidence: Stubs; new plan owns gate redefinition.
- **Runtime element governance at ingress (collection inner-type design Rule 2 / Acceptance criterion 7): every element entering via add/enqueue/push/insert/put/append or default [...] is governed against the inner-type bound; out-of-bound element discards the working copy. The SHIPPED read-site reach (Rule 4) is sound ONLY paired with this governance — the design names them a matched pair (§ Semantic Rule 5 soundness guard). Belongs to the runtime phase.**
  - source: `docs/Working/collection-inner-type-value-modifiers-2026-06-02.md` · evidence: src/Precept/Runtime/Evaluator.cs:79-99 Fire/Update = NotImplementedException; no ElementBounds/ValueBounds reference under src/Precept/Runtime/.
- **Runtime ingress governance of declared value-level constraints generally (guarantee-contract Part 2): Create is spike-level; Fire/Update/FromJson/Restore are NotImplementedException — governance (enforce declared value-level constraints per business-domain-types, not just type/presence) remains an unbuilt design commitment for the runtime phase. The target is recorded in canon (spec §0.7).**
  - source: `docs/Working/proof-engine-contract-grounding-2026-06-02.md § Ingress enforcement (archived; obligation carried here + in spec §0.7)` · evidence: Precept.cs:74-122, Evaluator.cs:59-157, PreceptValue.cs:18-22 per the grounding doc; runtime stub re-confirmed.
- **Runtime-phase parked requirements bundle (so none is silently dropped): D6.2 composite-period Period.Between runtime lowering; F-LANG-BIZ-11 boundary-precision enforcement for money (wire/persistence minor-unit checks, the Position-3 spinoff); the cross-unit factor application (= BUG-030 entry above); maxplaces runtime points 2-3 (= Slice 5 entry above).**
  - source: `docs/Working/compiler-readiness-plan-2026-05-24.md Phase 6 D6.2 + § Still open — gating Phase 4+` · evidence: Plan's own 'noted here so it is not silently dropped' markers; runtime stubbed.
- **F-LANG-SPEC-08 — the 10 deferred graph-analyzer modifiers (guarded/entry/isolated/universal/sealed-after/writeonce/advancing/settling/completing/absorbing), moved to the graph-analyzer roadmap by owner decision; carry as an explicit post-runtime 'Compiler 1.1' deferral, not dropped.**
  - source: `docs/Working/compiler-readiness-plan-2026-05-24.md Wave-0 decision + Phase 1 Workstream D` · evidence: Deferred-by-owner-decision; roadmap doc exists.
- **Low-priority triage carriers: (1) the 5 currency-precision-survey research follow-ups (opt-in discoverability, multi-currency arithmetic safety, hyperinflationary/non-ISO drift, Temenos comparator gap, crypto assets) — owner triages individually if/when blocking; (2) PRE0073 dormant single-basis Date gate on the divide path — re-aim with a reachable test only if a future operation makes the shape live; (3) language-server dedicated hover line surfacing the element bound on the collection-field card (tracked with the numeric/qualified element-bound projection follow-on); (4) review-skill deferred dev-tooling backlog (capture a real /code-review output sample before relying on the merge path; /plan lifecycle-stage frontmatter marker; independent high/ultra dials; /audit ownership of committed-canonical drift) — dev-tooling, not compiler-readiness.**
  - source: `compiler-readiness-plan-2026-05-24.md § Still open / Phase 6 W-C note + language-server.md:651 + review-skill-reshape-2026-06-07.md § Deferred to future (archived)` · evidence: Each explicitly parked in its source; carry as a triage list, not work items.
- **Sample-corpus Phase 5 catalog-coverage showcase: 4 samples never authored (priority-incident-queue, regulatory-audit-log, sensor-measurement-aggregation, regulatory-submission-package) targeting catalog members with ZERO idiomatic corpus uses (QueueBy/enqueueBy/dequeueBy, LogBy/appendBy, math-function surface: approximate/floor/ceil/clamp/sqrt/pow). RE-VERIFY the zero-use claims against the now-77-file corpus first (evidence is from 2026-05-25); fold into whatever sample/conformance phase the new plan defines; existing samples may adopt appendBy/enqueueBy idioms in a future quality sweep.**
  - source: `docs/Working/sample-corpus-expansion-plan.md § Phase 5 + § Phase 1 Implementation Drift` · evidence: ls samples/ (prior batch): none of the 4 filenames exist; 77 .precept files total.

## Dropped obligations (overtaken by events — with reasons)

- ~~Conditional-totality witness half — Decision 5 (assumed-condition + dominance + short-circuit-justification provenance on GuardInPath/FlowNarrowing witnesses), AC-13/13b planted-witness checker-rejection tests, witness-provenance Inventory items.~~ — Overtaken by the soundness pivot: the verify-don't-trust architecture carrying the ProofWitness DU + ProofChecker pass was deleted 2026-06-08 (9d54e958's docs deleted in the working tree; owner directive: do not resurrect). This half was never locked ('gated on the soundness-arch ProofChecker/ProofWitness DU … NOT locked'). If the fault-floor run reintroduces any checking/witness layer it will define its own shapes. *(source: `docs/Working/conditional-totality-short-circuit-narrowing-2026-06-07.md`)*
- ~~Part 2 of the prevention-features proposal — rule-level 'prevented'/'governed' enforcement-intent annotation making the §0.7 split author-declared and compiler-enforced.~~ — Overtaken by the niche decision's flag-soundness contract: the guarantee direction was inverted (fault floor stays prove-or-reject and total; everything beyond ships as always-true universal flags; silence promises nothing), which caps the silent-degradation harm Part 2 existed to surface — without new grammar. Any annotation surface would need a fresh Tier-2 owner conversation under the new framing. *(source: `docs/Working/compile-time-prevention-features-proposal-2026-06-06.md`)*
- ~~Adopt the §4 three-way prevented/rejected/governed authoring contract as the product guarantee (incl. 'reducible → reject with restructure suggestion').~~ — Overtaken: the niche decision's three-layer shape + teachable line is the adopted guarantee structure; never owner-locked; partially conflicts with the flag-sound posture (blanket forced-restructuring is the mandate shape the cost evidence rejected). *(source: `docs/Working/precept-guarantee-specification-2026-06-05.md`)*
- ~~OD-2 (where to declare the data-fragment edge) and OD-3 (severity policy derived from guarantee class).~~ — Settled/overtaken by the niche decision: the public promise is the flag-soundness contract, not a fragment enumeration (fragment edges may grow; flags only ever added), and severity is warning-default with per-diagnostic earned error promotion. The Karr/octagon extension question survives separately via the proof-power candidate list. *(source: `docs/Working/precept-guarantee-specification-2026-06-05.md §8`)*
- ~~The ingress-refinement keystone — ingress validation establishes proven refinements propagated to the interior (governance feeds the proof), with the refinement-scope/staleness rule.~~ — Overtaken by the niche decision: the adopted shape keeps governance at the result (atomic sweep) and gets universal claims from the flag-sound compile layer. Never adopted, never designed; reviving it would be a fresh strategy proposal requiring owner conversation — do not resurrect from this doc. (The doc's expressiveness/proof-power candidates were extracted separately and survive.) *(source: `docs/Working/proven-core-governed-boundary-2026-06-05.md`)*
- ~~Apply the guarantee-claims audit's specific text corrections (qualifiers on the evaluator.md Non-Negotiable box, README three-layer routing pointer, philosophy line edits).~~ — Overtaken: the fault-floor reframe rewrites the headline guarantee language wholesale; applying 06-02-era qualifier patches would encode pre-pivot framing. The underlying drift is carried instead by the owner-gated philosophy-reconciliation obligation in the register. *(source: `docs/Working/guarantee-claims-doc-audit-2026-06-02.md`)*
- ~~Lock the 2026-06-10 guarantee draft as the consolidated statement superseding the two 2026-06-05 strawmen.~~ — Overtaken: the lock never happened; this triage supersedes all three docs together, and the parallel fault-floor run + the new readiness plan produce the consolidated statement instead. The draft's open Points A/B/C and its obligation-position finding were extracted into the register. *(source: `docs/Working/compile-time-guarantee-statement-draft-2026-06-10.md frontmatter`)*
- ~~Restore re-validation recommendation ('re-confirm once the executable model lands; implement re-validates').~~ — Flipped by owner decision D4 (2026-06-02): Restore = trusted hydration, no re-validation; corrected in canon (spec §0.7, runtime-api.md:266/:270). Only the cleanup residue (spec §3A.2:1946 fix + RestoreConstraintsFailed removal) survives, in the register. *(source: `docs/Working/proof-engine-contract-grounding-2026-06-02.md`)*
- ~~notempty in element position (the collection plan's Phase-2 deferred Open Decision).~~ — Already closed: the modifier-name-axis-overlap design (Locked 2026-06-03, owner Direction A) retargeted notempty to StringOnly; shipped c3209ad2 + analyzer eeb9a9c6; spec :435/:1123/:1150/:1671/:1681 document per-element notempty; collection cardinality is mincount 1. Verified in code (Modifiers.cs:114 StringOnly) this pass. *(source: `docs/Working/collection-inner-type-value-modifiers-plan-2026-06-02.md`)*
- ~~Slice 3b-count (count-containment for collection defaults + revive CountBoundViolation) and the rest of the Phase-8 2/3a/3b deliverables; the 6 cross-stage dual-emission consolidations; the 2 wired Class-O relocations.~~ — Completed, not dropped — recorded here to prevent re-tracking: 3b-count delivered in substance via relational 2b-ii/BUG-018 (c27a382b; CountBoundViolation live, proof-engine.md:1731 verified this pass), with its notempty-on-collection sub-item overtaken by the string-only axis decision; Slices 2/3a/3b shipped (59fe2666/f7e5dee6/ecce5d51, all commits verified). *(source: `docs/Working/phase8-slices2-3-execution-plan-2026-06-01.md + phase8-slice1-emission-inventory-2026-05-31.md`)*
- ~~A1 price×quantity headline resolution; BUG-017/018/019/023/024 closures; relational slices 1a/2a/2b/2c-i; the guarantee-contract plan's four phases; the /review reshape.~~ — Completed, not dropped — recorded to prevent re-tracking: A1 fixed b7c17482; BUG-017 (9820b8c1), BUG-018 (c27a382b), BUG-019 (4e4e2961 + 4a003b64/fcbaaf71), BUG-023/024 (7ebbb68c) all verified Fixed in bugs.md; contract plan landed (5af46537/9fa6a16c/e7c1255f/e1c0a959); review reshape landed (f2b852ed). One housekeeping note: BUG-017/023/024 entries still sit physically under bugs.md ## Active with '(Move to § Fixed in a cleanup pass)' notes — do that cleanup move during the docs/Working reset. *(source: `spec-conformance-audit-2026-05-30.md / relational-rules-and-bounds-plan-2026-06-02.md / proof-engine-guarantee-contract-plan-2026-06-02.md / review-skill-reshape-2026-06-07.md`)*

## Triage summary

Merged and verified all three triage batches: 32 dispositions covering every non-KEEP item in docs/Working/ (directory listing cross-checked — bugs.md and the three compile-time-niche-* docs are the KEEP set; the deleted soundness docs sit as working-tree deletions and need no disposition). Result: 4 ARCHIVE, 28 SUPERSEDE. All four archive candidates passed independent re-verification this pass — modifier-name-axis-overlap-plan (Modifiers.cs:114 StringOnly Notempty, Precept0029ModifierAxisDisjoint.cs + tests on disk, all spec/collection-types/type-checker/proof-engine doc-syncs grep-verified; its 'Active' status field is stale, flip at archive time), proof-engine-contract-grounding (BUG-017/018/019 all Fixed with verified commits — I chased one scare: BUG-017's entry physically sits under ## Active but its Status line reads 'Fixed 2026-06-02 (9820b8c1)' with a move-in-cleanup-pass note), proof-engine-guarantee-contract-plan (spec §0.7 at line 262, philosophy carries-proof insertion live, README:117 pointer, all six commits exist), and review-skill-reshape (SKILL.md 305 lines with every designed section, CONTRIBUTING subsumption note, f2b852ed). No demotions needed. Key unbuilt-state claims re-confirmed at HEAD: ProofEngine.Analysis.cs:138 still carries the computed→unfoldable arm, no conformance test project exists, no clock-seam note in evaluator.md, analyzers stop at Precept0029, and all 31 cited commit hashes resolve in git. The consolidated register carries 45 deduped obligation entries: 6 locked-designs-unbuilt (headlined by field-reference bound enforcement — now fully unblocked since BUG-027 landed — plus computed-field default folding, Phase 8 Slice 3c ownership analyzer, the conformance suite, and the two conditional-totality halves), 7 open-bug clusters (BUG-020/021/022/025-026/028-029/030/031-032 — the BUG-031/032 obligation-position family being the core surviving fault-floor work), 5 open decisions feeding the parallel fault-floor run (Points A/B/C, Q4/Q5, PRE0141, log-units, OD-1), the doc-sync debts (the never-written evaluator.md cross-unit reduction rule is the highest-priority extraction; Stage-5 price promotion; D9 clock seam; 2c-ii doc package), 4 canon-drift items (BUG-030/proof-engine.md:1790 vs the locked allow-all decision is the biggest; Restore spec §3A.2 contradiction; spec §0.6 items 7-10 mislabel; owner-gated philosophy reconciliation), the incomplete Phase 7-12 board, and the owner-gated future-candidate lists (proof-power: Karr first; expressiveness: maintained aggregates first, all behind the Tier-2 gate). Eleven dropped/overtaken items recorded with reasons — dominated by the verify-don't-trust deletion and the niche decision's flag-soundness contract (witness half, prevented/governed annotation, three-way authoring contract, OD-2/OD-3, ingress-refinement keystone, audit wording patches), plus two completed-not-dropped blocks listed to prevent re-tracking. Synthesis-stage watch items: fold the element-entry-path coverage watch into the obligation-position family (one mechanism), reconcile the old Phase-7 register and the conformance suite's discovery role against the Stage-2 audit (one register), and re-ground Phase 8 Slice 4 witness richness so it cannot resurrect the deleted ProofChecker.

---

# Full per-doc triage detail

## `docs/Working/collection-inner-type-value-modifiers-2026-06-02.md`

- **Claimed:** Locked 2026-06-02 (design; owner-authorized after adversarial review)
- **Verified:** Compile-time half FULLY implemented and promoted: Phase 1 commit 4a003b64, Phase 2 fcbaaf71 verified in git; code live (Modifiers.cs:361 ElementPositionValueTokens, SemanticIndex.cs:376 DeclaredValueBounds, ProofEngine.Lengths.cs:218 MemberAccessStringLengthInterval filled); canon carries it (spec §2.3 grammar :1108-1123, modifier table :1671-1681; collection-types.md §Element value modifiers :612-795; proof-engine.md :1701/:1711/:1722; mcp.md:108; language-server.md:651). BUG-019 marked fully Fixed in bugs.md with both residual gaps closed. BUT the design's Rule 2 (runtime element governance at ingress) is UNBUILT — Runtime/Evaluator.cs Fire/Update throw NotImplementedException (lines 79-99); Acceptance criterion 7 explicitly runtime-gated.
- **Disposition:** supersede — Archive bar is 'TRULY fully implemented AND promoted'. The compile-time surface meets it, but the design specifies a runtime governance semantic (Rule 2, Acceptance 7) that cannot exist yet (runtime is a stub), plus three explicitly-deferred follow-ons. Live obligations ride in the doc, so it must flow into the new readiness plan rather than Archive.
- **Obligations:**
  - [VALID] Runtime element governance at ingress (Rule 2 / Acceptance criterion 7): every element entering via add/enqueue/push/insert/put/append or default [...] is governed against the inner-type bound; out-of-bound element discards the working copy. The read-site reach (Rule 4, shipped) is sound ONLY paired with this governance — the design calls them a matched pair (§ Semantic Rule 5 soundness guard).
    - Design § Semantic Rule 2/5; src/Precept/Runtime/Evaluator.cs:79-99 (Fire/Update = NotImplementedException; no ElementBounds/ValueBounds reference anywhere under src/Precept/Runtime/). Belongs to the runtime phase of the new plan.
  - [VALID] Two-axis element bounds on the key/ordering parameters (queue of T by P's P; lookup of K to V's K/V) — deferred follow-on design; grammar slot reserved.
    - Design § Scope (Out of scope, 'Deferred, not rejected') and § Resolved at lock item 1. No sample needs it; low priority.
  - [VALID] Field-reference element bounds (queue of string maxlength SomeField) inherit BUG-020's enforcement resolution (Supporting Decision 4) — that resolution (relational Slice 2c-ii) is itself still unbuilt.
    - Design Supporting Decision 4; bugs.md BUG-020 status: undeclared-name half fixed (e2b7c9c1), 'Enforcement half … is the merged relational-narrowing slice'; relational-rules-and-bounds-plan-2026-06-02.md row 2c-ii still 'Heavyweight — gates on 2c-i AND BUG-027' (2c-i and BUG-027 done, 2c-ii build not).
  - [VALID] Language-server dedicated hover line surfacing the element bound on the collection-field card (numeric/qualified element-bound projection follow-on).
    - docs/tooling/language-server.md:651 — '(A dedicated hover line surfacing the element bound on the collection-field card itself is a follow-on, tracked with the numeric/qualified element-bound projection.)' Minor polish item.

## `docs/Working/collection-inner-type-value-modifiers-plan-2026-06-02.md`

- **Claimed:** Active; Phase 1 ✅ 4a003b64, Phase 2 ✅ fcbaaf71
- **Verified:** Both phases verified landed (commits exist; code present; doc-syncs verified in canon — spec §2.3/§2.4 updated, collection-types.md element-value-modifiers section, proof-engine.md Strategy 9/10 rows, spec residual-gap sentence removed (grep 'residual gap' in spec = 0 hits); Slice 3 row marked complete in 96a95e75). The plan's Phase-2 deferred item 'notempty in element position' was subsequently RESOLVED by modifier-name-axis-overlap-2026-06-03.md (Locked; shipped c3209ad2/eeb9a9c6; spec :1123/:1150 now document string-only notempty per-element).
- **Disposition:** supersede — Execution is complete and promoted, but the plan carries two live forward obligations surfaced by its own adversarial reviews (sign-set element-band gap; element-entry-path family coverage check), and its Definition of done includes the runtime-gated Acceptance 7. With live obligations embedded, supersede so the new plan absorbs them; when in doubt, supersede.
- **Obligations:**
  - [VALID] Quantifier-binding element bounds feed only the INTERVAL proof path; sign-set-based proofs (division-by-zero, index lower-bound) over a rule-predicate binding still read field modifiers, not element bounds — a completeness gap in the safe (over-reject) direction.
    - Plan Phase-2 execution note ('Sign-set caveat'); consistent with the prove-or-reject fault floor (unprovable rejects today — closing it is completeness work, not soundness).
  - [VALID] Single enumerated coverage check over the WHOLE element-entry-path family, so a future element-introducing path cannot silently bypass the read-reach/governance matched pair (Phase-1 reviewer's strongest objection; design Falsifier 4).
    - Plan Phase-1 note 'Forward watch'. Converges with the obligation-position-completeness finding that survived the soundness pivot (the catalog ChildExpressionPositions / build-time coverage-checker line of work) — the new plan should fold this into that family rather than track it separately.
  - [DROPPED (Already closed: modifier-name-axis-overlap design (Locked 2026-06-03, owner Direction A) retargeted notempty to StringOnly; shipped c3209ad2 + analyzer eeb9a9c6; design archived (a497127b); spec §2.3 :1123 and modifier table :1671 document per-element notempty; collection cardinality is mincount 1.)] notempty in element position (Phase-2 deferred Open Decision).
    - git log c3209ad2/cceab68e/a497127b; docs/language/precept-language-spec.md:435,:1123,:1150,:1671,:1681; docs/Working/Archive/modifier-name-axis-overlap-2026-06-03.md.

## `docs/Working/compiler-readiness-plan-2026-05-24.md`

- **Claimed:** Active — Phases 1–6 complete; Phase 7 active; Phase 8 active; Phases 9–12 stubbed
- **Verified:** Phases 1–6 completion verified by commit trail (e.g. 38712543, 090764d3, 1e865ac1, 34d11e61, fd147dc2/d2946dc0) and green-suite claims consistent with bugs.md Fixed entries. Phase 7 INCOMPLETE: Slice 3 exchangerate slash unbuilt (live probe: exchangerate in 'USD/EUR' → PRE0076 'not a recognized ISO 4217 currency code'); Slices 4/5/6 parked/planned; Slice 2 Stage-5 promotion noted remaining. Phase 8 INCOMPLETE: Slices 1/2/3a/3b done (59fe2666, f7e5dee6, ecce5d51; BUG-017 since fixed 9820b8c1; BUG-018 count prover fixed c27a382b), but Slice 3c ownership ANALYZER unbuilt (analyzers stop at Precept0029; no stage-ownership analyzer), Slice 4 (witnesses) and Slice 5 (maxplaces proof) not started. Phases 9–12 are stubs. This is the direct predecessor of compiler-readiness-plan-2026-06-11.md.
- **Disposition:** supersede — This IS the document the new compiler-readiness-plan-2026-06-11.md replaces. Large parts executed, large parts open; the open phases/slices are precisely what the new plan must re-absorb and re-prioritize under the niche decision and fault-floor reframe.
- **Obligations:**
  - [VALID] Phase 7 Slice 3 — exchangerate slash syntax: parse 'USD/EUR' into from/to, retire the 'to' form, sweep docs/catalog example/tests (owner ruled slash canonical 2026-05-30).
    - Probe 2026-06-11 via precept_compile: field Rate as exchangerate in 'USD/EUR' → PRE0076 + PRE0068 (slash form rejects). No exchangerate commits since 05-31 in git log.
  - [VALID] Phase 7 Slice 2 residue — Stage-5 promotion of the cross-unit-cancellation design (D8/§168 spec lift + the evaluator runtime requirement: the runtime applies the exact UCUM factor at price-cancellation).
    - Slice log row 2: 'Remaining: Stage-5 promotion (D8/§168 + evaluator runtime requirement)'. The price-cross-unit-cancellation docs (separate batch) are the primary tracker; record here so the runtime value-application requirement isn't lost.
  - [VALID] Phase 7 Slice 5 — angle-unit dimension identity (deg/rad/gon reject today via empty dimension name, UnitDimensionHelper.cs:48, diverging from the locked allow+surface decision; surface approximate via ScaleIsRational).
    - Slice log row 5 'Planned (split from Slice 2)'; no subsequent commits touch it.
  - [VALID] Phase 7 Slice 6 — log-unit (dB/Np/B) cross-unit cancellation policy: UCUM log function stripped from catalog, no multiplicative factor exists; needs a small /design ruling (allow-with-what vs restrict) before execution.
    - Slice log row 6 'Parked — design ruling needed'; UcumAtomCatalog.cs:466 noted as stripping the log function.
  - [VALID] Phase 7 Slice 4 — absolute measurement positions (the 'instant' analog for units: absolute temperature/dBm/pH as a point type distinct from quantity amounts). New language surface → full Tier-2 owner-consultation lifecycle when taken up.
    - Slice log row 4 'Parked — new surface, not started; edge case business-wise; low priority.'
  - [VALID] Phase 7 unresolved register rows — A3 (date+literal-quantity), A4 (kg/hour compound), B1–B9 enforcement gaps, C1–C4 diagnostic-identity gaps, D-class doc-stale items from spec-conformance-audit-2026-05-30.md, plus the open-ended catalog-derived probe matrix and Phase 7's exit criteria (never reached).
    - Phase 7 § First seed + exit criteria (all unchecked). NOTE for the new plan: the parallel Stage-2 exhaustive spec-vs-impl audit is re-deriving this register fresh — reconcile/dedupe rather than carrying both registers.
  - [VALID] Phase 8 Slice 3c — diagnostic-emission ownership Roslyn analyzer (every DiagnosticCode owned by exactly one stage, declared in DiagnosticMeta.Stage, wrong-stage emission = build error). Design Locked 2026-06-01 (phase8-slice3-emission-ownership-2026-05-31.md); 3a/3b shipped, the analyzer enforcement did not.
    - src/Precept.Analyzers/ contains Precept0001–0029 only — no stage-ownership analyzer; mcp.md:108 confirms the 3a/3b ownership relocations shipped (Bind/Graph/Proof-owned codes).
  - [VALID] Phase 8 Slice 4 — counterexample/witness richness on failed proofs ('X could be 0 here'), structured for tooling. Not started; scope/shape decision open.
    - Phase 8 slice list (not started). CAUTION: this predates the soundness pivot — it is diagnostic-inspectability work (Principle 4), distinct from the DELETED verify-don't-trust witness-CHECKING architecture; the new plan should re-ground its shape and must not resurrect the ProofChecker design via this item.
  - [VALID] Phase 8 Slice 5 — maxplaces compile-time precision proof (decimal-scale abstract domain; PrecisionContainmentProofRequirement; flips MaxPlacesExceeded to proof-owned). Needs /research extension + /design before execution; owner-authorized spec evolution, not a rejection override.
    - Phase 8 slice list (Stub). Aligned with the prove-or-reject fault floor (makes maxplaces prevention rather than runtime-author-managed) — a natural fault-floor candidate for the new plan.
  - [VALID] Phase 9 (stub) — diagnostic completeness: every declared DiagnosticCode emits-or-retires; F-LANG-SPEC-06 (~startsWith first-arg enforcement), F-LANG-SPEC-09 (ChoiceElementTypeMismatch/ChoiceMissingElementType wire-or-retire), F-LANG-SPEC-12 (OutOfRange constant-literal check), F-LANG-SPEC-13 (NonOrderableCollectionExtreme); reconcile the 148-vs-162 code-count discrepancy; bidirectional CI.
    - Phase 9 stub. Overlaps the conformance-suite [Covers] meta-test and the Stage-2 audit — new plan should merge the three lenses. Several [StaticallyPreventable] codes being live-vs-dead is also a fault-floor property.
  - [VALID] Phase 10 (stub) — API surface solidity: F-API-01 typed field descriptors (explicit runtime-phase precursor — 'runtime builds against the final API shape'), F-X-03 extend [HandlesCatalogExhaustively] coverage (now concretized as BUG-022, Active in bugs.md, needs a small /design for the exemption model), F-PAR-04, F-TC-02, F-TC-03.
    - Phase 10 stub + bugs.md BUG-022 (Active, owner-aligned exemption shape 2026-06-03).
  - [VALID] Phase 11 (stub) — polish burn-down: ~20 P2 findings, F-NB-01, F-PRF-01, F-LS-03, F-CAT-01 re-verification of the 27 catalog-audit violations, and building the /audit lifecycle skill (referenced by CLAUDE.md but absent from .claude/skills/).
    - Phase 11 stub; ls .claude/skills/ shows design/execute/plan/promote/research/review only — no audit skill.
  - [VALID] Phase 12 (stub) — runtime gate verification + the 'compiler production-ready' exit-criteria checklist (full-suite stability, zero warnings, doc/code reconciliation, catalog sweep, diagnostic scenario matrix, owner sign-off).
    - Phase 12 stub + § Definition of compiler production-ready. The new plan will redefine the gate, but the checklist content is the seed.
  - [VALID] Runtime-phase deferred requirements parked in this plan: D6.2 composite-period Period.Between runtime lowering; the Slice-2 cross-unit runtime value application; F-LANG-BIZ-11 boundary-precision enforcement for money (wire/persistence minor-unit checks, the Position-3 spinoff).
    - Phase 6 D6.2 ('noted here so it is not silently dropped'); Phase 7 Slice 2 row; § Still open — gating Phase 4+ (F-LANG-BIZ-11).
  - [VALID] F-LANG-SPEC-08 — the 10 deferred graph-analyzer modifiers (guarded/entry/isolated/universal/sealed-after/writeonce/advancing/settling/completing/absorbing), moved to the graph-analyzer roadmap; ship as Compiler 1.1 after runtime.
    - Wave-0 decision + Phase 1 Workstream D (roadmap doc created). Deferred-by-owner-decision, not dropped — the new plan should carry it as an explicit post-runtime deferral.
  - [VALID] 5 currency-precision-survey research follow-ups (opt-in discoverability, multi-currency arithmetic safety, hyperinflationary/non-ISO drift, Temenos comparator gap, crypto assets) — owner triages individually if/when blocking.
    - Plan § Still open — gating Phase 4+. Low priority; carry as a triage list, not work items.
  - [VALID] PRE0073 dormant single-basis Date gate on the divide path — re-aim with a reachable test if a future operation makes that shape live.
    - Phase 6 W-C completion note ('Deferred to W-D / later'). Very low priority; conditional on future op-catalog changes.

## `docs/Working/compiler-readiness-review-2026-05-24.md`

- **Claimed:** Complete — awaiting owner triage on P0s (the 98-finding audit register)
- **Verified:** The audit itself completed; its findings were the input to the readiness plan. All 13 P0s and the bulk of P1s verified closed via Phases 1–6 (commit trail + bugs.md Fixed entries + canon Status fields now truthful). Open residue = exactly the finding families parked in plan Phases 9–11 (F-LANG-SPEC-06/09/12/13, F-API-01, F-X-03, F-PAR-04, F-TC-02/03, F-NB-01, F-PRF-01, F-LS-03, F-CAT-01, ~20 P2s) plus F-LANG-BIZ-11. A fresh exhaustive spec-vs-impl audit is running in parallel (Stage 2) and will produce the replacement register.
- **Disposition:** supersede — An audit register is not 'implemented and promoted' content; its closed rows are history and its open rows must transfer. The new readiness plan + the fresh Stage-2 audit jointly replace it. Open-finding obligations are enumerated once, on the plan's entry above (Phases 9–11 + F-LANG-BIZ-11), to avoid double-tracking.
- **Obligations:**
  - [VALID] Carry the still-open finding families (Phase 9: SPEC-06/09/12/13 + emit-or-retire; Phase 10: F-API-01/F-X-03/F-PAR-04/F-TC-02/F-TC-03; Phase 11: P2 polish + F-CAT-01 delta; F-LANG-BIZ-11) into the new plan — enumerated in detail under compiler-readiness-plan-2026-05-24.md's obligations to avoid duplication.
    - Review § 3 findings + plan Phases 9–11 stubs; spot-checks: no audit skill built, BUG-022 (= F-X-03 concretization) Active, typed-descriptor refactor absent (Runtime API still string-keyed per review Stage 10 — Evaluator.cs:79-99 signatures unchanged).
  - [VALID] Review § 6 'Wave' sequencing and § 7 production-ready exit criteria as inputs to the new plan's gate definition.
    - Review §6/§7. Content-level seed only; the niche decision/fault-floor reframe supersedes the framing (the old gate predates 'fault floor = prove-or-reject, universal layer = post-runtime gravy').

## `docs/Working/compiler-readiness-review-2026-05-24-appendices/`

- **Claimed:** Appendix folder — 5 sub-agent audit reports (archive-promotion backlog, catalog-system, collections-grammar, language-spec, type-system)
- **Verified:** Evidence backing the 2026-05-24 review. The 16 Archive promotions the backlog appendix drove were executed in Phase 1 Workstream E (✅ per plan; Archive/ docs carry Promoted-to headers). No independent live obligations beyond what the review/plan entries carry.
- **Disposition:** supersede — Travels with its parent review — same reasoning: audit evidence, not implemented-and-promoted content. Move the folder alongside the review into Superseded/ so cross-links stay resolvable.

## `docs/Working/computed-field-default-folding-2026-06-05.md`

- **Claimed:** Locked 2026-06-05 (design — fold computed fields into the shared default environment; Slice 2c-ii inclusion)
- **Verified:** NOT implemented. ProofEngine.Analysis.cs:138 still reads `else if (field.DefaultExpression is not null || field.IsComputed)` — the unconditional computed→unfoldable arm the design replaces. No commits after 210229ff (BUG-027 prereq, which DID land) touch this. BUG-028 remains un-re-scoped in bugs.md (computed facet still listed). Its companion 2c-ii build (field-reference-bound-enforcement, Locked 2026-06-04/05) is also unbuilt.
- **Disposition:** supersede — Locked-but-unbuilt design. Internally sound and in the never-over-reject direction; nothing about it implemented or promoted.
- **Obligations:**
  - [VALID] Build the computed-field default fold: add the IsComputed arm to BuildDefaultEnvironment (FoldValue over accumulated defaults, declaration-order, cycles rejected upstream), improving rules + ensures + the 2c-ii computed-bound default uniformly; acceptance matrix per design ACs 1–8; corpus soundness-tax sweep; doc-sync proof-engine.md:483/:1987/:2024; re-scope BUG-028 (strike the computed-unfoldable facet, keep construction-coarseness + general short-circuit).
    - Design Locked with all 4 decisions four-legged; prerequisite BUG-027 shipped (210229ff, ScanRulesAgainstDefaults live in ProofEngine.Satisfiability.cs); the change site verified unchanged at ProofEngine.Analysis.cs:138. Aligned with Principle-11 / prove-or-reject fault floor (adds rejections of genuinely-violating defaults; never over-rejects). AC #2 (matrix cell #17b) additionally depends on the unbuilt 2c-ii desugar; ACs #1/#3/#4 are standalone.
  - [VALID] Deferred refinement recorded by the design: dependency-ordered folding (consume the name binder's topological order to fold computed fields referencing later-declared fields).
    - Design § Scope Deferred + Decision 2 Alternatives. Completeness refinement in the safe direction; low priority.

## `docs/Working/conditional-totality-short-circuit-narrowing-2026-06-07.md`

- **Claimed:** Externally-Grounded; proof-engine half owner-approved 2026-06-07 and locked-in-direction; witness half gated on the soundness-arch ProofChecker/ProofWitness DU (NOT locked)
- **Verified:** Nothing built: no ReduceFacts/EffectiveGuard/AssumedConditions anywhere in src/Precept; no spec short-circuit paragraph landed; evaluator.md/precept-builder.md notation drift unfixed. The doc's gating dependency for its witness half — soundness-and-coverage-architecture-2026-06-07.md and its ProofChecker/ProofWitness DU — was committed in 9d54e958 and then DELETED (verify-don't-trust pivot; the deletions sit in the working tree). The proof-engine half's behavioral claims re-verified: probe-confirmed PRE0083 on `Y != 0 and X / Y` remains the live gap.
- **Disposition:** supersede — Approved-in-direction but unbuilt, and its witness half is structurally entangled with the deleted soundness architecture (stale references throughout: O-E, O-1, soundness-arch witness shapes, the demote-to-Unresolved backstop). Must be re-grounded inside the new plan/fault-floor framing rather than executed as-is.
- **Obligations:**
  - [VALID] Spec + evaluator-contract half (Decisions 2, 6, 7): state short-circuit and/or/not/if reduction semantics in the spec (desugaring anchor A and B ≡ if A then B else false, riding the locked §0.4.2 'exactly one is evaluated'); lock the strict-boolean BRANCH_FALSE/BRANCH_TRUE + if-lowering opcode contract (resolving evaluator.md:993); fix the &&/||/?: notation drift in evaluator.md AND precept-builder.md (six sites).
    - Owner-approved 2026-06-07 (doc header); pure doc/spec work independent of the deleted witness architecture; evaluator.md:1006-1008 still carries the drifted opcode rows.
  - [VALID] Proof-engine half (Decisions 1, 3, 4): path-sensitive intra-expression narrowing via a walk-time AssumedConditions context + EffectiveGuard substitution feeding the existing GuardInPath/FlowNarrowing/BuildNarrowedIntervals readers (no new ProofStrategy); replace the if/then/else Γ-shared UNION with per-branch narrowing; ship as proof-engine unit tests now, with the AUTHOR-VISIBLE diagnostic flip (PRE0083 disappearing) gated behind the evaluator/differential test landing (Decision 1 option (a)).
    - Owner-approved direction (two red-teams clean on soundness); probe-confirmed gap is real (rule Y != 0 and X/Y → PRE0083 Unresolved). CAVEAT for the new plan: the doc's own Philosophy row 8 states the demote-to-Unresolved backstop depended on the now-DELETED ProofChecker — the build-order/soundness-backstop reasoning must be re-derived against whatever the fault-floor run decides; do not execute from this doc's stale soundness-arch citations.
  - [DROPPED (Overtaken by the soundness pivot: the verify-don't-trust architecture carrying the ProofWitness DU + ProofChecker pass was deleted 2026-06-08 (commit 9d54e958's docs deleted in the working tree; owner directive: do not resurrect). The doc itself never locked this half ('gated on the soundness-arch ProofChecker/ProofWitness DU per O-E … NOT locked'). If the parallel fault-floor definition reintroduces any checking/witness layer, it will define its own shapes; this half is recorded as dropped-with-reason, not carried.)] Witness half — Decision 5 (assumed-condition + dominance + short-circuit-justification provenance on GuardInPath/FlowNarrowing witnesses), AC-13/13b (planted-witness checker-rejection tests), and the witness-provenance Inventory items.
    - Doc header 'Approval status (split)'; git status: soundness-and-coverage-architecture-2026-06-07.md and siblings deleted; MEMORY: verify-don't-trust DELETED 2026-06-08, real finding = obligation-position incompleteness.

## `docs/Working/conformance-suite-design-2026-06-01.md`

- **Claimed:** Locked 2026-06-01 (9 decisions, owner resolutions recorded; research Cited)
- **Verified:** ZERO execution. test/ contains no Precept.Conformance.Tests project; docs/contributing/ has no conformance-suite.md; no fence annotations exist in docs/language (grep expect=ok/expect=PRE → 0); the D9 clock-seam v1 runtime requirement was never recorded in docs/runtime (grep clock/IClock in evaluator.md+runtime-api.md → 0).
- **Disposition:** supersede — Locked-but-entirely-unbuilt design. Internally coherent and largely orthogonal to the soundness pivot (it tests the locked spec), but its framing is wired to the old plan's Phase 7/9/12 board, which the new plan replaces — the new plan must decide where (and whether whole) this program lands, especially given the parallel Stage-2 exhaustive audit overlaps its gap-discovery function.
- **Obligations:**
  - [VALID] The locked spec-conformance suite program itself: own-lane allowed-red Precept.Conformance.Tests; three-edge coverage (catalog↔impl, spec↔impl harvest+census, spec↔catalog reconciliation); [SpecRef]/[Covers] + coverage meta-test; feature-AC-sheet method; hand-asserted PRE####@stage negatives; fence-annotation convention + docs/language sweep; runtime facet authored as Skip'd v1 acceptance spec.
    - Design Locked with full four-leg decisions and owner resolutions (2026-06-01); grounded in research/architecture/spec-conformance-test-suite-survey.md. Carry to the new plan as a candidate program; reconcile its discovery role against the fresh Stage-2 audit (one register, not two) and re-home its Phase-7/9/12 seams onto the new board.
  - [VALID] D9 v1 runtime requirement — now() must resolve through an injectable per-operation clock (sampled once per fire), recorded in docs/runtime/evaluator.md (+ runtime-api.md). A cheap doc note implied by locked temporal Decision #9; never landed.
    - Design Decision 9 + Doc-update enumeration; grep clock/IClock over docs/runtime/evaluator.md and runtime-api.md returns nothing. Independent of whether the suite is built — it constrains the runtime build and should ride the runtime phase regardless.

## `docs/Working/conformance-suite-plan-2026-06-01.md`

- **Claimed:** Draft (Phase 0 'Active — next'; Phases 1–5 + R planned/stubbed)
- **Verified:** Phase 0 never started (no project, no infra files, no contributing doc, no fence lint, no plan slice-log row added). All exit criteria unchecked in reality.
- **Disposition:** supersede — A Draft plan with zero execution against a design that is itself being superseded into the new board. Its phasing (BusinessDomainTypes first, etc.) and its five carried open decisions are inputs to the new plan, not standing structure.
- **Obligations:**
  - [VALID] If/when the conformance program is taken up: Phase 0 infrastructure (project + lane, attributes, meta-tests, harvester, determinism fixture, contributing doc) and the area sequencing (BusinessDomainTypes → Temporal → …), plus the five carried open decisions (derived-AC triage cadence, existing-7000 relationship, agent-authoring QC, [Covers] exclusion hook, board reconciliation).
    - Plan §§ Phase summary / Open decisions / Phase 0–1 blocks. Subsumed under the design entry's primary obligation; the new readiness plan owns re-phasing.

## `docs/Working/dynamic-modifier-bounds-research-2026-06-01.md`

- **Claimed:** Research — 2026-06-01; Reconciled with §0.7 — 2026-06-02
- **Verified:** Research fully consumed downstream: D1/D2 + Q1 settled by spec §0.7 (landed 5af46537); Q2 settled by the relational-rules-and-bounds design (Locked, D2 = relational-path discharge; 2c-i SHIPPED 7ebbb68c and promoted e7faae3a); Q5/Q6/Q7 resolved as parent D5/D6/D7 inside field-reference-bound-enforcement-2026-06-04.md (Locked, re-locked 2026-06-05) and Q8 confirmed there via §3.5/§3A.1. The ENFORCEMENT build the research ultimately feeds (2c-ii/2c-iii) is still unbuilt (BUG-020 enforcement half open). Doc lives in docs/Working, not research/ — never promoted as a research artifact; cited by the consuming designs instead.
- **Disposition:** supersede — Its conclusions are absorbed into locked designs (promote-or-cite satisfied by citation), but the consuming designs are not yet built, and two of its open questions remain genuinely open — so the doc is not 'fully implemented and promoted'; it is a consumed input with residue.
- **Obligations:**
  - [VALID] Q3 residue — InvalidModifierBounds semantics for a field-reference min/max pair: fire only on a PROVABLE Floor > Ceiling from declared intervals vs never-fire-for-field-ref-pairs (downstream empty-interval catch). §0.7 ruled out silent-skip and reject-because-non-literal; the residual choice is unmade.
    - Research § Reconciliation Q3 (SHARPENED); not addressed in field-reference-bound-enforcement-2026-06-04.md (no InvalidModifierBounds decision there). Small diagnostic-policy decision; ride with the 2c-ii build.
  - [VALID] Q4 residue — vacuous (min X) and mutual (A min B + B min A) bound diagnostic policy: silently accepted today/by 2c-ii design (self-ref dropped at desugar with no diagnostic); whether a vacuous-bound warning (PRE0154 family) should fire is an explicit out-of-scope in the 2c-ii design ('the parent's Q4').
    - Research Q4 (STILL OPEN); field-reference-bound-enforcement-2026-06-04.md Decision 6 tradeoff ('flagging a vacuous self-bound is a separate diagnostic-policy question (the parent's Q4, out of scope)'). Kin to BUG-025/BUG-026 (satisfiability inspectability/severity policy) — bundle them in the new plan.
  - [VALID] The downstream build this research grounds — 2c-ii field-reference modifier bound enforcement (full family, BUG-020 enforcement half) and 2c-iii BUG-021 set-action lower-bound containment — both locked-designed, neither built.
    - relational-rules-and-bounds-plan-2026-06-02.md rows 2c-ii ('Heavyweight — gates on 2c-i AND BUG-027' — both gates now satisfied: 7ebbb68c, 210229ff) and 2c-iii (Stub); bugs.md BUG-020 enforcement-half open, BUG-021 open. Primary tracker is the relational plan (separate batch); recorded here so the chain isn't lost if that doc's triage differs.

## `docs/Working/compile-time-guarantee-statement-draft-2026-06-10.md`

- **Claimed:** Draft for discussion — NOT locked; intended, once locked, to supersede the two 2026-06-05 guarantee strawmen
- **Verified:** Never locked. Its factual claims verify: Evaluator.cs Fire/Update/Create throw NotImplementedException (runtime stubbed); Prospect enum exists at src/Precept/Runtime/Inspection.cs:6; BUG-020/021 still Active in bugs.md (prevention half incomplete as the doc states). Its framing (fault floor prove-or-reject, governance-at-result) matches the niche decision, but the parallel fault-floor run is producing the authoritative statement.
- **Disposition:** supersede — A discussion draft that was never locked; the compile-time-niche decision and the in-flight fault-floor definition run own this territory now. Internally honest but its 'supersedes-when-locked' event never happened — the new readiness plan / fault-floor statement replaces it.
- **Obligations:**
  - [VALID] Discussion Point A: settle prove-or-reject vs govern for statically-decidable-but-unproven-as-written rules (e.g. unguarded `set Balance = Balance - amount`)
    - Doc §Open discussion points A; this is exactly the line the parallel fault-floor definition run must draw — the niche decision fixed the fault floor as prove-or-reject but left the rule-preservation enforcement line open. Must be absorbed by the fault-floor run.
  - [VALID] Discussion Point B: decide whether to adopt certain/possible/impossible as guarantee vocabulary (requires /design; must reconcile runtime Prospect.Impossible at Inspection.cs:6 with compile-time graph/proof rejection — two different soundness regimes)
    - Doc §Proposed framing + Inspection.cs:6 verified (`enum Prospect { Certain, Possible, Impossible }`). Open vocabulary question, low priority, not settled anywhere in canon.
  - [VALID] Discussion Point C: specify where a value-level external violation lands — InvalidArgs vs ConstraintsFailed vs a Faulted FaultCode (QualifierMismatch/OutOfRange)
    - Doc point C, originally flagged by the 2026-06-01 clarity assessment §Q6; still unspecified in canon (runtime stubbed, evaluator.md/runtime-api.md/business-domain-types.md never map which outcome owns which violation class).
  - [VALID] Close obligation-position incompleteness — the 10 live fail-open positions (guards, messages, defaults, member-args) where fault obligations are never collected
    - Doc §Built vs promised; corroborated by bugs.md BUG-031 (faulting arithmetic in `when` guards collects NO obligation — false Proved) and BUG-032 (member-call args, `coll.at(A/B)`), both Active. Core fault-floor work — prevention half does not cover every position it claims.
  - [DROPPED (Overtaken: this triage supersedes all three docs together; the fault-floor run + new readiness plan produce the consolidated statement instead of this draft being locked.)] Consolidate the guarantee-thesis halves of the two 2026-06-05 strawmen into one locked statement
    - Doc frontmatter 'supersedes-when-locked'; lock never happened; niche decision packet (2026-06-10) now owns the guarantee shape (fault floor + flag-sound universal layer).

## `docs/Working/compile-time-prevention-features-proposal-2026-06-06.md`

- **Claimed:** Proposal / Draft — candidate proof-power features (Part 1) + enforcement-intent annotation (Part 2); not locked, opens an owner conversation
- **Verified:** Nothing built: grep for 'Karr' in src/Precept returns nothing; git log has no Karr/TVPI/affine commits; rule grammar still has no intent slot. Its verification log was probe-grounded at authoring and the cited fragment boundary (PRE0083 on `Total - A` under `rule A + B == Total`) is consistent with the still-Active relational bugs. Part 2's problem (silent prevented→governed migration) was independently confirmed by the niche packet's predictability findings.
- **Disposition:** supersede — Unbuilt proposal whose Part-2 framing predates the niche decision: the flag-soundness contract (fault floor prove-or-reject; silence beyond the floor promises nothing; per-diagnostic earned error promotion) resolves the visibility/predictability problem Part 2 attacked with per-rule annotation. Part 1's solver-free technique ranking remains valuable verified input for the new readiness plan.
- **Obligations:**
  - [VALID] Feature 1 — Karr affine-equality domain as a reduced-product component (closes the recurring 3-field balance-identity gap: `100/(Total-A)` under `rule A + B == Total` is Unresolved→PRE0083 today); gate on the ProofEngine.Intervals.cs single-pass architecture probe
    - No Karr code in src/Precept (grep verified). Serves the fault floor directly (more divisor/range discharges under prove-or-reject = fewer author rejections). Corpus demand verified (equipment-lease-agreement.precept:79). Candidate for the new readiness plan, R1/R2-compatible per research backbone.
  - [VALID] Features 2–4 — incremental numeric-aggregate deltas (extend the shipped count-delta walk to monotone `set X = X + e`), dominator-based fact propagation (reuse §0.5 dominators), DBM-emptiness guard-feasibility
    - None built (grep/git). All machinery-reusing, solver-free. Note: DBM guard-feasibility/liveness now maps onto the niche packet's Layer-2 strand/envelope analyses, which must ship under the FLAG-SOUNDNESS posture (warning-default), not prove-or-reject — the posture changed even though the technique survives.
  - [VALID] Features 5–6 — TVPI-rational (scaled 2-var relations like `SecurityDeposit <= MonthlyPayment * 3`) and congruence closure; optional tier, adopt only if a design pass confirms demand
    - Unbuilt; corpus demand real but thin (3 sample sites cited and grep-verified at authoring). Carry as demand-gated stubs in the new plan.
  - [VALID] Standing declines — convex polyhedra and predicate abstraction permanently off the interactive path (data-dependent latency breaks R2/determinism; theorem-prover step breaks R1)
    - Doc §What we explicitly decline; consistent with spec §0.6 #3 no-solver and the niche decision's solver-free scope lock. Record in the new plan so the boundary isn't re-litigated.
  - [DROPPED (Overtaken by the niche decision's flag-soundness contract: the guarantee direction was inverted (fault floor stays prove-or-reject and total; everything beyond ships as flags that are always true universal statements; silence promises nothing) — which caps the silent-degradation harm Part 2 existed to surface, without new grammar. Any annotation surface would now need a fresh Tier-2 owner conversation under the new framing; do not carry the old recommendation forward as-is.)] Part 2 — rule-level `prevented`/`governed` enforcement-intent annotation (spelling A, default candidate ii) making the §0.7 split author-declared and compiler-enforced
    - compile-time-niche-decision-packet-2026-06-10.md §Predictability assessment ('THE RESOLUTION... invert the guarantee direction... a flag is always a true universal statement; silence beyond the fault floor promises nothing'). The Part-2 honesty concern (one-character edit silently moves a rule across the line) was independently verified there (Probe33 twins) and is addressed by the contract, not annotation.

## `docs/Working/compile-time-vs-runtime-contract-clarity-2026-06-01.md`

- **Claimed:** Assessment — 2026-06-01 (clarity audit; no recommendations, documents where the compile/runtime contract lives and its tensions)
- **Verified:** Partially overtaken by canon movement since 06-01: the Restore contradiction it flagged is now resolved in runtime-api.md:266 (trusted hydration — Restore does NOT re-validate; RestoreConstraintsFailed marked 'superseded' at :270) and spec §0.7:272/§3A.4:1969 align — BUT spec §3A.2:1946 still says 'Restoring an entity in an invalid state is not allowed... constraint failure (persisted data violates current definition's rules)', so the contradiction survives inside the spec itself. The Q4/Q5 scoping silence and Q6 outcome-mapping silence remain real.
- **Disposition:** supersede — A point-in-time clarity audit whose central deliverable (the contract is scattered, never stated in one place) is exactly what the fault-floor definition run is fixing; several findings have shifted under it since 06-01. Keep as historical evidence, not active state.
- **Obligations:**
  - [VALID] Give the compile-time/runtime division of labor a single identifiable canonical home (today assembled from 7+ locations)
    - Doc §Where the contract lives table; absorbed by the parallel fault-floor definition run — the new readiness plan should make the resulting statement's canonical placement an explicit deliverable.
  - [VALID] Finish the Restore reconciliation: fix spec §3A.2:1946 (still claims restore validates constraints / 'invalid state not allowed') to match the locked trusted-hydration decision (§0.7:272, runtime-api.md:266), and remove RestoreConstraintsFailed from RestoreOutcome at API cleanup
    - Verified live: precept-language-spec.md:1946 vs :272/:1969 contradict each other today; runtime-api.md:270 self-notes 'RestoreConstraintsFailed is superseded... should be removed when the Restore API is finalized'. The niche packet caveat (3)/(8) independently demands this reconciliation.
  - [VALID] Q4/Q5: state explicitly whether 'no runtime faults / runtime checks are only defensive redundancy' (Principles 1/11) is scoped to definition-internal values and excludes externally-sourced inputs validated at the fire/update boundary
    - Still unstated in §0.1 (philosophy.md:49-55 verified unchanged); the 06-10 guarantee draft sketched the resolution (governance as the discharged precondition, §0.7 Composition) but never locked. Rides the fault-floor statement — carry as an explicit reconciliation item there.
  - [VALID] Q6: map which outcome type (InvalidArgs arg-gate vs ConstraintsFailed post-mutation vs Faulted QualifierMismatch/OutOfRange) owns each class of external-input value violation, and reconcile the evaluator.md type/presence-only arg gate with business-domain-types.md's value/qualifier boundary validation (same mechanism or two?)
    - Doc §Q6/§Q4 second silence; canon still silent; runtime is stubbed (Evaluator.cs NotImplementedException) so this must be specified before the Phase-12 runtime build. Identical to guarantee-draft point C.

## `docs/Working/field-reference-bound-enforcement-2026-06-04.md`

- **Claimed:** Locked 2026-06-04 (amended and re-locked 2026-06-05 — three soundness corrections); Slice 2c-ii, closes the enforcement half of BUG-020; gates on BUG-027
- **Verified:** Locked but NOT BUILT. bugs.md BUG-020 is still in §Active: 'undeclared-name half fixed (e2b7c9c1); Enforcement half... is the merged relational-narrowing slice' — i.e. unshipped. No LengthBandEdge/CountBandEdge/MinLengthRefField in src (grep verified); git log shows no implementation commits against ProofEngine.Lengths.cs for this design. The hard prerequisite BUG-027 IS now Fixed (commit 210209ff/210229ff, ScanRulesAgainstDefaults + PRE0164 verified in bugs.md §Fixed), so the design is fully unblocked.
- **Disposition:** supersede — Cannot archive — the content is a locked design for work that was never implemented. The design itself is high-quality, source-grounded, and its prerequisite has since landed; the new readiness plan must absorb it as buildable work (largest single unfinished obligation in this batch).
- **Obligations:**
  - [VALID] Build the field-reference bound enforcement slice (closes BUG-020 enforcement half): desugar each field-reference bound modifier to a synthetic span-tagged TypedRule at the field stamp; numeric dependent-op via 2c-i reuse; numeric default-containment riding BUG-027's ScanRulesAgainstDefaults fold (now landed — unblocked); length/count band-edge DU {Scalar|FieldRef|Unbounded} resolved at discharge via ExtractFieldInterval with the S3-soundness edge direction, empty-band contradiction guard, identity-degradation; rich message naming the referenced field; D5/D6/D7 single-emission-site reconciliation; self/mutual termination; the full test matrix incl. the cross-domain edge test
    - BUG-020 Active in bugs.md (enforcement half open); BUG-027 Fixed 2026-06-05 (the gate cleared); no band-edge code in src/Precept (grep). Squarely fault-floor work: a declared bound that does nothing is a prove-or-reject soundness hole — fully aligned with the niche decision's Layer 1. Design remains sound under the new strategy; decide at plan time whether to re-validate the lock or build as-is.
  - [VALID] The design's doc-update enumeration at execution: proof-engine.md §Strategy 8/9/10 + Obligation Generation Contract + default-satisfiability fold; diagnostic-system.md rich-message composition; spec §2.4 no-literal-only-qualifier check; business-domain-types.md:426 field-reference bound-interpretation clause (carried doc-drift from the parent design's D6); mcp.md + CompileToolDtos.cs projection check; language-server.md hover provenance/go-to-def; flip BUG-020 to Fixed
    - Doc §Doc-update enumeration — all promote-stage obligations of the unbuilt slice; business-domain-types.md:426 drift still present (the doc's own sources-consulted cite it). Travel with the build item above.

## `docs/Working/guarantee-claims-doc-audit-2026-06-02.md`

- **Claimed:** Audit — 2026-06-02 (Frank): claims/framing audit of philosophy.md, README.md, docs/runtime/ with recommended corrections
- **Verified:** Recommended corrections NOT applied: philosophy.md:49 still reads 'No errors. No bugs.' and :53 'They are compile-time impossibilities' verbatim (verified). CONTRADICTION 1 (Restore) was since resolved in canon the opposite way from both of the audit's options as written — trusted hydration (runtime-api.md:266 rewritten; RestoreConstraintsFailed marked superseded at :270) — except spec §3A.2:1946 still carries the old claim. The ambiguity/missing-qualifier findings about the headline guarantee remain factually accurate.
- **Disposition:** supersede — A claims audit whose subject matter (what the guarantee statement should say) is being wholesale rewritten by the fault-floor run; its specific wording prescriptions should not be applied as-is, and philosophy edits remain owner-gated. The still-live findings are carried as obligations.
- **Obligations:**
  - [VALID] Resolve the Restore contradiction residue: spec §3A.2:1946 vs the trusted-hydration decision; remove RestoreConstraintsFailed at Restore API cleanup
    - Same residual verified under the contract-clarity doc (precept-language-spec.md:1946 vs :272; runtime-api.md:270 self-note). The audit's 'decision item' was decided (trusted hydration) but the cleanup is incomplete — shared obligation, list once in the new plan.
  - [VALID] Reconcile philosophy.md's absolute claims ('No errors. No bugs.', 'compile-time impossibilities', unqualified 'no unproven evaluation faults') with the proof-gap/stub/external-bypass reality — owner-gated, as part of the fault-floor guarantee statement promotion
    - philosophy.md:49/:53 verified unchanged; the drift the audit documented is still live. Note: the audit's specific replacement wordings are superseded — the fault-floor statement defines the new claims; what survives is the obligation that philosophy/README/evaluator.md headline claims get reconciled to it (= the guarantee-spec strawman's OD-4, owner-only).
  - [DROPPED (Overtaken: the fault-floor reframe rewrites the headline guarantee language wholesale; applying 06-02-era qualifier patches now would encode the pre-pivot framing. The underlying drift is carried by the obligation above instead.)] Apply the audit's specific text corrections (qualifiers on evaluator.md Non-Negotiable box, README three-layer routing pointer, philosophy line edits)
    - Niche decision packet teachable line + flag-soundness contract define the replacement claims; the audit's wordings predate them.

## `docs/Working/modifier-name-axis-overlap-plan-2026-06-03.md`

- **Claimed:** Active — both slices marked ✅ Done 2026-06-03 (Slice 1 semantic retarget; Slice 2 no-overlap analyzer)
- **Verified:** FULLY IMPLEMENTED AND PROMOTED — verified against code and canon: Modifiers.cs:114-125 Notempty is StringOnly with the element-position/mincount-1 hover text; src/Precept.Analyzers/Precept0029ModifierAxisDisjoint.cs + test/Precept.Analyzers.Tests/Precept0029Tests.cs exist; doc-syncs all landed (spec :435/:1123/:1150/:1671/:1681 notempty string-only + mincount-1 discharge; collection-types.md :353/:631/:775/:793 reworded; type-checker.md :362 documents the catalog-generic BuildElementValueBounds satisfaction-shape loop, no *Kind switch; proof-engine.md :1257 count-band tracking). Companion design already archived (Archive/modifier-name-axis-overlap-2026-06-03.md). Status field 'Active' is stale.
- **Disposition:** archive — Meets the high archive bar: every plan item built, corpus/test exit criteria recorded against commits, and every doc-update obligation verified present in canonical docs. The only forward items were explicitly scoped out of this plan and are independently tracked (BUG-022). Flip the stale 'Active' status at archive time.
- **Obligations:**
  - [VALID] Broader catalog-discipline sweep — enroll surviving consumer-side modifier-*Kind switches / 'no consumer-side *Kind dispatch' analyzer (the plan's named follow-up), incl. the arm-shape coverage forward watch
    - Explicitly 'Follow-up, not this plan' in §Decisions captured; independently tracked as bugs.md BUG-022 (Active — needs a small /design for the exemption model). Not a reason to block archive; the new plan should pick BUG-022 up from bugs.md, not from this doc.

## `docs/Working/phase7-slice2-design-review-FRANK-2026-05-31.md`

- **Claimed:** Review — CONDITIONALLY APPROVED (2 conditions, 2 warnings) of price-cross-unit-cancellation-design-2026-05-31.md
- **Verified:** The reviewed design's Slice-2 build shipped (fe7d1187 verified in git; ScaleIsRational in UcumAtom.cs:23). Condition 2 (IsRatioScale timing) resolved by the plan re-scope (documented Slice-4 substrate, not built). Condition 1 (:168 'explicit' disambiguation) was added to the plan's doc-update enumeration but the actual promotion never landed — business-domain-types.md:168 still reads 'Unit conversion is explicit' unqualified. Warning 2 (the 'Proved' forward-reference must stay visible) has since hardened into BUG-030 (Active, false-Proved soundness hole) and the proof-engine.md:1790 'undecided policy' note — which contradicts the Locked design's Decision 2.
- **Disposition:** supersede — Historical review record; its subject design's promotion is incomplete and a later bug filing (BUG-030) now disputes the locked policy, so the content is not 'fully implemented and promoted'. When in doubt, supersede.
- **Obligations:**
  - [VALID] Condition 1 — promote the business-domain-types.md:168 disambiguation ('explicit' = visible-to-inspection/traceable, not manual conversion syntax)
    - business-domain-types.md:168 verified unchanged today. Carried jointly with the phase7-slice2-plan promotion obligation — list once.
  - [VALID] Warning 2 — keep the compile-time-'Proved'-without-runtime-factor forward reference visible until the runtime applies the factor
    - Now formalized as bugs.md BUG-030 (Active, 'a false Proved... anchor validating case'). BUT: BUG-030 and proof-engine.md:1790 both describe the cancellation policy as 'genuinely undecided' while price-cross-unit-cancellation-design-2026-05-31.md is Locked on allow-all-with-surfacing (Decision 2, owner-affirmed). The new readiness plan must reconcile this drift — either the bug/doc note credits the locked decision and narrows to 'factor application unbuilt', or the lock is being deliberately re-opened (owner call).

## `docs/Working/phase7-slice2-plan-2026-05-31.md`

- **Claimed:** Active — Phase 1 ✅ Done fe7d1187; Stage-5 promotion of D8/§168 + evaluator.md runtime requirement still pending; angle/log split to Slices 5/6; runtime factor application pinned to Phase 12
- **Verified:** Phase 1 build verified (commit fe7d1187 exists: 'exact cross-unit conversion surfacing + ScaleIsRational guard'; UcumAtom.cs ScaleIsRational shipped; LS hover files present). The pending items verified still pending: business-domain-types.md:168 un-disambiguated; evaluator.md contains NO cross-unit reduction-rule requirement (grep for reduction rule/ScaleToBase/denominator-unit negative) — so the slice's claimed 'durable carrier' of the runtime obligation was never written. The runtime gap is now also tracked as BUG-030 (Active). proof-engine.md:1790 still labels the policy 'undecided' (drift vs the Locked design).
- **Disposition:** supersede — Built but NOT promoted — fails the archive bar on its own recorded terms ('Stage-5 promotion... still pending'), and its most important deliverable (the evaluator.md runtime requirement doc) never landed. Superseding without extracting these would lose the runtime reduction-rule requirement, whose only carriers today are this doc, the locked design, and BUG-030.
- **Obligations:**
  - [VALID] Write the runtime reduction-rule requirement into docs/runtime/evaluator.md as a pending obligation: on `price × quantity` with same-dimension different-unit operands, convert by exact-rational k = ScaleToBase(u_q)/ScaleToBase(u_p) in decimal, target-directed to the price denominator unit (D8 rule 1), rounding at maxplaces; k=1 for same-unit
    - Plan §Runtime requirement + §Doc-update obligations; grep of evaluator.md confirms absent. This was the slice's explicit runtime deliverable ('the doc — not a tracked execution phase — is the durable carrier') and it is missing — highest-priority extraction from this doc.
  - [VALID] Stage-5 promotion of D8 extension + allow-all-with-surfacing + absolute-position exclusion + the :168 'explicit' disambiguation into business-domain-types.md
    - Plan phase row: 'Stage-5 promotion of D8/§168... still pending'; business-domain-types.md:168 verified unchanged. Also resolves half of the proof-engine.md:1790 'undecided policy' drift.
  - [VALID] Phase-12 runtime build: actually apply the conversion factor in the evaluator; un-skip/extend CrossUnit test with the magnitude assertion (4.00 USD/ft × 36 in → 12.00 USD); flip evaluator.md to Implemented; closes BUG-030
    - Evaluator stubbed (NotImplementedException verified); BUG-030 Active tracks the same false-Proved; the new readiness plan's runtime phase must own this.
  - [VALID] Slice 5 — give angle units (deg/rad/gon) a dimension identity so they cancel per the locked design (currently rejected PRE0114 via empty dimension name, a recorded divergence from the locked design)
    - Plan §Enumeration findings: 'a divergence from the locked design (which says allow + surface) → split to its own slice'. Never executed (no later slice-5 commits in git log).
  - [VALID] Slice 6 — design ruling on log-unit (dB/Np) cross-unit handling: the catalog holds no real dB↔Np factor (lg/ln stripped at UcumAtomCatalog.cs:466) and the field's libraries forbid multiplying log units; allow-with-what vs restrict needs deciding before executing
    - Plan §Enumeration findings ('gated on a design ruling — needs deciding, not executing'). Open language-policy decision; ScaleIsRational=false guard shipped as the interim soundness floor.
  - [VALID] Reconcile BUG-030 + proof-engine.md:1790 ('policy genuinely undecided') with the Locked Decision 2 (allow-all-with-surfacing, owner-affirmed 2026-05-31)
    - Verified contradiction: proof-engine.md:1790 'Whether such pairs should reject... auto-convert... is an undecided policy' vs price-cross-unit-cancellation-design-2026-05-31.md Locked Decisions 1–2 and the FRANK review approval. The 06-07 soundness enumeration filed BUG-030 cold without crediting the lock — the new plan must resolve which is authoritative.

## `docs/Working/precept-guarantee-specification-2026-06-05.md`

- **Claimed:** Draft — strawman for owner reaction, NOT locked; philosophy-adjacent
- **Verified:** Never locked; explicitly slated for supersession by the 06-10 guarantee draft (which itself never locked) and predates the niche decision. Its delivered-today table verifies: BUG-021 still Active (the silent-pass it cites), runtime evaluator still stubbed, BUG-027/PRE0164 born-valid check confirmed Fixed in bugs.md. Its three-way prevented/rejected/governed authoring contract and fragment-boundary enumeration are pre-pivot framing.
- **Disposition:** supersede — A guarantee-thesis strawman the strategy moved past twice (06-10 draft, then the niche decision's fault-floor + flag-sound-universal-layer shape). Task background explicitly classifies these strawmen as supersede candidates; do not adopt its conclusions as current strategy.
- **Obligations:**
  - [VALID] Wire FlowNarrowing into the IntervalContainment (assignment-range) obligation so a guard/relation discharges it the way it discharges a divisor (`when Balance >= Withdraw.Amount` ⟹ `Balance − Amount ≥ 0`) — verified at authoring that the correct guard does NOT discharge PRE0078 today
    - Doc §5/§6 item 1, probe-verified 2026-06-05; no subsequent fix commits found; fault-floor proof-power work (the author is otherwise stuck between reject and the BUG-021 silent-pass). Re-probe at plan time.
  - [VALID] Close BUG-021 — min-only fields get no lower-bound IntervalContainment obligation on set-actions (provably-below-min compiles clean)
    - bugs.md BUG-021 verified still in §Active. Core fault-floor breach (Principle-10/11).
  - [VALID] Semantic completability — per-(state,event,guard) DBM feasibility, witness-or-decline
    - Unbuilt (graph analyzer structural-only). Survives but REFRAMED: the niche decision places strand/envelope/liveness work in Layer 2 under the flag-soundness contract (warning-default, earned promotion), not prove-or-reject; the strawman's posture is superseded, the analysis is not.
  - [VALID] OD-1 — lock that the runtime evaluator and the compile-time ConstantFold share one expression-evaluation core (guarantee drift prevention)
    - Doc §8 OD-1 + static-vs-runtime-expression-evaluation-survey. Runtime still stubbed, so this is a design constraint the Phase-12 runtime build must inherit — cheap to lose, expensive to retrofit.
  - [VALID] OD-4 — owner-only reconciliation of philosophy.md's absolute phrasing with the delivered/over-approximate state
    - philosophy.md:49-53 verified unchanged; same item carried under the guarantee-claims audit. Rides the fault-floor statement promotion; list once.
  - [DROPPED (Settled/overtaken by the niche decision: the public promise is the flag-soundness contract, not a fragment enumeration (fragment edges may grow; flags only ever added), and severity is warning-default with per-diagnostic earned error promotion. The Karr/octagon extension question survives separately via the prevention-features proposal.)] OD-2 (where to declare the data-fragment edge) and OD-3 (severity policy derived from guarantee class)
    - Niche packet §Predictability assessment + §Right-sized scope Layer 2.
  - [DROPPED (Overtaken: the niche decision's three-layer shape + teachable line is the adopted guarantee structure; the strawman's contract (incl. 'reducible → reject with restructure suggestion') was never owner-locked and partially conflicts with the flag-sound posture (forced restructuring is a blanket-mandate shape the cost evidence rejected).)] Adopt the §4 three-way prevented/rejected/governed authoring contract as the product guarantee
    - Niche packet §Right-sized scope + §Expressiveness ('several capabilities are cheap ONLY in warning/obligation form; blanket prove-or-reject flips punitive').

## `docs/Working/proven-core-governed-boundary-2026-06-05.md`

- **Claimed:** Design-exploration brief — NOT a locked design, NOT a syntax proposal; feeds a /design pass
- **Verified:** Never locked, never fed a /design pass (no downstream design doc exists in Working or Archive for ingress-refinement/maintained-aggregates — verified by directory listing). Explicitly named by the 06-10 guarantee draft as a doc it would supersede. Its architecture (ingress checks establish proof facts the compiler propagates) was not adopted by the niche decision, which resolved the proven/governed split differently.
- **Disposition:** supersede — Pre-pivot exploration: the niche decision settled the guarantee architecture as fault-floor prove-or-reject + flag-sound universal layer + governance-at-the-result — not refinement-establishment-at-ingress. Internally coherent but its framing predates the pivot; classic supersede per the task background.
- **Obligations:**
  - [DROPPED (Overtaken by the niche decision: the adopted shape keeps governance at the result (atomic sweep) and gets universal claims from the flag-sound compile layer; the ingress-refinement architecture was never adopted, never designed, and would now be a fresh strategy proposal requiring owner conversation — do not resurrect from this doc.)] The keystone — ingress validation establishes proven refinements propagated to the interior (governance feeds the proof), with the refinement-scope/staleness rule
    - compile-time-niche-decision-packet-2026-06-10.md §Right-sized scope (three layers; none is ingress-refinement); the 06-10 guarantee draft also models governance at-the-result per §3A.4, not fact-establishing ingress.
  - [VALID] Maintained aggregates — compiler-owned incremental sum/count fields making `sum(lines) ≤ budget`-class rules expressible and provable (incremental view maintenance, not general reduce)
    - Independently re-validated by the niche packet: 'collection aggregation (the most-demanded expressiveness gap, mattering under either option)'. `sum` remains a locked rejection as a function (collection-types.md:860, PRE0030 probe-verified in the prevention proposal), so the maintained-aggregate construct is the live candidate shape. New language surface → Tier-2 owner conversation + /design gate; carry as a future candidate, not committed work.
  - [VALID] Conserved pools / transfer-only mutation (`A+B+C == Total` by construction); exclusive-interval collections; refinement-typed elements
    - Unaddressed expressiveness candidates with thinner evidence than aggregates (the 3-field balance identity recurs in 4 corpus precepts per fragment-boundary-corpus-validation). Carry as speculative future-candidate stubs only — each gated on owner conversation; note Karr (prevention proposal Feature 1) addresses the same balance-identity demand without new surface, so sequence Karr first.
  - [VALID] Interval multiplication for bounded products (`X·Y ≤ c` with both operands bounded) — standard interval arithmetic, no boundary machinery
    - Doc §4 table row; not built (no product-interval discharge in ProofEngine); a small solver-free proof-power candidate that needs no language surface — fold into the new plan's proof-power candidate list alongside Karr/TVPI.
  - [VALID] §6.5 — extend the relational record from strict-DBM (one field per side) to native octagon two-field sums; fix guarantee-spec §7's overstatement
    - The overstatement fix is moot (both strawmen superseded together), but the DBM→octagon two-field-sum extension itself remains a real, unbuilt proof-power item adjacent to the locked relational design lineage — record it in the candidate list with that lineage as the canonical owner.

## `docs/Working/phase8-slice1-emission-inventory-2026-05-31.md`

- **Claimed:** Phase 8 Slice 1 — read-only emission inventory & classification, complete 2026-05-31
- **Verified:** The inventory itself was completed and consumed by the Slice 2/3 designs (Slice 2 shipped 59fe2666; 3a f7e5dee6; 3b ecce5d51). But the design it exists to ground (Slice 3c ownership analyzer) is NOT in HEAD, and the inventory's counts are now stale (PRE0163/PRE0164/PRE0136 and the relational work added/changed emission sites since 2026-05-31).
- **Disposition:** supersede — An investigation doc whose primary consumer (the Slice 3 ownership design) is itself unfinished (3c analyzer unbuilt); its site counts and dual-emission table are stale against HEAD. Fails the archive bar (content not fully implemented downstream).
- **Obligations:**
  - [VALID] Emit-or-retire the ~18 spec-declared compiler diagnostics that have no emission path (the §6 spec/implementation gap; the Phase 9 'diagnostic completeness' charter). The list must be re-derived — it is stale against HEAD.
    - doc §6; the 148-vs-162 DiagnosticCode discrepancy; consult DiagnosticCoverageScanner Patterns 1/2/3 when re-deriving (literal grep is incomplete)
  - [VALID] NullInNonNullableContext (the one confirmed future-O): wire it through the existing proof-stage PresenceProofRequirement/UnprovedPresenceRequirement channel or retire it as a dup. Verified still unwired: zero grep hits in src/Precept/Pipeline/.
    - doc §6 'O — future obligation surface'; grep 2026-06-11 confirms no pipeline emission
  - [VALID] NonAssociativeComparison category/stage label tension (TypeSystem category on a Parse-stage code) — parked for Phase 9 diagnostic-identity.
    - Diagnostics.cs:103 still DiagnosticStage.Parse + DiagnosticCategory.TypeSystem; Parser.Expressions.cs:416 emits it
  - [DROPPED (Done — Slice 3a (f7e5dee6) consolidated all six (NoInitialState→Graph, CircularComputedField + Undeclared* family→Bind) and deleted the GraphAnalyzer.cs:85 dedup guard.)] Consolidate the 6 cross-stage dual-emissions (§5).
    - commit f7e5dee6; plan doc Slice 3a Outcome (6648 tests green)
  - [DROPPED (Done — Slice 3b (ecce5d51) relocated both; BUG-017 residual closed 9820b8c1.)] Relocate the 2 wired Class-O checks (OutOfRange, assignment-qualifier residual) to the proof stage.
    - commits ecce5d51, 9820b8c1; archive commit 7b8dfa89 message

## `docs/Working/phase8-slice2-dispatch-deepdive-2026-06-01.md`

- **Claimed:** Active — investigation grounding Phase 8 Slice 2 (code-mediation standardization) and Slice 3 (ownership architecture)
- **Verified:** Its §4 standardization recommendation shipped as Slice 2 (59fe2666: string round-trip killed, CreateDiagnostic reads ProofRequirementMeta.DiagnosticCode, fault map derived from [StaticallyPreventable], CI field pair collapsed) and that design is already archived (7b8dfa89). Its Tier-3 recommendation (the ownership analyzer as first member of a catalog↔DiagnosticCode consistency analyzer family) is NOT built — analyzers stop at Precept0029.
- **Disposition:** supersede — Half its conclusions shipped and were promoted via the archived Slice 2 design; the other half (ownership analyzer family, F-CAT-01) is unbuilt. Mixed state fails the archive bar.
- **Obligations:**
  - [VALID] The ownership/stage-consistency analyzer over DiagnosticCoverageScanner (Tier 3: 'keep adding invariants on that one scanner' — stage-ownership, [StaticallyPreventable] consistency, emit-or-retire).
    - doc §7 Tier 3; no DiagnosticStageOwnership analyzer in src/Precept.Analyzers/ (files stop at Precept0029ModifierAxisDisjoint); confirmed unbuilt by archive commit 7b8dfa89 message
  - [VALID] F-CAT-01 catalog audit (Phase 11) — the pending broad catalog-discipline audit the doc cites as the backstop for the controlled *Kind-switch smell.
    - doc §7 Tier 1; related live gap: BUG-022 (consumer-side catalog *Kind switches not enforced exhaustive, Active in bugs.md, needs a small /design for the Precept0019 exemption model)
  - [DROPPED (Done — all four delivered in Slice 2 (59fe2666), byte-identical verified, design archived with canon synced (diagnostic-system.md 'Emission shapes').)] Finish the half-done mediations: C string round-trip, D dual-surface re-hardcoding, B paired-field collapse, derive the DiagnosticCode→FaultCode map from [StaticallyPreventable].
    - commit 59fe2666; archive commit 7b8dfa89; plan doc 'Completed slices' §Slice 2

## `docs/Working/phase8-slice3-emission-ownership-2026-05-31.md`

- **Claimed:** Locked 2026-06-01 (amended — D6 added; D4 expanded to 6 duals)
- **Verified:** Partially delivered: D2 honest taxonomy + D4 dual consolidation shipped in 3a (f7e5dee6); D3/D5 value-level proof-ownership shipped in 3b (ecce5d51) with BUG-017 closed (9820b8c1); D6 stands (MaxPlacesExceeded still Type-owned, no precision ProofRequirement). D1 — the headline deliverable, the catalog-declared single-stage-ownership Roslyn analyzer (Slice 3c) — is NOT in HEAD. Archive commit 7b8dfa89 explicitly declined to archive this doc for exactly that reason.
- **Disposition:** supersede — The Locked design's central guarantee (D1: every DiagnosticCode owned by exactly one stage, analyzer-enforced, zero allow-list) is unbuilt. The new readiness plan must absorb 3c or explicitly drop it; the doc cannot be archived as implemented.
- **Obligations:**
  - [VALID] Build the Slice 3c ownership analyzer (D1): every Diagnostics.Create site's containing stage equals Diagnostics.GetMeta(code).Stage; literal + catalog-mediated + bounded Pattern-2 residue sites; target one documented carve-out (TypedConditional PRE0141). Compiler-internal hygiene, not fault-floor, but nothing in the niche decision invalidates it.
    - design D1 + Inventory; 7b8dfa89 commit message: 'the 3c single-stage-ownership Roslyn analyzer is not yet in HEAD (analyzers stop at Precept0029)'
  - [VALID] Resolve the PRE0141 bound-expression ownership open decision before 3c (walk bounds / split the code / accept carve-out / static-only bounds). Note: relational sub-slice 2c-ii (field-ref bounds desugar to rules participating in proof) will likely resolve this as a side effect — settle the interaction rather than deciding independently.
    - slices2-3 plan § Open decisions ('GATES Slice 3c'); relational plan 2c-ii scope (bounds-as-rules per §2.4 line 1112)
  - [VALID] D6 known gap: maxplaces enforcement points 2–3 (event-arg ingress + arithmetic-result-at-set) are runtime work — unimplemented because the runtime is a stub (Phase 12).
    - design D6 'Known gap'; business-domain-types.md:1581-1590; runtime Fire/Update are NotImplementedException per the contract-grounding doc
  - [VALID] Optional Slice 5 (maxplaces compile-time precision proof: decimal-scale domain + PrecisionContainmentProofRequirement) which would supersede D6 and make maxplaces prevention-not-detection.
    - design D6 'Superseded-by (if built)' — recorded option, never committed; the new plan should decide build-or-drop
  - [VALID] Doc-update items tied to the unshipped 3c: diagnostic-system.md ownership-invariant + analyzer-roster entries in docs/compiler/README.md.
    - design § Doc-update enumeration (the 3a/3b doc-syncs landed with their slices; the analyzer items ride with 3c)
  - [DROPPED (Done — delivered in 3a (f7e5dee6) and 3b (ecce5d51); doc-syncs landed same-pass per the plan's outcome notes.)] D2/D3/D4/D5 deliverables (Bind/Tooling taxonomy, relabels, proof-walk extension, dual consolidation, OutOfRange kept proof-owned, hover decouple).
    - commits f7e5dee6, ecce5d51, 9820b8c1; plan doc Slice 3a/3b Outcome blocks

## `docs/Working/phase8-slices2-3-execution-plan-2026-06-01.md`

- **Claimed:** Active — Slice 2 ✅, 3a ✅, 3b ✅, 3b-count 'Next', 3c stub
- **Verified:** Slices 2/3a/3b verified complete (59fe2666, f7e5dee6, ecce5d51). 3b-count was delivered in substance via the relational plan's 2b-ii instead (BUG-018, c27a382b, Reading-A prove-or-reject; TryCountContainmentProof now implemented at ProofEngine.Lengths.cs:333, CountBoundViolation live/Gate-2; design archived 95d05035) — note the 'notempty-on-collection as mincount 1' piece was overtaken by the notempty→string-only axis decision (cceab68e/c3209ad2). 3c is NOT built; the Definition of Done is unmet.
- **Disposition:** supersede — Tracking doc for a workstream whose definition-of-done (ownership analyzer green, no dual-stage emission enforced) is not met; remaining work transfers to the new readiness plan.
- **Obligations:**
  - [VALID] Slice 3c ownership analyzer + the PRE0141 bound-ownership gating decision (see slice3 design row — same obligation, single absorption).
    - plan § Lightweight stubs / § Open decisions; analyzer absent from src/Precept.Analyzers/
  - [VALID] OutOfRange consolidate-or-retire vs NumericOverflow/UnprovedModifierRequirement — owned by Phase 9 / F-LANG-SPEC-12 emit-or-retire; land in the new plan's diagnostic-completeness phase.
    - plan § Open decisions; design D5
  - [VALID] Slice 4 (witness richness — builds on the unified proof emission point) was gated on this workstream and never started. Still valid in principle, but must be re-scoped against the soundness pivot: the verify-don't-trust/witness-checker architecture was deleted 2026-06-08, and bugs.md BUG-030 still references 'the witness-checker' as its validating consumer — the new plan must restate what witness work survives.
    - plan § Scope gate ('unblocks Slice 4'); deleted docs in 9d54e958 working-tree deletions; bugs.md BUG-030 Status line
  - [DROPPED (Delivered via relational 2b-ii / BUG-018 (c27a382b): obligations on every mutation + default list literals, CountBoundViolation live in Gate-2; the notempty-on-collection sub-item was overtaken by the locked notempty→string-only decision.)] 3b-count (count-containment for collection defaults + revive CountBoundViolation).
    - bugs.md BUG-018 Fixed entry; ProofEngine.Lengths.cs:333; spec §0.6 line 258; commits c27a382b, cceab68e, c3209ad2, 95d05035

## `docs/Working/price-cross-unit-cancellation-2026-05-31.md`

- **Claimed:** Investigation — superseded by the Locked design; 'the hole is currently LIVE'; decision made (auto-convert)
- **Verified:** Its decision (auto-convert within dimension, allow-all-with-surfacing) was locked in the companion design and the compile-time surfacing shipped (fe7d1187: ScaleIsRational flag, hover conversion surfacing, un-skipped CrossUnit test). But the substantive hole it documents — the conversion factor is never actually applied — is still live and was re-filed 2026-06-07 as BUG-030 (Active, soundness). The design's promotion to canon never happened (d210b2b6: 'Stage-5 promotion pending').
- **Disposition:** supersede — Self-declares superseded by the design, but the design itself cannot be archived (promotion pending, factor application unbuilt, policy re-contested by BUG-030), so the evidence base rides with it into Superseded/ rather than claiming a finished lineage.
- **Obligations:**
  - [VALID] The cross-unit scale-factor hole itself: a same-dimension/different-unit price×quantity cancels with the UCUM factor dropped (false Proved under the §0.7 prove-or-reject lens). Now tracked as BUG-030 (Active).
    - bugs.md BUG-030; proof-engine.md:1790 'Known gap — cross-unit cancellation … unsound for cross-unit operands'
  - [VALID] Phase 7 Slice 4: absolute-position measurement values (point-type construct — thermostat readings, dBm, absolute pH). New language surface; Tier-2/3 consultation gate applies.
    - doc § Decision & scope correction; design § Out of scope; commit d2a90b26
  - [DROPPED (Done — the companion design was produced, reviewed (precept-reviewer + Frank), revised by owner ruling, and Locked 2026-05-31 (0eb8c4f3).)] Run /design on the cancellation policy.
    - commits d66c790d, 974d65cb, 0eb8c4f3

## `docs/Working/price-cross-unit-cancellation-design-2026-05-31.md`

- **Claimed:** Locked 2026-05-31 — auto-convert within dimension; Decision 2 allow-all-with-surfacing
- **Verified:** Compile-time scope shipped in fe7d1187 (ScaleIsRational catalog flag verified in src/Precept/Language/Ucum/UcumAtom.cs + UcumAtomCatalog.cs; hover surfaces '×1/12 (exact)'; PRE0114 reworded; CrossUnit_SameDimension test un-skipped and asserting no PRE0114). BUT: (1) Stage-5 promotion is PENDING (d210b2b6) — business-domain-types.md was never updated with the auto-convert/price extension or the :168 clarification, and proof-engine.md:1790 still records the policy as 'undecided' and the cancellation as 'unsound'; (2) the runtime factor application (the design's load-bearing reduction-rule obligation) is unbuilt; (3) BUG-030 (2026-06-07) re-frames the current compile-time accept as a live false-Proved and mis-states the policy as 'genuinely undecided' — directly contradicting this Locked design.
- **Disposition:** supersede — Fails the archive bar on both axes: not fully implemented (factor application unbuilt; the prove-or-reject reframe contests the 'Proved as forward-reference' tradeoff) and not promoted (canon still says undecided/unsound). The new plan must explicitly reconcile this locked decision with BUG-030 — this is the single biggest drift found in the batch.
- **Obligations:**
  - [VALID] RECONCILE: the locked auto-convert policy vs BUG-030's 'policy genuinely undecided' framing and proof-engine.md:1790's 'undecided' note. Either promote the locked decision to canon (business-domain-types § price/D8 + the :168 'conversion is explicit→visible-in-inspection' clarification, fix proof-engine.md:1790) and fix BUG-030 within it (carry/apply the factor), or have the owner explicitly re-open the decision under the §0.7 fault-floor lens. Do not let two contradictory records stand.
    - design status Locked 2026-05-31 (0eb8c4f3); proof-engine.md:1790; bugs.md BUG-030 'the doc records the policy as genuinely undecided'; business-domain-types.md has no auto-convert price text (grep 2026-06-11); d210b2b6 'Stage-5 promotion pending'
  - [VALID] Runtime evaluation obligation: the evaluator must look up the exact-rational factor(u_q→u_p) and apply it in decimal (the design's reduction rule). Unbuilt — the runtime is a stub. Under the niche decision's fault floor this is no longer dischargeable as 'documented forward reference': it is BUG-030.
    - design § Semantic Rules (reduction rule); PriceTimesQuantityTests.cs:173-180 comment ('the runtime application of the factor is the documented evaluator obligation'); bugs.md BUG-030
  - [VALID] Count-pair price-path gap: 'USD/each' × quantity in 'box' cancels silently because PRE0137 is gated on both-operands-quantity (TypeChecker.Expressions.cs:1351 family) — the design's acceptance criterion 'count still rejects' is NOT met on the price path. Pinned by characterization test CrossCountingUnit_PricePath_CurrentlyCancelsSilently_KnownGap.
    - fe7d1187 commit message 'Audit § E4 … cancels silently'; spec-conformance-audit E4 CONFIRMED; BUG-030 repro includes each×box
  - [VALID] Design doc-update enumeration residue: business-domain-types.md (price/D8 + :168), proof-engine.md QualifierChain side-condition framing, evaluator.md reduction-rule entry — verify each at promotion (catalog-system.md, language-server.md, diagnostic-system.md items landed with fe7d1187).
    - fe7d1187 doc-sync list covers catalog-system/language-server/diagnostic-system/proof-engine-reword only; business-domain-types git log shows no post-2026-05-31 commit
  - [DROPPED (Delivered in fe7d1187 (hover shows '[in_i] → [ft_i] ×1/12 (exact)' / '(approximate)'); standing falsifier, not unfinished work.)] Falsifier watch: hover must keep surfacing conversion + exact/approximate status (P8 visibility).
    - fe7d1187 commit message; docs/tooling/language-server.md § conversion surfacing

## `docs/Working/proof-engine-contract-grounding-2026-06-02.md`

- **Claimed:** Grounding — 2026-06-02 (read-only empirical verification of the prove-or-reject contract per fault class)
- **Verified:** Fully consumed: all three breaches it found were filed and are now Fixed — BUG-017 (9820b8c1), BUG-018 (c27a382b), BUG-019 (4e4e2961 + element-modifier closure 4a003b64/fcbaaf71). Its ingress/Restore findings fed the contract plan's Phases 3–4 which all landed (9fa6a16c, e7c1255f). One internal conclusion ('Restore re-validates is the correct target') was subsequently FLIPPED by owner decision D4 (Restore = trusted hydration), now recorded in spec §0.7 and runtime-api.md:270.
- **Disposition:** archive — An evidence record whose every finding landed: breaches fixed in code, doc framings propagated to canon, the contract single-sourced at spec §0.7. The one superseded internal conclusion (Restore) is corrected in canon, which is exactly what archive (reference-only) is for. Meets the high bar.
- **Obligations:**
  - [VALID] Runtime ingress enforcement of declared value-level constraints (governance, contract Part 2): Create is spike-level, Fire/Update/FromJson/Restore are NotImplementedException — governance remains an unbuilt design commitment (runtime phase / Phase 12). The target (enforce declared value-level constraints per business-domain-types, not just type/presence) is recorded in canon.
    - doc § Ingress enforcement (Precept.cs:74-122, Evaluator.cs:59-157, PreceptValue.cs:18-22); spec §0.7 carries the governance contract
  - [DROPPED (Overtaken by owner decision D4 (2026-06-02): Restore = trusted hydration, no re-validation; runtime-api corrected; RestoreConstraintsFailed marked superseded in canon.)] Restore re-validation recommendation ('re-confirm once the executable model lands; implement re-validates').
    - guarantee-contract plan D4; docs/runtime/runtime-api.md:270 supersession note
  - [DROPPED (All fixed: BUG-017 (9820b8c1), BUG-018 (c27a382b), BUG-019 (4e4e2961 + 4a003b64/fcbaaf71); verified in bugs.md Fixed section and spec §0.6 lines 249/258.)] The three contract breaches (BUG-017 computed-unbounded, count containment, length non-literal).
    - bugs.md BUG-017/018/019 Fixed entries with commit hashes

## `docs/Working/proof-engine-guarantee-contract-plan-2026-06-02.md`

- **Claimed:** Complete 2026-06-02 — all 4 phases landed (d4367254, 5af46537, c4a50887, 9fa6a16c, 63e08963, e7c1255f)
- **Verified:** Verified complete and promoted: spec §0.7 'The Compile-Time and Runtime Guarantee Contract' exists (spec line 33 TOC + §0.7 body); the owner-approved philosophy carries-proof insertion is live at philosophy.md:55; Phase-3 runtime-doc tightening landed (9fa6a16c); completion marked e1c0a959. The contract (prove-or-reject fault floor) is consistent with — indeed is the substrate of — the compile-time-niche decision; the parallel fault-floor-definition run may amend §0.7 but that is new work, not this plan's.
- **Disposition:** archive — All four phases verifiably landed in canon with commit evidence; the contract is single-sourced exactly as the plan's definition-of-done required. Meets the high archive bar.
- **Obligations:**
  - [VALID] Remove the superseded RestoreConstraintsFailed outcome at Restore-API finalization (RestoreOutcome may keep parse/state-resolution failures). Still present in code and example.
    - src/Precept/Runtime/RestoreOutcome.cs:18; runtime-api.md:261+270 (the supersession note itself defers removal to API finalization)
  - [DROPPED (Done — verified in canon (spec §0.7, philosophy.md:55, runtime-api/fault-system tightening 9fa6a16c, README pointer e7c1255f).)] All Phase 1–4 doc deliverables (canonical contract, composition statement, runtime-doc tightening, philosophy insertion, README pointer).
    - commits 5af46537, 9fa6a16c, e7c1255f, e1c0a959; grep of spec/philosophy 2026-06-11

## `docs/Working/relational-rules-and-bounds-plan-2026-06-02.md`

- **Claimed:** Active — 1a/2a/2b-i/2b-ii/2c-i ✅; 2c re-sequenced: BUG-027 → 2c-ii design correction → 2c-ii build → 2c-iii → Promote
- **Verified:** 1a (e2b7c9c1), 2a (9820b8c1), 2b-i, 2b-ii (c27a382b), 2c-i (7ebbb68c build / e7faae3a promote) all verified in git. The BUG-027 prerequisite is now ALSO done (210229ff, bugs.md Fixed). Outstanding: 2c-ii (field-ref bound enforcement — its first locked design field-reference-bound-enforcement-2026-06-04.md was found unsound by adversarial review and awaits the /design correction + build), 2c-iii (BUG-021), and the Promote-stage doc-syncs. BUG-020 enforcement half, BUG-021, BUG-025, BUG-026 all still Active in bugs.md.
- **Disposition:** supersede — Live plan with substantial unfinished, owner-prioritized soundness work (declared field-ref bounds still silently unenforced = BUG-020). Squarely fault-floor (prove-or-reject per §0.7) — fully aligned with the current strategy, so everything outstanding transfers to the new readiness plan.
- **Obligations:**
  - [VALID] Sub-slice 2c-ii: field-reference modifier bounds enforced (BUG-020 enforcement half) — desugar 'min Floor' to a synthetic relational rule at PopulateRules (§2.4 line 1112), lift the literal-only TryGetComparableModifierValue gate, rich author-facing message. The first locked design was UNSOUND (numeric-default mechanism + length/count integer-vs-string misread) — must re-lock via /design before build. Prerequisite BUG-027 is now fixed (210229ff), unblocking it.
    - plan § Execution progress 2c bullet + § 2c-ii stub; bugs.md BUG-020 'Enforcement half … is the merged relational-narrowing slice' (Active); commit 210229ff
  - [VALID] Sub-slice 2c-iii: BUG-021 set-action lower-bound containment (min/nonnegative/positive direction creates no obligation on a min-only field; provably-below-min compiles clean). Build-time confirm: match PRE0078 vs split PRE0079.
    - bugs.md BUG-021 (Active, no Fixed status); plan § 2c-iii stub
  - [VALID] Promote-stage doc-syncs: business-domain-types line 426 (bound-interpretation → field-references, D6), spec §3.8 InvalidModifierBounds (field-ref pairs fire only when provably contradictory), plus lifting D2/D4 R-NARROW-* mechanism residue into proof-engine.md/compiler-and-runtime-design.md beyond what e7faae3a covered.
    - plan § Promote stage; e7faae3a promoted the 2c-i cluster only
  - [VALID] BUG-025 (relational contradictions invisible to the satisfiability scan — no warning/attribution; inspectability) and BUG-026 (Error-vs-Warning severity policy for provable contradictions — needs /design). Both filed by this workstream, both Active.
    - bugs.md BUG-025/BUG-026 Active entries
  - [VALID] Completeness gaps logged for later slices: (a) min-bounded field divisor over-rejected (PRE0083 — min/nonzero → divisor-lower-bound fold not wired for the divisor obligation; safe direction); (b) quantifier-binding sign-set discharge; (c) notempty-in-element open decision (string elements only, post axis-overlap); (d) BUG-028/BUG-029 deferred follow-ons from BUG-027 (default-fold coarseness/short-circuit; AST-dump diagnostics).
    - plan § Discovered during execution (D20 note); collection-inner-type plan deferred items; bugs.md BUG-028/029 Active
  - [DROPPED (Done and verified — commits e2b7c9c1, 9820b8c1, 4e4e2961, c27a382b, 7ebbb68c/e7faae3a, 4a003b64/fcbaaf71; bugs.md shows all Fixed.)] Slices 1a/2a/2b-i/2b-ii/2c-i (BUG-017/018/019/020-undeclared/023/024 closures, relational narrowing core, element-type length modifiers).
    - plan § Execution progress; bugs.md Fixed entries; git log

## `docs/Working/review-skill-reshape-2026-06-07.md`

- **Claimed:** Locked 2026-06-07 (owner-approved; dev-tooling skill design)
- **Verified:** Fully implemented in f2b852ed: .claude/skills/review/SKILL.md (305 lines) carries all designed sections (## Dispatch with clause-ordered parse rule incl. clause-5 promote, ## Branches ×6, ## Result fold with degrade-gracefully fallback, ## Collision: intentional subsumption, ## Migration, mode propagation, dispatcher guards — all verified by grep). CONTRIBUTING.md rows 16/19 updated with the umbrella description + subsumption note (the design's full doc-update enumeration). The live skill description in the current session matches the design.
- **Disposition:** archive — Dev-tooling design, fully implemented and promoted to its canonical homes (the SKILL.md itself + CONTRIBUTING.md) in one commit. Not on the compiler readiness plan by its own frontmatter. Meets the high archive bar.
- **Obligations:**
  - [VALID] Decision 5's execution gate: capture one real /code-review output sample and specify the dedup key/severity map against the OBSERVED schema before relying on the merge path (the shipped skill text carries the section-don't-merge fallback, so the branch is correct either way — this is a verify-when-first-used check, not a blocker).
    - design Decision 5 + Falsifier 8; SKILL.md:87 carries the fallback
  - [VALID] Deferred-to-future items: /plan-emitted lifecycle-stage frontmatter marker (robust plan-vs-design inference), independent high/ultra effort dials, /audit ownership of committed-canonical drift.
    - design § Deferred to future — explicitly deferred by the lock, dev-tooling backlog, not compiler-readiness work
  - [DROPPED (Done — f2b852ed touched all six skill files, CLAUDE.md, CONTRIBUTING.md; completion check preserved verbatim per the commit message.)] The reshape itself + CONTRIBUTING sync + cross-ref sweep of sibling skills.
    - commit f2b852ed stat + message; CONTRIBUTING.md:16,19; SKILL.md section greps

## `docs/Working/sample-corpus-expansion-plan.md`

- **Claimed:** Status (Updated 2026-05-25) — 32 new samples authored; Phase 5 (catalog coverage showcase) added with 4 specs
- **Verified:** samples/ now holds 77 .precept files — the ~60 target was exceeded and Phases 1–4 shipped in substance (with recorded filename drift; Phase-4 samples tax-rate-configuration/product-catalog/unit-of-measure-reference/currency-exchange-rates all present). The 4 Phase-5 catalog-showcase samples (priority-incident-queue, regulatory-audit-log, sensor-measurement-aggregation, regulatory-submission-package) were NEVER authored — none exist in samples/. The doc's strategy framing (workflow-governance pivot, Salesforce/ServiceNow positioning, per-sample agent workflow with named reviewers) predates the entire Phase-7/8 readiness era and the soundness/niche pivots.
- **Disposition:** supersede — Stale 1,426-line plan whose corpus goal was met by other routes and whose framing is two pivots old; only one concrete deliverable cluster remains unshipped. Nowhere near the archive bar.
- **Obligations:**
  - [VALID] Phase 5 catalog-coverage showcase: the catalog members it targets had ZERO idiomatic corpus uses (QueueBy/enqueueBy/dequeueBy, LogBy/appendBy, and the math-function surface: approximate, floor, ceil, clamp, sqrt, pow). The 4 samples were never authored. Still valid as a catalog-coverage/conformance asset — but RE-VERIFY the zero-use claims against the now-77-file corpus before authoring (the grep evidence is from 2026-05-25), and fold into whatever sample/conformance phase the new plan defines.
    - plan §Phase 5 (lines 1148-1296); ls samples/ 2026-06-11 — none of the 4 filenames exist; corpus-wide 0 uses of appendBy/enqueueBy recorded at line 1010
  - [VALID] Phase 1 drift deferrals: §1.x specs whose primary value was catalog showcase (e.g. Clinical Trial §1.7, Product Recall §1.9 exercising appendBy/Irreversible) were re-prioritized into Phase 5 and ride with the obligation above; existing samples may adopt appendBy/enqueueBy idioms in a future quality sweep.
    - plan § Phase 1 Implementation Drift (line 979-1016)
  - [DROPPED (Overtaken/met — 77 samples exist; the corpus also became the proof-engine soundness corpus (SampleCompilesCleanTests) which is now the operative quality bar, replacing this plan's per-sample reviewer workflow.)] Phases 1–4 sample authoring to ~60 files.
    - ls samples/ | wc -l = 77; relational plan's corpus falsifier discipline

## `docs/Working/spec-conformance-audit-2026-05-30.md`

- **Claimed:** Active — candidate register; first seed of Phase 7 (total language conformance sweep)
- **Verified:** Phase 7 executed only Slices 1–2 (A1 price×quantity resolution b7c17482; cross-unit surfacing fe7d1187) before pivoting to Phase 8 (86e9dc13). Probed live 2026-06-11: A2 exchangerate slash form STILL fails (PRE0076; Types.cs:684 still ships the 'to' UsageExample — the 2026-05-30 owner ruling 'SLASH is canonical, remove to-form' was never implemented); A3 date+'30 days' still PRE0056; A4 'kg/hour' still PRE0075; B1 collection-in-interpolation still compiles clean. B4 (currency in 'USD') now errors (parse-level), so at least one B row changed under later work — the unverified B rows need re-probing. E1 partially mitigated by ScaleIsRational (fe7d1187); E2/E3 routed to never-executed Slices 5/6; E4 confirmed open (now inside BUG-030's repro family). Spec §0.6 items 7–10 STILL marked 'Specification-only' though PRE0153-0155/PRE0082 fire (D1 doc-drift confirmed live).
- **Disposition:** supersede — A live finding register with many verified-open rows; the parallel Stage-2 exhaustive spec-vs-impl audit is its direct replacement. Everything open must land in the new register/plan — this doc held the only record of several owner rulings.
- **Obligations:**
  - [VALID] A2 — OWNER-RULED, UNBUILT: exchangerate slash form is canonical ('USD/EUR' parses, split from/to; remove the 'to' form; update Types.cs:604-era UsageExample; sweep business-domain-types:178, collection-types:602 and to-form tests). Do not lose this ruling — it exists only in this register.
    - doc A2 OWNER RULING 2026-05-30; probe 2026-06-11: 'USD/EUR' → PRE0076; src/Precept/Language/Types.cs:684 still "exchangerate in 'USD' to 'EUR'"
  - [VALID] A3 (date ± '<n> days' typed-constant quantity → PRE0056) and A4 (time-denominator compound unit 'kg/hour' → PRE0075, despite business-domain-types:1137 mandating NodaTime denominator vocabulary) — both probed still broken.
    - probes 2026-06-11 (PRE0056, PRE0075); spec cites temporal-type-system.md:439-443, business-domain-types.md:128,1086,1137
  - [VALID] B-class enforcement gaps (B1–B9, spec-says-error-compiler-accepts): B1 re-confirmed open (collection in interpolation compiles clean); B4 appears changed (currency in 'USD' now errors); B2/B3/B5–B9 unverified — each row must be re-probed and dispositioned in the new audit.
    - probes 2026-06-11; doc §B (all rows were 'unverified')
  - [VALID] C-class diagnostic-identity gaps (C1 PRE0073 DurationDenominatorMismatch never fires, generic PRE0114; C2 DenominatorUnitMismatch/CompoundPeriodDenominator; C3 CollectionOperationOnScalar/ScalarOperationOnCollection possible swap; C4 ~15 temporal teachable-message drifts) — route with the A-class root-cause check or Phase-9 emit-or-retire.
    - doc §C and § Phase routing
  - [VALID] D-class doc-stale rows: D1 verified STILL live (spec §0.6 lines 241-244 mark items 7-10 'Specification-only' though PRE0153-0155/PRE0082 are implemented and firing); D2 (collection-types quantifier predicates 'Not yet implemented'), D3 (PRE0160-0162 'TBD'), D4 (temporal doc phantom code spellings) — re-verify and fix in a doc sweep.
    - precept-language-spec.md:241-244 grep 2026-06-11; bugs.md BUG-026 confirms the codes fire as warnings
  - [VALID] E-class unit-scale honesty: E2 log units' stripped lg/ln functions leave meaningless scales (Slice 6 design ruling needed); E3 angle units resolve to empty dimension → PRE0114 even same-unit, diverging from the locked allow+surface design (Slice 5); E4 price-path count-mismatch (subsumed by BUG-030 fix scope); E1 residual — interval proofs still read [pi]-approximated scales (ScaleIsRational now surfaces it; decide whether surfacing suffices under §0.7).
    - doc §E (all CONFIRMED by code read + probe); fe7d1187 covers only the surfacing flag; Slices 5/6 never executed (git log phase-7 ends at fe7d1187→86e9dc13 pivot)
  - [VALID] Audit self-coverage gaps: spec §1 lexer, §2.7 parser, §3.10 diagnostic catalog, §3A semantics never audited; the 162-code emits-from-a-real-path check.
    - doc § Coverage gaps; Stage-2 task in flight
  - [DROPPED (Fixed in Phase 7 Slice 1 (b7c17482, TryProjectUnitToDimension at four sites); its deliberately-left-open cross-unit caveat is now BUG-030.)] A1 price×quantity headline resolution.
    - doc A1 row 'FIXED (Phase 7 Slice 1)'; commit b7c17482

