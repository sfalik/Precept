# Verification Ledger — want-statement analysis

**Status**: Draft analysis - 2026-07-19 (agent-authored; evidence only; rulings remain the owner's)

Adversarial verification of every load-bearing claim from the six axis reports. Each verifier received the claim and its citations only (not the analyst's reasoning) and was prompted to refute. Verdicts: confirmed = citations check out and no counter-evidence found; refuted = counter-evidence found (shown); unverifiable = citations do not support the claim as stated.

**Totals:** confirmed: 80, refuted: 9, unverifiable: 1

## Axis: scope

### scope-1 — CONFIRMED (high)

**Claim:** The want doc adds an obligation family absent from every v3 slice: rules proven as invariants (preservation obligations at every write site of every field a rule mentions), while the v3 companions verify live that rule-preservation write-site checks are not generated today and belong to Phase 5 work outside v3's seven slices.

**Verifier:** Every citation checks out at the exact cited location, and an active hunt for counter-evidence failed. (1) Want doc: docs/Working/what-i-want-2026-07-16.md:20 reads verbatim "**Symmetric obligations.** A rule's obligations attach to every write site of *every* field the rule mentions"; :97 is the ReduceLimit bullet ("the symmetric write site... lowering OverdraftLimit threatens it even though Balance is untouched"), reinforced by :108 ("this write site carries the obligation too"). Lines 14-18 frame this as proof by induction (base case + every-handler-preserves-every-rule), matching "rules proven as invariants." (2) MVP companion: proof-engine-mvp-and-proof-phases-2026-07-12.md:76 contains the quoted text (claim uses em-dashes where the source uses parentheses; content identical) with a "verified by compile" annotation in the same paragraph; :273 reads "verified live, the compiler generates zero checks for that `nonnegative`" — exact. Lines 96, 473, 500 independently confirm "Those sites only begin rejecting once Phase 5 lands obligation generation," placing obligation generation in Phase 5 outside the MVP. (3) Plan: compiler-readiness-plan-2026-07-12.md:84-92 is a seven-row slice table (Slice 0, §3 money, §2 case-by-case, §4a witness, §1a single-fact, §6 constant-rule→bounds, structural-severity); no row generates rule-preservation obligations, and the plan contains no "Phase 5" (grep: zero hits — it is "one phase"). Closest counter-evidence candidate examined and rejected: the §6 slice (plan :91, :360-367) teaches IntervalContainment to CONSUME field-vs-constant rules as narrowing facts inside BuildNarrowedIntervals — premise-side use for discharging existing obligations, not generation of new write-site obligations; ruling #6 (:115) concerns existing bounded-write (min/max) checks becoming three-way, also not rule-obligation generation. Empirical confirmation via precept_compile: a precept with `rule Balance >= 0 - OverdraftLimit` plus an unguarded `set Balance = Balance - Withdraw.Amount` and an unguarded `set OverdraftLimit = ReduceLimit.NewLimit` compiles success:true, zero diagnostics, and the ONLY proof obligation in the entire file is the default-value Literal check on OverdraftLimit's nonnegative — the rule generates no preservation obligation at any write site of either mentioned field, exactly as the claim states. Minor precision note (not refuting): the two "verified live" citations (:76, :273) measure `nonnegative` modifier obligations rather than literal `rule` statements, but the empirical test above closes that gap for literal rules, and the want doc (line 22) treats modifiers as sugar for rules.

### scope-2 — REFUTED (high)

**Claim:** The want doc's inductive step (premise class d, 'all rules holding in the pre-state') requires combining at least two premises per proof — demonstrated by its own Deposit and OpenAccount examples — which is the fact-combination capability v3's ledger ruling #3 explicitly deferred ('build nothing that requires it', spec:256 override not authorized) and v3's Definition of Done fences out.

**Verifier:** The citations are accurate as quotations (want doc :19 four premise classes; :94-95 as described; ledger :51 'DEFER 1b... spec:256 override: NOT authorized — build nothing that requires it'; MVP doc :53 'machinery that adds inequalities together'; plan :532-533 DoD item 7 '1b (multi-fact)... not built'). But the claim's load-bearing identification — that the two-premise combination in the want doc's examples IS the fact-combination capability ledger #3 deferred — is contradicted by the deferring docs' own definition of that capability, on three grounds. (1) The 1a/1b boundary is drawn at combining two STATED relational facts (rules/guards), not at using two premises of any kind. proof-engine-mvp-and-proof-phases-2026-07-12.md:125 defines the boundary explicitly: 'This is exactly what separates 1b from 1a: 1a lets the engine read one richer fact and apply it once; here there is no one fact to apply, because the safety argument does not exist until two rules are combined,' and :102 defines 1b as 'deriving a bound that no single stated fact gives' (e.g., :113-114, two authored rules chained through a shared field). The want doc's Deposit proof uses ONE stated relational fact (the pre-state rule, premise class d) plus an arg modifier interval (Amount >= 0) — a single stated fact carries the safety argument; no two rules are chained; no third field is transitively chased through a second fact (spec:256's ban), since OverdraftLimit appears in the rule itself. Note also that v3's own in-scope 1a machinery already combines a stated fact with declared interval bounds (:55 classifies all guard-sum sites as single-fact 1a shapes even though their min-side discharge needs the guard fact plus field/arg intervals; :76 shows raw ranges always participate), so 'two premises' per se is not what DoD item 7 fences out — only multi-STATED-fact chaining is. (2) OpenAccount (want :94) is worse for the claim: it combines two arg interval bounds with zero stated relational facts — pure interval-containment reasoning of the already-shipped class (MVP doc :35-38 lists shipped interval/field-vs-field machinery) — and it is explicitly the BASE case using premise class (b), not the inductive step or premise class (d) the claim attributes it to. (3) 'Requires combining at least two premises per proof' is false as a universal even within the want doc's own examples: Withdraw (:96, 'the guard is the proof' — one premise) and ReduceLimit (:97, 'the proof closes by substitution' under the guard alone). Independent corroboration: the contemporaneous in-repo review of the same want doc, frank-review-what-i-want-2026-07-16-opinion.md:157-169, analyzes exactly the Deposit-class linear inductive fragment ('discharge by linear arithmetic over the collected facts including the IH... The BankAccount example is depth-1 linear and closes exactly this way') and classifies it as 'a genuine extension of the built one-field-vs-one-field engine and of 1a (multi-term linear facts), but... the *same class of reasoning*, and the... single-hop discipline from soundness §7 already applies' — i.e., NOT the deferred Farkas/simplex 1b solver; that review's only 1b mention (:88) is as part of the ratified package's stopping rule, and it locates the want doc's genuinely new requirement elsewhere (M1: inductive rule-preservation OBLIGATIONS — premise class (d) sourcing, base case, symmetric write sites — plus M2 certificate checker). A true kernel survives: the want doc does require machinery v3's plan does not build (rule-preservation obligations with the inductive hypothesis as a premise — v3's proof model has no such obligation class), and DoD item 7 does fence out 1b. But the claim as stated pins the gap on the deferred 1b fact-combination capability 'demonstrated by' Deposit and OpenAccount, and neither example demonstrates that capability under the deferring documents' own taxonomy. Files checked: docs/Working/what-i-want-2026-07-16.md (full), proof-engine-decision-ledger-2026-07-12.md:30-66, proof-engine-mvp-and-proof-phases-2026-07-12.md:35-143, compiler-readiness-plan-2026-07-12.md:515-533, frank-review-what-i-want-2026-07-16-opinion.md, frank-final-review-mvp-phases-and-1b-opinion-2026-07-12.md.

### scope-3 — CONFIRMED (high)

**Claim:** The want doc reverses v3's Decision B: the certificate checker moves from retracted-from-MVP format-only (no live re-checker, no checker-gated mint; §5b deferred as builder-side assurance) to a required live checker with two call sites, one of which — the runtime load-time gate that refuses unverified definitions — was never scoped in any prior document.

**Verifier:** Every citation checks out at the cited lines: want-doc:189-192 states 'One small checker component, two call sites' with the runtime load-time gate that 'refuses to govern under a definition whose certificate does not verify'; architecture.md:45 states the MVP ships 'no live re-checker module, no always-on production replay, and no checker-gated mint'; :50 accepts the uncaught-certificate-corrupting-bug tradeoff 'until §5b lands'; phases doc :280 grades §5b Large ('a second checking implementation'), :283 frames its value as builder-side, :285 makes only §5a (format) the prerequisite, :503 rates §5b 'Recommended but deferrable'; ledger:107 records Decision B retracted under the owner rulings of 2026-07-12. Counter-evidence hunt for prior scoping of the runtime load-time gate came up empty: docs/runtime/*.md contain zero certificate mentions; the checker research survey (witness-checking-soundness-architecture-survey.md:191) places the checker only as a post-proof compile-pipeline stage; the retracted B1/B2 fork (architecture:192) was about compile-time mint gating vs staged replay, not runtime load; and pre-want canon pointed the opposite direction — spec §0.7 (via devils-advocate-2026-07-14.md:160,170) has restored state 'trusted as valid at persistence time,' neither ingress nor sweep at load, i.e. trusted hydration with no verification gate. The runtime load-time certificate gate first appears in the want doc.

### scope-4 — CONFIRMED (high)

**Claim:** The want doc defines runtime scope the prior attempts deliberately excluded: evaluation-time work is declared premises only (arg and editable-write ingress, guards) with fault checks omitted and no post-mutation rule re-evaluation, plus the certificate premise-list becoming the runtime's derived validation surface — where v3 scoped no runtime and the meta-plan kept the runtime spec-only.

**Verifier:** Every citation checks out against the files, and a counter-evidence hunt found nothing that breaks the claim. Checked: (1) what-i-want-2026-07-16.md:153 — verbatim "fault-prevention checks are omitted at evaluation time... What runs at runtime are the declared premises (ingress validation of args and editable fields, guards) that the compiler's proofs depend on, and nothing beyond them" — supports "evaluation-time work is declared premises only... fault checks omitted"; (2) :187 — "the runtime does not re-evaluate them against the post-mutation configuration" — supports "no post-mutation rule re-evaluation" (the same sentence's qualifier "does not blindly trust the compiler either" is the certificate load-time check, which the claim's certificate clause already incorporates); (3) :198 — "the premise list is the runtime's validation surface — which guards and constraints must run and can never be skipped — derived from the proof, never maintained in parallel" — supports the derived-validation-surface component; (4) :129-131 — "Ingress is exactly two points: event args... and editable-field writes. Restore/rehydration is not ingress — data coming back from persistence is trusted and unchecked" — supports two-point ingress/restore-trusted; (5) compiler-readiness-meta-plan-2026-07-13.md:970-972 — verbatim "The runtime is spec-only by design... This plan does not build the evaluator" — supports "meta-plan kept the runtime spec-only" as deliberate; (6) "v3 scoped no runtime" verified against compiler-readiness-plan-2026-07-12.md — it is the MVP proof-engine build plan (title/purpose, lines 1-22), contains no runtime/evaluator slices, and lines 167-171 explicitly push runtime gates (GATE-I fault delivery etc.) to post-MVP separate initiatives; the §5b re-checker is likewise deferred (line 164). Counter-evidence probe: want doc lines 147-151/159 give the runtime additional duties (inspect, versioning, business-process respect, "not an exhaustive list") — but these are outside evaluation-time premise checking, and the claim correctly scopes its "only" to evaluation-time work, matching line 153's own "at evaluation time" qualifier. No misquote, wrong line, or missing qualifier found.

### scope-5 — CONFIRMED (high)

**Claim:** The missing-premise computation ('computes the guard or arg constraint that would close the proof and shows it') moves from explicitly deferred in the prior attempt (§4b derived safe-condition printing, graded Large, verdict Defer; only the numbers-plus-example half shipped in v3's §4a) to demanded core behavior in the want doc, whose entire what-must-not-compile table consists of missing-premise messages.

**Verifier:** Checked every citation at the cited locations. (1) what-i-want-2026-07-16.md:167 verbatim demands "computes the guard or arg constraint that would close the proof and shows it" with a worked example — exactly the derived-safe-condition capability. (2) The mutation table at :105-117 exists, and :103 frames every row as requiring "a teachable message naming the missing premise" (also :24). Minor caveat: rows 115-117 are structural/graph-analyzer rejections and row 114 a default counterexample, so "entire table consists of missing-premise messages" slightly overstates — but it mirrors the doc's own line-103 framing, not a misreading. (3) proof-engine-mvp-and-proof-phases-2026-07-12.md:242 verbatim grades "Exact safe condition — genuinely new... Large on its own"; :504 verbatim shows Phase 5 §4b "Defer — the Phase-1 numbers-and-example half covers most of the need"; :489 and :499 corroborate (§4b = ergonomic upgrade; Phase 1 slots only §4a numbers-and-example). (4) Cross-checked compiler-readiness-plan-2026-07-12.md (v3): §4a witness is slotted (lines 89, 344, 505, 517, 553-557); §4b/derived safe-condition is absent entirely. One doc-wide qualifier worth noting: the want doc is explicitly ideal-world with feasibility as follow-on work (:123, :171, :177), so "demanded" is demand-as-want, not ratified-buildable — this applies to the whole doc, not this claim specifically, and does not contradict the claimed movement from Defer to demanded.

### scope-6 — CONFIRMED (medium)

**Claim:** The want doc mandates rejection of dead rows (guards that can never be true given declared constraints including rules, with unreachability propagated to consequent states), conflicting with the 2026-07-12 structural-severity ruling that keeps UnsatisfiableGuard, AlwaysRejecting, and TautologicalGuard at Warning; v3's only dead-row Error (ledger #10) covers provably-always-violating writes, a different defect.

**Verifier:** Checked all four citations verbatim: what-i-want-2026-07-16.md:143 ("Dead rows are structural defects too... The ideal compiler rejects it") and :117 (dead-row mutation inside the :103 "must be a compile-time rejection" table, guard-contradicts-rule with unreachability propagated to Closed); compiler-readiness-plan-2026-07-12.md:139-140 keeps UnsatisfiableGuard, AlwaysRejecting (transition rows), and TautologicalGuard at Warning, backed by compiler-readiness-plan-2026-07-12-structural-severity.md:104-105/:323-325 ("a dead guard row is inert") with no revisit/flip-later qualifier found; proof-engine-decision-ledger-2026-07-12.md:65 ruling #10 scopes Error to provably-always-violating writes on reachable rows and explicitly says "the row being effectively dead is a consequence, not the classification" — a different defect from never-true guards. Counter-evidence hunt strengthened the claim: ledger #6 (:61) classifies "dead rows" among violation reports (non-blocking), and want doc :177 calls "warnings where there should be errors" dilution. Only nuance: AlwaysRejecting stays Warning for transition rows only (construction rows were already Error, structural-severity doc :96-97) — the cited plan line carries that qualifier; it does not undercut the conflict as stated.

### scope-7 — CONFIRMED (medium)

**Claim:** The want doc's modifiers-are-sugar-for-rules ruling imposes an architectural unification (one constraint mechanism underneath) that v3's engine design does not carry — v3 keeps declared bounds and rules as separate fact sources with separate folds, which is the asymmetry the §6 slice exists to patch.

**Verifier:** Checked: (1) want doc :22 verbatim match ("one constraint mechanism underneath — the premise classes above are spellings, not separate systems") and :131 ("however they were spelled, as modifiers or as rule statements"); (2) ledger:37 matches the claim's paraphrase exactly (IntervalContainment reads field-to-field rules but not field-vs-constant; `rule Source <= 100` ignored → PRE0078); (3) architecture §1.6 (lines 88-94) specifies the §6 fix as "a new fold ... beside the shipped relational fold" whose rationale is "closes that asymmetry", and the locked single-hop rail (line 92) forbids ExtractFieldInterval from folding constant rules — declared bounds stay a separate "bare declared interval" source even post-§6, so v3 patches the symptom without unifying the mechanism. Counter-evidence hunt: grep of all three v3 docs for sugar/desugar/unify/DesugarsToRule/relational-rules-and-bounds found zero hits — the desugar-based unification exists only in the superseded 2026-06-11 plan and superseded designs, not v3. Empirical confirmation via precept_compile at HEAD: `set Total = Cap` with `Cap ... max 100.0` (modifier spelling) → obligation Proved, interval capped at 100.0; identical constraint as `rule Cap <= 100.0` → obligation Unresolved, interval [−∞..+∞], PRE0078 emitted. The engine treats the two spellings of the same constraint differently, live. Minor precision note, not a defect: §1.6 names the patched asymmetry as relational-vs-constant rule shapes, and §6 patches by adding a parallel fold rather than unifying — which strengthens, not weakens, the claim's "does not carry the unification" thesis.

### scope-8 — CONFIRMED (medium)

**Claim:** The want doc retires two framings folded into v3's ratified ruling #1 — the per-proof PROVEN/GOVERNED author-facing visibility surface and the fault-scoped reading of 'no deferral' — replacing them with a uniform guarantee (no author-facing tiers) and no-deferral widened to all business rules.

**Verifier:** All three citations check out verbatim. (1) want doc :159 states no-deferral quantified over business rules ("A business rule whose enforcement the compiler cannot prove complete is rejected — never handed to the runtime"), genuinely wider than the fault-scoped R1 Reading N (proof-engine-boundary-ruling-2026-07-06.md:238-242, which scoped "no deferral" to fault prevention only and explicitly did NOT prohibit runtime governance of rules). (2) want doc :161 states the uniform guarantee with "no author-facing distinction between 'compile-time-checked rule' and 'runtime-governed rule,' no tiers of trustworthiness" — directly incompatible with Q9's author-facing per-obligation PROVEN/GOVERNED disposition surface (boundary-ruling :196-199, rendered on precept_proofs + LS hover); the :161 qualifier "mechanisms differ and are visible" concerns machinery visibility, not a per-rule trust label, and does not restore the surface. (3) ledger :24 verbatim folds both framings ("'no deferral' read as fault-scoped (R1 Reading N, ruling :238); the per-proof PROVEN/GOVERNED visibility surface (Q9, :196)") into ruling #1, ratified at :25 and marked "input to the readiness-plan rebuild" (v3). Counter-evidence hunt: grepped the want doc for any retained PROVEN/GOVERNED/tier surface (none — :199 inspect-provenance is mechanism visibility, not tiers); checked post-want reviews — Frank's opinion (frank-review-what-i-want-2026-07-16-opinion.md:66-142) independently corroborates the delta (relational invariants move from "GOVERNED, not proven" to "Proven inductively, or rejected"; F11 at :468 notes the want doc CLOSES the folded-items-ratified-by-adjacency issue). No document preserves either framing against the want doc. Minor note only: "v3's ratified ruling #1" is shorthand — the ruling lives in the 2026-07-12 decision ledger that feeds the v3 plan — but this does not change the claim's substance.

### scope-9 — CONFIRMED (medium)

**Claim:** Nothing in v3's build list is removed by the want doc: all seven v3 slices (Slice 0 verdict/certificate/soundness fixes, §3 money, §2 case-by-case, §4a witness, §1a single-fact, §6 constant-rule, structural-severity) survive as substrate for the want's premise classes, and the ratified rulings on prove-or-reject, the certificate criterion, and the no-escape-hatch stand unchanged.

**Verifier:** All citations check out at the cited lines: the seven-slice table is at compiler-readiness-plan-2026-07-12.md:86-92; prove-or-reject and the four premise classes at what-i-want-2026-07-16.md:14-24; the guarantee-non-negotiable criterion at :177; the proof-carrying-code/small-trusted-kernel precedent at :201 (consistent with ledger #2's admissibility parts 1 and 3); and rulings #1, #2, #9 at proof-engine-decision-ledger-2026-07-12.md:25, :34, :64 exactly as characterized. Counter-evidence hunt: read the full want doc (209 lines), grepped the plan for want-doc reconciliation (none — plan predates the want doc), scanned the adversarial review doc. Each slice is affirmatively required or supported by the want doc: §3 money by the PlanRepayment division (:98, :112), §2 by guards-as-premises (:96), §4a by the must-not-compile table's teachable messages (:103-117), §6 by modifiers-are-sugar-for-rules (:22), §1a as the authored-fact substrate for premise class (d) (:19), Slice 0's three-way verdict/certificate by :107-117 and :187, structural-severity by :135-143. No passage removes a slice, softens prove-or-reject (:14, :159 reaffirm it), reintroduces an escape hatch (:167 re-argues against one), or replaces the certificate criterion. Two non-refuting nuances: the want doc EXTENDS prove-or-reject beyond ruling #1's folded fault-scoped rider (all business rules, no post-mutation re-evaluation at :187) — an extension, not a reversal; and the want's one-checker-two-call-sites (:189-192) goes beyond v3 architecture Decision B's MVP re-checker retraction — but the claim names only rulings #1/#2/#9, not Decision B, and this adds work rather than removing any.

### scope-10 — CONFIRMED (medium)

**Claim:** The want doc changes the acceptance frame from v3's corpus-calibrated bar ('turn prove-or-reject on without rejecting the corpus's own house style') to a rejection-side operational contract: every premise-deletion mutation in its table must fail to compile with a teachable message, a test class v3's Definition of Done does not contain.

**Verifier:** All four citations verify verbatim at the cited lines. what-i-want-2026-07-16.md:103 literally requires each mutation row to be "a compile-time rejection with a teachable message naming the missing premise" (table at :105-117), and :121 calls the table "the promise in operational form" with the exact quoted sentence. compiler-readiness-plan-2026-07-12.md:80 contains the exact corpus-house-style framing, and :512-534 is the DoD structured as capability-green (item 1) + corpus-green (items 4, 6). Counter-evidence hunt: grepped the entire v3 plan for mutation/premise-deletion/teachable/negative-test/must-fail-to-compile/deletion-row — zero hits; examined v3's actual rejection-side tests (fail-open hole regressions at :268-271, conservative-reject exit criteria at :328, "rejection message shape" pointer at :350) and confirmed they are a different class (unproven-input-rejects-conservatively soundness fixes), not premise-deletion mutations of an accepted exemplar with message-content requirements. Caveat: v3's DoD does contain rejection-side tests (item 3), so the claim holds only under its stated scoping to the specific premise-deletion-with-teachable-message test class — which is accurate as written.

### scope-11 — CONFIRMED (medium)

**Claim:** The want doc's live checker materially alters the meta-plan's stated discharge-soundness residual and its owner decision point ('is soundness-modulo-a-searched-residual an acceptable handoff bar?'): certificate replay is the independent confirmation whose absence created the residual, shrinking it without eliminating it, and the want's obligation model also widens the meta-plan's prevention denominator beyond faultable positions to rule-preservation and establishment obligations.

**Verifier:** Checked every citation verbatim and hunted for counter-evidence; the claim held. (1) meta-plan:976-981 says exactly what is claimed: the discharge-arithmetic false-Prove residual "stays open because there is no executable discharge oracle to test verdicts against," certifying "soundness modulo a searched residual, not soundness outright." (2) meta-plan:1000-1005 is the owner decision point verbatim: "Is soundness-modulo-a-searched-residual an acceptable handoff bar?" — closable only when "a discharge oracle exists to test against." (3) The :53-57 citation is slightly imprecise — those lines are the glossary entry for "faultable operation" ("Every faultable operation is the thing an obligation gets attached to, wherever it appears as a faultable position"), not the denominator table — but the actual denominator definition at meta-plan:106 (Denominator B, "Prevention guarantee") confirms the claim's substance verbatim: creation-completeness = "every faultable position carries an obligation." So the prevention denominator IS defined over faultable positions. (4) want:189-194 supports "checker replays every step": line 194 — "the checker simply walks those steps and confirms each one. Every step is re-verified"; line 191 — the compiler-stage checker "audits the proof engine's work... catching proof-engine bugs," i.e., exactly the false-Prove class the residual names. (5) want:18-20 states establishment ("every initial event *establishes* every rule") and preservation ("Every handler *preserves* every rule") obligations plus symmetric write-site attachment — obligations not anchored to faultable positions, so the denominator-widening half holds; want:98 further confirms "Business rules are not the only obligations: faults... get the identical premise-and-certificate treatment," and the memory-recorded measured fact that `rule` currently emits no write-site obligations corroborates these are new denominator entries. Counter-evidence hunt: the meta-plan does mention certificates (lines 731-732, 744, 866) but only as the v1/v3 certificate FORMAT substrate — no live re-checker; its stated residual mitigations remain property-based testing + behavioral hunt, and the residual is stated open. So the want's mandatory two-call-site checker is genuinely new relative to the meta-plan, supporting "materially alters." The claim's hedge "shrinking it without eliminating it" is consistent with both docs: the checker independently confirms derivations but is not the ground-truth executable oracle (runtime evaluation) whose absence the meta-plan cites, and the want nowhere claims the checker closes the false-Prove residual outright. Only caveat worth recording: the :53-57 cite should more precisely be :106 (Denominator B row); the glossary lines cited still state the obligation-to-faultable-position attachment, so this is imprecision, not misreading.

## Axis: feasibility

### feasibility-1 — CONFIRMED (high)

**Claim:** The compiler today accepts 7 of the 11 mutations the want doc says must not compile (rows 1, 2, 3, 4, 5, 7, 11 all compile with success:true and zero diagnostics), so the rule-obligation machinery is essentially the entire measured distance to the want.

**Verifier:** Checked both citations and re-ran every probe live. (1) Want doc: docs/Working/what-i-want-2026-07-16.md § What must not compile (lines 101-121) contains exactly 11 mutation rows, each of which "must be a compile-time rejection" — rows 1,2,3,4,5,7,11 are the ones the claim names, matching the table verbatim. (2) Feasibility doc: docs/Working/want-analysis-2026-07-19/feasibility.md § 2.3 table and § 2.4 score state exactly what the claim states (7 of 11 clean-accept; rows 6,8 rejected; rows 9,10 warning-only). (3) Empirical reproduction via precept_compile (2026-07-19, git status clean at HEAD): I extracted the worked example verbatim (want doc lines 30-90), confirmed the baseline matches § 2.2 exactly (success:true, 0 diagnostics, the same 3 Proved obligations — divisor non-zero + two default checks, none referencing the rule Balance >= -OverdraftLimit), then applied each of the 7 mutations one at a time against the original: all 7 (delete Withdraw guard, delete ReduceLimit guard, remove Deposit Amount nonnegative, remove OpeningBalance min 0.0, remove OpenAccount OverdraftLimit-arg nonnegative, remove PlanRepayment guard, change CloseAccount guard to Balance < -OverdraftLimit) returned success:true with diagnosticCount:0 and unchanged Proved obligations. Spot-checked row 9 (unreachable Suspended): success:true with 1 PRE0080 warning, confirming the 7-vs-2-vs-2 partition is real, not an artifact of row selection. Two harmless nuances, neither refuting: (a) strictly the compiler *accepts* 9 of 11 (rows 9,10 also return success:true, just with warnings) — the claim's parenthetical explicitly scopes its 7 to "success:true and zero diagnostics," which is accurate, and the stronger fact only reinforces the distance conclusion; (b) row 7's gap is technically a fault-family fail-open hole (no obligation minted for the nonnegative write) rather than rule-preservation machinery per se, but under the want doc's own framing ("Modifiers are sugar for rules," line 22) it is the same obligation class, and the claim's hedge "essentially" also covers the residual non-rule work (trivial Warning→Error severity flips for rows 9/10). The interpretive tail mirrors the cited doc's own § 2.4 and Bottom line ("the rule-obligation machinery is the whole distance, not a refinement"). No counter-evidence found.

### feasibility-2 — CONFIRMED (high)

**Claim:** No new proof theory is required: every obligation class in the want doc falls in decidable quantifier-free linear arithmetic plus sign reasoning on a loop-free, function-free, non-recursive surface, with direct precedent in Event-B per-event invariant-preservation obligations, SPARK/Liquid-Haskell linear discharge, proof-carrying code, and DRAT-style checkable certificates.

**Verifier:** Every citation checks out. (1) feasibility.md § 1.2 (lines 40-47) states the claim nearly verbatim ("Every obligation class the want doc generates falls inside fragments that have been decidable and industrially practiced for decades") and grounds each element: loop-free/function-free/non-recursive surface confirmed in the want doc (line 125: "not Turing-complete, no loops, no functions") and spec §0.4/:158 ("No loops... eliminates the need for fixpoint computation") and §0.8/:294 ("No iteration constructs"); Event-B per-event invariant-preservation named in § 1.2 bullet 2 and independently grounded in research/philosophy/formal-spec-languages-comparators.md (Event-B "discharges a deductive proof obligation per event that invariants are preserved"). (2) proof-engine-linear-solver-feasibility-2026-07-12.md § 2 contains the "Prior art" paragraph (line 91) citing SPARK/GNATprove and Liquid Haskell linear-arithmetic discharge. (3) certificate-steps-membership-2026-07-12.md § Language Design Grounding (lines 74-76) cites DRAT (Wetzler/Heule/Hunt 2016) with a primary-source mirror I verified exists at research/references/witness-checking/drat-format-drat-trim-wetzler-heule-hunt-2016.txt; PCC (Necula & Lee) is cited in the want doc itself (line 201). I hunted for counter-evidence: (a) the nonlinear-division case (-Balance / Months) is outside QFLA, but the claim's "plus sign reasoning" is exactly the doc's treatment and the want doc's own nonlinear obligation discharges via sign reasoning; the resulting incompleteness is authorized by prove-or-reject, requiring no new theory; (b) time-referencing rules (want doc Open Questions, line 207) would need a different mechanism, but the want doc deliberately leaves them open, so they are not an obligation class in the want doc; (c) structural checks (unreachable/dead-end) are graph reachability, not arithmetic, but trivially decidable — a phrasing quibble, not a theory gap; (d) grep across the cited docs and spec for undecidability/research-grade counter-claims found none. The obligation classes actually enumerated (rule establishment/preservation, symmetric write sites, fault family, dead rows) all reduce to QF linear implications over 2-6 variables plus sign-set reasoning, per the machinery table in feasibility.md § 1.1.

### feasibility-3 — CONFIRMED (high)

**Claim:** Rule-preservation obligations do not exist in the compiler: the baseline worked example mints only 3 proof obligations (divisor non-zero plus two default-vs-bound checks) and none reference the rule Balance >= -OverdraftLimit, matching the prior assessment's statement that 'prove rule R still holds after this write' is not a generated obligation.

**Verifier:** Checked: (1) feasibility.md § 2.2 (docs/Working/want-analysis-2026-07-19/feasibility.md:91-97) — verbatim 3-obligation list matches the claim; (2) prior-engineering-assessment.md § 1 caveat (docs/Working/proof-engine-linear-feasibility-supporting-2026-07-12/prior-engineering-assessment.md:26) — the quoted sentence exists verbatim with no ignored qualifiers; (3) live empirical re-run: extracted the worked example verbatim from docs/Working/what-i-want-2026-07-16.md:30-90 and compiled via precept_compile on 2026-07-19 — success:true, 0 diagnostics, exactly 3 obligations ("Divisor must be non-zero" Proved/CompositionalConstraint; "Default value of 'DailyWithdrawalLimit' must satisfy 'positive'" and "'max'" Proved/Literal), none referencing Balance >= -OverdraftLimit; (4) source hunt for counter-evidence: grep for preservation machinery in src/Precept/Pipeline/ found none; CollectObligations (src/Precept/Pipeline/ProofEngine.cs:208-270) walks rule conditions only for fault-shape obligations inside the rule expression, and rules otherwise enter proofs only as trusted premises via ApplyTrustedRuleFacts (ProofEngine.Composition.cs) — no code path mints a "rule holds after this write" obligation. Minor precision caveat (does not break the claim): a rule whose own expression contained e.g. a division would mint a fault obligation referencing that rule, but that is a different obligation class and the baseline example's rule has no such subexpression.

### feasibility-4 — CONFIRMED (high)

**Claim:** Certificate machinery (ProofVerdict DU, certificate record, CertificateSteps catalog) is fully designed with 21 step kinds, 11 premise kinds, and per-step bounded recompute rules, but zero code exists: a grep for ProofVerdict or Certificate over src/Precept/**/*.cs returns no files.

**Verifier:** Checked all three citations. (1) docs/Working/certificate-steps-membership-2026-07-12.md: line 43 states "twenty-one CertificateStepKind members"; Decision 1 (line 333) states "eleven closed premise kinds"; § Semantic Rules line 162 states "Recompute rules (one per step kind — the checker contract). Each rule is a single bounded computation, no search"; § Inventory of what will be built lists src/Precept/Language/CertificateStepKind.cs, CertificateStep.cs, CertificatePremise.cs as future files. (2) compiler-readiness-plan-2026-07-12.md § Slice 0 (lines 183-199) designs the ProofVerdict DU as replacing the existing binary ProofDisposition enum. (3) Re-ran the grep and widened it: case-insensitive 'proofverdict|certificate' across src/, tools/, AND test/ *.cs returned zero matches (grep exit 1, clean no-match). Minor nuance, not a refutation: "fully designed" glosses the doc's own status — "Externally-Grounded, not Locked" (line 538) with one parked Open question 1 (step assignment for non-identity modifier/dimension sub-arms, lines 305/307/325) whose candidate resolution could grow membership past 21; the counts, recompute-rule contract, and zero-code facts are otherwise exactly as claimed.

### feasibility-5 — REFUTED (high)

**Claim:** The current MVP plan explicitly RETRACTED the independent certificate re-checker (Decision B: 'no live re-checker, no checker-gated mint'), while the want doc makes that checker load-bearing at two call sites (compiler stage + runtime load gate) — a direct plan-vs-want tension requiring an owner ruling.

**Verifier:** The two cited passages exist as quoted, but the claim misreads both scope and status, and the asserted "direct plan-vs-want tension requiring an owner ruling" does not survive the surrounding qualifiers.

1. The plan did NOT retract the checker — it retracted the *MVP decision* and explicitly retains the checker as a committed later phase. plan-2026-07-12.md:127 reads in full: "RETRACTED — **not an MVP decision.** The **MVP** ships the §5a certificate *format* only: no live re-checker, no checker-gated mint. §5b (the checker) is a later phase." The claim's quoted clause ('no live re-checker, no checker-gated mint') is prefixed by "The MVP ships..." — it is MVP-scoped, not a wholesale retraction. Corroborating: plan:164 lists "§5b — independent re-checker | Deferred (Decision B) — a later phase | Builder-side drift-defense value is **wanted post-MVP**"; and the architecture doc (compiler-readiness-plan-2026-07-12-architecture.md:43–50) says the certificate format is "designed in now **so the §5b checker is a later addition rather than a retrofit**" and names the future component `CertificateChecker`. The plan plans FOR the checker; "retracted the checker" is a dropped-qualifier misreading.

2. No direct contradiction between the docs. The want doc (what-i-want-2026-07-16.md:189–192) makes the checker load-bearing at two call sites in the *end state* — that part of the claim is accurate — but it nowhere places the checker in the compiler-readiness MVP; its only MVP-scoping statements (lines 179–183, 208) concern the single-precept proof boundary and sagas. A deferral-to-later-phase in an internal MVP plan is compatible with an end-state requirement, especially since the runtime load-gate call site cannot exist before the runtime is built and standing owner context is that Precept ships only when both compiler and runtime are complete. Deferral ≠ tension.

3. "Requiring an owner ruling" is backwards chronologically. The plan is dated 2026-07-12; the want doc is 2026-07-16 and is the owner's authoritative statement — per project memory, "certificates (one checker, two call sites)" is among its owner-dispositioned rulings. The owner ruling on the checker's role therefore already exists; what remains is mechanical plan reconciliation (the pre-dated plan's §5b characterization — "builder assurance, not author unblocking", architecture:43 — is superseded by the want's guarantee-load-bearing runtime-gate role), not an open two-sided fork awaiting a ruling.

What I checked: docs/Working/compiler-readiness-plan-2026-07-12.md lines 115–170 and 205–225; docs/Working/what-i-want-2026-07-16.md lines 129, 179–208 (full Certificates section); docs/Working/compiler-readiness-plan-2026-07-12-architecture.md §1.3 (Decision B, lines 43–50) and §2 preamble; grep for all certificate/checker/MVP mentions in both cited docs.

### feasibility-6 — CONFIRMED (high)

**Claim:** Mutation 1 (delete Withdraw's guard) is accepted today with zero diagnostics — an unbounded withdrawal amount subtracted from Balance raises no obligation.

**Verifier:** Checked: (1) docs/Working/want-analysis-2026-07-19/feasibility.md § 2.3 row 1 states exactly the claim (`success: true`, 0 diagnostics, "ACCEPTED — GAP"). (2) Extracted the worked example verbatim from docs/Working/what-i-want-2026-07-16.md lines 30-90, compiled via precept_compile: baseline gives success:true, 0 diagnostics, 3 proof obligations — matching § 2.2 verbatim. (3) Applied mutation 1 (removed the `when Balance - Withdraw.Amount >= -OverdraftLimit and Withdraw.Amount <= DailyWithdrawalLimit` guard from Withdraw's first row, all else unchanged) and recompiled: success:true, diagnosticCount:0, and the proof-obligation list is identical to baseline (divisor non-zero + two DailyWithdrawalLimit default checks) — no obligation references the rule `Balance >= -OverdraftLimit` or the `set Balance = Balance - Withdraw.Amount` write, so an unbounded Amount subtraction genuinely mints no obligation. Counter-evidence hunt: the mutation also leaves the two later `from Active on Withdraw` rows shadowed by the now-unguarded first row, yet even that produces no warning; git status is clean at HEAD so the MCP server reflects committed code, not session edits. The want doc's table row (line 107) confirms the mutation's intended shape ("Cannot prove ... `Amount` is unbounded"), so no interpretation gap. No counter-evidence found.

### feasibility-7 — CONFIRMED (high)

**Claim:** Mutation 2 (delete ReduceLimit's guard, the want doc's flagship symmetric-write-site case) is accepted today with zero diagnostics — the write to OverdraftLimit carries no obligation from the rule that mentions it, though the substrate (write-site enumeration, ConstraintInfluenceEntry.ReferencedFields) already exists.

**Verifier:** Every element of the claim checked out under adversarial verification. (1) Citation check: docs/Working/want-analysis-2026-07-19/feasibility.md § 2.3 row 2 states verbatim "success: true, 0 diagnostics ... ACCEPTED — GAP. The symmetric write site (the want doc's flagship case): the write to OverdraftLimit carries no obligation from the rule that mentions it"; § 2.2's method note confirms mutations were applied one at a time against the verbatim worked example. The want doc (docs/Working/what-i-want-2026-07-16.md, § Worked example and the "What must not compile" table) does present ReduceLimit as the symmetric-write-site case ("Delete ReduceLimit's guard | Cannot prove Balance >= -NewLimit — the rule mentions OverdraftLimit, so this write site carries the obligation too"), so "flagship symmetric case" is a fair characterization. (2) Empirical reproduction: I extracted the worked example verbatim from the want doc, deleted only ReduceLimit's guard clause, and compiled via precept_compile at HEAD (2026-07-19). Result: success:true, diagnosticCount:0, and the minted obligations are exactly the same three as baseline (divisor non-zero, two DailyWithdrawalLimit default checks) — zero obligations reference the rule Balance >= -OverdraftLimit or the OverdraftLimit write. (3) Substrate check: src/Precept/Pipeline/ProofLedger.cs:130-134 is exactly the ConstraintInfluenceEntry record with ReferencedFields, and it is live (populated from semantics.ConstraintRefs in ProofEngine.Analysis.cs:11-28, ProjectConstraintInfluence), not a dead record. src/Precept/Pipeline/GraphAnalyzer.FieldWriteSites.cs contains whole-program write-site identification covering every write-site category (transition-row actions, event-handler/construction rows, state hooks, per-state editable access modes, field-level editable, computed fields), catalog-driven via ActionWriteSemantics.EstablishesValue. One minor nuance that does not refute the claim: this code performs boolean existence checks per field (HasAnyWriteSite short-circuits via Any()) in service of the FieldNeverSet warning — it classifies write sites but does not materialize a write-site list, so the cross-product obligation minting would need to re-walk and collect rather than reuse a stored enumeration. The feasibility doc's own row 6 classifies the item "(c) Genuinely new (substrate exists)", consistent with this. No counter-evidence found: no diagnostic, obligation, or proof strategy at HEAD connects rules to write sites of referenced fields.

### feasibility-8 — CONFIRMED (high)

**Claim:** Mutation 3 (remove nonnegative from Deposit's Amount — the inductive-hypothesis case) is accepted today with zero diagnostics.

**Verifier:** Checked: (1) The cited row exists as stated — docs/Working/want-analysis-2026-07-19/feasibility.md § 2.3, row 3 ("Remove `nonnegative` from `Deposit`'s `Amount`" | "`success: true`, 0 diagnostics" | "ACCEPTED — GAP. The inductive-hypothesis case"). (2) The mutation corresponds to the want doc's own must-not-compile table (docs/Working/what-i-want-2026-07-16.md:109) and the worked example's Deposit event (line 51), and the "inductive-hypothesis" framing matches the want doc's characterization of Deposit (line 95: "the inductive hypothesis"). (3) Live re-probe on 2026-07-19: extracted the worked example verbatim from the want doc, changed only `event Deposit(Amount as decimal nonnegative)` to `event Deposit(Amount as decimal)`, and compiled via mcp__precept__precept_compile. Result: success:true, diagnosticCount:0, empty diagnostics array; the 3 minted proof obligations (divisor non-zero, two DailyWithdrawalLimit default checks) match the documented baseline exactly, none referencing the rule `Balance >= -OverdraftLimit` or the Deposit write. No counter-evidence found: the acceptance is not a parse failure artifact (summary reports the full 3-state/8-event/12-transition/1-rule structure), and the surrounding § 2.3 context contains no qualifier the claim omits.

### feasibility-9 — CONFIRMED (high)

**Claim:** Mutations 4 and 5 (remove min 0.0 from OpeningBalance; remove nonnegative from OpenAccount's OverdraftLimit arg) are both accepted today with zero diagnostics — no base-case rule-establishment obligations exist for initial events.

**Verifier:** Checked three ways, hunting for refutation each time. (1) Citation integrity: docs/Working/want-analysis-2026-07-19/feasibility.md § 2.3 rows 4-5 state exactly what the claim says (both mutations "success: true, 0 diagnostics", classified ACCEPTED — GAP; row 4's note: "No base-case establishment obligation exists for initial events"), with no qualifiers the claim omits. (2) Live reproduction: extracted the worked example verbatim from docs/Working/what-i-want-2026-07-16.md § Worked example and compiled via precept_compile — baseline gives success:true with exactly the 3 obligations § 2.2 lists (divisor + 2 DailyWithdrawalLimit default checks). Mutation 4 (drop `min 0.0` from OpeningBalance) and mutation 5 (drop `nonnegative` from OpenAccount's OverdraftLimit arg) each compile success:true, diagnosticCount:0, with an obligation list identical to baseline — zero obligations reference the rule `Balance >= -OverdraftLimit` or the OverdraftLimit write. (3) Counter-evidence hunt in the proof engine: grep for establishment/initial obligation generation found none; the only initial-event-adjacent proof machinery is initial-state satisfiability (src/Precept/Pipeline/ProofEngine.Analysis.cs:47-99), which checks StateResident *ensures* against *defaults* only and is skipped entirely when a construction handler exists (lines 60-69, `HasConstructionHandler` early return) — the worked example has one (`OpenAccount ... initial`), so even that check does not run. No mechanism mints rule-establishment obligations for initial events. Both halves of the claim (zero diagnostics on mutations 4 and 5; no base-case obligation machinery) hold.

### feasibility-10 — CONFIRMED (high)

**Claim:** Mutation 7 (remove PlanRepayment's guard) is accepted with zero diagnostics because the write of -Balance/Months into the nonnegative field MonthlyRepayment mints no containment obligation at all — a live fail-open obligation-generation hole, distinct from the missing rule machinery.

**Verifier:** Checked both citations and reproduced empirically. (1) feasibility.md § 2.3 row 7 and § 2.2 say exactly what the claim states, including "a live fail-open position, not merely a missing rule feature." (2) Live probe: applied mutation 7 to the want doc's worked example and compiled via precept_compile — success:true, 0 diagnostics, exactly 3 obligations, none referencing MonthlyRepayment's nonnegative bound; divisor obligation stays Proved. (3) Independence probe: a minimal file with NO rule construct, writing literal -5.0 into a nonnegative field, compiles clean with ZERO obligations — the write-site modifier-containment obligation is never minted regardless of rule machinery, confirming "no containment obligation at all" and "distinct from the missing rule machinery." (4) compiler-readiness-plan-2026-07-12.md: Slice 0 is under "## 4. The slice list" and does plan 4 fail-open hole fixes; caveat: those 4 holes are discharge-path holes and do not include this obligation-generation hole, but the claim does not assert they do — the plan itself says the four are "the found instances, not the closed set." Only tension found: feasibility § 2.4 loosely groups mutation 7 with the rule-obligation family; the no-rule probe resolves this in the claim's favor.

### feasibility-11 — CONFIRMED (high)

**Claim:** Mutation 11 (CloseAccount guard contradicting the rule) is accepted with zero diagnostics: the satisfiability scan folds guards against declared field bounds only, never against rules, and Balance has no declared bounds; the consequential unreachability of Closed is specification-only (spec § 0.6 item 12).

**Verifier:** Checked all three citations plus a live empirical probe; found no counter-evidence. (1) Empirical: reproduced mutation 11 exactly — took the worked example verbatim from docs/Working/what-i-want-2026-07-16.md § Worked example, changed only the CloseAccount guard to `Balance < -OverdraftLimit`, compiled via precept_compile: result `success: true, diagnosticCount: 0` — no PRE0082 (dead guard), no PRE0080 (unreachable Closed), matching the claim and feasibility.md § 2.3 row 11 word-for-word. (2) Code: src/Precept/Pipeline/ProofEngine.Satisfiability.cs:10 header states the scan detects "guards and rules whose conjunction with the fields' declared bounds is empty"; the guard path (ScanTransitionRowGuards:172 → IsGuardUnsatisfiableUnderFieldBounds:212 → BranchHasEmptyFieldInterval, seeded by ExtractFieldInterval) folds guard leaf constraints against field-declared bounds only — ExtractFieldInterval (ProofEngine.Intervals.cs:202) reads only declared min/max and flag-modifier bounds, never rules. Rules are checked in separate passes (rule-vs-own-bounds PRE0159, rule-vs-rule PRE0155, rule-vs-defaults) but never conjoined with a transition guard — so guard-vs-rule contradiction is indeed invisible. `Balance` is declared `field Balance as decimal` with no bounds → Unbounded interval, so the fold cannot empty. (Additional reinforcing detail: the mutated guard is field-vs-expression, not field-vs-literal, so its leaf isn't even foldable — a second independent reason for acceptance, consistent with, not contradicting, the claim.) I checked the one plausible counter-path — spec § 0.6's "rule-sourced relations live" (relational rules feed obligation discharge) — but that mechanism feeds fault-obligation discharge in the discharge loop, not the satisfiability scan, and the live probe settles it empirically. (3) Spec: docs/language/precept-language-spec.md § 0.6 Implementation status, item 12 row reads "Sharpened reachability/routing diagnostics from proven-dead guards | Specification-only — depends on #9 + #10", exactly supporting the "specification-only" characterization of the consequential unreachability of Closed.

### feasibility-12 — CONFIRMED (medium)

**Claim:** Mutations 9 and 10 (unreachable state; dead-end Frozen) are detected today with correct messages (PRE0080 'State Suspended is unreachable from initial state Active'; PRE0119 'State Frozen has no outgoing transitions and is not marked terminal') but as Warnings with success:true; the Warning-to-Error flip for exactly these codes is ruled in the readiness plan but not built at HEAD.

**Verifier:** Every citation checked out and two live compile probes reproduced the claim exactly. (1) Empirical: extracted the worked example verbatim from docs/Working/what-i-want-2026-07-16.md:30-90 and applied each mutation via precept_compile. Mutation 9 (add `state Suspended`, no inbound row) → success:true, 1 warning, PRE0080 line 12: "State 'Suspended' is unreachable from initial state 'Active'". Mutation 10 (delete `event Unfreeze` + its row) → success:true, 1 warning, PRE0119 line 11: "State 'Frozen' has no outgoing transitions and is not marked 'terminal'". Both match the claimed codes, messages, severities, and success:true. (2) Catalog: src/Precept/Language/Diagnostics.cs:707 (UnreachableState, Severity.Warning) and :720 (StructuralSinkState, Severity.Warning) as cited; numeric codes confirmed UnreachableState=80, StructuralSinkState=119 in DiagnosticCode.cs (lines 212, 125); file is clean vs HEAD ed57d7fd, so the observed behavior is HEAD behavior. (3) Plan: compiler-readiness-plan-2026-07-12.md § 2 ("Decisions captured — the ruling log", line 100) contains "The structural-severity family" (lines 131-136) ruling the Warning→Error flip for UnreachableState and StructuralSinkState (among others in the family); the slice table (line 92) and the slice stub (lines 370-376, which cites the exact Diagnostics.cs :707/:720 lines) both show status Stub, i.e., not built. Counter-evidence hunt: grepped src/Precept for any other severity override on these codes — none exists; the catalog Severity.Warning is the only severity source. Feasibility.md § 2.3 rows 9-10 (lines 113-114) state exactly what the claim summarizes. One precision note that does not weaken the claim: the ruled flip set covers more codes than these two (DeadEndState, uninhabitability family), so "for exactly these codes is ruled" is true in the sense that these codes are explicitly in the ruled set, not that the set contains only them.

### feasibility-13 — CONFIRMED (medium)

**Claim:** Only mutations 6 and 8 are rejected today as the want demands: PRE0083 'Division is unsafe: Months can be zero on event PlanRepayment from state Active' and PRE0079 'Default value 20000.0 for field DailyWithdrawalLimit violates declared max' — both match the table's substance, though PRE0083 does not suggest the closing premise.

**Verifier:** Checked (1) the citation: docs/Working/want-analysis-2026-07-19/feasibility.md § 2.3 rows 6 and 8 say exactly what the claim says — row 6: PRE0083 error "Division is unsafe: 'Months' can be zero on event 'PlanRepayment' from state 'Active'", classified REJECTED-TODAY, "Matches the table's demand ... in substance ... does not suggest the closing premise (`positive`/`nonzero`)"; row 8: PRE0079 error "Default value 20000.0 for field 'DailyWithdrawalLimit' violates declared 'max'", REJECTED-TODAY, matches the demand. § 2.4 confirms the "only" qualifier: "2 of 11 mutations are rejected today (6, 8)"; 9/10 are WARNING-ONLY with success:true (detected, not rejected); 1,2,3,4,5,7,11 compile clean. (2) The want doc table (docs/Working/what-i-want-2026-07-16.md:112,114) demands "Divisor can be zero — the division is unsafe" and "Default value violates max 10000.0", matching both diagnostics in substance. (3) Empirical re-verification via precept_compile on the worked example extracted verbatim from the want doc: mutation 6 → success:false, PRE0083 line 37 col 29, message character-for-character as quoted, with no suggested fix in the text; mutation 8 → success:false, PRE0079 line 6 col 47, message character-for-character as quoted. Spot-checked the "only" claim live: mutation 2 (delete ReduceLimit's guard) and mutation 11 (dead-row CloseAccount guard) both compile success:true with 0 diagnostics, as the doc records. No counter-evidence found — the doc's rows, the want doc's demands, and live compiler behavior all agree with the claim, including the "does not suggest the closing premise" qualifier on PRE0083.

### feasibility-14 — CONFIRMED (high)

**Claim:** General rule preservation with all pre-state rules as hypotheses likely forces the fact-combination build ('§1b': Fourier-Motzkin elimination with Farkas certificates, ~1,500-2,200 code LOC + 2,500-4,000 test LOC), which is gated on two owner spec decisions — overriding the locked single-hop sentence at precept-language-spec.md:256 and ratifying Farkas certificates against the legibility bar at :225 — while the want doc's own worked example needs only the one-fact '§1a' build (~500-700 engine LOC, inside the locked spec).

**Verifier:** Checked all citations at source. (1) precept-language-spec.md:256 verbatim contains the locked single-hop sentence ("the engine never combines two separately-declared facts to derive a bound neither states alone"); :225 is Proof philosophy #3, the legible-certificate admissibility bar. (2) proof-engine-linear-solver-feasibility-2026-07-12.md §§2,5 and prior-engineering-assessment.md §3 both give §1b = Fourier-Motzkin + Farkas certificates at ~1,500-2,200 code LOC + 2,500-4,000 test LOC with exactly the two owner gates named (spec:256 override, spec:225 Farkas ratification); §1 of each gives §1a at ~500-700 engine LOC, inside the locked spec, no gate. (3) The "likely forces" link is sourced to the prior assessment's §1 caveat ("prove rule R still holds after this write ... the one shape that would commonly need two facts ... if that ever changes, the §1b question reopens") and drawn as the conclusion in want-analysis-2026-07-19/feasibility.md §1.3 and bottom line; the claim's hedge matches. (4) Want doc line 19 confirms premise class (d) "all rules holding in the pre-state"; the worked example declares exactly one rule (line 38; baseline compile "1 rules"), so fact combination is structurally impossible within it and §1a suffices, matching the claim. Counter-evidence hunt: verified neither owner gate has since been resolved — spec at HEAD retains both sentences, and no doc/memory records the spec:256 override or Farkas ratification; line 225's rewrite from outright solver ban to admissibility criteria does not discharge the ratification, which every cited doc reserves to the owner. No misquotes, wrong lines, or dropped qualifiers found.

### feasibility-15 — REFUTED (medium)

**Claim:** Counter-premise suggestion exists today only for collection count bounds ('guard with when F.count < N'); the numeric family's rejections (PRE0083, PRE0078) name the fault but not the closing premise, though spec § 0.6 philosophy #2 already mandates printing the weakest precondition, and computing it on this fragment is textbook backward substitution.

**Verifier:** The claim's universal qualifier "only for collection count bounds" is false; the rest of the claim checks out. What I verified: (1) src/Precept/Pipeline/ProofEngine.Diagnostics.cs:163-168 does contain the count-bound suggestions exactly as cited ("guard with `when {field}.count < {n}`" / "`when {field}.count > {m}`"). (2) PRE0083 (DivisionByZero, Diagnostics.cs:821) and PRE0078 (NumericOverflow, Diagnostics.cs:690) message templates name the fault only; live compile probe confirmed PRE0083 emits "Division is unsafe: 'Count' can be zero in the computed expression for field 'Avg'" with no closing premise in the message. (3) Spec § 0.6 Proof philosophy #2 (precept-language-spec.md:223) does mandate "rejected, with the weakest precondition that would discharge it printed verbatim" for unresolved bounded-write obligations. (4) The feasibility doc citations (§ 1.1 row 10, § 2.3 mutation row 6) say what the claim says. COUNTER-EVIDENCE against "only": PRE0116 UnprovedPresenceRequirement is a live-emitting proof-obligation rejection that computes and prints an instantiated closing premise for a NON-count-bound obligation — live probe (precept_compile, this session) produced: "PRE0116 ... Cannot prove that 'Name' is present (used on event 'Publish' from state 'Draft') — guard with 'when Name is set', initialize it earlier, or make it required" (template at src/Precept/Language/Diagnostics.cs:891, emission via ProofRequirement.cs:305 / ProofEngine.cs:289). That is a counter-premise suggestion for a presence obligation, not a collection count bound. Additionally, UnguardedCollectionAccess (Diagnostics.cs:568, "guard with if {0}.count > 0") and UnguardedCollectionMutation (Diagnostics.cs:576, "guard with 'when {0}.count > 0' ... or apply 'notempty'") print instantiated closing guards for emptiness obligations, which are distinct from declared mincount/maxcount bounds; and PRE0083/SqrtOfNegative carry generic closing-premise guidance in catalog FixHint/RecoverySteps metadata (Diagnostics.cs:823-826, 831-834), though not in the emitted message. Note the cited feasibility row 10 itself claims only "Partially exists ... Live for count bounds ... Not live for the numeric family" — the analyst's strengthening of that to "exists today only for collection count bounds" is the part that breaks. The numeric-family half of the claim (PRE0083/PRE0078 lack the closing premise in emitted text) and the spec-mandate half are both confirmed; the "textbook backward substitution" characterization is a feasibility judgment consistent with the cited fragment (linear, loop-free) that I could not falsify.

### feasibility-16 — CONFIRMED (medium)

**Claim:** The structural-severity ruling keeps UnsatisfiableGuard (dead rows) at Warning, which conflicts with the want doc's requirement that the ideal compiler rejects dead rows — an owner-level severity decision, not an engineering constraint.

**Verifier:** Both citations check out verbatim and the counter-evidence hunt found corroboration, not refutation. (1) Ruling side: docs/Working/compiler-readiness-plan-2026-07-12.md:139-140 lists "Stay Warning: ... UnsatisfiableGuard" in the § 2 structural-severity family exactly as cited; the companion doc compiler-readiness-plan-2026-07-12-structural-severity.md:104 and :323 confirm the ruling ("Warning (no change)"; "a dead guard row is inert ... not an uninhabitable entity"), and slice-design-structural-severity-2026-07-12.md:55,210 codifies it as the ruled stay-Warning set with an acceptance criterion that a tripping definition compiles with HasErrors==false. Code agrees: src/Precept/Language/Diagnostics.cs:770 has UnsatisfiableGuard at Severity.Warning with message "this row can never fire". (2) Want-doc side: docs/Working/what-i-want-2026-07-16.md:143 (§ Structural integrity) says "Dead rows are structural defects too. A row whose guard can never be true given the declared constraints is unreachable ... The ideal compiler rejects it with an explanation", and the dead-row mutation appears in the must-not-compile table (line 117) under the header "each must be a compile-time rejection ... a compiler that accepts any of these mutations is not the compiler this document asks for". The code's PRE0082 trigger ("guard condition can never be true given the precept's declared constraints") is definitionally the want doc's dead row, so the conflict is real, and the want doc (07-16) postdates the ruling (07-12) with no later reconciliation found (grep across docs/Working). (3) "Owner-level severity decision, not engineering constraint" holds: PRE0082 detection is already live (ProofEngine.Satisfiability.cs; compile-time-niche-evidence-inventory-2026-06-10.md:287 "PRE0082 IS LIVE today"), so the flip is a severity-metadata change like the six flips the same slice ships; the stay-Warning rationale is canon-wording interpretation ("catches"/"reports" vs "rejects"), not a technical inability. Corroborating: bugs.md:108 already flags Warning-severity UnsatisfiableGuard as letting the precept "build and produce an engine", and d2-proven-violation-philosophy-analysis-2026-07-03.md:67 confirms HEAD warns rather than rejects. One nuance that does not refute the claim: the specific CloseAccount mutation in the want-doc table would also be caught indirectly via UnreachableState (which the ruling does flip to Error) because Closed becomes unreachable — but the § Structural integrity bullet requires rejecting the dead row itself (its ratio-vs-75 example involves a dead path, not necessarily an unreachable state), so Warning-severity PRE0082 still fails the want in the general case.

### feasibility-17 — CONFIRMED (medium)

**Claim:** Performance is not a constraint: the full 77-sample corpus compiles in ~44 ms with the worst single file at ~1.23 ms (fresh HEAD benchmark), and both linear-proof builds add microseconds per obligation against ~40x single-file headroom.

**Verifier:** Checked both citations at their exact locations. proof-engine-linear-solver-feasibility-2026-07-12.md § 4 (lines 112-119) states verbatim: fresh-HEAD benchmark via tools/Precept.Bench/CompileBench.cs (file confirmed to exist), "Full 77-sample corpus: 44.0 ms", "Worst single file: 1.23 ms", "~40× headroom" against a 50 ms interactive budget, and "both §1a and §1b add microseconds against a millisecond-scale budget". want-analysis-2026-07-19/feasibility.md § 1.3 (line 68) contains the claim nearly word-for-word: "Performance: not a constraint. Full 77-sample corpus compiles in ~44 ms, worst single file ~1.23 ms, and both linear-proof builds add microseconds per obligation… against ~40× single-file headroom." Corroboration: samples/ contains exactly 77 .precept files. Counter-evidence hunt: the older ~3 ms worst-file figure in project records is explicitly superseded in the cited § 4 ("better than the documented ~3 ms"); the only qualifier is a BenchmarkDotNet ShortRun N=3 method caveat, which the source states none of its conclusions are sensitive to — the claim's "~" hedging does not misrepresent it. No misquote, missing qualifier, or contradicting evidence found.

### feasibility-18 — CONFIRMED (high)

**Claim:** The overall one-person-plus-AI-agents verdict is yes: the existing 7,962-LOC proof engine (measured across 11 files including ProofLedger.cs) and ~3,600 tests were built under this working model, the remaining machinery is of comparable total size and specified to delegation-ready detail, and the bottleneck is owner decision throughput on the gated items (spec:256, spec:225, checker-in-MVP, structural severities), not engineering capacity.

**Verifier:** Checked every leg of the claim against source. (1) LOC: reproduced `wc -l src/Precept/Pipeline/ProofEngine*.cs ProofLedger.cs` — exactly 7,962 total across 11 files (10 ProofEngine.* partials + ProofLedger.cs at 147), matching the claim's parenthetical precisely. (2) Doc citations: feasibility.md § 1.3 (line 70) contains the bottleneck sentence near-verbatim ("The bottleneck is owner decision throughput on the gated items (spec:256 override, spec:225 ratification, checker-in-MVP vs retracted, structural severities), not engineering capacity") plus the comparable-size and delegation-ready statements; § Bottom line (line 129) carries the yes verdict on the same grounds. (3) The gated items are real: precept-language-spec.md:256 is the single-hop-bound retention sentence and :225 is the legible-certificates admissibility criterion (both grep-confirmed at those exact lines); the checker retraction is live in compiler-readiness-plan-2026-07-12.md:127 ("RETRACTED — not an MVP decision... no live re-checker") while the want doc makes the checker load-bearing — a genuine owner-ruling gate. (4) "Built under this working model": git history shows a single human author (~1,998 of 2,000 commits under shane/sfalik/Shane variants, 2 bot commits) with 234 explicit "Co-Authored-By: Claude" trailers — consistent with one-person-plus-AI-agents. Two minor imprecisions found, neither refuting: (a) "~3,600 tests" undercounts — 4,059 [Fact]/[Theory] attributes and 6,933 runner-discovered test cases (Theory expansion); the figure is inherited from CLAUDE.md and the error runs in the claim's favor (more was built under this model, not less); (b) "structural severities" is listed among owner-gated decisions, but the same feasibility doc (mutation 9) says the Warning→Error flip is "ruled but unbuilt" — that one item is arguably decided build-backlog rather than pending decision throughput; the other three gates are genuinely owner-gated. The "comparable total size" leg is the doc's own estimate (§1a + §1b + Slice 0 ≈ one more proof-engine's worth) and the claim correctly attributes it as such rather than asserting an independent measurement.

### feasibility-19 — CONFIRMED (medium)

**Claim:** If time-referencing rules (open question 1) are admitted as rules rather than guards, the inductive proof shape in this analysis does not cover them — no event writes the current moment, so there is no inductive step for time passing; if time belongs only in guards, the machinery enumeration is unaffected.

**Verifier:** Checked both citations verbatim. (1) docs/Working/what-i-want-2026-07-16.md § Open questions (line 207) states the open question exactly as the claim frames it: "nothing writes `today`, there is no ingress point for it, and the inductive model has no step where pure passage of time fits. Do rules that reference the current moment exist at all, or does time belong only in guards... Deliberately left open." (2) docs/Working/want-analysis-2026-07-19/feasibility.md § 1.3, "Consequences of the two open questions" paragraph (lines 72-74, confirmed inside § 1.3 which spans lines 49-78): "if such rules are admitted as rules, the proof shape in this report does not cover them (they would need a different mechanism entirely); if time belongs only in guards, nothing in this report changes... The machinery table above is built on the second reading" — the claim nearly verbatim. Counter-evidence hunt: the want doc's proof shape (lines 16-19) has only base-case + per-handler inductive-step legs with four premise classes, none covering time passing without an event, so "does not cover them" is structurally correct; the feasibility machinery table (#1-#13) contains no temporal-mechanism row. The spec does expose `now()` (precept-language-spec.md:1597, () → instant), so temporal expressions exist in the corpus, but that does not contradict this conditional claim about the want doc's open question and the analysis's coverage. No dropped qualifiers or misreadings found.

## Axis: comprehensiveness

### comprehensiveness-1 — CONFIRMED (high)

**Claim:** Quantified collection rules are a structural gap: canon explicitly classifies `rule each x in C (x > 0)` as 'a runtime governance rule over the current elements' that does not carry a bound back to read sites, which is deferral under the want doc's no-deferral ruling, and the want doc describes no proof theory (per-action frame axioms, quantifier instantiation, existential-fact destruction on remove) for proving such rules preserved at collection write sites.

**Verifier:** Checked all three citations against source. (1) collection-types.md:796 matches verbatim: the quantifier predicate `rule each x in C (x > 0)` "is a runtime governance rule over the current elements" and, unlike inner-type value modifiers, "a quantifier does not" carry a bound back to a read-site. (2) collection-types.md:800-861 confirmed: line 802 status is "Locked design ... Not yet implemented"; the section is syntax/grammar/binding-scope only, with zero preservation-proof treatment. (3) Want doc read in full: § No deferral (line 159) rejects any business rule the compiler cannot prove complete; the four premise classes (line 19) and runtime surface (line 153: "ingress validation of args and editable fields, guards ... and nothing beyond them") are scalar-shaped, and the doc never mentions collections, quantifiers, frame axioms, or element-level rule preservation. Counter-evidence hunt: grepped all of docs/compiler and docs/language for quantifier proof treatment — the only proof-engine mentions (proof-engine.md:1703, collection-types.md:628, type-checker.md:362) concern element-bound intervals on the binding variable INSIDE a predicate (proving obligations within the quantifier expression), not preservation of the quantified rule at collection write sites; mincount/maxcount proof (collection-types.md:792) covers cardinality only. Nothing anywhere describes write-site preservation, instantiation-on-add, or existential destruction on remove for quantified rules. Caveat: "deferral under the no-deferral ruling" is the claim's inference, not a quote, but it follows directly from the accurately-quoted texts — a `rule` is a business rule, and canon assigns its quantified form to runtime enforcement without compile-time proof, the exact pattern § No deferral forbids.

### comprehensiveness-2 — REFUTED (high)

**Claim:** The modifiers-are-sugar ruling makes quantified proof theory unavoidable even if the each/any/no syntax never ships, because element value modifiers like `queue of string maxlength 200` can only desugar into universally quantified rules over elements.

**Verifier:** The claim's own second citation contradicts it, and the documented enforcement mechanism shows a non-quantified desugaring path.

(1) Misreading of the cited line. collection-types.md:796 does NOT present element value modifiers and quantifier predicates as "two spellings of per-element constraints." It explicitly contrasts them as different mechanisms with different power: "(1) an inner-type value modifier — `queue of string maxlength 200` — declares a static per-element type bound the proof engine reads at write- and read-sites ... (2) a quantifier predicate — `rule each x in C (x > 0)` — is a runtime governance rule over the current elements ... The two are complementary: the inner-type modifier carries a bound back to a read-site (so `.peek` proves into a capped field); a quantifier does not." Complementary-and-different is the opposite of two-spellings-of-one-thing.

(2) The documented enforcement of element value modifiers requires no quantified proof theory. collection-types.md:625-628 (§ Element value modifiers) specifies: "The element bound participates in proof identically to the equivalent scalar-field modifier" — a write-site obligation (each value introduced via enqueue/add/push/etc. must be provably within the bound, "the same containment obligation a `set` into a bounded field carries"; default-literal elements checked the same way) plus read-site interval carrying (.peek/.min/.max carry the bound as a value interval). Every proof obligation is a scalar containment check at a single site; the "every element satisfies the bound" invariant follows by induction over write sites, with no universally quantified reasoning in the prover.

(3) That inductive discharge is exactly the want doc's own proof model. docs/Working/what-i-want-2026-07-16.md:16: "proof by induction over reachable configurations" — per-write-site scalar checks are the inductive step. So even granting the modifiers-are-sugar ruling (line 22: "`nonnegative` on `OverdraftLimit` is a compact spelling of `rule OverdraftLimit >= 0`; ... one constraint mechanism underneath"), the semantic content of `maxlength 200` being universally quantified does not force quantified PROOF THEORY: the obligation reduces to scalar checks + induction, which is the mechanism the corpus already documents.

(4) The want doc itself contains no collection/element/quantifier content (grep for queue/set of/maxlength/mincount/desugar/quantif/element yields nothing relevant) — the "unavoidable" inference is the analyst's, not the ruling's, and it has a documented counterexample.

What survives: the weaker observation that an element modifier's *meaning* is a per-element (universally quantified) property is true, and quantifier bindings do interact with element bounds (collection-types.md:628). But the specific claim — that quantified proof theory is UNAVOIDABLE because element modifiers "can only desugar into universally quantified rules" — is refuted by the write-site/read-site type-bound mechanism at collection-types.md:625-628, which the claim's own citation at line 796 names.

### comprehensiveness-3 — REFUTED (high)

**Claim:** The temporal fault family is a structural gap: canon accepts `Instant.Plus(Duration)` runtime OverflowException as 'an accepted edge case', and the constraint vocabulary rejects min/max on instant, datetime, zoneddatetime, and timezone, so under prove-or-reject no declarable premise can close an instant-arithmetic overflow obligation — the compiler would have to reject temporal arithmetic used by 49 of 78 samples (now()) unless new premise vocabulary or an explicit range axiom is added.

**Verifier:** Citations partly check out — temporal-type-system.md:624 'accepted edge case' quote is verbatim; min/max rejection on instant/datetime/zoneddatetime/timezone confirmed in canon (lines 640/892/966/1027), in coverage/temporal.md rows 103/153/169/184/348, and empirically (PRE0033 'The min constraint does not apply to instant fields'). But two load-bearing assertions are contradicted: (1) The blast-radius figure is false. samples/ contains 77 .precept files, not 78; 49 contain now() but almost exclusively as bare assignment (set LastActivityAt = now() — no arithmetic, no Instant.Plus, no overflow obligation). Only 4 samples do now()+duration arithmetic (insurance-renewal-processing, saas-user-provisioning, insurance-claim-adjudication, saas-license-management), so prove-or-reject would threaten ~4 samples, not 49 — a ~12x inflation from conflating 'uses now()' with 'uses temporal arithmetic'. (2) 'No declarable premise can close' is overstated: it derives only from the min/max field-constraint rejection and ignores guards, which the want doc explicitly counts as premises (line 153: 'declared premises (ingress validation of args and editable fields, guards)'; PlanRepayment's guard Balance < 0.0 closes the nonnegative obligation). Instant comparison operators are supported per canon, and I verified via precept_compile that a guard 'when FiledAt < 2100-01-01T00:00:00Z' type-checks cleanly — an instant-bounding guard plus constant duration is a declarable premise of exactly the interval shape the want doc's regime uses. The narrower kernel (canon's accepted-edge-case posture conflicts with prove-or-reject; no field-constraint can bound temporal point types) is genuine, but the claim as stated is contradicted on both its universal and its quantitative impact.

### comprehensiveness-4 — UNVERIFIABLE (high)

**Claim:** The time open question has two unstated model consequences even under the guard-only resolution: now()-derived guard facts must be explicitly barred from the inductive hypothesis (they decay with no event to invalidate them), and cross-field rules over now()-sourced timestamp fields (the dominant corpus pattern, e.g. `to Scheduled -> set ScheduledAt = now()`) are provable only from a clock-monotonicity axiom that is none of the four premise classes; event-anchored ensures referencing now() already exist in the live corpus and fit the guard-side resolution.

**Verifier:** All citations check out exactly (global-meeting-scheduler.precept:184-187 entry hooks; saas-customer-success.precept:104 and saas-customer-onboarding.precept:92 ensures; want doc line 207 open question; want doc line 19's four premise classes exclude clock monotonicity; now()-writes are 429/437 corpus now() uses; the want doc never mentions now()/clock/monotonicity so 'unstated' is true; the ensures do fit the guard-side resolution). But the two central 'model consequence' assertions do not hold as stated against the cited model: (1) 'must be explicitly barred from the inductive hypothesis' — per want doc line 19 the inductive hypothesis is premise class (d) 'all rules holding in the pre-state'; guard facts are class (c), per-handler, and structurally never enter it, and under guard-only resolution rules cannot mention now(), so the exclusion is automatic, not an obligation needing explicit barring (it becomes real only under an unstated invariant-strengthening engine the want doc does not posit); (2) 'provable only from a clock-monotonicity axiom' — a now()-referencing guard (premise class (c), e.g. `when StartedAt <= now()`) plus documented intra-fire now() consistency (docs/language/temporal-type-system.md:566: 'internal consistency (same inputs, same outputs within a single fire)') closes cross-field timestamp rules with no monotonicity axiom, and premise-addition is the model's native closure mechanism (want doc line 167); 'only' holds solely for the corpus-as-written, a qualifier the claim omits. Additionally, no cross-field ordering rule over two now()-sourced fields exists in the live corpus — the cited sample's now()-fields carry only is-set ensures (global-meeting-scheduler.precept:133-137), and the one live rule touching a now()-sourced field (prior-auth-appeal.precept:103) has an arg-sourced other operand (:360, :366) closeable without monotonicity. Citations accurate; the headline consequences survive only under charitable readings the claim does not state.

### comprehensiveness-5 — CONFIRMED (high)

**Claim:** Dynamic interpolated qualifiers are a structural gap: field types like `money in '{CatalogCurrency}'` depend on other fields' values, and those qualifier-source fields are editable in live samples, so a write to CatalogCurrency re-denominates every dependent money field while the want doc's editable-ingress rule (field's modifier-rules plus rules mentioning the field) does not catch it and the no-post-mutation-re-evaluation ruling means nothing re-validates dependents — the value-level model has no concept of type-level write-site obligations.

**Verifier:** What I checked. (1) Sample: samples/inventory-item.precept lines 45-60 show interpolated qualifiers on quantity/money/price fields ('{StockingUnit}', money in '{CatalogCurrency}' at line 57, price at line 60); more at 63-72; line 89 makes the qualifier-source fields (CatalogCurrency, StockingUnit, PurchaseUnit, SaleUnit) editable in Unlisted; line 94 uses an interpolated qualifier in an ensure. One citation nit: cited line 76 is a blank line (the interpolated money fields are at 57-72), but every substantive assertion is verified at the other cited/adjacent lines. (2) Spec: docs/language/business-domain-types.md § Declaration-position interpolation (1473-1511) and § Resolution semantics (1513-1558, Tier 2 dynamic at 1531-1545) exist as cited; the coverage doc rows (exhaustive-gap-analysis-2026-07-14/coverage/business-domain.md lines 147-153) match those line ranges. Crucially, D14's enforcement tiers (business-domain-types.md:1870) are assignment-side only — "literal assignment (compile), event-arg input (runtime boundary), and set time (runtime)" — with no enforcement point at qualifier-source mutation; D9 narrowing guards arithmetic, not source writes. (3) Want doc: line 131 defines editable-write ingress as exactly "the field's modifier-rules and every rule that mentions the field"; line 187 states the runtime does not re-evaluate rules against the post-mutation configuration; the want doc never mentions qualifiers/interpolation. I tested the strongest counter-argument — that under "modifiers = sugar for rules" (line 22) the `in '{CatalogCurrency}'` qualifier might count as a rule mentioning CatalogCurrency and so be evaluated at its write — and it fails: the Modifiers catalog (src/Precept/Language/Modifiers.cs, ModifierKind) contains Optional/Nonnegative/Positive/Min/Max/Maxlength/etc. but not `in`; the qualifier is type surface, neither a modifier nor a rule statement, so the ingress definition does not reach dependent fields. (4) Empirical: precept_compile on a minimal precept (currency field editable in Draft, `Total as money in '{CatalogCurrency}'` accumulated via an event) returns success:true, 0 diagnostics, and the proof-obligation list contains no obligation at the CatalogCurrency write site — only arithmetic-side qualifier compatibility. (5) Corroboration: the superseded readiness plan docs/Working/compiler-readiness-plan-2026-06-16.md:605-617 independently documents this exact gap class ("silently-wrong-money false-Proved" via qualifier-source mutation in samples/fee-schedule.precept) and proposed a qualifier-source write-site obligation (Slice 3.2) that was never built (plan superseded by v3 per project state) — confirming the gap is real and that no such obligation exists today or in the want doc's model. No counter-evidence found.

### comprehensiveness-6 — CONFIRMED (high)

**Claim:** The `omit` access mode is a model gap: 25 of 78 samples omit fields per state, and the want doc's uniform guarantee ('a rule holds, period') has no vocabulary for what a rule mentioning a structurally absent field means in an omit state — resolution requires a semantics ruling (state-scoped vacuity, compile rejection, or presence-based redefinition), each changing what the state-indexed inductive hypothesis contains.

**Verifier:** All three citations verified: (1) samples/insurance-claim-adjudication.precept:79-81 contains exactly the cited omit declarations, and the same file's global rule `PaymentAmount <= LossAmount` (line 66) mentions a field omitted in UnderReview/Investigation — the collision the claim describes; (2) spec-parser.md coverage doc contains the 2.2-access-mode-omit block (lines 35, 72, 89) naming OmittedFieldReadInState/OmittedFieldSetInTargetState; (3) want doc § No deferral contains verbatim 'the guarantee is uniform — a rule holds, period.' The want doc has no omit/structural-absence vocabulary anywhere (its only access-mode content is `modify … editable` ingress at line 99; the lone 'omitted' hit at line 153 is about fault checks, not fields). Counter-evidence hunt for an existing ruling failed and instead corroborated the claim: spec D130 (precept-language-spec.md:1081) enumerates state-anchored read contexts and excludes global rules; diagnostic-system.md:417 says a global rule 'holds in every state'; spec rule 5 (line 1074) says an omitted field 'resets to default' while spec line 2036 says omitted fields are 'structurally absent' (corpus asserts both a value and an absence semantics); an empirical precept_compile probe of a global rule over a field omitted in one state compiled CLEAN with no diagnostic or obligation; and docs/Working/compiler-readiness-plan-2026-06-11.md:135 plus buildability-review-flagship-slices-2026-07-02.md:75 explicitly list the 'omit × global-rule join' as an unadjudicated open item with 'no recorded disposition in canonical docs at HEAD'. One minor inaccuracy: the denominator is 77 samples at HEAD (git ls-files), not 78; the 25 count is exact (anchored grep for `in … omit`). This off-by-one does not alter the claim's substance.

### comprehensiveness-7 — CONFIRMED (medium)

**Claim:** The non-finite `number` posture conflicts with the want: canon specifies runtime faults when NaN/infinity reach rounding functions and says 'the runtime must handle it defensively', while the want omits runtime fault checks entirely — finiteness must become a proven interval obligation (mechanical when operands are bounded; unbounded number fields feeding floor/round must reject).

**Verifier:** Checked both citations verbatim. primitive-types.md:652-658 does specify "Runtime fault" for +∞/-∞/NaN reaching all four rounding functions and states "the runtime must handle it defensively" — the in-sentence qualifier ("proof engine may be able to prove non-finiteness is impossible for specific expressions") does not soften the mandate for runtime defensive handling. The want doc § The runtime's role (line 153) affirmatively excludes evaluation-time fault-prevention checks ("fault-prevention checks are omitted at evaluation time... the evaluator just computes"), line 98 places faults (division by zero, overflow, out-of-range) under the identical premise-and-certificate compile-time treatment, and line 159 (No deferral) rejects anything unprovable rather than handing it to the runtime. Grepped the want doc for NaN/infinity/finite — no carve-out for non-finite handling exists, confirming the omission. The prescriptive tail follows: under prove-or-reject, non-finiteness is a fault-family obligation, mechanical when operands are bounded (min/max ingress premises exclude NaN — it fails all comparisons — and infinities), and unprovable for unbounded number fields feeding floor/round, forcing rejection. Only caveat: "interval" as the specific proof mechanism is an analyst inference, but it matches canon's own hint at line 660 ("Statically preventable when the proof engine can bound the expression range") and the want's bound-based worked example. No counter-evidence found.

### comprehensiveness-8 — REFUTED (medium)

**Claim:** A three-way doc self-contradiction on integer overflow (arbitrary-precision/no-overflow vs overflow-is-a-type-error vs checked-overflow) blocks specifying the integer arm of the fault family that the want's identical-premise-treatment ruling requires.

**Verifier:** The three-way contradiction existed but was owner-ruled and fixed on 2026-07-14, before the want doc (2026-07-16) — the claim cites a stale state. (1) primitive-types.md:81 at HEAD does not say "arbitrary-precision/no-overflow"; it says "Fixed-width 64-bit whole numbers; no rounding. Overflow handling — compile-time proof-or-reject vs. adopting an arbitrary-precision representation — is a deferred post-MVP decision," rewritten by commit 90f0792f ("rule + fix the four Phase-1 stop-and-fix contradictions"), whose diff shows the old "Arbitrary-precision whole numbers; no overflow" text being removed and an Open Questions entry added. (2) The cited stopAndFix item 1 (coverage/primitive-types.md:122) is a historical record quoting the pre-fix line-81 text; the bundle's own STATUS.md:31 records "Stop-and-fix gate: CLEARED (owner-ruled 2026-07-14)... integer-overflow stance (fixed-width today; overflow model deferred post-MVP)... Phase 2: NEXT (unblocked)" and points to coverage-report.md § "Stop-and-fix items — resolved." (3) "Blocks" is mischaracterized: the integer overflow model is unspecified because of a deliberate owner deferral (overflow prevention parked post-MVP; representational-overflow cells tagged "parked — not a fresh finding"), not an unresolved doc self-contradiction; lines 197 ("Overflow is a type error") and 203 ("Checked overflow") were knowingly "left as the target end-state" under that same ruling per the 90f0792f commit message. What survives: the want doc (line 98) does give overflow identical premise treatment, and the overflow model is genuinely open — but as a made deferral decision potentially in tension with the later want, not as the cited three-way contradiction.

### comprehensiveness-9 — REFUTED (medium)

**Claim:** Non-quantified collections are a large but shape-true mechanical extension (~3,400 enumerated cells across 9 kinds): every obligation kind already exists in the ProofRequirements catalog and discharge sources (guards, mincount-1 static discharge, sibling-row negation, count-band tracking, sequential invalidation) are already specified; the log-by-P uniqueness design is guard-premise-or-reject, matching the want's model.

**Verifier:** The claim's quantitative anchor is contradicted by its own citation: docs/Working/exhaustive-gap-analysis-2026-07-14/coverage/collection.md:5 states "Cells: draft 933, added 27, total 960" for the whole collection unit, and assembled/collection.cells.json contains exactly 960 cells (~67 quantifier-related, so non-quantified collections is ~900 cells, not ~3,400). The ~3,400 figure is a corpus-wide total misattributed to collections: STATUS.md:37 gives the per-unit breakdown (collection = 960 of 4,592 merged cells; 3,413 draft cells span ALL nine coverage units), and the 3,446 figure in evidence/exhaustive-cell-probe-register-historical-leads.md is the prior run's total across all four type-system matrices, with the collection slice only gids 1468→~2477 (~1,010). The count is inflated ~3.5x. Everything else in the claim verified: (1) collection-types.md:431 reads verbatim "P uniqueness is a precondition on append, not a runtime fault," and L384/L431 specify the guard-premise-or-reject model (guard absent → compile reject via UnguardedCollectionAccess; guard false at runtime → transition rejects), matching the want doc's premise-class model (what-i-want-2026-07-16.md:19,24); (2) catalog-system.md:139,544 lists 13 ProofRequirement kinds including CountContainment, KeyPresence, IndexBounds, all present as sealed records in src/Precept/Language/ProofRequirement.cs; (3) all five discharge sources are specified — guards (collection-types.md L749-766), mincount-1 static discharge (L354/L427), sibling-row negation (L792), count-band tracking (docs/compiler/proof-engine.md:1257), sequential action tracking (collection-types.md L997/L1071); (4) "9 kinds" is correct. If the analyst restates the count as ~900-960 cells (or ~1,010 in the prior corpus), the remainder of the claim would stand.

### comprehensiveness-10 — CONFIRMED (medium)

**Claim:** State-scoped ensures (66 of 78 samples) generalize the induction to a state-indexed invariant map and add a mid-lifecycle establishment-obligation category (every inbound transition row and entry hook must establish the target state's ensures) that the want doc's construction-only base case does not describe — same proof shapes, but the certificate and diagnostic surfaces need the new obligation category.

**Verifier:** Checked: (1) samples/hotel-reservation-management.precept:83-92 are literal `in <State> ensure` declarations — citation exact. (2) docs/Working/exhaustive-gap-analysis-2026-07-14/coverage/spec-semantics-proof.md §3A.1 table enumerates ensure anchor cells ensure-in-state/to-state/from-state/on-event/guarded-accept — the claimed anchor set. (3) Read docs/Working/what-i-want-2026-07-16.md in full (209 lines): base case (line 18) covers only default-configuration satisfaction plus initial-event establishment; inductive step (line 19) is handler *preservation*; the `ensure` construct, state-scoped invariants, and transition/entry-time establishment obligations appear nowhere — the doc's worked example has no state ensures. Entry hooks (`to State -> set`) are a real sample idiom explicitly used to make state ensures hold (vehicle-service-appointment.precept:23; global-meeting-scheduler.precept:180-182; library-hold-request.precept:44-48), supporting the establishment-obligation category. ONE CORRECTION the parent should carry: the count is 64 of 77 samples with state-scoped (`in`-anchored) ensures, not 66 of 78 — samples/ holds 77 .precept files; the 66 figure counts files merely containing the word "ensure", which includes product-catalog.precept (comment only, no ensure construct) and clinic-appointment-scheduling.precept (only `on <Event> ensure` arg constraints, not state invariants); no bare or `to`-anchored ensures exist in samples (verified by grep). The corrected count does not change the claim's substance (large majority of samples).

### comprehensiveness-11 — CONFIRMED (medium)

**Claim:** The symmetric write-site sweep must enumerate write-site categories beyond handler sets and editable edits — entry hooks, state actions, stateless event hooks, and all 15 collection actions — or the symmetric-obligation guarantee is silently unsound; computed fields (`<-`) additionally make every write to an input a write site for rules mentioning the computed field, transitively.

**Verifier:** Checked all four citations and hunted for counter-evidence. (1) Entry hooks verified exactly at samples/global-meeting-scheduler.precept:184-187 (`to Scheduled -> set ScheduledAt = now()` etc.). (2) spec-parser.md coverage blocks 2.2-state-action (4 cells) and 2.2-stateless-event-hook (3 cells) exist as cited; the doc's count is "18 ActionStatement variants" as the claim's citation states. (3) Computed GrossProfit exists but at inventory-item.precept:72, not :76 (off-by-4; line 76 is blank) — content real, citation line imprecise. (4) spec-typecheck.md line 78 confirms the 3.8-computed-field block (self-ref, transitive cycle, type mismatch, with-default, write-target). Substance: want doc line 20 states obligations attach to "every write site of *every* field the rule mentions" with no category restriction, and the want doc never mentions hooks, state actions, or computed fields (grep empty) — so these are genuinely beyond its enumerated examples (handler sets, editable edits). The spec independently corroborates at precept-language-spec.md:1971: the guarantee "applies to all mutation surfaces: event-driven transitions, stateless event hooks, direct field updates, and state entry/exit actions." The computed-field transitivity inference is sound: §3.8 makes computed fields unwritable in all positions (ComputedFieldNotWritable) and permits acyclic transitive computed chains, so input writes are the only channel that changes them. One label imprecision: ActionKind.cs has 15 total actions of which Set is scalar (strictly 14 collection actions), but "15 actions" is the established project count (comprehensiveness.md M3, prior audits) and all 15 are mutation actions, so the sweep-completeness point is unaffected. No evidence found that any of these write-site categories is exempt or already covered by the want doc's enumeration.

## Axis: clarity

### clarity-1 — CONFIRMED (high)

**Claim:** The third-arm dispute is answered unambiguously by the want doc — no post-mutation sweep exists ('the runtime does not re-evaluate them against the post-mutation configuration'; runtime runs the declared premises 'and nothing beyond them') — but closure requires deleting the losing canon sentences, which are still live at HEAD (spec 3A.4:1969 'This post-mutation sweep is one of two enforcement points' and the sweep/fault-trap clauses in the spec:272 boundary paragraph).

**Verifier:** Checked all citations at their exact locations. (1) Want-doc quotes are verbatim: what-i-want-2026-07-16.md:187 "the runtime does **not** re-evaluate them against the post-mutation configuration"; :153 "What runs at runtime are the declared premises (ingress validation of args and editable fields, guards)... and nothing beyond them." (2) Hunted for qualifiers: grep of the full want doc for sweep/post-mutation/re-evaluat/re-check/ensure hits only lines 153 and 187, both same-direction; the "only proven rules" escape hatch is closed by the No-deferral section (:159 — unproven rules are rejected, never handed to runtime), so the answer is unambiguous within the doc. (3) Canon sentences verified live at committed HEAD ed57d7fd via git show (spec file unmodified in working tree): precept-language-spec.md:1969 begins exactly "This post-mutation sweep is one of two enforcement points"; :272 contains both the sweep clause ("every constraint is checked against the resulting working copy, §3A.4") and the fault-trap clause ("the runtime fault traps backstop any out-of-contract value"). (4) The deletion-required framing matches forensic doc §6 (third-arm-forensic-analysis-2026-07-15.md:219): "Whichever way it goes, the other sentence gets deleted." No counter-evidence found.

### clarity-2 — CONFIRMED (high)

**Claim:** The base-case relational rule the forensic said was worked nowhere in the corpus (rule Floor <= Ceiling over two independently-set fields) is now fully covered: handler writes are proven inductively via symmetric write-site obligations, and editable-field writes evaluate every rule mentioning the field at the ingress point.

**Verifier:** Every citation checks out verbatim. what-i-want-2026-07-16.md:19-20 states the inductive step and symmetric obligations ("obligations attach to every write site of *every* field the rule mentions"); line 97 works the symmetric write site (ReduceLimit: relational rule, obligation at the OverdraftLimit write, compiler rejects without the guard); line 131 states that an editable-field write evaluates "the field's modifier-rules *and* every rule that mentions the field" at the ingress point, and that this evaluation "is exactly the premise that closes the editable write sites in the compiler's proof." third-arm-forensic-analysis-2026-07-15.md §1 (lines 19-29) does say the Floor<=Ceiling base case "appears nowhere in the entire corpus" / "worked in none of them." Refutation attempts failed: (1) both write paths for the Floor/Ceiling shape are covered — handler sets by induction+symmetric obligations, editable edits by ingress evaluation of relational rules against stored state — plus base-case establishment at line 18; (2) the want doc does not reintroduce the sweep (line 187: runtime does not re-evaluate against post-mutation configuration; line 153: only declared premises run); (3) no ignored qualifiers — line 131's "no static proof can cover" clause makes ingress evaluation the declared premise, not deferral; (4) the literal Floor/Ceiling example is not worked verbatim in the want doc, but the claim asserts coverage by mechanism, which line 131's general rule subsumes. Working tree is clean, so cited lines match committed HEAD.

### clarity-3 — CONFIRMED (high)

**Claim:** The fault-gate scope dispute is answered unambiguously (prove-or-reject covers all business rules, with faults an additional family getting identical treatment), but the fossil text that lets any future draft re-derive the narrow reading remains quotable at HEAD: spec:266's 'no result outside a declared bound' under the 'Fault prevention' heading and P11's 'constraint range impossibility' at spec:112.

**Verifier:** Checked all citations at the exact cited lines. want-doc:14 reads verbatim "the compiler **proves or rejects all business rules**"; want-doc:98 reads "faults (division by zero, overflow, out-of-range) get the identical premise-and-certificate treatment" — together they support "answered unambiguously" as stated (the doc's "ideal world" framing qualifies feasibility, not the scope answer). spec:266 contains "no result outside a declared bound" inside the "**Fault prevention — established entirely at compile time.**" paragraph; spec:112 is P11 and contains "constraint range impossibility"; both verified identical at committed HEAD via git show, working tree clean. Forensic doc §11 item 1 matches the claim's fossil characterization nearly verbatim ("still quotable ... every future draft can re-derive the gate faithfully"). Counter-evidence hunt found none: no HEAD text deletes/reverses either fossil phrase; the forensic doc's extra third fossil location (ruling:130) is an omission, not a contradiction, since the claim does not assert exhaustiveness. Minor pedantry only: spec:266's "Fault prevention" is a bold paragraph lead rather than a markdown heading, but the cited forensic doc itself calls it a heading.

### clarity-4 — CONFIRMED (high)

**Claim:** ANSWERED AMBIGUOUSLY — multi-write handler semantics: the want supports both a per-plan reading ('Every handler preserves every rule', pre-to-post-state) and a per-write reading ('obligations attach to every write site'), leaving PRE0078's per-write-vs-per-plan question (fable:98 via collation F12) and the fate of working-copy atomicity/ConstraintsFailed undecided.

**Verifier:** All citations verified exactly: want-doc :19 ("Every handler preserves every rule" from pre-state premises — per-plan) and :20 ("obligations attach to every write site of every field the rule mentions" — per-write-site) are quoted accurately and genuinely support both readings; collation F12 (:184) attributes the PRE0078 per-write-vs-per-plan question to fable:98, and the underlying passage in fable-analysis-of-frank-response-2026-07-11.md confirms it verbatim ("the constraint binds the final copy, not every intermediate assignment"), including the discriminating transient case (set Balance = -50 then +100); spec :1967 is exactly §3A.4 Mutation Atomicity (working copy, post-mutation sweep) with ConstraintsFailed at :1938. Counter-evidence hunt failed: I read the full want doc — it never mentions working copy, intermediate assignments, atomicity, or ConstraintsFailed; the worked example (ReduceLimit :97, OpenAccount :94) leans per-plan but no example discriminates the readings for a multi-write handler; lines 153/187 remove runtime post-mutation rule re-evaluation but leave the working-copy mechanism's and ConstraintsFailed's fate unstated (the related Fire-sentence rewording is explicitly flagged "not resolved here," :203). Fable's own per-plan recommendation at fable:98 is analyst opinion, not an owner ruling, consistent with F12's "nothing in the ledger addresses it."

### clarity-5 — CONFIRMED (high)

**Claim:** ANSWERED AMBIGUOUSLY / UNANSWERED — the fault-registry and runtime outcome taxonomy: the want makes OutOfRange/Length/Count registry membership behaviorally moot (both families reject when unproven) but never speaks to [StaticallyPreventable] membership, FaultCode taxonomy, or what happens to ConstraintsFailed, so the runtime API surface must be reconciled separately.

**Verifier:** Checked: (1) want-doc lines 98 and 153 verbatim — line 98 says faults get "identical premise-and-certificate treatment" as rules, line 153 says fault-prevention checks are omitted at runtime; with the No-deferral section this makes fault-vs-rule classification of OutOfRange/Length/Count behaviorally moot (both paths reject when unproven), as claimed. (2) Forensic §11 item 2 verbatim matches: "disposition is unaffected... what is unresolved is registry membership and the [StaticallyPreventable] claim." (3) Exhaustive grep of the full 209-line want doc (plus a full read of lines 85-209) for StaticallyPreventable, FaultCode, ConstraintsFailed, registry, taxonomy, EventOutcome: zero hits — the doc's only runtime-api.md reference (line 151) concerns Version immutability, and its only outcome language is informal (Unmatched/reject/refusals). (4) Counter-evidence hunt: the Certificates section's "does not re-evaluate post-mutation" arguably implies EventOutcome.ConstraintsFailed (result-types.md:114, "post-mutation constraints violated") becomes unreachable, but the want never names the variant or disposes of it, and UpdateOutcome.ConstraintsFailed (result-types.md:137) plausibly survives as the editable-write ingress refusal — so the API surface does need separate reconciliation, exactly as claimed. All cited terms confirmed as real runtime-API surface (docs/runtime/result-types.md, fault-system.md, src/Precept/Runtime/Evaluator.cs, StaticallyPreventableMap in ProofEngine.Diagnostics.cs). Minor note: the want names only "out-of-range" (not length/count) among fault examples; the claim's grouping of the three codes follows the forensic doc's own grouping and is sound.

### clarity-6 — REFUTED (high)

**Claim:** UNANSWERED as enumeration — the want gives the obligation treatment but never enumerates the complete obligation-family list; presence (unset-optional read) and qualifier/dimensional compatibility, named floor families absent from every prove-or-reject enumeration, are still unenumerated, and the canonical doc must carry the full family list or the next draft re-derives it from the fossil.

**Verifier:** The claim's first leg checks out, but its load-bearing assertion is contradicted at HEAD.

What holds: docs/Working/what-i-want-2026-07-16.md itself never enumerates the obligation-family list. Line 98's "faults (division by zero, overflow, out-of-range)" is an illustrative parenthetical; grep of the whole want doc finds no mention of presence/unset-optional or qualifier/dimensional families. The cited forensic passages also exist as cited (prove-or-reject-scope-forensic-2026-07-15.md §8 "Pointing the other way" bullet 1, lines 241-245; §11 item 3, line 302).

What refutes the claim: the assertion that presence and qualifier are "absent from every prove-or-reject enumeration" and "still unenumerated," so that "the canonical doc must carry the full family list or the next draft re-derives it from the fossil," is false at HEAD. docs/compiler/soundness-and-coverage.md — which self-describes as "the canonical home for how the prove-or-reject guarantee is made sound" (lines 14-16) and which the forensic itself names as "the authoritative boundary" — carries in §3.1 (lines 179-209) a closed, exhaustive per-FaultCode enumeration ("every member is dispositioned below," keyed to FaultCode.cs as source of truth) that includes both allegedly missing families: `UnexpectedNull` → `Presence` (optional set before access) = **in** (line 193), and `QualifierMismatch` → `QualifierCompatibility`/`QualifierChain`/`AssignmentQualifier`/`DimensionalProduct` = **in** (line 199). §3.2 (lines 211-230) additionally dispositions position/creation-site totality. This enumeration was committed in e13b94cd (2026-07-16 00:03, git-verified: the `UnexpectedNull` row is absent at 3e3fb0dd and present at e13b94cd) — i.e., after the 07-15 forensic snapshot the claim relies on, and before today (2026-07-19). The claim thus flattens the forensic's own explicit qualifier into a stale assertion: the forensic marked this exact point "UNCERTAIN... should be closed before any ruling" because §3.1 "was not read" (§8 line 243), and its falsifier 6 (line 327) states that §3.1 enumerating presence/qualifier/position-totality would dissolve the floor-stronger items — which is precisely what §3.1 now contains. The claimed risk ("the next draft re-derives it from the fossil") therefore rests on a false premise: the full family list already exists in a canonical compiler doc and does not need re-derivation. Only the narrow want-doc-internal observation survives; the "UNANSWERED as enumeration" conclusion does not.

### clarity-7 — CONFIRMED (high)

**Claim:** Yardstick failure (structural): the want doc cannot be the single yardstick for all 4,592 gap-analysis cells — the three pure-surface units (spec-parser 286, spec-preamble-lexer 187, spec-typecheck 433; ~20% of cells) are outside its vocabulary by explicit design ('the surface is the negotiable part'), so gap analysis needs a two-yardstick protocol: canonical boundary doc for disposition-dependent cells, language spec for surface cells.

**Verifier:** Checked: (1) want:125 quote is verbatim ("...where it doesn't, the surface is the negotiable part"), and the want doc contains no parser/lexer/typecheck/syntax/grammar content anywhere (grep empty) and never claims single-yardstick status. (2) Counts measured directly from assembled/*.cells.json: spec-parser 286, spec-preamble-lexer 187, spec-typecheck 433; all-cells.json total 4,592; 906/4,592 = 19.7% ≈ 20%. (3) Report Part 3 probe rows 9-12 exist at docs/Working/want-analysis-2026-07-19/clarity-dispute-closure.md:113-116 with the pattern (line 118) and two-yardstick protocol (line 120) exactly as claimed. Counter-evidence hunt: all 906 cells in the three units cite only docs/language/precept-language-spec.md (no boundary-doc cites); apparent boundary-keyword hits were substring noise ("fault" in "default"). One immaterial precision loss: the report says the three units are "almost entirely" pure-surface (probe row 12 brushes the want's ingress model but is still ruled outside); the claim drops that hedge, which does not affect the structural conclusion.

### clarity-8 — CONFIRMED (high)

**Claim:** Yardstick conflict found in probe: the two degenerate-period cells (rule '1 month' == '30 days', expected accept-with-warning:DegeneratePeriodComparison) appear overturned by the want — a constant-false rule fails the base case and must reject, and accept-with-warning is the 'warnings where there should be errors' dilution the want names — so boundary-dependent cells' expected verdicts must be re-adjudicated, not inherited.

**Verifier:** Checked: (1) docs/Working/exhaustive-gap-analysis-2026-07-14/assembled/temporal.cells.json — both cells exist as cited: gid 389 id temporal/period/op/eq-degenerate-literal-warning (exercise `rule '1 month' == '30 days' because "x"`, expected accept-with-warning:DegeneratePeriodComparison) and gid 390 (neq variant, same expected verdict). (2) docs/language/temporal-type-system.md:797,863 — spec confirms the eq comparison "is always `false`" statically and mandates a warning while keeping the construct legal, so the cells faithfully reflect the spec and the conflict is spec-vs-want as claimed. (3) docs/Working/what-i-want-2026-07-16.md — line 177 verbatim names "warnings where there should be errors" as dilution; line 24 defines acceptance as "no reachable operation can violate any rule"; a constant-false rule fails the base case (line 18 — the claim cites line 19, a one-line offset: 18 is the base-case bullet, 19 the inductive step), so the want demands reject for the eq cell, overturning accept-with-warning. Caveat the parent should weigh: the constant-false reasoning covers only gid 389; gid 390's rule is constant-TRUE (trivially provable theorem under want line 24), so the want does not obviously overturn its accept verdict — its warning is an intent-lint, not warnings-where-errors. The claim's operative conclusion (re-adjudicate boundary-dependent expected verdicts, don't inherit) survives for both cells, and "appear overturned" is hedged, but "the two degenerate-period cells appear overturned" over-extends the constant-false argument to the neq cell.

### clarity-9 — CONFIRMED (high)

**Claim:** Yardstick underdetermination: unguarded-arithmetic accept cells (e.g. primitive/integer/op-add, 'set Total = CountA + CountB' expected accept) are unadjudicable until the number-model decision is made, because the want places overflow in the fault family requiring proof and only an arbitrary-precision number model deletes that obligation.

**Verifier:** Checked all three citations: (1) primitive-types.cells.json does contain id primitive/integer/op-add with exercise 'set Total = CountA + CountB', expected accept, no bounds on operands; (2) what-i-want-2026-07-16.md:98 explicitly lists overflow in the fault family receiving "identical premise-and-certificate treatment" (line 153 confirms: runtime omits overflow guards because compile-time proofs cover them); (3) collation index §3 F10 (line 166) is the two-sided overflow fork, marked "decided; two-sided; must be reproduced, not resolved." Counter-evidence hunt found reinforcement, not refutation: docs/language/primitive-types.md:686 (and :81) states the number-model decision is exactly the claimed dichotomy — "compile-time proof-or-reject ... or adopting an arbitrary-precision representation (removing overflow as a failure mode) is a post-MVP decision, not yet ruled" — and that until ruled, integer arithmetic "carries no live compile-time guarantee." Under fixed-width int64 with prove-or-reject, an unguarded add on unbounded fields provably can overflow and would have to reject, contradicting the cell's "accept"; under arbitrary precision the obligation vanishes and "accept" stands. The cell's verdict therefore genuinely hinges on the unmade number-model decision, as claimed.

### clarity-10 — CONFIRMED (high)

**Claim:** Vocabulary gaps that leave probed cells only adjudicable by extending the model: computed fields (<-) never appear in the want (no guard, no ingress slot — which premise classes serve their expressions is unstated), ensure/state-scoped rules are never named (presumably rule spellings under modifiers-are-sugar, but unstated), and where a rule-expression's own fault obligations attach (division inside a rule) is unspecified.

**Verifier:** Checked: (1) full read of docs/Working/what-i-want-2026-07-16.md (209 lines) plus grep — zero occurrences of "computed", "<-", or "ensure"; the language genuinely has these constructs (spec lines 514/850/909/1351 for the <- computed-field arrow, 341-344/946-960 for ensure and state-scoped rules; both used throughout samples/), so the vocabulary gap is real. (2) Want lines 22 and 131 check out as the vocabulary-defining passages: only "modifiers or rule statements" are named as spellings; ensure/state-scoped rules are unstated, matching the claim's "presumably rule spellings" hedge. (3) Ingress is defined as exactly two points (want line 129: event args, editable-field writes) and computed fields fit neither (spec line 1085 makes editable-on-computed a compile error), confirming "no guard, no ingress slot". (4) The doc's only fault-attachment treatment is division in a handler set expression (lines 98/112/153); where a fault obligation inside a rule's own expression attaches is nowhere stated — resolving the cited cell requires extending lines 20+98 by inference. (5) Both cited cell IDs exist in the assembled JSON files with exercises matching the claim (a <- computed-field division, and division inside a rule expression). No counter-evidence found: no passage in the want doc names or implicitly covers computed fields, ensures, or rule-internal fault attachment.

### clarity-11 — CONFIRMED (medium)

**Claim:** All remaining named disputes are answered unambiguously by subtraction: rows 10/11 spelling split dead (one constraint mechanism), router provenance-vs-proof-carrying dissolved (ingress checks are proof premises, not a disposition), merely-unprovable and proven-violating writes both reject, four-disposition taxonomy collapses to prove-or-reject, defaults hole closed by the base case, set/rule evadability closed structurally, undecidable-bound hole closed (unprovable = reject), F2 ban is broad, F3/F11 dissolved by owner supersession.

**Verifier:** Checked every cited want-doc line at HEAD ed57d7fd (docs/Working/what-i-want-2026-07-16.md:22,26,114,119,143,159,161,167) — all quotes accurate and support the per-item verdicts: one constraint mechanism kills the rows 10/11 spelling split (cross-verified against proof-engine-boundary-ruling-2026-07-06.md:226, where rows 10-11 are the 'Expressible — governed' lane that no longer exists); want:26/119 make ingress checks proof premises, dissolving F5's router; want:159 gives unprovable=reject (D7, D10, broad F2 with want:153's 'nothing beyond them'); want:143/117/114 give proven-violating=reject (D8) and the base-case closure of the defaults hole (want:19/99); want:161 collapses the four-disposition taxonomy; want:22+159 close set/rule evadability structurally (no governed lane to escape into). Verified collation index §3 F2-F11 descriptions match. Adversarial trace of the weakest link — F11, which the source report (clarity-dispute-closure.md:56) marks 'ANSWERED by supersession, partially' with untraced folded items — resolved in the claim's favor: per proof-engine-decision-ledger-2026-07-12.md:24, Reading N (fault-scoped no-deferral) is superseded by want:14/159, Q9 (PROVEN/GOVERNED visibility) by want:161, GATE-H (three-category taxonomy) by want:98, Hybrid/origin boundary by the axis removal; sole residue is the overflow interim-statement reproduction obligation, which is F10's decided reproduce-don't-resolve item, not a live dispute. Caveats that do not overturn the claim: the source attaches drafting residues (F6 'for declared rules'; F7 defaulted event args; F8 never names `ensure`) as canonical-doc obligations, and the claim's 'remaining' scope correctly excludes the disputes the source marks ambiguous (D6, D9, F4/F9, F12(a)).

### clarity-12 — CONFIRMED (medium)

**Claim:** The 07-11 thesis failed to close the argument for three structural reasons the want doc avoids (agent-authored, proposed a new boundary that became fork F5, added disposition vocabulary that became fork F6); the want settles the thesis's contested cell against it (reject, not govern), dissolves its proof-carrying partition, kills Recommendations A/F/G/H, and leaves open only its philosophy:49 'No bugs' temper, the DbC comparator row, and the definition-evolution lane (now scoped to post-MVP restore).

**Verifier:** Checked every doc citation at the cited lines and hunted for counters. Thesis:5 confirms agent authorship (Fable/Claude) vs want:4 (Shane). Thesis:31/33/151 confirm the four-disposition vocabulary and proof-carrying boundary originate as the thesis's own constructs; prove-govern-collation-index-2026-07-15.md:135-146 confirms these are exactly forks F5 ("provenance or proof-carrying?" — "the thesis and both position papers keep proof-carrying as the only boundary that matters") and F6 ("Two dispositions or four?"). Want:159 (no deferral — unprovable rule enforcement = reject, never handed to runtime), :143 (dead rows rejected), :161 (uniform guarantee, no tiers), :119 (unconsumed modifiers still proven; asymmetry is spec-strength only), :177 (warnings-where-errors = dilution) jointly settle the thesis's contested cell (thesis:151 GOVERN) as reject, dissolve the proof-carrying partition, and kill Recs A (thesis:356), F (:366), G (:368), H (:370). Open-set check: philosophy.md:49 still contains "No errors. No bugs." (the "wrong answer" clause was tempered separately in commit 33dde428 on 2026-07-12, leaving 'No bugs' as precisely the open remainder the claim names); philosophy.md comparator table has no Eiffel/DbC row (grep empty); want:129 scopes definition-evolution to post-MVP restore. Recs D and E are dissolved (not open) by want:98/153 (overflow gets identical premise-and-certificate treatment) and want:19-21/131 (modifiers/rules are real premises with symmetric write-site obligations). No counter-evidence found. Two immaterial nits: cited range 346-370 excludes Rec I's line 372 (its substance verified at want:129), and the "report Part 2 table" citation is the analyst's own report, not present in the repo — all substantive assertions were verified from primary docs instead.

### clarity-13 — CONFIRMED (medium)

**Claim:** Out-of-envelope data has no stated behavior under the want: spec:272 currently backstops host-injected/tampered values with runtime fault traps and next-operation sweep re-governance, both of which the want removes, while restore is declared trusted-and-unchecked — sound for untampered same-definition data but undefined for anything else; the canonical doc must state the trust boundary explicitly.

**Verifier:** Checked all four citations verbatim. spec:272 does state both backstops for out-of-contract values: "the next operation through the contract re-governs it (every constraint is checked against the resulting working copy, §3A.4), and the runtime fault traps backstop any out-of-contract value the evaluator would otherwise fault on." The want removes both: want:153 ("fault-prevention checks are omitted at evaluation time... What runs at runtime are the declared premises... and nothing beyond them") and want:187 ("the runtime does not re-evaluate them against the post-mutation configuration"). want:129 declares restore "trusted and unchecked, because it was governed when it was written," with schema evolution (older-definition data) explicitly deferred post-MVP — supporting the "sound only for untampered same-definition data" qualifier. Counter-evidence hunt: full read of the want doc plus grep for host/inject/tamper/bypass/envelope/trust. The only near-miss is want:192's certificate load-gate, which covers tampered *definitions*, not tampered/injected entity *data* — it does not state behavior for out-of-envelope data. No passage in the want doc (including Open Questions) addresses host-injected or tampered persisted values, so "no stated behavior under the want" holds. The closing "must state the trust boundary explicitly" is a recommendation, not a factual assertion.

### clarity-14 — CONFIRMED (medium)

**Claim:** The band-suppression/constraint-deletion tradeoff (Fable's argument that authors delete unprovable rules, leaving fewer declared truths — the reason both position papers made the opt-in construct a co-requisite) is structurally mooted by the uniform-guarantee ruling but never named as an accepted tradeoff in the want; the canonical doc's four-leg rationale needs it stated.

**Verifier:** Checked: (1) want:161 contains the uniform-guarantee ruling ("no author-facing distinction... no tiers of trustworthiness"), which structurally forecloses the opt-in governance construct — corroborated by frank-review-what-i-want-2026-07-16-opinion.md:534-535 ("what-i-want closes that exit"). (2) want:119 is the closest the want comes to deletion (unconsumed modifiers, "the author saying less") but does not name the rejection-pressure deletion dynamic; grep of the full want doc for band/suppression/deletion terms confirms the tradeoff is never named as accepted. (3) Collation F4/F9 quotes verified against fable-analysis-of-frank-response-2026-07-11.md (Claim 8, "delete the band... fewer declared truths"; recommendation making the construct a co-requisite of Error). (4) "Never named" independently confirmed by two review docs: frank-review:51/466/538 ("re-closes the escape hatch without naming the band-suppression cost"; F9 "REOPENS (silent)"; "missing from what-i-want, and it must be restored") and want-analysis-2026-07-19/clarity-dispute-closure.md F9 row ("the suppression cost is unnamed") + :145 ("Named tradeoffs for four-leg completeness: the constraint-deletion/band-suppression dynamic (F4/F9)... as accepted-tradeoff entries, not silences"). (5) Grep of committed canonical docs (philosophy.md, compiler-and-runtime-design.md, soundness-and-coverage.md, proof-engine.md): the tradeoff appears nowhere, so "needs it stated" is not already satisfied. ONE CAVEAT on the parenthetical only: the constraint-deletion argument was Fable's alone, not both papers' shared reason — Fable's adjudication introduces it as "new, neither doc stated it" (Claim 8) and says "Frank's paper never engages this consequence" (Claim 9); Frank's reason for the opt-in exit was the addressability/explicit-authored-signal argument (frank position paper :190), and Frank conditioned GOVERN on it while Fable made it a co-requisite of Error. Both parties did make the construct a condition (F4 header), so the operative claim stands; only the reason-attribution to Frank is inaccurate.

### clarity-15 — CONFIRMED (high)

**Claim:** Overall verdict: the want doc can end the arguments — every extracted dispute is answered, dissolved, or owner-declared open, a categorically stronger position than any prior attempt — but only if the short canon-deletion pass executes (five committed spec/philosophy passages, one uncommitted file, the owner-gated Fire sentence) and the canonical doc adds the enumerations and vocabulary mappings listed in Part 4, since the forensics' root cause stands: arguments in this corpus end when the losing sentence is deleted, not when the winning one is written.

**Verifier:** Checked: (1) Report Part 4 exists at docs/Working/want-analysis-2026-07-19/clarity-dispute-closure.md:124-160 with the deletion list and the must-add enumeration/vocabulary items; its Assessment paragraph states the every-dispute-answered/dissolved/declared-open conclusion and the "categorically better position" comparison the claim summarizes. (2) All five committed deletion-list passages verified live at HEAD ed57d7fd by reading them from the commit: precept-language-spec.md:266 ("Fault prevention" heading incl. "no result outside a declared bound"), :272 (boundary paragraph with §3A.4 re-governance clause and fault-trap backstop), :112 (P11 "constraint range impossibility"), :1969 ("This post-mutation sweep is one of two enforcement points… the sweep governs *the result*"), philosophy.md:19 (Fire sentence, owner-gated per want:203). (3) Both forensic citations verified verbatim: third-arm §6 ("the other sentence gets deleted — which is the mechanism that stops the recurrence") and prove-or-reject §11 item 1 ("Deletion, not decision — the same shape as the §0.7/§3A.4 fix"). (4) The report's dispute tables (D1-D11, F1-F12): all rows are answered/dissolved/owner-declared-open except two "ANSWERED AMBIGUOUSLY" residues (D6 registry taxonomy, F12(a) per-write-vs-per-plan), which the claim's conditional on Part 4's additions absorbs (Part 4 must-add items 1-3 cover them). ONE CORRECTION the parent should apply: "one uncommitted file" is stale — soundness-and-coverage.md §1.1 was COMMITTED at e13b94cd (2026-07-16, ancestor of HEAD; working tree clean), with the commit message explicitly flagging it as "KNOWN DEFECT… NOT correct as they stand." The §1.1 framing ("The only line that matters is whether an operand carries a constraint," committed lines 85-87) is still live and still needs the rewrite, so the deletion-pass inventory and the verdict are unaffected — but it is a sixth committed canon edit, not a pre-commit rewrite. Minor wording note: the report's Assessment says "categorically better than the 07-11 thesis"; the claim widens this to "any prior attempt."

## Axis: meta

### meta-1 — CONFIRMED (high)

**Claim:** Restore-is-trusted loses its only backstop under the want: current canon justifies skipping restore validation because 'a stale value is re-governed by the next operation's post-mutation constraint sweep', and the want removes that sweep while keeping restore unchecked, so restored-stale or out-of-band data falsifies the inductive pre-state-rules premise and a rule violation persists silently through subsequent proven handlers (e.g. Deposit, proven only if the rule held before).

**Verifier:** Checked every citation at source: runtime-api.md:266 contains the exact canon justification ("a stale value is re-governed by the next operation's post-mutation constraint sweep (spec §3A.4)"), corroborated by spec §0.7:272 and §3A.4:1969 ("re-governed only by the next operation's sweep" — canon's own "only"). Want doc verified: :129 restore trusted/unchecked and not ingress; :153 runtime runs declared premises "and nothing beyond them"; :187 no post-mutation re-evaluation; :19 premise class (d) = all rules holding in pre-state; :95 Deposit proven "only if the rule held before the event". philosophy.md:71 contains the ORM-bypass critique verbatim. Counter-evidence hunted and rejected: (1) spec:272's second backstop (runtime fault traps) covers faults, not rule violations, and want:153 removes fault checks too; (2) want:129's post-MVP schema-evolution parenthetical is deferred, covers old-definition data only (not out-of-band writes), and supplies no MVP backstop; (3) guards persist but the Deposit path is guardless and rests exactly on premise (d); (4) certificates verify the definition's proof, not restored data; (5) editable-write ingress fires only on edits, not on the restore-then-Deposit path. The failure scenario (restored Balance < -OverdraftLimit, Deposit fires, only Amount>=0 checked, violation persists silently) follows directly from the cited text with no missing qualifier.

### meta-2 — CONFIRMED (high)

**Claim:** The certificate checker as described ('walks those steps and confirms each one') verifies proof soundness but not proof coverage — it cannot detect an obligation the proof engine never emitted, so the claim 'no unproven definition can ever govern' requires the trusted checker to enumerate obligations from the definition itself, the move PCC's consumer-side VC generation makes and the repo's own witness-checking survey states as conclusion C3 ('witness-checking does NOT address generation-completeness').

**Verifier:** Checked: (1) docs/Working/what-i-want-2026-07-16.md:187-194 — quotes accurate; certificate is compiler-emitted "per obligation" (l.187), checker "simply walks those steps and confirms each one" (l.194), guarantee "no unproven... definition can ever govern" (l.192). Grepped the whole want doc: no passage has the checker enumerating obligations from the definition; l.198 says the runtime's validation surface is "derived from the proof", so a never-emitted obligation is invisible to the walk-the-steps checker. l.192's own "rooted in verification the runtime performed itself, not in provenance it assumes" makes the claim's "requires trusted-side enumeration" inference tight — a compiler-side coverage audit would be provenance-assumed. (2) research/architecture/compiler/witness-checking-soundness-architecture-survey.md:172-176 — C3 verbatim: "witness-checking does NOT address generation-completeness"; :173: "a missed obligation (BUG-017 shape) is a coverage hole the checker is blind to"; same at Threats :147. (3) :94 — PCC leg present and Tertiary-flagged exactly as the claim states; "consumer-side VC generation" is not literally in the cited line but is an accurate characterization of Necula & Lee's architecture and the claim discloses the Tertiary flag. Counter-evidence hunt: survey C3 proposes a catalog-driven coverage audit (:174) as the coverage mechanism — but that is compiler/build-side (provenance from the runtime's perspective), so it does not undercut the claim about the want doc's load-time guarantee; readiness plan (compiler-readiness-plan-2026-07-12.md:56-57,127,164) confirms the §5b re-checker has no obligation-enumeration role. No misquote, missing qualifier, or contradicting passage found.

### meta-3 — CONFIRMED (high)

**Claim:** The want doc's obligation-attachment rule ('obligations attach to every write site of every field the rule mentions') misses constraints activated by pure state transitions: the language has state-scoped ensures ('in State ensure', 'to State ensure') which the want doc never mentions, and a no-write transition row like the example's Freeze can enter a state whose ensure the unchanged data violates while carrying no obligation under write-site attachment.

**Verifier:** Every citation checks out, with one trivial off-by-one. (1) The quoted attachment rule is verbatim at docs/Working/what-i-want-2026-07-16.md:20 (cited as :19; line 19 is the adjacent inductive-step bullet): "A rule's obligations attach to every write site of *every* field the rule mentions". Lines 77-79 are exactly the Freeze rows — `from Active on Freeze -> transition Frozen`, no set actions, no writes. (2) State-scoped ensures are live language surface, not just token-table entries: spec :327 (`ensure` = state/event assertion keyword), :341 (`in State ensure ...`), :342 (`to State ensure ...` entry gate) as cited, plus grammar at :991-998 and semantics at :1884-1886 — "in <State> ensure — residency truth. The constraint holds for every operation while the entity is in this state" and "to <State> ensure — entry truth. The constraint is checked on any transition into this state." So entry into a state triggers ensure checks regardless of writes. (3) philosophy.md:15 lists "ensures (state- or event-scoped constraints that must hold in specific contexts)" as first-class, as cited. (4) "Never mentions" verified by exhaustive grep: the want doc contains zero occurrences of "ensure"/"ensures" in any form. (5) Counter-evidence hunt: the want doc's four premise classes (:19 — modifiers, arg constraints, guard, rules-holding-in-pre-state) cannot close a target-state-scoped ensure, because premise (d) only supplies rules that held in the PRE-state (Active), and an `in Frozen`/`to Frozen` ensure is not required to hold there; "Modifiers are sugar for rules" (:22) enumerates spellings that do not include ensures; the doc's deliberate-omission list (Open questions, :207-208) covers time-referencing rules and cross-precept only — ensures are not a declared deferral. The example itself declares no ensures, so the violation scenario is hypothetical, but the claim words it that way ("a no-write transition row like the example's Freeze can enter a state whose ensure..."). The gap is real as stated.

### meta-4 — CONFIRMED (medium)

**Claim:** The editable-write ingress rule-set ('every rule that mentions the field') closes the induction only if 'mentions' is defined to include guard-position occurrences, desugared cross-field modifiers (A min B is legal and references another field), and derived-field dependents — derived '<-' fields exist in the language and samples but are wholly absent from the want doc, and editing a source field implicitly changes derived fields that other rules read.

**Verifier:** Checked: (1) want doc line 131 quotes verbatim — "every rule that mentions the field" is the editable-write ingress rule-set and "mentions" is never defined anywhere in the doc (also used at lines 20, 108); (2) spec 1352-1354 confirms cross-field modifiers — constraint modifiers are rule shorthand (§2.4) and "A min B" is explicitly legal with any-field scope; (3) research doc line 14 confirms derived '<-' fields are "computed, not constrained, and therefore need no preservation proof at all"; (4) grep of the full want doc for '<-', 'computed', 'derived', 'accumulator', 'formula' — derived fields are indeed wholly absent (sole 'derived' hit, line 198, means "derived from the proof"); (5) guard-position leg verified against spec grammar line 935 (rule BoolExpr ("when" BoolExpr)? because StringExpr) and line 1866 (rule holds wherever its guard is satisfied) — an edit to a guard-only-mentioned field can newly activate a rule; (6) derived-dependent leg verified: computed fields cannot be editable (ComputedFieldNotWritable, spec 1085/1667) and rules see all field names (spec 1344), so an edit reaches a rule on a computed field only implicitly via its sources, which a textual "mentions" misses; samples/invoice-line-item.precept is exactly this shape (editable UnitPrice/Quantity/DiscountPercent/TaxRate feeding the Subtotal→LineTotal chain). Two minor caveats, neither refuting: the sample has 5 '<-' field declarations, not 6 (the sixth grep hit is a comment on line 9); and derived-field rules statically implied by source-field modifiers (the sample's own case, per its header comment) discharge without dependent-inclusion — the inclusion is load-bearing only for derived-field rules not provable from source constraints, which the language can express, so the "only if" claim over the language stands.

### meta-5 — CONFIRMED (medium)

**Claim:** Nothing in the want's model checks the proof theory's arithmetic against the runtime evaluator's arithmetic, and a live drift instance exists at HEAD: 'number' is IEEE 754 double by spec, but its interval transfers for addition/subtraction use the exact-decimal AddTransfer/SubtractTransfer with no outward float-rounding widening while multiply/divide get ULP widening — under the want's check-free evaluator a verified certificate could be unsound at runtime, the exact 'drift pole' the repo's static-vs-runtime survey concludes no sound surveyed system tolerates.

**Verifier:** Checked every citation verbatim and hunted for counter-evidence. (1) src/Precept/Language/Operations.cs:171/:175 assign exact AddTransfer/SubtractTransfer (bare l.Add(r)/l.Subtract(r), :1332-1334) to the number ops labeled "IEEE 754", while :179/:188 assign NumberMultiplyTransfer/NumberDivideTransfer (:1340-1356) which widen outward by a relative 1e-15 'ulp' margin — the asymmetry is real, and no doc/comment/history justifies add/sub exactness (IEEE addition rounds: 0.1+0.2=0.30000000000000004 > 0.3, escaping an exact-decimal interval [0,0.3]). (2) The transfers are live: ProofEngine.Intervals.cs:105 dispatches them and proof-engine.md:1703 documents interval propagation through number arithmetic discharging PRE0078 bound obligations. (3) primitive-types.md:265, survey:171, and want:153 all match verbatim as cited. (4) First clause holds: the 209-line want doc has zero mentions of shared-core/fold/IEEE/double/rounding; its certificate checker (:185-198) re-walks the proof derivation within the same proof theory and never checks that theory's arithmetic against the evaluator's. Candidate counter-evidence examined and rejected: docs/Working/shared-expression-evaluation-core-2026-07-06.md binds only the concrete VALUE fold (FoldValue/EvaluateBinaryOp, which never folds number-lane values per its companion analysis §3) — not the interval-transfer layer; and the survey's point-4 assertion that "the interval layer is sound-by-approximation and cannot drift" is an assumption the missing add/sub widening falsifies, not a qualifier the claim ignored. Caveat (does not undermine the claim as stated): the runtime evaluator is not yet built, so the instance is spec/model-level drift — accurately conveyed by the claim's "by spec" and "could be unsound" phrasing.

### meta-6 — REFUTED (medium)

**Claim:** The time-referencing open question reaches far beyond 'ExpiryDate > today': date-ordering rules over timestamp fields written from now() — a dominant corpus pattern — cannot close from the four premise classes without either a clock-monotonicity axiom (whose truth the runtime does not control) or author-written clock-comparison guards, so under no-deferral the question's resolution decides the provability of a large existing rule family, and the model additionally needs a single-time-snapshot-per-operation semantics.

**Verifier:** The claim's mechanism is real but its quantitative core — "a dominant corpus pattern" whose provability the open question "decides" for "a large existing rule family" — is contradicted by the actual sample corpus. What checked out: docs/Working/what-i-want-2026-07-16.md:207 (time-referencing open question) and :19 (the four premise classes a/b/c/d) are quoted correctly; research/architecture/compiler/fragment-boundary-corpus-validation-2026-06-05.md:116 does say "all the date-ordering rules (timestamps written by `set …At = now()` entry hooks)". But that parenthetical is factually wrong against the samples, and the claim inherits the error. Exhaustive intersection (all 77 samples/*.precept) of every field written by `set X = now()` (417 write sites, 49 files) against every `rule` and `ensure` comparison clause finds exactly ONE sample with date-comparison rules over a now()-written operand: samples/prior-auth-appeal.precept (AppealFiledDate = now() at :175; rules :94-95, :98-100, plus :103 via ExternalReviewRequestedAt = now() at :346) — roughly 6 rules total. Three other hits (apartment-rental-application:61-62, insurance-claim-adjudication:67, insurance-renewal-processing:55, saas-trial-to-paid:50) are mere `X is set` presence triggers, trivially satisfied by any now() write with no clock reasoning. The actual date-ordering rule family (~30 rules across ~15 samples: CheckOutDate > CheckInDate, ReturnedByPatronDate >= LoanedToPatronDate, ExpiresAt > EffectiveAt, EventDate > InquiryDate, …) is over arg-written or editable fields, not now(): library-inter-library-loan writes its rule-constrained *Date fields from event args (:261 set LoanedToPatronDate = LoanToPatron.LoanedOn, :297, :309 — the now()-written *At audit stamps at :215-344 are a disjoint, unconstrained field set); currency-exchange-rates:26-27 declares EffectiveAt/ExpiresAt `editable`, so rule :44 closes via editable-write ingress validation (want doc :131) — premise classes suffice, no clock axiom. Verified this held at the corpus doc's own date too (git rev 210229ff, before 2026-06-06: identical write sources). Additional counter-evidence on the citation itself: line 116's own context classifies these rules as provable-in-fragment (the "large majority" bucket), and docs/language/temporal-type-system.md:566 (Locked Decision #9) already states "internal consistency (same inputs, same outputs within a single fire)" — undercutting "additionally needs" for the snapshot semantics. What survives: for prior-auth-appeal specifically, the obligation `now() <= FileAppeal.AppealDeadline` (from :175 under :168-169) genuinely cannot close from the four premise classes without a clock premise or an author-written guard — but that is one sample's ~6 rules, not a dominant pattern, so the open question's resolution does not decide a large rule family.

### meta-7 — CONFIRMED (medium)

**Claim:** The model is unique as a four-property combination (domain-expert DSL + induction over reachable configurations + no-deferral + certificate-gated lighter runtime) while every ingredient is precedented: Event-B's abstract machine is 'structurally identical' with the mapping 'exact' but ships no governing runtime; Dafny/SPARK/Whiley/F*/Liquid Haskell are all SMT-backed and developer-facing; the PCC citation is structurally faithful with consumer-side obligation enumeration as the one fidelity condition; and the nearest historical analog is design-by-contract, which checks at runtime what the want proves and removes.

**Verifier:** Every citation checks out. "Structurally identical" is verbatim at formal-spec-languages-comparators.md:27 and "The mapping is exact" at :164 (§4); no-governing-runtime is supported by §3 table :130 and §4 :176 (code generation exists per :123, but the survey explicitly distinguishes generated code from a deployable enforcement runtime with an inspection API). Dafny/Frama-C/SPARK are at type-proof-stage-contract-survey.md:47-49 as cited, with Whiley/F*/Liquid Haskell at :46/:50/:51 of the same table — all developer-facing with solver discharge. PCC structure matches witness-checking-soundness-architecture-survey.md:94/:135, and the want doc (what-i-want-2026-07-16.md:16, :157-159, :187-201) verbatim contains all four properties plus the PCC/small-kernel citation; the consumer-side obligation-enumeration fidelity condition matches PCC's consumer-side VCGen. DbC as nearest historical analog with runtime checking is supported by research-r3-positioning.md §1.5, and the want's removal of post-mutation re-evaluation (:187) is exactly what DbC never removed. I found no surveyed system in the repo combining all four properties. Three non-breaking caveats: (1) "SMT-backed" is loose for Whiley, whose native prover WyTP is a bespoke theorem prover (Boogie/Z3 is an alternative backend per the cited table row :46); (2) the survey's PCC characterization is flagged Tertiary/training-data (:143, primary fetch failed), though the asymmetry principle is independently Primary-grounded there via certifying-algorithms and Verasco; (3) uniqueness is uniqueness-within-the-surveyed-corpus — the source analysis itself marks "UNKNOWN whether an unstudied system exists" (soundness-uniqueness-value-philosophy.md:94), a universal-claim limit the compressed claim omits but does not contradict.

### meta-8 — CONFIRMED (high)

**Claim:** The strongest steelman against adopting the posture is market precedent, not technical cost — Eiffel and .NET Code Contracts tried structural entity-bound contracts in this ecosystem and failed commercially, a demand-side risk the want doc does not engage — while the technical steelmen are rebutted on measured evidence: ~89% of the 77-file corpus's explicit invariants (122/149 rules, 515/565 ensures) are already in the solver-free fragment as written, the residue is small and named (4 three-field balance identities, 7 coefficient rules) with Karr's affine-equality domain as the named polynomial solver-free extension, and the want's own surface-shrinks rule bounds the 'prove one more class' slide.

**Verifier:** Every citation checks out at the cited location. (1) Market-precedent leg: docs/Working/precept-identity-thesis-supporting-2026-07-11/research-r3-positioning.md line 86 is exactly the claimed point — "DbC precedent undercuts 'no one has tried structural, entity-bound enforcement.' Eiffel did, decades ago; .NET itself tried it (Code Contracts) and discontinued it," with §1.5 (line 43, "Design-by-contract — Eiffel, .NET Code Contracts") and the comparison table (line 70) naming the lane's shortfall as "commercial traction in the .NET ecosystem specifically." (2) Want-doc non-engagement: grepped docs/Working/what-i-want-2026-07-16.md (209 lines) for eiffel/contracts/market/commercial/adoption/demand/buyer/competition/traction/category — zero hits; the only "precedent" mention (line 201) is technical (proof-carrying code). The demand-side risk is genuinely unengaged there. (3) Measured evidence: research/architecture/compiler/fragment-boundary-corpus-validation-2026-06-05.md line 199 states verbatim "~82% of rules (122/149), ~91% of ensures (515/565). Combined explicit-invariant in-fragment fraction ≈ 89%"; line 20 confirms the 77-file corpus; line 187 confirms the residue "4 balance + 7 coefficient + 1 nonlinear numeric invariants across 77 files"; line 202 gives the ACCEPTABLE verdict naming the 3-field balance identity as the bounded extension. (4) Karr: solver-free-static-analysis-techniques-survey.md C1 (line 176) recommends "Adopt Karr's affine-equality domain... highest value-per-cost," explicitly solver-free + polynomial (O(program·k³), Müller-Olm/Seidl POPL'04), closing the #1 named gap (A+B==C); R2 boundary (lines 49, 167) places Karr inside the safely-polynomial band, polyhedra outside. (5) Want doc line 177 is verbatim the guarantee-non-negotiable/surface-negotiable criterion ("Shrinking the language surface is balance"), supporting the claim's bounding argument. Minor compressions that do not refute: the claim omits the 1-2 value-nonlinear (X/Y) residue occurrences the doc counts alongside the 4+7; the corpus doc conditions its verdict on the engine deciding X-Y<=c directly and warns authored samples may under-represent production multi-field accounting (Threats to Validity, ~line 163) — but the claim's "in the fragment as written" mirrors the doc's own phrasing, which is a fragment-membership claim, not an engine-implementation claim; and applying "in this ecosystem" to Eiffel is loose (Eiffel is not .NET), though the cited source itself groups both in one lane with a .NET-specific commercial-traction shortfall. Want-doc line 175 ("business problems... will continually expand") slightly tempers but does not contradict the bounding claim, which rests on guarantee-dilution being forbidden while surface-shrinking is sanctioned.

### meta-9 — CONFIRMED (medium)

**Claim:** Philosophy alignment is strong and mostly strengthening — philosophy.md line 55 ('the compiler proves a structural fact — that the value carries its constraint … the runtime enforcement is what makes that carried constraint true') already states the want's ingress-premise model — with three flags, none a contradiction: the owner-gated Fire-sentence rewording also has sibling superseded-mechanism sentences at spec:1354 and runtime-api.md:266 that need the same deliberate pass; the prevention promise's absolute phrasing outruns restore-trust until S1 closes; and honesty-about-approximation requires the proof theory to model 'number' float behavior on every operation.

**Verifier:** Every citation checks out against the files. (1) philosophy.md:55 quote is verbatim-accurate: "The compiler proves a structural fact — that the value carries its constraint — never the value itself; the runtime enforcement is what makes that carried constraint true, not a second line of defense" — and the surrounding sentence ("the engine enforces that constraint the moment the value enters") matches the want doc's ingress-premise model (want-i-want-2026-07-16.md:127-131, 153: ingress = event args + editable writes, ingress validation is the premise that closes the proof, no re-checking of proven bounds). Line 55 scopes to runtime-supplied values in calculations (the fault family), and the business-rule side is exactly what the claim's own Fire-sentence flag covers, so the claim is internally coherent. (2) philosophy.md:19 contains the exact Fire sentence want:203 flags for owner-gated rewording ("evaluates all applicable constraints against the resulting configuration, and commits only if every constraint holds" — verbatim match to want:203's quote). (3) The sibling superseded-mechanism sentences exist as cited: spec:1354 says a rule condition "is checked against the complete working copy *after* all mutations" and runtime-api.md:266 says "a stale value is re-governed by the next operation's post-mutation constraint sweep" — both describe the post-mutation-evaluation mechanism the want supersedes (want:187: the runtime "does **not** re-evaluate them against the post-mutation configuration"). I found additional siblings the claim omits (spec:1969 "This post-mutation sweep is one of two enforcement points"; runtime-api.md:889), but the claim never asserted exhaustiveness, so this extends rather than refutes it. (4) philosophy.md:49 prevention phrasing is genuinely absolute ("No errors. No bugs… the invalid configuration is not reachable"), and the restore-trust gap is real: want:129 says restore is trusted and unchecked with schema evolution deferred post-MVP, and runtime-api.md:266 confirms Restore does not re-validate — so an invalid-under-current-definition configuration can exist between restore and the next operation. The "S1" label is analyst-internal and not mappable to a repo artifact, but the substantive gap it points at is verified. (5) philosophy.md:23-25 states the honesty-about-approximation commitment as cited, and primitive-types.md:83/265 confirm `number` is IEEE 754 double, "approximate by design," with the spec already conceding "the proof engine cannot prove exact equality on `number`" — supporting the derived requirement that proofs over `number` must model float behavior to avoid presenting approximation as exactness. No contradicting evidence found in philosophy.md or the cited docs; the three flags are judgment calls whose factual anchors all verify.

## Axis: canon

*14 load-bearing claim(s) exceeded the verification cap and were recorded unverified.*

### canon-1 — CONFIRMED (high)

**Claim:** Committed canon at docs/language/precept-language-spec.md:1969 states, verbatim: "This post-mutation sweep is one of two enforcement points." - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § Certificates ¶1 (no post-mutation re-evaluation); § The runtime's role (runtime runs declared premises and nothing beyond them)). Correction direction: Delete the enforcement-point framing entirely; §3A.4 returns to atomic all-or-nothing write mechanics; restored state is trusted with no re-governing sweep; relational-rule enforcement on edits moves to the editable-write ingress point..

**Verifier:** Checked: (1) docs/language/precept-language-spec.md:1969 in committed HEAD (via git show) contains the exact verbatim sentence "This post-mutation sweep is one of two enforcement points." and continues "The sweep then re-checks every constraint against the completed working copy." (2) Want doc § Certificates ¶1 (line 187) states the runtime "does not re-evaluate them against the post-mutation configuration" — direct contradiction. (3) § The runtime's role (line 153) states what runs at runtime is "the declared premises (ingress validation of args and editable fields, guards) ... and nothing beyond them" — a full-constraint post-mutation sweep exceeds this. Counter-evidence hunt: grepped the want doc for sweep/post-mutation/re-evaluate/re-check/restore qualifiers; found none permitting a sweep. Line 129 additionally supports the correction direction: restored data is "trusted and unchecked" (contradicting the spec's "re-governed only by the next operation's sweep"), and ingress is defined as exactly event args + editable-field writes, matching the proposed relocation of edit-time relational-rule enforcement.

### canon-2 — CONFIRMED (high)

**Claim:** Committed canon at docs/language/precept-language-spec.md:1967 states, verbatim: "Constraints are evaluated against the working copy after all mutations complete. If every constraint passes, the working copy is promoted to become the entity's committed state." - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § Certificates ¶1). Correction direction: Keep multi-action write atomicity; remove constraint evaluation as the commit condition — commit is unconditional after premise-validated execution because preservation was proven at compile time..

**Verifier:** Checked both citations against committed HEAD (working tree is clean, matches HEAD). (1) The quote is verbatim-exact: docs/language/precept-language-spec.md:1967 (§ 3A.4 Mutation Atomicity) contains exactly "Constraints are evaluated against the working copy after all mutations complete. If every constraint passes, the working copy is promoted to become the entity's committed state." — constraint evaluation is the commit condition, and line 1969 reinforces it ("The sweep then re-checks every constraint against the completed working copy"). (2) The want doc contradiction is real: docs/Working/what-i-want-2026-07-16.md § Certificates ¶1 (line 187) states "Once the compiler has proven every rule, the runtime does **not** re-evaluate them against the post-mutation configuration". Corroborating context I hunted for qualifiers in: line 153 ("What runs at runtime are the declared premises (ingress validation of args and editable fields, guards)... and nothing beyond them" — no post-mutation sweep survives), and line 203's philosophy flag explicitly says the "commits only if every constraint holds" sentence "needs a deliberate, owner-approved rewording — the constraint evaluation moves to proof-plus-premise-checks, verified by certificate." (3) The correction direction is faithful to the want doc: nothing in the want doc removes write atomicity (line 151 keeps "strict immutable versioning and write guarantees"), and with the post-mutation sweep removed, the only remaining gates are premise checks (ingress validation, guards) that run before mutation — making commit unconditional after premise-validated execution, exactly as the claim states. Found no qualifier in the want doc preserving any post-mutation constraint evaluation.

### canon-3 — CONFIRMED (high)

**Claim:** Committed canon at docs/language/precept-language-spec.md:149 states, verbatim: "Constraints are evaluated against the working copy. If all constraints pass, the working copy is promoted. If any constraint fails, the working copy is discarded." - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § Certificates ¶1). Correction direction: §0.2's copy of the sweep model; correct in lockstep with §3A.4 (same direction as line 1967)..

**Verifier:** Checked committed HEAD (git show HEAD:docs/language/precept-language-spec.md): line 149 contains the claimed sentences verbatim ("Constraints are evaluated against the working copy. If all constraints pass, the working copy is promoted. If any constraint fails, the working copy is discarded."). Checked docs/Working/what-i-want-2026-07-16.md § Certificates ¶1: "Once the compiler has proven every rule, the runtime does **not** re-evaluate them against the post-mutation configuration" — this directly contradicts line 149's post-mutation sweep model, and the want doc's own Philosophy-flag paragraph confirms this wording family requires rewording under the want. Checked line 1967: it is the §3A.4 Mutation Atomicity copy of the same sweep model (explicitly called "This post-mutation sweep" on the next line), so the correct-in-lockstep direction is coherent; grep confirms lines 149 and 1967 are the only two occurrences of the promote/discard wording. One non-load-bearing inaccuracy: the claim's correction-direction clause calls line 149 "§0.2's copy" — it is actually in §0.3 Governance, Not Validation (§0.2 spans lines 114–135 and contains no sweep-model text). Line number, quote, contradiction, and lockstep target all verify; only the section label is mislabeled.

### canon-4 — CONFIRMED (medium)

**Claim:** Committed canon at docs/language/precept-language-spec.md:162 states, verbatim: "A transition row is a flat sequence: evaluate a guard, execute assignments left-to-right, check rules and ensures." - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § The runtime's role). Correction direction: Drop 'check rules and ensures' as a runtime execution step; the sequence is ingress validation, guard, assignments, commit..

**Verifier:** Checked: (1) The quote is verbatim and correctly located — docs/language/precept-language-spec.md:162 (spec § 0.4 "Execution Model Properties", property 2 "No control-flow branches") contains exactly "A transition row is a flat sequence: evaluate a guard, execute assignments left-to-right, check rules and ensures." Verified against committed HEAD via `git show HEAD:docs/language/precept-language-spec.md` (identical; working tree clean for that file), so "committed canon" is accurate. (2) The contradiction holds: the want doc § The runtime's role (docs/Working/what-i-want-2026-07-16.md:145ff) states "fault-prevention checks are omitted at evaluation time... What runs at runtime are the declared premises (ingress validation of args and editable fields, guards) that the compiler's proofs depend on, and nothing beyond them," and § Certificates reinforces "the runtime does not re-evaluate them against the post-mutation configuration." Checking rules/ensures after assignments is a step "beyond" the declared premises, so listing it in the execution sequence contradicts the want. (3) The correction direction (ingress validation → guard → assignments → commit) matches the want doc's runtime surface exactly. Counter-evidence hunt: the spec sentence sits in a section framed as "language design choices... that make tractable compile-time reasoning possible," so one could argue its point is control-flow shape rather than a runtime-checking commitment — but it plainly describes the row's execution sequence, and the same sentence also appears in the archived vision doc (docs/archive/language-design/precept-language-vision.md:129), confirming it is inherited pre-want phrasing, not a deliberate post-want statement. No qualifier in surrounding context (lines 150-175) softens "check rules and ensures" into a compile-time-only claim.

### canon-5 — CONFIRMED (medium)

**Claim:** Committed canon at docs/language/precept-language-spec.md:1354 states, verbatim: "A *rule condition* — and a *constraint modifier*, which is rule shorthand (§2.4) — is checked against the complete working copy *after* all mutations, so declaration order is irrelevant and any field is in scope." - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § Certificates ¶1). Correction direction: Keep the scoping conclusion; re-justify it via rule-as-proof-obligation-over-all-write-sites semantics, not a post-mutation runtime check..

**Verifier:** Checked all citations against committed HEAD and hunted for softening qualifiers; found none. (1) Verbatim quote: `git show HEAD:docs/language/precept-language-spec.md` line 1354 contains the quoted sentence character-for-character, including the italics markers ("A *rule condition* — and a *constraint modifier*, which is rule shorthand (§2.4) — is checked against the complete working copy *after* all mutations, so declaration order is irrelevant and any field is in scope."). Working tree is clean, so this is committed canon, not a session edit. (2) Context check: the sentence sits in §3.5 Scope Rules and is explicitly framed as tracking "the **evaluation model**, not preference" — it is an evaluation-order claim (post-mutation checking), not a merely-declarative scoping remark; no qualifier ("as if", "conceptually") softens it. The adjacent table row for modifier expressions repeats the same justification. (3) Contradiction: docs/Working/what-i-want-2026-07-16.md § Certificates ¶1 (line 187) states verbatim: "Once the compiler has proven every rule, the runtime does **not** re-evaluate them against the post-mutation configuration" — a direct denial of the spec's post-mutation-check evaluation model. Want doc line 153 reinforces it ("fault-prevention checks are omitted at evaluation time... no re-checking of proven bounds on writes"). (4) Correction direction is consistent with the want doc's own framing (rules proven at compile time from premises; certificate records the proof), and correctly preserves the scoping conclusion, which survives independently under proof-over-configurations semantics. No counter-evidence found.

### canon-6 — CONFIRMED (high)

**Claim:** Committed canon at docs/language/precept-language-spec.md:1903 states, verbatim: "Rules and ensures are evaluated exhaustively — every applicable constraint is checked, and all violations are reported." - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § The runtime's role; § Certificates ¶1). Correction direction: Runtime validation surfaces are ingress and guards only; rules/ensures stop being a runtime validation surface; collect-all survives only where validation actually runs (arg ingress)..

**Verifier:** Checked: (1) verbatim quote verified at committed HEAD via `git show HEAD:docs/language/precept-language-spec.md`, line 1903 exactly, working tree clean; (2) surrounding spec context (lines 1899-1922) confirms this is runtime-evaluation semantics, not compile-time — outcome 4 at line 1917 says "Mutations would violate a rule or ensure; rolled back," so no saving qualifier exists; (3) want doc § The runtime's role states "What runs at runtime are the declared premises (ingress validation of args and editable fields, guards)... and nothing beyond them," and § Certificates ¶1 states "the runtime does not re-evaluate them against the post-mutation configuration" — both directly contradict spec:1903's exhaustive runtime evaluation of rules/ensures. Correction direction matches the want doc. One non-refuting nit: the claim's parenthetical "(arg ingress)" understates the surviving collect-all surface — want-doc ingress is args AND editable field writes; the claim's main clause ("ingress and guards only") states it correctly.

### canon-7 — CONFIRMED (high)

**Claim:** Committed canon at docs/language/precept-language-spec.md:1938 states, verbatim: "`ConstraintsFailed` | Post-mutation constraint violations (collect-all semantics)." - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § Certificates ¶1). Correction direction: The outcome presupposes post-mutation evaluation and cannot arise; remove or re-scope the construction outcome taxonomy..

**Verifier:** Checked: (1) `git show HEAD:docs/language/precept-language-spec.md` line 1938 is verbatim "| `ConstraintsFailed` | Post-mutation constraint violations (collect-all semantics). |" — exact line number and text match in committed HEAD (and working tree), inside the § 3A.2 Construction outcomes table, so the outcome does presuppose post-mutation constraint evaluation at construction. (2) docs/Working/what-i-want-2026-07-16.md § Certificates ¶1 (line 187) states the runtime "does not re-evaluate [rules] against the post-mutation configuration"; corroborated by line 22 (one constraint mechanism — modifiers/ensures are sugar for rules, so nothing escapes the proof model), line 18 (base case: construction establishes every rule provably from arg constraints), line 198 (runtime validation surface = ingress premise list only), and line 203 (explicitly flags post-mutation constraint evaluation as the thing the want removes). Under the want, construction-time failures map to Rejected (guard) or InvalidArgs (arg constraint); no path yields post-mutation ConstraintsFailed. Counter-evidence hunt: grepped the want doc for qualifiers permitting residual post-mutation checks (ensures carve-out, unproven-constraint runtime fallback) — none exist; grepped HEAD spec for other ConstraintsFailed context that might re-scope the outcome to ingress — line 1938 is the sole occurrence. The contradiction stands as claimed.

### canon-8 — CONFIRMED (high)

**Claim:** Committed canon at docs/language/precept-language-spec.md:1946 states, verbatim: "Restoring an entity in an invalid state is not allowed — the governance guarantee applies from the moment an entity is loaded, not just when it is mutated." - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § The compiler's promise, ingress paragraph (restore is trusted and unchecked)). Correction direction: Restoration loses its constraint-failure arm; restore trusts persisted data (also contradicts spec §0.7:272 itself); the load-time check that exists under the want is the certificate check on the definition, not data validation..

**Verifier:** Checked: (a) git grep against committed HEAD confirms the quoted sentence appears verbatim at docs/language/precept-language-spec.md:1946, in the §3A "Restoration outcomes" block that also includes a "constraint failure (persisted data violates current definition's rules/ensures)" arm; (b) want doc line 129 (inside ## The compiler's promise, lines 12–134): "Restore/rehydration is *not* ingress — data coming back from persistence is trusted and unchecked, because it was governed when it was written" — a direct contradiction of load-time governance/constraint-failure on restore; (c) spec §0.7 line 272: "Restored state is trusted as valid at the time it was persisted: hydration is fast and does not re-validate… reconstituted, not re-governed on load" — confirming spec:1946 also contradicts the spec itself; (d) want doc line 153 confirms the only load-time check under the want is certificate verification of the definition ("a trust it establishes by verification, once, at load (see Certificates)"), matching the claimed correction direction. Counter-evidence hunt found only the schema-evolution parentheticals (want:129 post-MVP migration; spec:1946 "Future migration logic"), which are consistent with each other and do not rescue spec:1946's data-validation-at-load stance. No misquote, wrong line, or ignored qualifier.

### canon-9 — CONFIRMED (medium)

**Claim:** Committed canon at docs/language/precept-language-spec.md:268 states, verbatim: "Because mutations execute on a working copy that is discarded if any constraint fails (§3A.4), an invalid configuration never persists — this is prevention, not detection." - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § Certificates ¶1). Correction direction: The ingress framing of §0.7 survives; this clause tying governance to the sweep goes — invalid configurations never persist because handlers are proven to preserve rules..

**Verifier:** Checked: (1) Verbatim quote — git show HEAD:docs/language/precept-language-spec.md line 268 (commit 342e66db lineage, clean spec file in working tree) contains the exact quoted sentence. (2) Meaning — the clause's §3A.4 cite resolves to spec lines 1967-1969, which explicitly describe a "post-mutation sweep" that "re-checks every constraint against the completed working copy" after all mutations complete; the alternative ingress-only reading of "any constraint fails" is ruled out by that cited section and by §0.7's boundary paragraph (line 272: "every constraint is checked against the resulting working copy, §3A.4"). (3) Contradiction — want doc § Certificates ¶1 (docs/Working/what-i-want-2026-07-16.md:187) states the runtime "does **not** re-evaluate them against the post-mutation configuration," a direct mechanism contradiction; corroborated by want line 153 (only ingress validation of args/editable fields plus guards run at runtime, "nothing beyond them") and want line 203 (owner flags the philosophy doc's equivalent "evaluates all applicable constraints against the resulting configuration" sentence as needing rewording under this want). (4) Correction direction — matches want line 19 ("Every handler *preserves* every rule", the inductive step) and the surviving ingress framing at line 153. No counter-evidence found.

### canon-10 — CONFIRMED (high)

**Claim:** Committed canon at docs/language/precept-language-spec.md:272 states, verbatim: "the next operation through the contract re-governs it (every constraint is checked against the resulting working copy, §3A.4), and the runtime fault traps backstop any out-of-contract value the evaluator would otherwise fault on." - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § The compiler's promise (restore trusted); § The runtime's role (no fault checks at evaluation time)). Correction direction: Both clauses go: no next-operation sweep re-governs out-of-contract data, and no runtime fault traps exist; the paragraph's trusted-restore first half survives..

**Verifier:** Checked: (1) Verbatim quote verified at docs/language/precept-language-spec.md:272 in both working tree and committed HEAD (git show) — exact match, correct line. (2) Want doc sections verified: § The compiler's promise line 129 ("Restore/rehydration is *not* ingress — data coming back from persistence is trusted and unchecked") supports the surviving trusted-restore half; § The runtime's role lines 151-153 ("fault-prevention checks are omitted at evaluation time... the evaluator just computes... nothing beyond them") contradicts the spec's fault-traps clause; § Certificates ("the runtime does **not** re-evaluate them against the post-mutation configuration") additionally negates the spec's parenthetical "every constraint is checked against the resulting working copy," contradicting the next-operation-re-governs clause. (3) Counter-evidence hunt: grepped want doc for backstop/trap/sweep/re-govern/out-of-contract/host-inject/bypass — nothing endorses either spec clause; the post-MVP schema-evolution note concerns the restore path itself, not next-operation re-governance. Correction direction matches the want doc.

### canon-11 — CONFIRMED (medium)

**Claim:** Committed canon at docs/language/precept-language-spec.md:266 states, verbatim: "no result outside a declared bound. The compiler delivers it by discharging, at every fault-prone operation, an obligation that each operand *carries* a sufficient constraint" - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § The compiler's promise ('Modifiers are sugar for rules … one constraint mechanism underneath'); § Worked example (PlanRepayment)). Correction direction: Fault-gate residue 1 of 3: separate true faults from bound containment (rule preservation, same prove-or-reject disposition); restate discharge as induction from the four premise classes. The 'there is no deferral' clause survives..

**Verifier:** Checked: (1) spec quote is verbatim at docs/language/precept-language-spec.md:266 in both working tree and committed HEAD (last commit touching file: 342e66db; git diff HEAD clean), inside § 0.7 'Fault prevention', whose final sentence is 'there is no deferral' — matching the claim's survival note. (2) Want doc line 22 contains the cited 'Modifiers are sugar for rules … one constraint mechanism underneath' verbatim; PlanRepayment worked example (lines 65-68, 98) closes both the divide-by-zero fault and the nonnegative bound via the same premise-and-certificate machinery. (3) The contradiction is genuine, not framing: spec:266 discharges obligations per fault-prone operation via a per-operand carried constraint ('a declared modifier or rule, an author guard, or a statically-known safe value') and proves only 'a structural fact… never the concrete runtime value'; the want doc (lines 16-20) mandates induction over reachable configurations with four premise classes including the inductive hypothesis ('all rules holding in the pre-state' — absent from the spec's enumeration; grep confirms the spec never mentions induction/inductive/premise class) plus symmetric write-site obligations. Behavioral divergence derivable from the cited texts: want line 109 requires rejecting Deposit when 'nonnegative' is removed from Amount (inductive step fails; Balance carries no declared bound), an obligation spec:266's fault-gate never generates; want line 95 ('No guard or arg bound alone gets there') is inexpressible in the carrier model. The spec's adjacent governance paragraph (runtime working-copy re-check of every constraint) further conflicts with want line 153 ('no re-checking of proven bounds on writes… the evaluator just computes'). Steelman attempted and failed: reading 'a declared… rule' in the carrier list as covering the inductive hypothesis still leaves no write-site rule-preservation obligations and no combination of premises, which the want doc's Deposit/ReduceLimit proofs require. Minor nuance found but not refuting: want line 98 lists 'out-of-range' in the fault family, but grants it 'identical premise-and-certificate treatment', so the correction direction's 'same prove-or-reject disposition' qualifier absorbs it.

### canon-12 — CONFIRMED (medium)

**Claim:** Committed canon at docs/language/precept-language-spec.md:112 states, verbatim: "division by zero, overflow, empty collection access, constraint range impossibility — is linked to a compiler diagnostic that prevents it. Runtime fault checks exist only as defensive redundancy, never as the primary enforcement mechanism." - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § The compiler's promise (one mechanism); § The runtime's role). Correction direction: Fault-gate residue 2 of 3: drop 'constraint range impossibility' from the fault-class list; 'defensive redundancy' runtime fault checks become none — confidence comes from the certificate verified at load..

**Verifier:** Checked: (1) The quote is verbatim at docs/language/precept-language-spec.md:112 in both working tree and committed HEAD (git show HEAD:...) — exact character match, correct line. (2) The contradiction is real: the want doc's § The runtime's role states "fault-prevention checks are omitted at evaluation time. No divide-by-zero tests, no overflow guards, no re-checking of proven bounds on writes — the evaluator just computes… What runs at runtime are the declared premises… and nothing beyond them", directly contradicting spec:112's "Runtime fault checks exist only as defensive redundancy." I hunted for a reconciling qualifier via line 112's cross-reference to spec §0.7 (line 262ff): §0.7 instead reasserts that "runtime fault traps backstop any out-of-contract value" — the residue the claim identifies, not a reconciliation; the want doc has no out-of-contract carve-out. (3) The correction direction is supported: § Certificates roots runtime confidence in a load-time certificate check ("the same checker, run once when a definition is loaded… verification the runtime performed itself"), and dropping 'constraint range impossibility' from a list scoped to "fault class that the evaluator can produce" matches the want's architecture (constraints are proven theorems + ingress-governed premises; "the runtime does not re-evaluate them against the post-mutation configuration" — a constraint violation is a governance refusal, not an evaluator fault). Corroborating internal evidence: docs/Working/compiler-readiness-plan-2026-06-16-design-revision-notes.md:120 (GATE-F) independently flags exactly these two phrases in Principle 11/L112 as amendment sites. No misquote, wrong line, missing qualifier, or misreading found.

### canon-13 — CONFIRMED (medium)

**Claim:** Committed canon at docs/language/precept-language-spec.md:110 states, verbatim: "Runtime fault traps exist only as defensive redundancy for paths the compiler has already proven unreachable." - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § The runtime's role). Correction direction: Principle 10's trap clause goes; the prove-or-reject remainder of P10 is aligned..

**Verifier:** Checked: (1) The quote is verbatim at docs/language/precept-language-spec.md:110 (closing sentence of Principle 10 "Totality"), identical in working tree and committed HEAD (git status clean for the file), so "committed canon" holds. (2) The want doc's § The runtime's role (docs/Working/what-i-want-2026-07-16.md:153) states fault-prevention checks are "omitted at evaluation time. No divide-by-zero tests, no overflow guards... the evaluator just computes. What runs at runtime are the declared premises... and nothing beyond them"; the worked example (line 98) says "no zero-check anywhere." Spec says traps exist (as defensive redundancy); want doc says they are omitted — direct contradiction. Hunted for qualifiers permitting defensive-redundancy traps (including § Certificates) and found none; the only runtime verification sanctioned is the one-time load-time certificate check, not an evaluation-time trap. (3) Correction direction checks out: the remainder of P10 (prove safety or emit a diagnostic requiring author-supplied constraints) matches the want doc's no-deferral ruling (line 159) and suggest-but-never-insert premise flow (line 169). Note: the claim is under-inclusive — Principle 11 (spec line 112) has a sibling clause ("Runtime fault checks exist only as defensive redundancy, never as the primary enforcement mechanism") contradicting the want doc the same way — but that does not weaken the claim as stated.

### canon-14 — CONFIRMED (medium)

**Claim:** Committed canon at docs/language/precept-language-spec.md:213 states, verbatim: "Only a provably-false fold rejects; an unknown/unfoldable default never rejects (per Proof philosophy #1–2)." - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § The compiler's promise, base case; § What must not compile (default row); § No deferral). Correction direction: Defaults flip from flag posture to prove-or-reject: a live default that cannot be proven to satisfy the rules leaves the base case open and must reject (construction-overwritten placeholders excepted)..

**Verifier:** Checked: (1) the quote is verbatim-exact at precept-language-spec.md:213 in both committed HEAD (git show) and working tree; (2) the want doc genuinely asserts the opposite posture — line 18 (base case requires provable satisfaction by the default configuration), line 24 (rejection = proof cannot close), line 99 (live default 500.0 "must satisfy positive and max 10000.0"), line 114 (default 20000.0 mutation "must be a compile-time rejection"), lines 157-159 (No deferral: what cannot be proven complete is rejected) — so an unprovable/unfoldable live default must reject under the want doc, while canon says it never rejects; (3) the correction direction is accurate: "flag posture" matches canon's own framing (Proof philosophy #2 calls these violation reports; diagnostic-system.md:417 repeats "an unknown/unfoldable default never rejects"), and the construction-overwritten-placeholder exception is grounded in want doc line 99's qualifier ("no constructor arg sets it, so the base case has a default to evaluate"). Counter-evidence hunt found no want-doc passage endorsing the never-reject posture; only softening is that unfoldable defaults may be rare in practice, which does not dissolve the posture contradiction.

### canon-15 — CONFIRMED (high)

**Claim:** Committed canon at docs/philosophy.md:19 states, verbatim: "executes mutations, evaluates all applicable constraints against the resulting configuration, and commits only if every constraint holds" - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § Certificates, philosophy flag (the want names this sentence)). Correction direction: Owner-gated: Fire's mechanism becomes arg-ingress validation, guard evaluation, mutation, unconditional commit — constraints hold by proof, verified by certificate..

**Verifier:** Checked all citations against both working tree and committed HEAD; found no counter-evidence. (1) Verbatim quote: docs/philosophy.md:19 (the Fire bullet) contains exactly "executes mutations, evaluates all applicable constraints against the resulting configuration, and commits only if every constraint holds" — identical in `git show HEAD:docs/philosophy.md` line 19, and `git status --porcelain` shows both cited files clean, so "committed canon" is accurate. (2) The want doc names this sentence: docs/Working/what-i-want-2026-07-16.md:203, inside § "Certificates — lighter runtime, with confidence" (line 185), reads "**Philosophy flag (not resolved here):** `docs/philosophy.md` currently describes Fire as 'evaluates all applicable constraints against the resulting configuration, and commits only if every constraint holds.' Under this want, that sentence needs a deliberate, owner-approved rewording — the constraint evaluation moves to proof-plus-premise-checks, verified by certificate." (3) Contradiction is real: want doc line 187 says the runtime "does **not** re-evaluate them against the post-mutation configuration" and line 153 says runtime checks are only the declared premises (ingress validation of args and editable fields, guards) — directly opposed to philosophy.md:19's post-mutation evaluate-all-constraints-then-conditionally-commit mechanism. (4) Correction direction checks out as owner-gated ("deliberate, owner-approved rewording", line 203) and its content (arg-ingress validation + guard evaluation + mutation + unconditional commit, constraints held by proof, verified by certificate) is a faithful paraphrase of want doc lines 98, 153, 187, 192, and 203. No missing qualifiers found: the flag is explicitly marked "not resolved here," which the claim preserves via "Owner-gated."

### canon-16 — CONFIRMED (medium)

**Claim:** Committed canon at docs/philosophy.md:51 states, verbatim: "it evaluates atomically — the change and the check are the same act" - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § Certificates ¶1; § The runtime's role). Correction direction: Owner-gated: drops per-operation rule evaluation as the enforcement mechanism; the surrounding guarantee claim survives with proof as the mechanism..

**Verifier:** Checked all three legs of the claim against committed HEAD and the want doc; found no counter-evidence. (1) Verbatim quote and line number: `git show HEAD:docs/philosophy.md` line 51 contains exactly "it evaluates atomically — the change and the check are the same act" (identical in working tree; the sentence appears within the long "Two guarantees" paragraph at line 51). (2) The contradiction is real: the philosophy sentence asserts per-operation rule evaluation for rules about both "what the entity *is*" and "what the operation *brings in*", while docs/Working/what-i-want-2026-07-16.md § Certificates ¶1 (line 187) states "the runtime does **not** re-evaluate them against the post-mutation configuration" and § The runtime's role (line 153) states "fault-prevention checks are omitted at evaluation time... What runs at runtime are the declared premises (ingress validation of args and editable fields, guards)... and nothing beyond them." I tested the friendliest counter-reading — that "the change and the check are the same act" could describe ingress premise checks atomic with the operation — but it fails because the sentence explicitly covers entity-state rules ("what the entity *is*"), which under the want are held by proof and never evaluated at operation time. The want doc itself corroborates that philosophy.md's runtime-evaluation model conflicts with the want: its closing "Philosophy flag (not resolved here)" calls out the sibling Fire sentence (philosophy.md:19, "evaluates all applicable constraints against the resulting configuration, and commits only if every constraint holds") as needing "a deliberate, owner-approved rewording." (3) Correction direction checks out: "owner-gated" matches both CLAUDE.md ("Do not edit docs/philosophy.md without explicit owner approval") and the want doc's flag; and the surrounding guarantee at line 51 ("No operation can produce a result that violates a declared rule — the invalid configuration is not reachable") does survive under the want's uniform-guarantee ruling (§ No deferral: "the guarantee is uniform — a rule holds, period"), with only the mechanism changing to proof + premise checks + certificate. Note the claim correctly cites line 51's "evaluates atomically" sentence, which is distinct from (but consistent in model with) the Fire sentence at line 19 that the want doc's flag quotes.

### canon-17 — CONFIRMED (high)

**Claim:** Committed canon at docs/runtime/runtime-api.md:171 states, verbatim: "A mutation row matched and executed, but constraint evaluation found violations." - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § Certificates ¶1). Correction direction: ConstraintsFailed outcome presupposes the sweep; remove/re-scope (adjacent rows :168 'all post-mutation constraints passed' and :180 'same constraint checking' carry the same model)..

**Verifier:** Checked committed HEAD via `git show HEAD:docs/runtime/runtime-api.md`: line 171 contains the quoted sentence verbatim ("A mutation row matched and executed, but constraint evaluation found violations." in the ConstraintsFailed row); line 168 reads "all post-mutation constraints passed" and line 180 "same guards, same ensures, same constraint checking" as claimed; working tree matches HEAD. Want doc § Certificates ¶1 (docs/Working/what-i-want-2026-07-16.md:187) states the runtime "does **not** re-evaluate them against the post-mutation configuration," and line 153 limits runtime work to declared premises (ingress validation of args/editable fields, guards) "and nothing beyond them"; the section's philosophy flag confirms constraint evaluation "moves to proof-plus-premise-checks." ConstraintsFailed is defined as post-mutation, collect-all constraint evaluation after a mutation executed — it presupposes exactly the sweep the want doc eliminates. I looked for a rescuing reading (constraints-as-ingress on editable writes would surface as InvalidArgs/Rejected premise failures, not a post-mutation collect-all outcome) and found none; no counter-evidence located.

### canon-18 — CONFIRMED (high)

**Claim:** Committed canon at docs/runtime/runtime-api.md:361 states, verbatim: "constraint evaluation (collect-all) → commit or discard" - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § The runtime's role; § Certificates ¶1). Correction direction: Fire pipeline loses its post-mutation evaluate/discard stage; ends at commit. Arg validation and guards are exactly the premises that stay..

**Verifier:** Checked: (1) docs/runtime/runtime-api.md:361 contains the verbatim quote "constraint evaluation (collect-all) → commit or discard" as the tail of the Fire pipeline; confirmed identical at committed HEAD via `git show`, working tree clean. (2) runtime-api.md:171 confirms the stage is post-mutation ("ConstraintsFailed | Post-mutation constraint violations (collect-all semantics)"), ruling out a benign reading. (3) Want doc § Certificates ¶1 (line 187): "the runtime does not re-evaluate them against the post-mutation configuration"; § The runtime's role (line 153): runtime runs only "ingress validation of args and editable fields, guards... and nothing beyond them" — direct contradiction with the cited pipeline stage. (4) Correction direction verified: Fire's premises per the want doc are arg validation + guards (editable-field ingress belongs to Update, line 386), and removing the post-mutation stage leaves the pipeline ending at commit. Counter-evidence hunt found no qualifiers near line 361, no want-doc passage retaining post-mutation evaluation (the "premise list is the runtime's validation surface" sentence enumerates ingress-side premises only), and no reconciliation note in runtime-api.md — the old sweep model also persists at lines 168, 266, 889, consistent with the claim.

### canon-19 — CONFIRMED (high)

**Claim:** Committed canon at docs/runtime/runtime-api.md:386 states, verbatim: "Update runs the field-write pipeline: access mode check → type validation → patch application to working copy → computed field recomputation → constraint evaluation (collect-all) → commit or discard." - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § The compiler's promise, ingress paragraph). Correction direction: Update validation moves to ingress: incoming value checked against the field's modifier-rules and every rule mentioning the field before application, not whole-entity post-application evaluation..

**Verifier:** Checked: (1) the quote is verbatim at docs/runtime/runtime-api.md:386 and the file matches committed HEAD (git diff HEAD empty, clean status); (2) the want doc's "## The compiler's promise" section (lines 12-134) contains the ingress paragraphs at lines 127-131, and line 131 states editable-field-write validation = "the field's modifier-rules *and* every rule that mentions the field" evaluated "at the ingress point" — matching the claim's correction direction; (3) hunted for qualifiers or endorsements of post-application evaluation in the want doc and found the opposite: line 187 ("the runtime does **not** re-evaluate them against the post-mutation configuration") and line 153 ("What runs at runtime are the declared premises (ingress validation of args and editable fields, guards) ... and nothing beyond them") both directly contradict the runtime-api pipeline's patch-to-working-copy-then-collect-all-constraint-evaluation-then-commit-or-discard model. The want doc's sole approving cite of runtime-api.md (line 151) covers only immutable versioning, not the Update pipeline. The "whole-entity post-application" characterization is supported: the pipeline evaluates constraints after patch application and computed-field recomputation on the working copy, and runtime-api's applicable-constraints model is state-scoped, not edited-field-scoped.

### canon-20 — CONFIRMED (medium)

**Claim:** Committed canon at docs/runtime/runtime-api.md:646 states, verbatim: "`InspectFire` runs the same pipeline as `Fire`— same guard evaluation, same action chain, same constraint checking." - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § Certificates (inspect cites provenance)). Correction direction: 'Same constraint checking' inherits the sweep; inspect previews ingress/guard outcomes and cites proof provenance instead of predicting a post-mutation constraint verdict..

**Verifier:** Checked: (1) `git show HEAD:docs/runtime/runtime-api.md` line 646 contains the quoted sentence verbatim (including the `Fire`— spacing); no working-tree diff on the file. (2) The phrase "same constraint checking" does refer to the post-mutation sweep: runtime-api.md:361 defines Fire's pipeline as "…computed field recomputation → constraint evaluation (collect-all) → commit or discard", and lines 266/889 call it the "post-mutation constraint sweep". (3) The want doc contradicts this for the target design: what-i-want-2026-07-16.md:187 ("the runtime does **not** re-evaluate them against the post-mutation configuration"), :153 (runtime runs only declared premises — ingress validation of args/editable fields and guards — "and nothing beyond them"), :199 ("Inspect can cite provenance, not just verdicts"), :149 (inspect listed as a runtime obligation). Counter-evidence hunt: the "same pipeline as Fire" invariant itself survives the want (both sides drop the sweep together), but the claim already scopes the contradiction to "same constraint checking" inheriting the sweep, which is accurate. Only nit: the cited section header is a paraphrase — the actual header is "## Certificates — lighter runtime, with confidence"; the "inspect cites provenance" content is the bullet at line 199 within that section, so the citation resolves correctly.

### canon-21 — CONFIRMED (high)

**Claim:** Committed canon at docs/runtime/runtime-api.md:266 states, verbatim: "a stale value is re-governed by the next operation's post-mutation constraint sweep (spec §3A.4)" - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § The compiler's promise, ingress paragraph). Correction direction: Trusted-restore first half survives; the sweep clause goes — nothing re-governs restored data. Also: the runtime load-time certificate gate the want requires appears nowhere in runtime canon; adding it is in this correction's scope..

**Verifier:** Checked: (1) Verbatim quote — docs/runtime/runtime-api.md line 266 in both working tree and committed HEAD reads exactly "...a stale value is re-governed by the next operation's post-mutation constraint sweep (spec §3A.4)." — character-for-character match. (2) Contradiction — the want doc's ingress paragraph (docs/Working/what-i-want-2026-07-16.md:129, inside § The compiler's promise, confirmed in HEAD) states "Restore/rehydration is *not* ingress — data coming back from persistence is trusted and unchecked, because it was governed when it was written," and line 187 states the runtime "does **not** re-evaluate [rules] against the post-mutation configuration"; line 153 adds "no re-checking of proven bounds on writes." So the want doc eliminates the post-mutation constraint sweep mechanism the canon sentence relies on — the contradiction is real, and the trusted-restore first half of the canon sentence does survive as claimed. (3) Certificate gate absent from runtime canon — `grep -rni "certificat" docs/runtime/` returns zero matches across all 7 runtime docs (runtime-api.md, evaluator.md, descriptor-types.md, result-types.md, fault-system.md, precept-builder.md, README.md), while the want doc line 192 explicitly requires "A load-time gate in the runtime — the same checker, run once when a definition is loaded. The runtime refuses to govern under a definition whose certificate does not verify." One minor nuance (not a refutation): "nothing re-governs restored data" is marginally strong — under the want, an editable-field write evaluates every rule mentioning that field (want doc:131), and such relational rules could incidentally read other restored stale values — but that is ingress validation of a new write, not a post-mutation sweep, and does not rescue the canon sentence.

### canon-22 — CONFIRMED (high)

**Claim:** Committed canon at docs/runtime/runtime-api.md:889 states, verbatim: "A stale value surfaces at the next operation, whose post-mutation constraint sweep (spec §3A.4) governs the resulting working copy." - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § The compiler's promise, ingress paragraph). Correction direction: Same as :266 — the sweep clause goes..

**Verifier:** Checked: (1) `git show HEAD:docs/runtime/runtime-api.md | grep -n` confirms the quoted sentence appears verbatim at committed line 889, and the parallel sweep clause at line 266 ("Same as :266" is accurate); working tree is clean, so committed = current. (2) The want doc's ingress paragraph is at docs/Working/what-i-want-2026-07-16.md:127-131, inside § The compiler's promise (section spans lines 12-134), so the section citation is correct. (3) The contradiction holds: want doc line 129 says "Ingress is exactly two points: event args ... and editable-field writes. Restore/rehydration is not ingress — data coming back from persistence is trusted and unchecked"; line 153 says runtime runs the declared premises "and nothing beyond them"; line 187 says the runtime "does not re-evaluate them against the post-mutation configuration." A next-operation "post-mutation constraint sweep" that catches stale restored values is precisely the mechanism the want doc excludes. The non-sweep half of :889 (restore trusted, no re-validation) agrees with the want, consistent with the correction direction that only the sweep clause goes. No qualifiers in surrounding context of either file undermine the claim.

### canon-23 — CONFIRMED (high)

**Claim:** Committed canon at docs/runtime/result-types.md:114 states, verbatim: "`ConstraintsFailed` | Post-mutation constraints violated (rules, state ensures, event ensures)" - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § Certificates ¶1). Correction direction: Outcome-type taxonomy loses its post-mutation-violation arm..

**Verifier:** Checked: (1) docs/runtime/result-types.md:114 in working tree AND committed HEAD (git show; last commit touching file: 342e66db) — quote is verbatim: "| `ConstraintsFailed` | Post-mutation constraints violated (rules, state ensures, event ensures) | Stage 9-10 | Yes |"; the scope note at line 121 confirms it covers ALL post-fire constraints evaluated at runtime. (2) Want doc § Certificates ¶1 (docs/Working/what-i-want-2026-07-16.md:187): "Once the compiler has proven every rule, the runtime does **not** re-evaluate them against the post-mutation configuration" — reinforced at line 153 (runtime runs only ingress premises "and nothing beyond them") and line 203 (constraint evaluation moves to proof-plus-premise-checks). Contradiction is genuine. Counter-evidence hunt: verified no qualifier reconciles them — premise failures under the want map to ingress-time outcomes (InvalidArgs, Unmatched/Rejected in EventOutcome), not post-mutation ones, so the correction direction (taxonomy loses its post-mutation-violation arm) follows.

### canon-24 — CONFIRMED (high)

**Claim:** Committed canon at docs/runtime/evaluator.md:505 states, verbatim: "This ensures that constraint evaluation sees the post-mutation state, and that failed rows leave no side effects." - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § The runtime's role; § Certificates ¶1). Correction direction: Fire lifecycle steps 6–7 (evaluate constraint plans, commit-or-discard) go; commit is unconditional after premise-validated execution. Same family: :141/:144, :1585, :1789..

**Verifier:** Checked against committed HEAD (working tree clean; `git show HEAD:docs/runtime/evaluator.md`). (1) Verbatim quote: line 505 reads exactly "This ensures that constraint evaluation sees the post-mutation state, and that failed rows leave no side effects." (followed by "On success, the working array becomes the committed `Version.Slots`..."), preceded by Fire lifecycle steps 6 ("Evaluate: Run constraint plans against the working copy", line 502) and 7 ("Commit or discard: If constraints pass ... if constraints fail, return the array", line 503). (2) Contradiction: want doc § Certificates ¶1 (line 187) states "the runtime does **not** re-evaluate them against the post-mutation configuration", and § The runtime's role (line 153) limits runtime work to "the declared premises (ingress validation of args and editable fields, guards) ... and nothing beyond them" — post-mutation constraint evaluation with conditional commit is exactly what these exclude. Want doc line 203 further flags the same "commits only if every constraint holds" semantics in philosophy.md as needing owner-approved rewording, corroborating the correction direction (steps 6–7 constraint-eval/discard go; commit unconditional after premise-validated execution; this is consistent with no-deferral at line 159, which leaves no unproven rules for runtime to evaluate). (3) Family cites: line 141 (In Scope: "Constraint evaluation ... collect violations"), line 144 ("After mutations and before constraint evaluation" recompute), line 1585 ("Rows matched, but constraint(s) failed post-mutation" → ConstraintsFailed), line 1789 (Decision 7 body: "Constraint evaluation collects all violations") — all in the same post-mutation-evaluation family. Counter-evidence hunt: evaluator.md contains no reference to the want doc and no qualifier softening line 505 (the doc header's "compiler proofs are intended to eliminate runtime fault paths" covers faults, not constraint re-evaluation); no evidence found that the quote is misread, misplaced, or qualified.

### canon-25 — CONFIRMED (high)

**Claim:** Committed canon at docs/runtime/evaluator.md:1700 states, verbatim: "Constraints are evaluated against the working copy AFTER action execution and computed field recomputation" - and this contradicts the want doc (docs/Working/what-i-want-2026-07-16.md, § Certificates ¶1). Correction direction: The constraint-evaluation guarantees table restates the sweep; corrects with the Fire-lifecycle family..

**Verifier:** Checked: (1) exact-match grep of docs/runtime/evaluator.md in both working tree and committed HEAD — the quoted sentence appears verbatim at line 1700 in both, as the Description cell of the "Post-mutation evaluation" row in the § Constraint Evaluation Guarantees table; git status shows the file clean, so "committed canon" holds. (2) docs/Working/what-i-want-2026-07-16.md § Certificates ¶1 (line 187) states the runtime "does not re-evaluate them against the post-mutation configuration," and line 153 says fault-prevention/constraint re-checks are omitted at evaluation time — a direct contradiction with the evaluator.md guarantee. (3) Surrounding context of evaluator.md:1700 (e.g., "No constraint skipping") reinforces the post-mutation-sweep reading; no qualifier limits it. The want doc's own philosophy flag confirms the correction direction (Fire's "evaluates all applicable constraints against the resulting configuration" family of wording is what must change). No misquote, line drift, or missing qualifier found.
