# Soundness Build Plan — Phases P3–P8 Heavyweight Expansion — 2026-06-07

**Status**: Draft — revised 2026-06-07 (cross-phase reconciliation + rigor-parity round). **Resolved this round:** (1) **the O-1 (a) interval-dict backstop had no owning phase** — the online `IntervalContainmentWitness` accepting arm (strategy `IntervalContainment`, `ProofEngine.cs:1088`, a DISTINCT dispatch arm from FlowNarrowing/`:1077`, verified) that P6's `if Y>0 then X/Y` route discharges through under O-1 (a) was assigned to NO phase, so P6 step-0's grep would vacuously block the very capability O-1 (a) unlocks; **assigned it explicitly to P3 items-in-scope** as a named Tier-A-leftover arm, fixed P3 step 5 to build it, and pointed P6 step-0/Dependencies at the P3-owned arm. (2) **the P3→P6 dominance-leg coupling** — added a RESERVED `(node, branch-polarity)` dominance/polarity provenance slot to the P3-built `GuardInPath`/`FlowNarrowing` witness shapes (empty at P3, populated by P6 step 8) so P6 EXTENDS rather than mutates them, and added P6 exit (g-iv) re-mutation-killing the net-new dominance-re-walk path. (3) **commit-co-occurrence demoted to non-load-bearing** in P3 exit (f-ii) and step 4 + P4 exit (e)(iv): a same-commit co-occurrence has no relative order, so the SOLE ordering enforcers are now behavioral (revert/mutate-the-arm-and-the-un-suppress-fails, mirroring P1 exit (d'-iii)) + runtime-observable (ledger-state), with git-adjacency retained only as a coarse secondary guard. (4) **P4's Class-C flip hard-gated on GREEN P3 arms** (planted-false-reject + 100% mutation-kill), not merely "arms exist" / "should land at-or-after". (5) **P6 given its own AC-8 delta obligation** (exit (j)) — its net-new `ReduceFacts`/`EffectiveGuard`/dominance-leg LOC falls outside the P3 re-run (which precedes P6) and is now sized against the ceiling by P6 itself. (6) **P7's BMC bound made a committed artifact** with a derivation + non-vacuity assertion (the enumeration provably reaches the `(a*b)*c` fault-prone shape), so "exhaustive to the bound" is a checkable figure, not a possibly-trivial cap. (7) **P5's harness-ownership reassignment dropped** — the obligation-ledger-snapshot harness's canonical owner stays P1 step-10b (per the parent plan); P5 is a soft CONSUMER with first-needer-builds-under-the-canonical-home, and the cross-phase question is surfaced UP to the parent rather than originated here. (8) **P3 soundnessClass clarified** to foreclose the FlowNarrowing Class-B-re-verify vs P6-Class-C-widen conflation. These six blocks expand the former lightweight stubs P3–P8 of [soundness-build-plan-2026-06-07.md](soundness-build-plan-2026-06-07.md) to **heavyweight detail per explicit owner direction**, deliberately overriding the `/plan` N+1 rule (which would keep only the current + next phase heavyweight and leave P3–P8 as stubs). Every block here matches the rigor of the P1/P2 heavyweight blocks in the parent plan — concrete `file:line`, verified-against-source steps with a per-step *Validate*, testable exit criteria including a labelled **BLOCKING SOUNDNESS GATE**, day-banded effort, doc-update obligations (including canonical promotion into `docs/compiler/soundness-and-coverage.md`), and a soundness class. They inherit the parent plan's hard constraints verbatim: the checker-first sequencing constraint (no phase ships a new `Proved`-emitting capability before its checker/demote backstop), the O-1 (a)/(b) branching (P3's FlowNarrowing arm and P6's narrowing witness half handle both branches explicitly), and the standing canonical-promotion obligation (Definition-of-done item 9). When the parent plan's summary table, Open decisions, or Soundness-sequencing argument disagrees with a detail here, the parent plan is canonical and this expansion is reconciled to it on the next pass.

---

## Heavyweight phase block — P3: Replicate the checker across the remaining 10 strategies + cross-cutting predicates (Class B replication + the one Class-C un-suppression)

**Goal**: Take every remaining `ProofWitnessKind` shape NOT produced-and-checked in P2 through the proven witness→checker-arm→planted-false-witness-test→mutation-kill→soundness-case loop in lockstep (reusing P2's `ProofChecker`/`ProofWitness` spine), POPULATE/WIRE the two P2-built cross-cutting predicate SPINES (`StalenessClean` 5-arm fact-kind-aware; `CrossUnitFactorPreserved` `UcumParser` leaf, O-6-gated) — both BUILT in P2 conservative-reject-only, P3 fills their family-wide arms and applies them across the remaining strategies (P3 does NOT re-build the spines, per the parent Backstop-map predicate-ownership rule) — and UN-SUPPRESS the single deferred P1/O-9 guard-self-narrow `Proved` (a Class-C micro-ordering) strictly after its `GuardInPath` checker arm rejects a planted false witness and mutation-kills 100% — so that when P3 closes, every `Proved`-emitting cell in the proof engine carries a re-checkable witness an always-on checker re-verifies, with each over-prove cell sound-by-rejection or sound-by-online-certificate, never trusted.

**Items in scope**: New checker arms in `src/Precept/Pipeline/ProofChecker.cs` (P2-created) — one `[HandlesCatalogMember(ProofWitnessKind.X)]` arm per remaining shape NOT built in P2: `GuardInPathWitness` (the four-arm guard switch at `Strategies.cs:713-735`; also the un-suppress backstop for P1/O-9 AND P6's `!=`-then-branch discharge route); `FlowNarrowingWitness` (LeftField/RightField + StalenessSets, Contributors gated by P2 O-1 disposition; `Strategies.cs:1078-1149`, both-operand staleness gate `:1105-1106`); **the online `IntervalContainmentWitness` accepting arm UNDER O-1 (a)** — the backstop for the narrowing-derived `IntervalContainment` `Proved` path (`TryIntervalContainmentProofNarrowed`, strategy `IntervalContainment`, dispatched at `ProofEngine.cs:1088` — a DISTINCT dispatch arm from `TryFlowNarrowingProof`/strategy `FlowNarrowing` at `ProofEngine.cs:1077`, verified). This arm is the O-1 (a) backstop that P6's interval-dict route (`if Y > 0 then X/Y`) discharges through; it is NOT subsumed by the FlowNarrowing online arm (the two are separate strategies producing separate witness shapes). UNDER O-1 (b) this row has no online arm and its narrowing-derived `Proved` demotes to the P2-wired bare-bounds path (no P3 build — the demote target is P2's). **It is enumerated here as an explicit Tier-A-leftover arm built when O-1 disposes (a)**, so the O-1 (a) interval-dict backstop has a single named owning phase (P3) and a single named arm (`IntervalContainmentWitness` accepting arm) — closing the prior gap where P6 step-0 greps for an arm no phase had been assigned to build. `QualifierCompatibilityWitness` / `QualifierChainWitness` / `AssignmentQualifierWitness` (UnitBinding-carrying, conservative-reject until P4/O-6 — `QualifierChain` has NO `ProofStrategy` member, `AssignmentQualifier`+`QualifierCompatibility`+`QualifierGuardNarrowing` all map to `ProofStrategy.QualifierCompatibility` per `TryDischarge` at `ProofEngine.cs:1078-1083`, so each keys its arm on its own `ProofWitnessKind` shape); the 7-arm `DeclarationAttributeWitness` sub-DU (DimensionNarrowing/ModifierDeclared/NumericViaSatisfaction/AccessorNonnegative/MinCountNonempty/FunctionReturnNonnegative/PresenceGuaranteed — `Strategies.cs:85-227`); `DimensionalProductWitness`; `LengthContainmentWitness`/`CountContainmentWitness`/`CollectionGrowthWitness` (Tier-A shapes produced only by P3-reached paths, NOT exercised in P2's single Tier-A loop-validation cell); `SatisfiabilityWitness`/`ConstructabilityWitness` reject-direction arms (`ScanRulesAgainstDefaults` is-false at `Satisfiability.cs:79`). **The `GuardInPathWitness` and `FlowNarrowingWitness` shapes RESERVE an additive `(node, branch-polarity)` dominance/polarity provenance slot (empty/`None` at P3-build time, populated by P6 step 8) — so P6's dominance leg EXTENDS rather than MUTATES the P3-built witness shape, and no P3 checker arm is silently re-opened by P6. The slot is declared-but-empty at P3; P6 is the sole populator (see P6 step 8 + P6 exit (g)).** New work in `src/Precept/Pipeline/ProofChecker.cs`: POPULATE/WIRE the two P2-built shared cross-cutting predicate SPINES `StalenessClean(StalenessSets, citedFields, ObligationFactKind)` and `CrossUnitFactorPreserved(UnitBinding)` — both BUILT in P2 (conservative-reject-only); P3 fills their family-wide arms and wires them into the qualifier/`DimensionalProduct`/narrowing discharge sites ONCE (P3 does NOT re-build either spine — the single builder is P2, per the parent Backstop-map predicate-ownership rule). Discharge-side changes (untrusted) in `ProofEngine.Strategies.cs` / `.Intervals.cs` / `.Qualifiers.cs` / `.QualifierNarrowing.cs` / `.Lengths.cs`: each remaining `Try*` (`TryGuardInPathProof:713`, `TryFlowNarrowingProof:1078`, `TryQualifierCompatibilityProof`, `TryQualifierGuardNarrowingProof:96`, `TryAssignmentQualifierProof:36`, `TryDeclarationAttributeProof:85`, `TryDimensionalProductProof`, `TryLengthContainmentProof`, `TryCountContainmentProof`, `TryCollectionGrowthProof:706`) changes `bool`→`ProofWitness?` and constructs its witness in-body; `TryDischarge` (`ProofEngine.cs:1054`) propagates. The P1/O-9 un-suppress edit (remove the guard-self-narrow discharge suppression tag/gate added in P1 step-2). `test/Precept.Tests/ProofEngine/WitnessCheckerTests.cs` (P2-seeded) gains per-shape valid-accept + planted-false-reject cases including the five-narrowing-path staleness fixtures and the two cross-unit states (pre/post-O-6); a P3-intra-ordering meta-test for the un-suppress. Per-arm mutation-kill via the O-10 mechanism wired in P2 step 0, AS each arm lands. AC-8 full-surface trusted-base re-run.

**Decisions required before kickoff**: O-7 (satisfiability/constructability CLEAN-direction trusted-base expansion — lean (b) keep the clean direction carved out, deliver only the reject-direction re-check; the dominant clean case folds to null per `Satisfiability.cs:79` rejecting only on is-false, so re-confirming clean is vacuous without pulling `BuildDefaultEnvironment` into the base — verified). AC-8 full-surface re-run with the pre-decided overflow LOC action established at P2 close (a concrete LOC gate: within ceiling → proceed; exceeds → owner-authorized raise-with-justification OR move the cross-unit dimension-granularity or satisfiability clean-direction cell to stated-non-closure — both already carved out). INHERITED, NOT re-litigated: O-1's (a)/(b) disposition from P2 (shapes the FlowNarrowing arm — see Dependencies); O-5 verification ceiling (owner-pending, settled before P2's trusted-base sizing locked); O-10 mutation mechanism (settled in P2 step 0). No NEW language surface and no NEW `Proved`-emitting prover capability is introduced by P3 except the single P1/O-9 un-suppression, whose Class-C disposition is fully pre-decided (un-suppress strictly after the `GuardInPath` arm's planted-false-witness test rejects AND mutation-kills 100%).

**Step-by-step execution**:

1. **Establish the per-arm cadence and the arm-ordering.** Order the remaining shapes so each lands witness→arm→planted-false-test→mutation-kill before the next, reusing P2's `WitnessCheckerTests` harness and the O-10 mutation mechanism. Recommended order: (1) the Tier-A leftovers Length/Count/CollectionGrowth (cheapest, no staleness/unit carriers — shake out the per-arm cadence); (2) GuardInPath (the staleness-carrying arm + the un-suppress backstop + the P6 edge — highest leverage); (3) FlowNarrowing (O-1-shaped); (4) the qualifier family + DimensionalProduct (UnitBinding/CrossUnit-gated, conservative-reject until P4); (5) DeclarationAttribute 7-arm sub-DU; (6) Satisfiability/Constructability reject-direction. *Validate*: each arm's PR slice is green only when its planted-false-witness test rejects (non-empty `CheckLedger` rejection) AND its O-10 mutation run reports 100% kill on that arm — assert via the `WitnessCheckerTests` case + the per-arm mutation report; an arm without both is not landed.
2. **Populate/wire the two P2-built cross-cutting predicate SPINES family-wide in `ProofChecker.cs`** (before the arms that consume them; the spines were BUILT in P2 conservative-reject-only — P3 fills their family-wide arms and routes every consuming arm through them, it does NOT re-author either spine). (2a) `StalenessClean(StalenessSets sets, ImmutableArray<string> citedFields, ObligationFactKind kind)` — P2 ships the spine; P3 POPULATES its 5-arm family-wide application: apply `ReassignedBefore` UNIVERSALLY to every citedField; apply `CountEstablishedBefore`/`CountInvalidatedBefore` ONLY when kind is the count>0 fact — mirroring the asymmetry verified at `ProofLedger.cs:51-82` (`ReassignedBefore` invalidates every fact about a replaced field; the count sets apply only to the count>0 fact). The kind parameter is load-bearing: a kind-blind predicate over-rejects (treating a count-establish as a full reassignment) or under-rejects. (2b) `CrossUnitFactorPreserved(UnitBinding b)` — P2 ships the spine (the `b.SourceUnitRecoverable=false → reject` conservative branch + the conservative-reject else-stub); P3 WIRES it into the qualifier/`DimensionalProduct` arms family-wide; P4 (not P3) flips the else-branch to the `UcumParser`-factor-derived accept. P3's wired behavior: if `b.SourceUnitRecoverable` is false → conservatively reject; else (the P2 conservative-reject else-stub, which P4 later flips) → re-parse `b.LeftSourceUnitText`/`b.RightSourceUnitText` with the trusted `UcumParser` leaf and reject a same-dimension-different-unit cancellation unless `b.UcumFactorApplied` is independently re-derivable — never trusting a prover-resolved `DeclaredQualifierMeta` (red-team c) and never re-running the ~900-line resolution engine (red-team d). *Validate*: a `StalenessClean` unit test asserts a value-fact obligation under only `CountEstablishedBefore` does NOT reject while a count>0 obligation under `CountInvalidatedBefore` DOES; a `CrossUnitFactorPreserved` unit test asserts `SourceUnitRecoverable=false` rejects unconditionally and (post-O-6 path) kg-vs-g rejects unless the factor re-derives. Both predicates mutation-kill 100% as standalone trusted leaves.
3. **`GuardInPathWitness` arm + discharge-side witness construction.** Change `TryGuardInPathProof` (`Strategies.cs:713`) `bool`→`ProofWitness?`; construct `GuardInPathWitness(CitedGuard, GuardKind∈{Numeric/Presence/KeyPresence/IndexBounds}, StalenessSets)` in-body where the discharging `GuardConstraint` is known (the four-arm guard switch at `:723-735`, the per-branch every-branch-proves loop at `:764-784`). The arm calls `StalenessClean(sets, {citedGuardField}, factKind)` and re-runs `NumericConstraintSubsumes` (`Strategies.cs:977`) / the presence leaf against the cited guard — it NEVER re-extracts branches (the R2/Alethe trap). *Validate*: `WitnessCheckerTests` — a valid numeric-guard witness accepts; a planted witness whose CitedGuard field is in `ReassignedBefore` is rejected (the §0.6-item-7 over-prove the count-establish/invalidate logic must catch); mutation of the band comparator (`<=` → `<`) makes a test fail (100% kill).
4. **UN-SUPPRESS the P1/O-9 guard-self-narrow `Proved` — strictly AFTER step 3 is green.** This is the single Class-C output of P3. Remove the P1 step-2 suppression tag/gate that forces a guard-self-narrowed divisor (e.g. from `Open` on `Go` when `Y != 0 and X / Y > 0`) to `Unresolved`. The un-suppress diff MUST be ordered strictly after the step-3 `GuardInPathWitness` arm's planted-false-witness test rejects AND the arm mutation-kills 100%. *Validate* (the intra-P3 micro-ordering, made testable — the LOAD-BEARING leg is behavioral, not git-history): (i) **the behavioral revert/mutate assertion (load-bearing, mirrors P1's suppression-deletion meta-test)** — assert that a build/test state in which the un-suppress is PRESENT but the step-3 `GuardInPathWitness` arm is REVERTED (or its band comparator mutated so it would accept a planted false witness) FAILS: with the arm reverted/broken, the un-suppressed `when Y != 0 and X / Y > 0` → `Proved`/`GuardInPath` carries a witness the (now-absent-or-broken) checker cannot re-verify, so `CheckLedger` rejects it (the hollow-`Proved` it would become). This is a TRUE behavioral constraint — the un-suppress cannot be green without a green arm — not a commit-adjacency check; (ii) a `precept_compile` probe on `when Y != 0 and X / Y > 0` flips from `Unresolved` (P1) to `Proved`/`GuardInPath` carrying a non-null, checker-verified `GuardInPathWitness`; re-run the P1 step-10b corpus diff to confirm the now-allowed `Proved` carries a checked witness (it is no longer a hollow-`Proved` violation because its checker arm exists). (iii) a SECONDARY (non-load-bearing) meta-test asserts the un-suppress diff and the GuardInPath-arm-green state co-occur in the same or a later commit — a coarse commit-adjacency guard, explicitly NOT the soundness enforcer (a same-commit co-occurrence has no relative order; leg (i) is what actually enforces "the un-suppress cannot be green without its arm").
5. **`FlowNarrowingWitness` arm, BRANCHED on the P2 O-1 disposition (write BOTH).** Change `TryFlowNarrowingProof` (`Strategies.cs:1078`) `bool`→`ProofWitness?`; construct `FlowNarrowingWitness(Narrowed, Contributors, LeftField, RightField, StalenessSets)` where leftField/rightField are recovered at `:1097-1099` and the both-operand staleness gate is at `:1105-1106`. The arm ALWAYS calls `StalenessClean(sets, {LeftField, RightField}, value-fact-kind)` and rejects if EITHER operand is in `ReassignedBefore` (red-team blocker a). UNDER O-1 (a) ONLINE CERTIFICATE: the arm additionally re-verifies the all-contributor Contributors (per-branch `NarrowByConstraint` results, cross-branch OR-union arms, sibling-reject negation exclusions, unconditional field-to-field half-lines from `BuildNarrowedIntervals` at `Intervals.cs:519-654`) cheaper-than-produced — `Narrowed` re-derives from the named contributors without re-running the prover's narrowing engine. **ALSO under O-1 (a), build the SEPARATE online `IntervalContainmentWitness` accepting arm (the items-in-scope Tier-A-leftover)** for the narrowing-derived `IntervalContainment` `Proved` path (`TryIntervalContainmentProofNarrowed`, strategy `IntervalContainment`, `ProofEngine.cs:1088` — a DISTINCT dispatch arm from `TryFlowNarrowingProof`/strategy `FlowNarrowing` at `ProofEngine.cs:1077`, verified): change `TryIntervalContainmentProofNarrowed` (`Intervals.cs:489`) `bool`→`ProofWitness?` and construct an `IntervalContainmentWitness(Computed, NarrowingProvenance)` whose accepting arm re-derives the narrowed `Computed` from its named per-field interval contributors cheaper-than-produced. This is the arm P6's interval-dict route (`if Y > 0 then X/Y`) discharges through under O-1 (a); it is NOT the FlowNarrowing arm and must be built as its own named arm so P6 step-0's `IntervalContainmentWitness` grep matches a live arm rather than vacuously blocking P6's interval-dict capability. UNDER O-1 (b) this `IntervalContainmentWitness` arm is NOT built — the narrowing-derived `IntervalContainment` `Proved` demotes to the P2-wired bare-bounds path (P2 exit (i)/(e)), so there is no P3 online arm and P6's interval-dict route demotes rather than discharges. UNDER O-1 (b) — TWO ROUTES, the bare-bounds demote does NOT cover both (verified against `TryFlowNarrowingProof:1078`): the arm ships with NO online Contributors re-check, and the fallback SPLITS by what the narrowing rests on. **(b-1) Interval-derived narrowing** (a `Computed` interval narrowed from per-field interval facts, e.g. a half-line `if Y > 0` then-branch): DEMOTES to the P2-wired bare-bounds path (the re-activated `TryIntervalContainmentProof` at `Intervals.cs:467` / bare `ExtractFieldInterval` at `Intervals.cs:202`), never left `Proved` — a bare per-field interval exists to re-derive against. **(b-2) Relational field-to-field narrowing** (the discharge rests entirely on a RELATION between two operands — `A - B >= 0` discharged by `A >= B`; verified `TryFlowNarrowingProof` matches `leftField`/`rightField` against guard/rule branches via `GuardRelationImpliesObligation` and proves on the relation alone, with NO per-field interval involved): there is NO bare-bounds interval to demote to — for bare `number` operands with no bounds/defaults both operands are `(-inf,+inf)`, so a bare `ExtractFieldInterval` re-derivation of `A - B` is `(-inf,+inf)` and cannot REPRESENT, let alone re-verify, the relation. This sub-case therefore CONSERVATIVELY REJECTS (the bare-bounds backstop does not cover the guard-fact/relational witness family — exactly the boundary the parent Backstop-builds map states). "Demote-to-bare-bounds" collapses into "conservative-reject" for (b-2); the two routes are NOT one mechanism. *Validate*: under (a) — a planted `FlowNarrowingWitness` whose Contributors do not re-derive `Narrowed` is rejected; under (b-1) — a narrowing-derived interval with no certificate demotes to `Unresolved` (asserted via the demote path having a live caller, parallel to P2 exit (e)); under (b-2) — a relational FlowNarrowing obligation (e.g. `sqrt(A - B) >= 0 when A >= B`, A/B bare `number`) conservatively REJECTS (asserted as Unresolved with NO demote target reachable — there is no bare-bounds interval, so the reject is the live fallback, not a demote); under ALL — a FlowNarrowing obligation whose LeftField OR RightField is in `ReassignedBefore` is rejected (the staleness fixture); mutation-kill 100% on whichever arm ships.
6. **Qualifier family + DimensionalProduct arms (UnitBinding-carrying, conservative-reject until P4/O-6).** Change `TryQualifierCompatibilityProof`, `TryQualifierGuardNarrowingProof` (`QualifierNarrowing.cs:96`), `TryAssignmentQualifierProof` (`QualifierNarrowing.cs:36`), `TryDimensionalProductProof` `bool`→`ProofWitness?`. Construct `QualifierCompatibilityWitness(UnitBinding, StalenessSets?)`, `QualifierChainWitness(LeftAxis, RightAxis, LeftComparable, RightComparable, UnitBinding)`, `AssignmentQualifierWitness(SourceComparable, Target, NarrowingGuard?, StalenessSets?, UnitBinding)`, `DimensionalProductWitness(LeftVector, RightVector, Product, CuratedAlias, UnitBinding)`. Each arm: re-runs its leaf (`QualifiersAreCompatible` at `Qualifiers.cs:118` for the compat declaration branch; `ChainQualifiersMatch` at `Qualifiers.cs:105` — the DISTINCT cross-axis leaf QualifierChain needs, NOT `QualifiersAreCompatible`; value-equality for assignment; vector-multiply+alias for product), calls `StalenessClean` when narrowing-derived (`NarrowedValueFromGuard` at `QualifierNarrowing.cs:49/113/116`, `NarrowedPeriodDimensionFromGuard` at `:182`), and calls `CrossUnitFactorPreserved` on its UnitBinding. Until P4/O-6 supplies the raw `SourceUnitText`, `SourceUnitRecoverable=false` on the dimension-granularity anchor paths (PriceTimesQuantity/QuantityTimesQuantity) → `CrossUnitFactorPreserved` conservatively rejects (over-rejection, never the BUG-030 false-accept). *Validate*: `WitnessCheckerTests` — the BUG-030 anchor cases (PriceTimesQuantity QualifierChain, QuantityTimesQuantity DimensionalProduct, price Plus/Minus QualifierCompatibility) conservatively reject pre-O-6 (`SourceUnitRecoverable=false`) — asserted as a measured conservative-reject, NOT a claimed closure; a qualifier-compat witness narrowed by a stale guard (field in `ReassignedBefore`) rejects; mutation-kill 100% per arm.
7. **`DeclarationAttributeWitness` 7-arm sub-DU + Satisfiability/Constructability reject-direction arms.** (7a) Change `TryDeclarationAttributeProof` (`Strategies.cs:85`) `bool`→`ProofWitness?`; construct the correct sub-shape per the verified multi-modal switch: DimensionNarrowing (Dimension arm `:88-100`, carries StalenessSets for the `NarrowedPeriodDimensionFromGuard` fallback), ModifierDeclared (`:103-153`), NumericViaSatisfaction (`:205-215`), AccessorNonnegative (`:176`), MinCountNonempty (`:192-202`), FunctionReturnNonnegative (`:164`), PresenceGuaranteed (`:218-225`). The DimensionNarrowing arm calls `StalenessClean`. (7b) Record `SatisfiabilityWitness`/`ConstructabilityWitness` onto the ledger verdict channel from `ScanRulesAgainstDefaults`/`CheckInitialStateSatisfiability` (`Satisfiability.cs`); the arm re-evaluates the is-false fold via the trusted `ConstantFold` leaf to catch a fabricated false-clean witness — REJECT-direction only; the null/clean direction is carved out per O-7 (re-confirming it is vacuous and would pull `BuildDefaultEnvironment` into the base). The four friction-direction scans (UnsatisfiableGuard/TautologicalGuard/VacuousRule/ContradictoryRule) are NOT recorded. *Validate*: a DeclarationAttribute witness of the wrong sub-shape for its discharge site is rejected (e.g. a Modifier witness on a Dimension discharge); a default-violates-rule witness whose carried fold re-evaluates to false rejects, while a null-folding rule is asserted not-claimed-confirmed; mutation-kill 100% per sub-arm.
8. **AC-8 full-surface trusted-base re-run + per-strategy demote/conservative-reject fallback disposition.** Re-run the AC-8 LOC sizing against the full corrected surface (the 5-arm `StalenessClean`, the `UnitBinding`/`CrossUnitFactorPreserved` spine + `UcumParser` leaf — these ENROLL into the §4 trusted base at their P2 spine-build, so the P3 re-run re-sizes them as ALREADY-in-base, not a P3 net-add — the 7-arm DeclarationAttribute DU, the shared in-base leaves now counted: `UcumParser`/`ConstantFold`/`NarrowByConstraint`/`ExtractFieldInterval`/sign tables, +~150–250 LOC over the P2 baseline) against the pre-decided overflow action set at P2 close. **§4 enrollment is single-event (no double-count): `CrossUnitFactorPreserved` + `UcumParser` enroll at P2 (spine-build); P3's re-run re-sizes the full base including them; P4 UPDATES (does not re-add) their §4 row to reflect the factor-derived else-branch it adds.** Record, for EVERY non-numeric strategy whose online arm proves un-buildable, its backstop disposition (demote-to-bare-bounds where an interval path exists; conservative-reject otherwise — the crosscutting fallback answer, never a silent trust). *Validate*: the recorded LOC figure is compared against the ceiling and the pre-decided action is applied (proceed / owner-authorized raise / cell-to-non-closure); a meta-assertion confirms every `ProofWitnessKind` member has either an accepting arm, a demote target, or a conservative-reject disposition (no shape is trusted-without-arm — the `[HandlesCatalogExhaustively(ProofWitnessKind)]` exhaustiveness check from P2 fails the build otherwise).

**Dependencies**: HARD PREREQUISITE — P2 (canonical, plan summary table + Backstop-builds-and-dependents map): P3 reuses P2's `ProofChecker` spine + `ProofWitness` DU + the two predicate SPINES (`StalenessClean`, `CrossUnitFactorPreserved` — built conservative-reject-only in P2; P3 populates their family-wide arms) + the wired bare-bounds demote target — verified none exists today (no `ProofChecker`/`ProofWitness`/`ProofWitnessKind` in `src/Precept`). P3 cannot start before P2's loop-validates-itself gate (a planted checker bug caught by a test) passes. **INTERIM-NULL-WITNESS HANDOFF (the P2 exit (c) scoping P3 closes): P2's zero-hollow-`Proved` gate (exit (c)) is REWIRED-STRATEGY-SCOPED — the qualifier family / `GuardInPath` / `FlowNarrowing` / `DeclarationAttribute` / `DimensionalProduct` / P3-only Tier-A shapes still discharge to `Proved` with a NULL witness in the P1→P3 interim, which P2's `CheckLedger` TOLERATES (not an internal-error rejection — per P2 step 5's rewired-strategy-scoped null-witness rule). P3 is the phase that CLOSES this: each P3 `Try*` rewire (`bool`→`ProofWitness?`, expansion line 11) makes its strategy's `Proved` carry a witness, and AT THAT POINT a null witness on that strategy genuinely becomes a hollow-`Proved` rejection. So P3 exit (a)'s "zero null-Witness `Proved` across ALL strategies" is the full-surface closure of P2 exit (c)'s P2-rewired-only scope — P3 does not REQUIRE P2 to have rejected the un-rewired qualifier `Proved` (P2 could not, without over-rejecting the corpus); P3 EARNS the full coverage by rewiring those discharge bodies itself.** INHERITED FROM P2 — the O-1 (a)/(b) disposition (P2 step 9, the phase-shaping gate): step 5's FlowNarrowing arm ships its online all-contributor certificate under (a) OR demote-to-bare-bounds-only under (b); P3 does not re-dispose O-1, it consumes the recorded outcome. AC-8 baseline + pre-decided overflow action established at P2 close feed step 8's full-surface re-run. O-5 (owner-pending, settled before P2 trusted-base sizing) and O-10 (mutation mechanism, settled in P2 step 0) are upstream of P3's per-arm mutation-kill cadence. P3 IS A HARD DEPENDENCY EDGE OF P6 (canonical, P6 stub Dependencies + the Backstop-builds map): P6's narrowing-derived `Proved` discharges through the P3 GuardInPath/FlowNarrowing/interval-dominance arms — verified P6's headline `!=` then-branch routes to GuardInPath (conditional-totality probe (c): X/Y>0 when Y!=0 → Proved/GuardInPath), which the P2 interval-only demote backstop does NOT cover, so P6-before-P3 conservatively REJECTS P6's headline capability (never demotes it). P3 carved `CompositionalConstraint` and `SuppressedByStructure` OUT of scope (the P2 rewires produce them, so P2 builds-and-checks them per the produced-implies-arm invariant). NOT a dependency of P4: P4 (BUG-030 source-unit capture) lands the raw `SourceUnitText` upstream of projection; P3's qualifier/DimensionalProduct arms conservatively reject (`SourceUnitRecoverable=false`) until then — P3 WIRES the P2-built `CrossUnitFactorPreserved` spine into those arms (P2 is the spine's sole builder; P3 populates its family-wide application), and P4 later flips its else-branch from conservative-reject to factor-derived-reject, so P3 precedes P4 but does not block on it.

**Exit criteria** (testable):
- (a) Every remaining `ProofWitnessKind` shape NOT produced-in-P2 has an accepting checker arm: a corpus pass over `samples/*.precept` ∪ `test/Precept.Tests/ProofEngine/` fixtures finds zero null-Witness `Proved` obligations across ALL strategies (extends P2 exit (c) from the Tier-A cell to the full surface) — verified via `CheckLedger` returning empty on the clean corpus.
- (b) NO-OVER-REJECTION GATE: the full proof-engine corpus compiles with zero checker rejections after all P3 arms land (extends P2 exit (d) family-wide) — verified by running the corpus through the always-on checker.
- (c) `StalenessClean` fires on all FIVE narrowing paths: a planted false witness on each of GuardInPath, FlowNarrowing (LeftField OR RightField in `ReassignedBefore`), QualifierCompatibility-narrowing, AssignmentQualifier, DeclarationAttribute.DimensionNarrowing is rejected; AND the count/value fact-kind asymmetry holds (a count>0 obligation under `CountInvalidatedBefore` rejects; a value obligation under only `CountEstablishedBefore` does not) — `WitnessCheckerTests`, all green.
- (d) `CrossUnitFactorPreserved` conservatively rejects the BUG-030 anchor paths pre-O-6 (`SourceUnitRecoverable=false`) — the reject is asserted as a MEASURED conservative-reject count, NOT a claimed BUG-030 closure.
- (e) The FlowNarrowing arm matches the inherited O-1 disposition, with the O-1 (b) fallback SPLIT by route (the bare-bounds demote does not cover relational facts): under (a) a planted non-re-deriving Contributors witness rejects; under (b-1) an INTERVAL-DERIVED narrowing with no certificate demotes to `Unresolved` (the bare-bounds path has a live caller) AND the measured demote count of interval-derived `Proved`→`Unresolved` is reported; under (b-2) a RELATIONAL field-to-field obligation (e.g. `sqrt(A - B) >= 0 when A >= B`, A/B bare `number`) conservatively REJECTS to `Unresolved` with NO demote target — verified there is no bare-bounds interval to demote to for an unbounded-operand relational obligation, so the fallback is conservative-reject, not a demote-via-live-caller — AND the measured conservative-reject count of relational `Proved`→`Unresolved` is reported separately from the (b-1) demote count.
- (e') **O-1 (b) DEMOTE/CONSERVATIVE-REJECT NEUTRALITY GATE (a constraining whitelist, not a report — parallel to P6 exit (f) regression-neutrality).** Under O-1 (b) ONLY (vacuous under (a), which ships the online certificate): the relational-FlowNarrowing family + narrowing-derived `IntervalContainment` demote/conservative-reject to `Unresolved` is an author-facing over-rejection, not merely an internal-checker event — so it MUST be a bounded whitelist, not an open measured count. ENUMERATE the specific corpus members whose `Proved`→`Unresolved` demotion (b-1) or conservative-reject (b-2) is EXPECTED — the relational-FlowNarrowing fixtures (the `RelationalNarrowingCoreTests` family, e.g. `sqrt(A - B) >= 0 when A >= B` with bare-`number` A/B, verified `Proved`/FlowNarrowing at baseline via `TryFlowNarrowingProof`'s `GuardRelationImpliesObligation` `Strategies.cs:1142`) and the narrowing-derived `IntervalContainment` cell — and ASSERT NO OTHER corpus obligation regresses `Proved`→`Unresolved`. This turns the (e) measured counts (the b-1 demote count + the b-2 conservative-reject count) from a REPORT into a CONSTRAINING whitelist: any obligation outside the enumerated set that regresses FAILS the gate. **OWNER-FLAG (surfaced, not resolved here): under O-1 (b), `sqrt(A - B) >= 0 when A >= B` (and the relational-FlowNarrowing family) — which compiles CLEAN today (verified `Proved`/FlowNarrowing) — newly emits PRE0083 / `Unresolved`. This is a USER-VISIBLE capability loss (an author-facing reject→reject regression from a previously-accepted program), not just an internal sequencing detail. The O-1 disposition is P2's; this gate records the downstream author-facing cost so the owner sees it before O-1 (b) locks.** (Vacuous under O-1 (a): the online all-contributor certificate re-verifies the relational narrowing, no demote, no author-facing loss.)
- (f) **BLOCKING SOUNDNESS GATE — every shipped P3 `Proved`-emitting cell is FILLED in the falsifiable sense (named witness shape + accepting checker arm + planted-false-witness test that rejects + a 100%-mutation-kill result from the O-10 mechanism), AND the single Class-C un-suppression is ordered-correct.** Two legs, both required: (f-i) for EACH P3 arm (the 10 strategies + the two cross-cutting predicates), assert its planted-false-witness test produces a non-empty `CheckLedger` rejection AND its O-10 mutation run reports 100% kill — an arm missing either is not landed (no `Proved`-widening trusted-without-justification, F-3). (f-ii) THE INTRA-P3 MICRO-ORDERING (the un-suppress is a Class-C output that MUST follow its backstop) — enforced by a BEHAVIORAL leg (load-bearing) plus a RUNTIME-OBSERVABLE leg (load-bearing), with a commit-history leg explicitly demoted to non-load-bearing, so the ordering is NEVER enforced by git-history assertion (a same-commit co-occurrence has no relative order and cannot encode "strictly after"): **(f-ii-behavioral)** the SOLE ORDERING ENFORCER (mirrors P1 exit (d'-iii)'s suppression-deletion meta-test): assert that a build/test state in which the un-suppress is PRESENT but the step-3 `GuardInPathWitness` arm is REVERTED or its band comparator MUTATED-to-accept-a-planted-false-witness FAILS — with the arm broken, the un-suppressed `when Y != 0 and X / Y > 0` → `Proved`/`GuardInPath` carries a witness `CheckLedger` rejects (the hollow-`Proved` it becomes). This is a true behavioral constraint (the un-suppress cannot be green without a green arm), the mechanical guarantee that P3 ships no `Proved`-widening ahead of its checker arm — NOT a commit-adjacency check. **(f-ii-runtime)** the RUNTIME-OBSERVABLE leg (load-bearing, analogous to P6 exit (g-ii)): assert that in the PRE-un-suppress configuration the four Class-C guard-self-narrow fixtures (the step-10a `TransitionRowContext`/`StateHookContext`/`EventHandlerContext`/`ConstraintContext` self-dominating cases) have divisor disposition `Unresolved`, AND in the POST-un-suppress configuration they are `Proved`/`GuardInPath` carrying a NON-NULL, checker-verified `GuardInPathWitness` (the checker arm accepts it, `CheckLedger` empty on those rows) — so the post-un-suppress state is observable directly from the obligation-ledger; a post-un-suppress `Proved` with a null or checker-rejected witness FAILS. **(f-ii-commit)** the COMMIT-CO-OCCURRENCE leg (SECONDARY, NON-LOAD-BEARING — retained only as a coarse adjacency guard): a meta-test asserts the un-suppress diff co-occurs in the same or a later commit than the GuardInPath arm's green state. This leg does NOT enforce "strictly after" (same-commit changes have no relative order); it is documented as secondary so a reviewer does not mistake it for the ordering enforcer — (f-ii-behavioral) is.
- (g) AC-8 full-surface LOC re-run recorded; the pre-decided overflow action applied (proceed within ceiling / owner-authorized raise-with-justification / cell-to-stated-non-closure) — Lock-blocking, not after-the-fact.
- (h) Doc-state gate (mechanical, parallel to P1 exit (e)): a doc-lint asserts `bugs.md` BUG-031 advances from Fixed-with-note to fully-Fixed on the un-suppress, and soundness-case §2 gains the `when Y!=0 and X/Y>0` → Proved/GuardInPath filled row + the §4 audit rows for every P3 arm; absence fails the check.

**Effort**: **XL (12–16 days)**. The cost is the lockstep replication breadth: 10 remaining strategy arms + 2 cross-cutting predicates, each taken witness→arm→planted-false-test→100%-mutation-kill before the next, atop the proven P2 spine (so no new spine/DU/mutation-harness cost, and no predicate-spine cost — P2 builds both predicate spines conservative-reject-only — the trusted base is ~750–900 LOC total per soundness-case §4, of which P2 already built the Tier-A core + 2 Tier-B arms + the two predicate spines; P3 adds the ~150–250 LOC of remaining arms + the 5-arm family-wide `StalenessClean` APPLICATION + the family-wide `CrossUnitFactorPreserved` WIRING into the qualifier/`DimensionalProduct` arms — the `UcumParser` leaf already exists in source (`Qualifiers.cs:90`), not net-new LOC). The DeclarationAttribute 7-arm sub-DU and the five-narrowing-path `StalenessClean` fixtures are the breadth drivers; the FlowNarrowing arm carries the O-1-branch contingency (under (a) the all-contributor certificate is the single genuine investigation-risk arm; under (b) it collapses to demote-only, cheaper). **Under O-1 (a) P3 ALSO builds the online `IntervalContainmentWitness` accepting arm (the interval-dict backstop P6 rides) — a modest addition (re-derive the narrowed `Computed` from its named per-field interval contributors, no all-contributor relational certificate), bounded within the ~150–250 LOC remaining-arms budget; under O-1 (b) this arm is NOT built (the route demotes to bare-bounds), so it adds nothing.** The un-suppress is a one-edit micro-ordering, not a cost driver. Band assumes the P2-settled O-10 hand-rolled mutation harness extends per-arm without re-tooling.

**Doc-update obligations** (CLAUDE.md routing): CLAUDE.md routing. `docs/compiler/proof-engine.md` — the per-strategy witness-construction for the remaining 10 `Try*` sites (the `bool`→`ProofWitness?` return change), the two cross-cutting predicates (`StalenessClean` 5-arm fact-kind-aware realizing §0.6 item 7; `CrossUnitFactorPreserved` O-6-gated realizing invariant 4), the DeclarationAttribute 7-arm sub-DU discharge, the Satisfiability/Constructability reject-direction-only re-check with the clean-direction carve-out (O-7). `docs/language/precept-language-spec.md` §0.6 — item 7 (sequential proof flow / staleness) recorded as re-verified family-wide by the checker, NOT GuardInPath-only. `docs/runtime/runtime-api.md` + `result-types.md` + `descriptor-types.md` — the full witness shape family now on the proof model (extends P2's Witness-field addition). `docs/tooling/mcp.md` — `precept_proofs` DTO carries the full witness family. `docs/tooling/language-server.md` — hover reads the full witness family. `docs/compiler/diagnostic-system.md` — the witness-checker internal-error surface covers all strategies now. `docs/compiler/README.md` — proof-engine stage maturity (witness-checking complete across all strategies; drop any remaining stub marker). PROMOTION (Definition-of-done item 9, standing per-phase obligation): `docs/compiler/soundness-and-coverage.md` — promote P3's content into its **The trusted checker** section (the remaining arms + the two cross-cutting predicates + the per-strategy fallback dispositions) and its **Trusted base** section (the AC-8 full-surface re-run), advancing those sections Design→Incremental. `docs/Working/bugs.md` — BUG-031 Fixed-with-note → fully-Fixed on the un-suppress (record the `when Y!=0 and X/Y>0` → Proved/GuardInPath flip now carries a checked `GuardInPathWitness`). `docs/Working/soundness-case-2026-06-07.md` — §2 per-strategy soundness-argument rows filled (named witness + arm + planted-false test + mutation-kill) for every P3 cell INCLUDING the un-suppressed GuardInPath row; §4 trusted-base audit rows for every P3 arm + the AC-8 re-run figure; §5.3 per-arm mutation-kill results; the measured FlowNarrowing demote count (under O-1 (b)) and the measured cross-unit conservative-reject count (BLOCKING per-phase soundness-case update).

**Soundness class**: **CLASS B (replication) with ONE CLASS C output.** The bulk of P3 is Class-B checker replication — it adds re-verification arms over `Proved` verdicts that ALREADY exist (the 10 strategies discharge to `Proved` today via `TryDischarge` at `ProofEngine.cs:1078-1090`); replicating their checker arms emits no NEW `Proved` capability, only constrains existing ones, so it cannot itself introduce a hollow `Proved`. Its backstop is P2 (Class B — the `ProofChecker` spine + `ProofWitness` DU + wired bare-bounds demote that P3 reuses; verified none exists pre-P2). The single CLASS C output is the P1/O-9 guard-self-narrow un-suppression (step 4): un-suppressing the deferred `when Y!=0 and X/Y>0` → Proved/GuardInPath is a `Proved`-widening, and per the soundness-sequencing constraint it lands STRICTLY AT-OR-AFTER its backstop — the P3 `GuardInPathWitness` checker arm (step 3), enforced by exit (f-ii)'s commit-ordering meta-test. The FlowNarrowing arm P3 builds re-verifies the FlowNarrowing `Proved` verdicts that exist TODAY (Class-B re-verification of an EXISTING `Proved`, not a new one); the NEW FlowNarrowing-discharged `Proved` that P6 later produces ride this same arm but are P6's Class-C output, gated by P6 exit (g) — P3 introduces no new FlowNarrowing `Proved`. The FlowNarrowing arm under O-1 (a) ships an online certificate (Class-B re-verification of an existing `Proved`); under O-1 (b) its fallback is route-split (the bare-bounds demote does NOT cover relational facts): an INTERVAL-derived narrowing demotes to the P2-wired bare-bounds path, while a RELATIONAL field-to-field narrowing (discharge resting on `A >= B` alone, no per-field interval — verified `TryFlowNarrowingProof:1078`) has NO bare-bounds interval to demote to and CONSERVATIVELY REJECTS (the guard-fact/relational witness family is outside the demote backstop's interval-only coverage, per the parent Backstop-builds map). Both O-1 (b) sub-routes are sound-by-rejection/sound-by-demote — never Class-C-without-backstop. The qualifier/DimensionalProduct arms NARROW (conservative-reject until P4/O-6), widening nothing. No P3 cell ships a `Proved`-widening ahead of its checker arm: the `[HandlesCatalogExhaustively(ProofWitnessKind)]` exhaustiveness check (built in P2) structurally forbids a new witness shape shipping without an arm, and the one un-suppression is commit-ordered behind its arm.

---

## Heavyweight phase block — P4: BUG-030 cross-unit closure — source-unit capture upstream of the Unit→Dimension projection (O-6)

**Goal**: Make the BUG-030 cross-unit factor-drop CHECKABLE by capturing the raw authored source-unit strings at obligation-stamping time — UPSTREAM of the Unit→Dimension projection that destroys them — and threading them onto the witness `UnitBinding` so P2's `CrossUnitFactorPreserved` predicate re-parses them with `UcumParser` and rejects a same-dimension-different-unit cancellation; until the source unit is reachable the cell conservatively rejects (over-rejection, never false-accept), and the cross-unit reject/auto-convert/split POLICY stays a deferred Tier-3 consultation (only the checkability mechanism is in scope).

**Items in scope**: Source files touched: (1) `src/Precept/Pipeline/ProofEngine.cs` — the obligation-stamping sites where qualifier reqs are copied verbatim onto obligations (TypedBinaryOp arm `:305-310` `obligations.Add(new ProofObligation(req, bin, ...))`; the TypedFunctionCall/TypedMemberAccess arms `:312-323` carry the same shape; CreateActionProofSite path at `:448-507` for action-borne qualifier obligations) — the capture must read the raw qualifier text off the operand `TypedExpression` (`TypedTypedConstant`/`TypedFieldRef`/`TypedArgRef` DeclaredQualifiers) BEFORE any resolution and stamp it onto the obligation/witness-precursor. (2) `src/Precept/Language/ProofRequirement.cs` — the four cross-unit-bearing requirement subtypes whose subjects must carry recoverable source-unit provenance: `QualifierCompatibilityProofRequirement` (`:113`), `QualifierChainProofRequirement` (`:122`), `AssignmentQualifierProofRequirement` (`:140`), `DimensionalProductProofRequirement` (`:172`) — plus `ProofSubject` (`:11`, ParamSubject/SelfSubject) which today resolves to an operand only at discharge time. (3) `src/Precept/Language/ProofWitness.cs` (created P2) — the `UnitBinding` shared carrier `UnitBinding(QualifierAxis Axis, string? LeftSourceUnitText, string? RightSourceUnitText, bool SourceUnitRecoverable, bool UcumFactorApplied)` ride on `QualifierCompatibilityWitness` / `QualifierChainWitness` / `DimensionalProductWitness` / `AssignmentQualifierWitness` — populated by P4 (P2 ships it with `SourceUnitRecoverable=false`). (4) `src/Precept/Pipeline/ProofChecker.cs` (created P2) — the shared `CrossUnitFactorPreserved(UnitBinding)` predicate (P2 ships it conservative-reject-only); P4 flips its else-branch to `UcumParser.Parse(LeftSourceUnitText)`/`Parse(RightSourceUnitText)`→factor-derive→reject-unless-equal. (5) `src/Precept/Pipeline/ProofEngine.Qualifiers.cs` — `TryProjectUnitToDimension` (`:944`) / `TryProjectCompoundPrice` (`:914`) / `TryResolveDimensionVector` (`:62-99`) are the projection sites the capture must run UPSTREAM of (NOT modified — they keep projecting; the capture is a parallel raw-string thread); `ResolveQualifierOnAxis` (`:269`) and the field/arg/typed-constant DeclaredQualifiers readers are the resolution surface the capture must NOT re-run (the ~900-line engine the checker must avoid per red-team d). Predicates/codes: `CrossUnitFactorPreserved` (the cross-unit predicate — SPINE built in P2 conservative-reject-only, populated/wired family-wide by P3, else-branch flipped by P4); `SourceUnitRecoverable` flag (the conservative-reject gate); `UcumParser.Parse` (`Qualifiers.cs:90`, the trusted leaf). Witness shapes: `UnitBinding` (the carrier), `QualifierChainWitness` (the PriceTimesQuantity anchor), `DimensionalProductWitness` (the QuantityTimesQuantity anchor). The cross-unit POLICY (reject/auto-convert/split) is explicitly OUT of scope.

**Decisions required before kickoff**: O-6 (cross-unit source-unit capture reachability) — the gating decision, PoC'd in P2 step 9-adjacent reachability probe per the O-6 Open-decisions entry (soundness-build-plan `:59-63`). O-6 (a): on field/typed-constant/arg operands the raw authored unit string ('USD/kg', 'g') IS reachable off the operand's DeclaredQualifiers at stamping time → thread onto `UnitBinding`, checker re-parses with `UcumParser`. O-6 (b): for derived-subexpression operands whose unit only exists post-resolution → `SourceUnitRecoverable=false`, conservative reject (friction, never false-accept). P4 commits the capture for the (a) operands and ships (b) as the explicit unrecoverable fallback. No NEW decision is introduced in P4 — O-6 is the only gate, and its (a)/(b) branches are BOTH shipped (the (b) branch is the conservative-reject path the cell already takes pre-P4). NOTE: P4 does NOT introduce the cross-unit POLICY decision — that stays a deferred Tier-3 consultation against `business-domain-types.md` (Decisions-captured `:41`). If the P2 reachability PoC concluded (b)-wholesale (no operand is recoverable), surface that to the owner before P4 kickoff — P4 would then be a no-op-beyond-P2 (the cell already conservatively rejects) and should be re-scoped or closed as already-stated.

**Step-by-step execution**:

1. **Confirm the P2 reachability PoC outcome and pin which operand classes are O-6 (a)-recoverable.** Read the recorded O-6 PoC disposition (the disposable PoC the O-6 entry mandates in P2, soundness-build-plan `:63`). Enumerate the operand classes the four cross-unit reqs resolve through: `ResolveSubject` (`ProofEngine.cs:852`) → `TypedTypedConstant.DeclaredQualifiers` (`Qualifiers.cs:319`), `TypedArgRef.DeclaredQualifiers` (`:273`), and the field-path branch. The raw unit string lives on `DeclaredQualifierMeta.Unit.UnitCode` (`DeclaredQualifierMeta.cs:35`) and `CompoundPrice.UnitCode` (`:103`) BEFORE `TryProjectUnitToDimension` (`:944`) collapses it to a `.Dimension` whose only field is `DimensionName`. *Validate*: a unit-test fixture over the BUG-030 anchors asserts that, at the TypedBinaryOp arm (`ProofEngine.cs:305`), `bin.Left`/`bin.Right` carry a resolvable `DeclaredQualifierMeta.Unit` with a non-null `UnitCode` for `unitPrice in 'USD/kg' × qty in 'g'` (the PriceTimesQuantity QualifierChain anchor, `Operations.cs:649`) and `kg × (1/g)` (the QuantityTimesQuantity DimensionalProduct anchor, `Operations.cs:605`) — proving the raw string is present at stamping time, not only post-projection.
2. **Add the raw source-unit capture at obligation-stamping time.** In `ProofEngine.cs` the qualifier reqs are stamped as verbatim catalog copies (`obligations.Add(new ProofObligation(req, bin, ctx, Unresolved, null, null))` at `:307`, mirrored `:314`/`:321`). Add a capture helper `TryCaptureSourceUnitText(ProofSubject subject, TypedExpression site, SemanticIndex semantics) → (string? text, bool recoverable)` that reads the operand's `DeclaredQualifierMeta.Unit.UnitCode` / `CompoundPrice.UnitCode` WITHOUT calling `TryProjectUnitToDimension`/`TryResolveDimensionVector` (the projection sites) and WITHOUT re-running `ResolveQualifierOnAxis`'s full resolution (red-team d — no ~900-line engine re-run; read the already-resolved DeclaredQualifiers off the typed operand only). Thread the captured (LeftSourceUnitText, RightSourceUnitText, SourceUnitRecoverable) onto the obligation so it survives to discharge/witness construction. For the BinaryOp qualifier reqs (QualifierCompatibility/QualifierChain/DimensionalProduct) capture BOTH operands from `bin.Left`/`bin.Right`; for AssignmentQualifier capture the single source operand. *Validate*: `precept_compile` on the PriceTimesQuantity anchor, then inspect via `precept_proofs` (or a ProofEngine unit test) that the obligation now carries `LeftSourceUnitText="USD/kg"`, `RightSourceUnitText="g"`, `SourceUnitRecoverable=true` — non-null where pre-P4 it was absent.
3. **Populate `UnitBinding` from the captured strings in the four cross-unit `Try*` discharge bodies.** The qualifier discharge functions (`TryQualifierCompatibilityProof`, the chain branch via `ChainQualifiersMatch` at `Qualifiers.cs:105`, `TryDimensionalProductProof`, `TryAssignmentQualifierProof`) construct their witnesses in-body per the P2 protocol (every `Try*` returns `ProofWitness?`). P4 populates `new UnitBinding(axis, obligation.LeftSourceUnitText, obligation.RightSourceUnitText, SourceUnitRecoverable: <captured>, UcumFactorApplied: <re-derivable>)` instead of P2's `SourceUnitRecoverable:false` stub. The `DimensionalProductWitness`/`QualifierChainWitness` carry the captured raw strings ALONGSIDE the projected DimensionVector/comparable strings (the projected values stay for the dimension-layer match; the raw strings are the only re-checkable cross-unit input). *Validate*: a `WitnessCheckerTests` fixture asserts a `QualifierChainWitness` built for `'USD/kg' × 'g'` now carries `UnitBinding{LeftSourceUnitText="USD/kg", RightSourceUnitText="g", SourceUnitRecoverable=true}` where the P2-era witness carried `SourceUnitRecoverable=false`.
4. **Flip `CrossUnitFactorPreserved` from conservative-reject-only to `UcumParser`-factor-derived reject.** P2 ships the predicate as: if `SourceUnitRecoverable=false` → reject (conservative); else (P2 stub) → reject anyway (no strings to parse). P4 implements the else-branch: re-parse `LeftSourceUnitText`/`RightSourceUnitText` with `UcumParser.Parse` (the trusted leaf, `Qualifiers.cs:90`), compute the conversion factor, and ACCEPT iff the same-dimension operands have a re-derivable factor that the engine's cancellation applied (`UcumFactorApplied`), REJECT a same-dimension-different-unit drop (kg↔g ×1000 with no factor). The predicate NEVER trusts a prover-resolved `DeclaredQualifierMeta` (red-team c) and NEVER re-runs `ResolveQualifierFromExpression` (red-team d) — `UcumParser` on the raw strings is the only input. *Validate*: `WitnessCheckerTests` — (i) `'USD/kg' × 'kg'` (same unit) accepts; (ii) `'USD/kg' × 'g'` (same dimension, different unit, no factor applied) REJECTS (the BUG-030 case, was a false `Proved` pre-P4); (iii) `SourceUnitRecoverable=false` (a derived-subexpression operand) conservatively rejects.
5. **Wire the O-6 (b) unrecoverable fallback explicitly and mutation-target the predicate.** For operands where step-1 found no recoverable raw string (derived subexpressions whose unit only exists post-resolution), step-2's capture returns `recoverable=false`, step-3 sets `SourceUnitRecoverable=false`, and step-4's predicate conservatively rejects — over-rejection, friction-safe, never false-accept. Add the `CrossUnitFactorPreserved` arm + `UcumParser` leaf to the O-10 mutation harness target (the mechanism wired in P2 step 0). *Validate*: mutation-kill — perturb the predicate (e.g. flip the factor-equality `==` to `!=`, drop the `SourceUnitRecoverable` guard so it accepts on false, or stub `UcumParser` to a constant factor) and assert a `WitnessCheckerTests` case goes red for each mutant; reverting restores green (per-arm mutation-kill as the P3 cadence requires for each new arm).
6. **Corpus regression + measured over-rejection footprint.** Run the full proof-engine corpus (`samples/*.precept` ∪ `test/Precept.Tests/ProofEngine/` fixtures) through the checker post-P4 and diff against the P2-committed corpus snapshot. Record the COUNT of obligations that move pre-P4-`Proved`→post-P4-`Unresolved` (the BUG-030 anchors that newly reject) and the count of `SourceUnitRecoverable=false` conservative-rejects (the O-6 (b) friction footprint) — a measured figure, not an asserted "acceptable." *Validate*: the BUG-030 anchor samples (the cross-unit cancellation fixtures, seeded from the `WitnessCheckerTests` F-5 corpus) now report a checker rejection / `Unresolved` where they were silently `Proved`; no UNRELATED corpus obligation regresses (same-unit price/quantity products stay `Proved`).

**Dependencies**: HARD upstream dependency: P2 (canonical dependency, soundness-build-plan P4 stub `:239` "pending P2 (UnitBinding carrier + CrossUnit predicate) and the O-6 PoC"). P2 builds (a) the `ProofWitness` DU including the `UnitBinding` shared carrier (soundness-arch Inventory `:194-195`), (b) the `ProofChecker` spine + the shared `CrossUnitFactorPreserved` predicate SPINE (soundness-arch checker inventory `:227`), shipped conservative-reject-only until P4 supplies the raw strings — P2 is the spine's sole BUILDER; P3 POPULATES/WIRES it family-wide into the qualifier/`DimensionalProduct` arms; P4 flips its else-branch (the predicate-ownership rule, parent Backstop-map), and (c) the O-6 reachability PoC (the disposable probe the O-6 entry mandates in P2, `:63`) — P4 cannot commit the capture until that PoC confirms which operand classes are (a)-recoverable. P4 ALSO depends on the O-10 mutation mechanism (wired in P2 step 0) for its step-5 per-arm mutation-kill. Cross-phase relationship to P3: P3 builds the QualifierCompatibility/QualifierChain/AssignmentQualifier/DimensionalProduct checker arms as conservative-reject-until-P4/O-6 (soundness-build-plan P3 stub `:229` "UnitBinding, conservative-reject until P4/O-6") — so P3 ships those arms rejecting, and P4 is what flips them to factor-derived acceptance. **HARD GATE (not "should"): the step-4 else-branch FLIP (the Class-C factor-derived-accept) is hard-gated on the P3 QualifierChain/DimensionalProduct/QualifierCompatibility/AssignmentQualifier arms being GREEN — planted-false-witness tests reject AND 100% mutation-kill — at the flip commit (P4 exit (e)(iv-green)), NOT merely "at-or-after those arms exist".** A flip against a still-placeholder/not-yet-mutation-killed P3 arm is a checker-first violation (a `Proved`-widening whose backstop has not passed its falsifiable bar). The emission-side capture mechanism (steps 1-2 — raw source-unit capture, no accept-widening) is independent and MAY proceed in parallel with P3; only the step-4 flip carries the hard green-arm gate. NOT a dependency of any later phase's kickoff (P4 is a leaf-ish closure; P6 does not depend on P4). Per O-1 branching: P4 is ORTHOGONAL to the O-1 (a)/(b) narrowing branch — the cross-unit cells are qualifier/dimension shapes, not the narrowing-derived IntervalContainment cell O-1 gates — so no (a)/(b) disposition split applies here (unlike P3's FlowNarrowing arm and P6's narrowing witness half, which must handle both O-1 branches).

**Exit criteria** (testable):
- (a) The BUG-030 PriceTimesQuantity anchor (`unitPrice in 'USD/kg' × qty in 'g'`) — verified via `precept_compile` + a `WitnessCheckerTests` fixture — produces a checker rejection / `Unresolved` where pre-P4 it was a (silently) `Proved` cross-unit cancellation; the QuantityTimesQuantity anchor (`kg × (1/g)`) likewise.
- (b) A same-UNIT product (`'USD/kg' × 'kg'`) still accepts (no over-rejection regression on the legitimate cancellation).
- (c) The captured obligation carries `LeftSourceUnitText`/`RightSourceUnitText`/`SourceUnitRecoverable` for the (a)-recoverable operands — asserted by a ProofEngine stamping test — and `SourceUnitRecoverable=false` on a derived-subexpression operand drives a conservative reject (the O-6 (b) path), asserted by a `WitnessCheckerTests` fixture.
- (d) The `CrossUnitFactorPreserved` predicate re-parses ONLY the raw strings via `UcumParser` and never re-runs the resolution engine — asserted structurally (the predicate body references no `ResolveQualifierFromExpression`/`TryProjectUnitToDimension`/`TryResolveDimensionVector` call; a test or analyzer guard catches a regression that re-introduces the engine into the trusted-base path).
- (e) **BLOCKING SOUNDNESS GATE (Class C — this phase CHANGES AN ACCEPT)**: (i) for the `CrossUnitFactorPreserved` arm there exists ≥1 planted false-witness fixture (a `UnitBinding` asserting a same-dimension-different-unit pair with `UcumFactorApplied=true` but no re-derivable factor) that the checker MUST reject; (ii) the arm mutation-kills 100% via the O-10 mechanism (each perturbation — factor-equality flip, `SourceUnitRecoverable`-guard drop, `UcumParser` stub — makes a test go red); (iii) a full-corpus pass shows the cross-unit cells either factor-derived-reject (recoverable) or conservative-reject (unrecoverable) — ZERO cross-unit cancellation survives as a `Proved` without a re-derived `UcumParser` factor (no false `Proved` escapes); (iv) the BUG-030 factor-derived-accept (the Class-C `Proved`-widening) ships strictly behind its GREEN backstop arms, enforced by THREE legs (a runtime-observable leg + a green-arm leg as the load-bearing pair, with commit-co-occurrence demoted to secondary): **(iv-green)** the P3 QualifierChain/DimensionalProduct/QualifierCompatibility/AssignmentQualifier arms are GREEN in the falsifiable sense at the flip commit — their planted-false-witness tests REJECT AND they mutation-kill 100% via the O-10 mechanism (not merely "exist" as conservative-reject stubs); a flip landing against a still-placeholder or not-yet-mutation-killed P3 arm FAILS this leg (mirrors P6 exit (g-i)). **(iv-runtime)** the RUNTIME-OBSERVABLE leg (mirrors P6 exit (g-ii) / P3 exit (f-ii-runtime)): assert that in the PRE-flip configuration the BUG-030 anchors (PriceTimesQuantity QualifierChain, QuantityTimesQuantity DimensionalProduct) are `Unresolved`/conservative-reject with `SourceUnitRecoverable=false`, AND in the POST-flip configuration they are factor-derived-reject (kg×g, no re-derivable factor) or factor-derived-accept (kg×kg, re-derivable) carrying a NON-NULL `UnitBinding` the checker re-verifies (`CheckLedger` empty on an accepted row, non-empty on a planted false-factor row) — so the ordering is observable directly from the obligation-ledger, not only from commit history. **(iv-commit)** a SECONDARY, NON-LOAD-BEARING commit-co-occurrence meta-test (the factor-derived-accept path co-occurs at-or-after the green-arm state) — retained as a coarse adjacency guard, explicitly NOT the soundness enforcer; (iv-green)+(iv-runtime) are. Together these make "no `Proved`-widening ships ahead of its backstop" a behavioral fact, not a git-adjacency claim.
- (f) Measured over-rejection footprint recorded: the count of corpus obligations moving `Proved`→`Unresolved` and the count of `SourceUnitRecoverable=false` conservative-rejects are reported as figures in soundness-case §1.1/§6 (F-5) and `bugs.md`, not hand-waved.

**Effort**: **M (4–6 days)**. The capture helper (steps 1-2) is a focused read of already-resolved DeclaredQualifiers off the typed operand threaded onto the obligation — bounded, but carries genuine reachability risk (which operand classes are (a)-recoverable), which is why the P2 PoC de-risks it upfront; the `UnitBinding` population (step 3) and the `CrossUnitFactorPreserved` flip (step 4) reuse the P2-built carrier + the P2-built predicate spine that P3 already wired family-wide (P4 is a thin wiring layer over P2/P3 infrastructure, not a forked definition). **Band-grounding (predicate-ownership rule): P4's ONLY predicate work is the else-branch flip — a single method body, since P2 built the spine and P3 already wired it family-wide — so the M (4–6 d) band rests on the capture-helper REACHABILITY RISK (which operand classes are O-6 (a)-recoverable), NOT on building or wiring the predicate.** The `UcumParser` leaf already exists (`Qualifiers.cs:90`); the mutation-kill (step 5) and corpus diff (step 6) reuse the P2 harness + snapshot. Band shifts toward the high end (6 d) if the P2 PoC found multiple distinct operand-resolution paths (field vs typed-constant vs arg vs CompoundPrice) each needing a separate capture branch, or toward the low end (4 d) if only the field/typed-constant anchors are recoverable and derived operands are uniformly (b)-conservative-reject.

**Doc-update obligations** (CLAUDE.md routing): CLAUDE.md routing (language-surface NOT touched — internal soundness mechanism only; no catalog/spec keyword change): (1) `docs/compiler/proof-engine.md` — update the § "Known gap — cross-unit cancellation" (`proof-engine.md:1790`) from "unsound for cross-unit operands" to "checkable: the source unit is captured upstream of the Unit→Dimension projection and the checker re-derives the UCUM factor; same-dimension-different-unit drops reject (recoverable) or conservatively reject (unrecoverable)"; record the stamping-time capture + the `UnitBinding` raw-string carrier + the `CrossUnitFactorPreserved` `UcumParser` re-derivation. (2) `docs/Working/bugs.md` — BUG-030 → Fixed-with-note (the factor-drop is now checkable and rejects on the recoverable anchor paths; the cross-unit POLICY stays deferred; record the measured over-rejection footprint from step 6 and the `SourceUnitRecoverable=false` conservative-reject fraction). (3) `docs/Working/soundness-case-2026-06-07.md` — §1.1/§6 mark F-5 closed-by-rejection on the recoverable paths (the live unsound hole both agents independently found, `:60`/`:149`), state the (b)-unrecoverable cells as conservatively-rejecting; the §4 trusted-base row for `CrossUnitFactorPreserved` + `UcumParser` (ENROLLED at P2 spine-build, NOT added here — single-event enrollment, no double-count with P3's full-surface re-run) is UPDATED to reflect the factor-derived else-branch P4 adds (the conservative-reject else-stub becomes the `UcumParser`-factor-derived accept) (BLOCKING per-phase soundness-case update). (4) `docs/runtime/runtime-api.md` + `result-types.md` — the `UnitBinding` fields surfacing on the proof model (mechanical runtime-API routing). (5) `docs/tooling/mcp.md` — `precept_proofs` DTO surfaces the captured source units. (6) CANONICAL PROMOTION (standing per-phase obligation, Definition-of-done item 9): promote P4's content into `docs/compiler/soundness-and-coverage.md` — into its cross-unit / Trusted-base sections (the source-unit capture + `UnitBinding` raw-string carrier + the `CrossUnitFactorPreserved` `UcumParser` arm + the O-6 (a)/(b) disposition); advance the relevant section Design→Incremental, and update the `docs/compiler/README.md` proof-engine cross-reference. The cross-unit POLICY remains a deferred Tier-3 consultation against `business-domain-types.md` — NOT promoted as policy here, only the checkability mechanism.

**Soundness class**: **Class C — the factor-derived-accept on recoverable same-dimension-different-unit operands is a NEW accept path (the Proved-widening); net of friction the phase NARROWS.** Stated once to avoid the self-contradiction of "widening" and "widens nothing" in one breath: the Class-C widening is the new accept path — a same-dimension-different-unit product with a re-derivable `UcumParser` factor is accepted where today the whole dimension-granularity match accepts indiscriminately. That accept is backstopped by the P2 `CrossUnitFactorPreserved` predicate + the P3 QualifierChain/DimensionalProduct/QualifierCompatibility/AssignmentQualifier arms, and lands at-or-after both (exit (e)(iv)). NET OF FRICTION the phase narrows (the dominant ship-time effect is making a previously-false `Proved` REJECT — conservative-reject until O-6). The Soundness-sequencing argument's "widens nothing on its own — it NARROWS (conservative-reject until O-6)" (soundness-build-plan `:286`) is the NET-OF-FRICTION reading, not a claim that no accept path is introduced; both directions land at-or-after their backstop. Backstop dependency: the `CrossUnitFactorPreserved` predicate + the `UnitBinding` carrier are the Class-B backstop, BUILT IN P2 (the checker foundation phase) and shipped conservative-reject-only; the P3 QualifierChain/DimensionalProduct/QualifierCompatibility/AssignmentQualifier checker arms (built conservative-reject-until-P4/O-6) are the per-shape arms P4 flips to factor-derived. So every P4 accept lands strictly at-or-after its backstop (P2 predicate + P3 arms) — no `Proved`-widening ships ahead of the checker. Until O-6 makes the source unit reachable, the cell conservatively rejects (over-rejection, friction, never false-accept), so even the un-flipped state is sound-by-rejection.

---

## Heavyweight phase block — P5: Short-circuit spec + evaluator opcode lock + notation-drift fix

**Goal**: Make the short-circuit `and`/`or`/`if` evaluation semantics — the LOW-RISK intent-recovery half of conditional totality (the BRANCH_FALSE/BRANCH_TRUE rows designed in commit `004eede1`) — EXPLICIT in the language spec and locked in the evaluator contract, so the fault-reachability guarantee P6's narrowing soundness is conditioned on is stated rather than implicit; capability-neutral (emits NO new `Proved`), so it is Class-A-ish (parallel-eligible w.r.t. the checker-first constraint) but a HARD CONTENT PREREQUISITE of P6.

**Items in scope**: Spec text only + evaluator-doc lock + notation fix — no code, no `.precept` surface (operators/precedence already exist per spec §2.1:833-835). Concrete artifacts:
1. `docs/language/precept-language-spec.md` — a new short-circuit reduction-rules paragraph added in §3A.1 Constraint Semantics (`:1851`, the "Language Semantics" home) and/or as a §0.4-adjacent execution-model note: the `A and B`/`A or B`/`not A`/`if C then T else E` left-to-right reduction rules (B evaluated ⟺ A true; B evaluated ⟺ A false), the desugaring anchor (`A and B ≡ if A then B else false`, `A or B ≡ if A then true else B`) deriving short-circuit from the already-locked §0.4.2:162 "exactly one is evaluated", and the result-invariance-under-purity argument (§0.4.6:170 — short-circuit changes only fault-reachability, not value). The §2.1 precedence table (`:833-835`) is REFERENCED, not modified.
2. `docs/runtime/evaluator.md` — resolve the `:993` pending-lock note's `BRANCH_FALSE` clause ("whether `BRANCH_FALSE` treats numeric zero as falsy or only `false`") to the STRICT-BOOLEAN lock (grounded in §3.6 `boolean,boolean → boolean` typing); lock BRANCH_TRUE symmetrically; ADD the explicit `if/then/else` lowering contract `[eval C][BRANCH_FALSE L_else][eval T][JUMP L_end][L_else: eval E][L_end:]` (verified absent — the opcode table at `:995-1011` carries NO named `if`-conditional opcode, only BRANCH_FALSE/BRANCH_TRUE/JUMP); record the compiler-narrowing ⟺ evaluator-short-circuit consistency invariant; disambiguate against Decision 7 (`:1787` "Collect All Violations, Don't Short-Circuit") which is VIOLATION-collection, NOT boolean short-circuit — they coexist.
3. `docs/runtime/evaluator.md:1006-1008` + `docs/runtime/precept-builder.md:470-472` — the `&&`/`||`/`?:` → `and`/`or`/`if` notation-drift fix on the SIX verified opcode-table PROSE rows (3 per doc). All other `&&` occurrences in both docs (evaluator.md `:641,:838,:844,:848,:953,:1250,:1344,:1354`; none other in precept-builder.md) are inside C# code fences — EXEMPT (benign C# operator).
4. `docs/compiler/soundness-and-coverage.md` — the `## Relationship to conditional totality` section (`:53`, currently "Section forthcoming") gains the short-circuit-as-stated-fault-reachability-contract framing (Design→Incremental).

**Decisions required before kickoff**: NONE new. The short-circuit `and`/`or`/`if` semantics + evaluator opcode lock are part of the conditional-totality Decisions 1/2/3/4/6/7 cluster, owner-approved 2026-06-07 (Decisions captured table; conditional-totality doc `:28-39`; build-plan P5 stub `:244` "none new (owner-approved)"). Specifically: the strict-boolean BRANCH_FALSE lock is grounded in §3.6 `boolean→boolean` typing (conditional-totality `:92`); the if-lowering `[eval C][BRANCH_FALSE L_else][eval T][JUMP L_end][L_else: eval E]` is conditional-totality Decision 6, owner-approved. P5 is a SPEC-TEXT + DOC-LOCK pass on already-locked direction — it does NOT open language surface, so the CLAUDE.md Tier-2/Tier-3 pre-design consultation gate does not re-trigger (no new keyword/operator/construct; operators + precedence already exist). The only judgement call inside P5 is the spec PLACEMENT (§3A.1 semantic home vs a §0.4-adjacent execution-model note) — a doc-organization choice, not a language decision; resolve by matching where §0.4.2's "exactly one is evaluated" already lives so short-circuit reads as its derived consequence.

**Step-by-step execution**:

1. **Spec: short-circuit reduction rules (intent-recovery prose).** In `precept-language-spec.md` add the reduction rules for `and`/`or`/`not`/`if` (the conditional-totality §"Short-circuit reduction rules" block `:138-149`) — placed in §3A.1 (`:1851`, the Language-Semantics home where guard/constraint semantics already live) with a back-reference to §0.4.2 (`:162`) and §2.1 (`:833-835`). State: left-to-right evaluation; `A and B` evaluates B ⟺ A true; `A or B` evaluates B ⟺ A false; `not A` no short-circuit; `if C then T else E` evaluates exactly one branch (the §0.4.2:162 rule). Add the desugaring anchor (`A and B ≡ if A then B else false`) framing short-circuit as the DERIVED consequence of the already-locked if/then/else single-evaluation rule, and the result-invariance-under-purity note (§0.4.6:170 — short-circuit changes only fault-reachability, not value). *Validate*: a doc-lint (or grep assertion in a doc-test) confirms §3A.1 now contains the tokens "short-circuit", "evaluated ⟺", and the desugaring `≡ if A then` — and that §2.1's precedence rows (`or` 10, `and` 20, `not` 25) are UNCHANGED (diff touches no precedence row); `precept_compile` on a probe `rule UnitCount != 0 and TotalCost / UnitCount <= 1000.00` still emits PRE0083 (P5 changes NO compiler behavior — the spec text is descriptive of evaluation, the narrowing that discharges it is P6; a behavior change here would mean P5 leaked P6's prover).
2. **Evaluator: resolve the `:993` BRANCH_FALSE/BRANCH_TRUE strict-boolean lock.** Edit the `:993` "Pending implementation" note: strike the `BRANCH_FALSE`-falsiness clause from the pending list and replace with a LOCKED statement — `BRANCH_FALSE`/`BRANCH_TRUE` consume a strict `boolean` (not numeric-zero-as-falsy), grounded in §3.6 `boolean,boolean → boolean` typing (the operands of `and`/`or` are boolean-typed, so a numeric falsy coercion is unreachable). Leave the other three pending items (LOAD_ARG absent/null, RETURN fall-through, stack pooling) untouched — they are out of P5 scope. *Validate*: grep `evaluator.md:993`-region for "strict boolean" / absence of "treats numeric zero as falsy ... `BRANCH_FALSE`" in the PENDING list; a doc-lint asserts the BRANCH_FALSE clause moved from pending → locked. No code probe (the evaluator is a 161-line/5×NotImplementedException stub — verified; nothing to run).
3. **Evaluator: lock the if/then/else LOWERING contract (verified-absent — net-new doc text).** Add to the opcode-semantics section the lowering `if C then T else E` → `[eval C][BRANCH_FALSE L_else][eval T][JUMP L_end][L_else: eval E][L_end:]` (conditional-totality Decision 6). This is required because the opcode table (`:995-1011`) carries NO named `if`-conditional opcode — only BRANCH_FALSE/BRANCH_TRUE (`&&`/`||`) + JUMP (`?:`) — so the `and`/`or` BRANCH lock alone does not pin `if`. *Validate*: doc-lint asserts the lowering sequence string `[eval C][BRANCH_FALSE` is present in evaluator.md; cross-check the lowering uses ONLY opcodes that exist in the `:995-1011` table (BRANCH_FALSE/JUMP) — no invented opcode.
4. **Evaluator: record the compiler-narrowing ⟺ evaluator-short-circuit consistency invariant + disambiguate Decision 7.** Add a short invariant note: the proof engine's P6 narrowing (B reasoned under facts⁺(A)) is sound ⟺ the evaluator short-circuits (B reached only when A true); a divergence is a P11 guarantee break (conditional-totality Philosophy row 11 `:60`). Add one sentence distinguishing this from Decision 7 (`:1787`): Decision 7 ("don't short-circuit") governs VIOLATION collection (collect-all rules/ensures), an orthogonal axis from boolean-connective operand short-circuit — both hold simultaneously. *Validate*: doc-lint asserts the invariant note AND the Decision-7 disambiguation sentence are present; a reviewer-grep for "⟺" near "short-circuit" returns the consistency-invariant text. (The MECHANICAL differential enforcement of this invariant is P7's skeleton-against-stub, blocked on the evaluator — P5 only RECORDS the invariant; cite that forward-dependency, do not claim it enforced.)
5. **Notation-drift fix — the six verified prose rows ONLY.** Edit `evaluator.md:1006-1008` (`Short-circuit for \`&&\`` → `\`and\``; `\`||\`` → `\`or\``; `Unconditional (for \`?:\`)` → `(for \`if\`)`) and `precept-builder.md:470-472` (same three rows). Leave ALL C#-fence `&&` untouched (evaluator.md `:641,:838,:844,:848,:953,:1250,:1344,:1354` are C# `if (...&&...)` — exempt). *Validate*: `grep -n '&&\|||\|?:' docs/runtime/evaluator.md docs/runtime/precept-builder.md` returns ONLY C#-fence lines afterward (the six opcode-table prose rows are gone); a doc-lint asserts the opcode-table rows for BRANCH_FALSE/BRANCH_TRUE/JUMP name `and`/`or`/`if`, not `&&`/`||`/`?:`, in BOTH docs.
6. **Promote into the canonical home.** Fill `soundness-and-coverage.md` `## Relationship to conditional totality` (`:53`, "Section forthcoming") with the short-circuit-is-the-stated-fault-reachability-contract framing + a cross-link to the spec §3A.1 short-circuit prose and the evaluator lock; advance that section Design→Incremental. *Validate*: doc-lint asserts the section is no longer "Section forthcoming" and cross-links resolve (the referenced spec §3A.1 anchor and evaluator opcode-lock anchor exist).

**Dependencies**: NO soundness/`Proved`-backstop dependency blocks P5 kickoff — P5 is order-independent W.R.T. THE CHECKER-FIRST CONSTRAINT (it emits no `Proved`, so its placement cannot violate constraint-1; build-plan P5 stub `:246`, Soundness-sequencing `:289`). It MAY freely interleave with P1/P2 (parallel-eligible). **ONE soft TOOLING prerequisite, not a soundness edge:** exit (g-i)'s capability-neutrality gate CONSUMES the obligation-LEDGER-SNAPSHOT harness — the generic pre/post `(site, kind, disposition, strategy)` snapshot mechanism the parent plan's P1 step-10b stands up (build-plan `:142`). **Per the parent plan, P1 step-10b is the canonical OWNER of this harness** (it builds it there as P1's `pre-p1-obligation-ledger.txt` regression oracle); P5 is a soft CONSUMER of it, not a co-owner. The harness is generic enough to reuse, so the operational rule is FIRST-NEEDER-BUILDS-UNDER-THE-CANONICAL-HOME: P5 reuses P1's instance when P1 has landed; **if P5 is scheduled before P1, P5 may stand up the same harness early so its gate is runnable, and P1 then reuses it** — but the canonical home and unconditional owner remain P1 step-10b, not a symmetric either-owns. **The expansion does NOT assert a new P1↔P5 tooling-ownership relationship the parent plan lacks; if the owner wants the harness re-labelled phase-agnostic tooling in the parent's P1 step-10b, that reconciliation belongs UP in the parent plan (a surfaced cross-phase question), not originated here.** Either way no `Proved`/checker dependency exists. BUT P5 is a HARD CONTENT PREREQUISITE of P6 (a hard dependency EDGE, not prose): P6's narrowing soundness is conditioned on the short-circuit semantics P5 locks (build-plan P6 Dependencies `:252` "AND P5 (a CONTENT prerequisite — P6's narrowing soundness is conditioned on the short-circuit semantics P5 locks; this is a hard scheduling edge)"). A scheduler MUST NOT read "no soundness urgency / order-independent" as "deferrable past P6" — P5 must land before/with P6 (P5 stub `:246` spells out this exact non-tension). P5 does NOT depend on P1/P2/P3/P4 for any `Proved`/checker output (the checker spine is irrelevant — P5 ships no witness, no `Proved`); the only P1-shared artifact is the reusable ledger-snapshot tooling above. Do not invent a P2 dependency: the canonical graph has P5 standalone-buildable.

**Exit criteria** (testable): All runnable (doc-lints / greps / one negative-behavior probe), never "phase complete":
- (a) Spec §3A.1 contains the short-circuit reduction rules + the desugaring anchor (`≡ if A then`) + the result-invariance-under-purity note — asserted by a doc-test grepping the section; §2.1 precedence rows (`or` 10 / `and` 20 / `not` 25) are byte-unchanged (diff over `:833-835` is empty).
- (b) evaluator.md `:993` pending list NO LONGER contains the BRANCH_FALSE-falsiness clause; a LOCKED strict-boolean statement for BRANCH_FALSE/BRANCH_TRUE exists — doc-lint asserts both (removed-from-pending AND present-as-locked).
- (c) evaluator.md contains the if/then/else lowering `[eval C][BRANCH_FALSE L_else][eval T][JUMP L_end][L_else: eval E]` using only opcodes present in the `:995-1011` table — doc-lint asserts the string present + no invented opcode token.
- (d) The compiler-narrowing ⟺ evaluator-short-circuit consistency invariant is recorded AND disambiguated from Decision 7 — doc-lint asserts both sentences present.
- (e) **Notation-drift fully fixed**: `grep -n '&&\|||\|?:' docs/runtime/evaluator.md docs/runtime/precept-builder.md` returns ZERO opcode-table-PROSE hits (only C#-fence lines remain); the six rows name `and`/`or`/`if` in both docs.
- (f) `soundness-and-coverage.md` `## Relationship to conditional totality` is filled (Design→Incremental), no longer "Section forthcoming", cross-links resolve.
- (g) **BLOCKING SOUNDNESS GATE — CAPABILITY-NEUTRALITY (the Class-A invariant for a spec/doc phase): P5 introduces ZERO new `Proved` and ZERO compiler-behavior change.** Two legs, both required: **(g-i)** run the full proof-engine corpus (`samples/*.precept` ∪ `test/Precept.Tests/ProofEngine/` fixtures) through the obligation ledger BEFORE and AFTER the P5 doc edits and assert the per-obligation `(site, kind, disposition, strategy)` ledger is BYTE-IDENTICAL — P5 touches no `src/Precept` code, so any ledger delta means a leak (a code edit smuggled into a doc phase, or P6's narrowing prematurely shipped). This CONSUMES the obligation-ledger-snapshot harness P1 step-10b canonically owns (build-plan `:142`); if P5 is scheduled before P1, P5 may stand up the same harness early (first-needer-builds under the canonical P1 home — see Dependencies), so this gate is runnable regardless of P5's position relative to P1. The pre/post snapshots must be equal. **(g-ii)** `precept_compile` on the headline probe `rule UnitCount != 0 and TotalCost / UnitCount <= 1000.00` STILL emits PRE0083 / divisor `Unresolved` (the conditional-totality flip is P6's job, gated behind the executor; if P5 makes this discharge, P5 has leaked P6's prover and the gate FAILS). This gate is what licenses calling P5 "Class-A-ish / parallel-eligible": it mechanically proves P5 emits no `Proved` and changes no verdict, so it cannot violate the checker-first constraint regardless of scheduling.

**Effort**: **S (1–3 days)**. Spec paragraph + two evaluator-doc locks (BRANCH strict-boolean resolution of `:993` + the net-new if-lowering contract) + the consistency-invariant note + the six-row notation fix across two docs + the one canonical-promotion section. All doc text on already-owner-approved direction; the only net-new authoring is the if/then/else lowering contract (verified absent) and the consistency-invariant prose. No code, no tests beyond the doc-lints and the capability-neutrality snapshot probe (which CONSUMES the P1-step-10b-owned obligation-ledger-snapshot harness — P5 reuses P1's instance, or stands up the same harness early under the canonical P1 home if scheduled before P1; see Dependencies). Lower bound of the trusted-base LOC budget — this phase adds 0 LOC to the trusted base (it is the one phase in the plan that ships only spec/doc text).

**Doc-update obligations** (CLAUDE.md routing): Per CLAUDE.md routing (Language surface → spec + type docs; Runtime → evaluator.md/precept-builder.md; status-change → cross-referencing tables):
- `docs/language/precept-language-spec.md` §3A.1 (`:1851`) — the short-circuit reduction-rules paragraph + desugaring anchor + result-invariance note; back-reference §0.4.2 (`:162`), §0.4.6 (`:170`), §2.1 (`:833-835`). (Language-semantics prose only — no new keyword/operator/construct, so no catalog entry precedes it; the operators already exist.)
- `docs/runtime/evaluator.md` — resolve the `:993` BRANCH_FALSE/BRANCH_TRUE strict-boolean lock; ADD the if/then/else lowering contract; record the compiler-narrowing ⟺ evaluator-short-circuit consistency invariant + the Decision-7 (`:1787`) disambiguation; fix the `:1006-1008` notation-drift prose rows.
- `docs/runtime/precept-builder.md:470-472` — the same three opcode-table notation-drift rows.
- **CANONICAL PROMOTION (standing per-phase obligation, Definition-of-done item 9): `docs/compiler/soundness-and-coverage.md` `## Relationship to conditional totality` (`:53`)** — promote the short-circuit-as-stated-fault-reachability-contract framing in; advance Design→Incremental. Per build-plan Definition-of-done item 9, the conditional-totality pieces promote into `precept-language-spec.md` §0.4/§3A + `proof-engine.md` + `evaluator.md` — P5 delivers the §3A + evaluator legs of that promotion (the §0.4-adjacency back-reference + the proof-engine.md narrowing leg are P6's).
- `docs/compiler/README.md` — if the soundness-and-coverage maturity row references conditional-totality, note the §-Relationship section advanced off "forthcoming" (mechanical CLAUDE.md status-change cross-table routing; Discovered-during-planning build-plan `:299`).
- NO `docs/Working/bugs.md` touch (P5 closes no BUG — it ships no `Proved` and fixes no false-clean). NO `soundness-case-2026-06-07.md` §2/§4 row (P5 adds no verdict to the soundness theorem; it states the fault-reachability CONTRACT P6's §2 rows will later rest on — a forward-reference, not a filled row).

**Soundness class**: **Class A (emission/no-`Proved`) — strictly, "Class-A-ish": capability-NEUTRAL.** P5 emits NO `Proved` and changes NO verdict (exit gate (g) proves it: the obligation ledger is byte-identical pre/post, and the headline probe still rejects). It is spec text + evaluator-doc lock + notation fix — not even an emission ADD (unlike P1's Class-A obligation adds), so it sits BELOW Class A on risk: it cannot move any site's disposition at all. **Backstop dependency: NONE required FOR P5 ITSELF** — the checker-first constraint (constraint-1: no `Proved`-emitting capability ships before its checker/demote backstop) is vacuous for P5 because P5 ships no `Proved`. P5 therefore needs no earlier phase's output as a soundness backstop (it does not depend on P2's `ProofChecker` spine). **What P5 IS, in the sequencing argument (build-plan Soundness-sequencing `:289`): the BACKSTOP-PROVIDER for P6, not a backstop-consumer.** P5 locks the short-circuit fault-reachability semantics that P6's Class-C narrowing `Proved` is *conditioned on* — without P5's stated invariant, P6's "B is safe because A dominates it" reasoning would rest on an unstated evaluator behavior. So P5 supplies a CONTENT prerequisite into P6's Class-C soundness, even though P5 itself carries no soundness risk. The build-plan Class-C entry for P6 (`:287`) names this: P6's soundness is load-bearing on the short-circuit semantics, and P5 is where they are locked. Earlier phase that "built" P5's substance: commit `004eede1` designed the BRANCH_FALSE/BRANCH_TRUE rows P5 recovers-and-renames; no proof-engine phase precedes P5 (it is standalone-buildable).

---

## Heavyweight phase block — P6: Conditional-totality narrowing (proof-engine half) — AssumedConditions / EffectiveGuard / ReduceFacts

**Goal**: Widen what compiles to `Proved` to cover dominating intra-expression conditions — make `rule Y != 0 and X / Y > 0` and `rule (if Y != 0 then X / Y else 1) > 0` discharge their divisor obligation at the proof-engine disposition level by threading a single-pass occurrence-typing-style `AssumedConditions` context (pre-reduced `ReducedFactSet`, O-B locked) through `WalkExpression`, substituting one `EffectiveGuard` helper at the six guard-reading discharge sites, and computing both polarities through the net-new `ReduceFacts` de-Morgan reducer with forfeit-on-disjunction — a Class C (`Proved`-widening) capability that lands at-or-after its P3 GuardInPath/FlowNarrowing/interval-dominance checker arms (the demote backstop does NOT cover the guard-fact witness family, so P6-before-P3 conservatively REJECTS rather than demotes) and conditioned on the P5 short-circuit spec.

**Items in scope**: FILE-LEVEL artifacts: (1) `src/Precept/Pipeline/ProofLedger.cs` — `ProofObligation` (`:24-32`) gains an IsDefault-safe `AssumedConditions` field carrying a pre-reduced `ReducedFactSet` (the disjunctive `ImmutableArray<ImmutableArray<GuardConstraint>>` + parallel `ImmutableArray<ImmutableArray<FieldToFieldConstraint>>` branch shape with a distinguished FORFEIT `[[]]` sentinel — O-B locked), empty outside an `and`/`or`/`if`, mirroring the three existing staleness sets (`:34-82`); NO new `ProofStrategy` member (enum stays 11, `:100-114`). (2) `src/Precept/Pipeline/ProofEngine.cs` — `WalkExpression` (`:275-365`) gains a Γ parameter threaded alongside `ctx`: the `TypedBinaryOp` And/Or arms (`:305-310`) refine the RIGHT operand only (left under bare Γ); the `TypedConditional` arm (`:329-333`) refines Then under facts⁺(C) / Else under facts⁻(C) (condition under bare Γ); each obligation stamped `AssumedConditions = Γ` at its site. One `EffectiveGuard(obligation, semantics)` helper. (3) `src/Precept/Pipeline/ProofEngine.Strategies.cs` — net-new `ReduceFacts(condition, polarity) -> ReducedFactSet` (polarity-parameterized de-Morgan recursion over and/or/not + forfeit-on-disjunction, leaf arms REUSING `ExtractGuardLeafConstraints`/`ExtractFieldToFieldLeaf`/`NegateOp` (`:1057`)/`InvertOp` (`:1046`) verbatim); `TryGuardInPathProof` (def `:713`; four-arm guard switch `:723-735`) and `TryFlowNarrowingProof` (def `:1078`; four-arm guard switch `:1109-1121`, both-operand `ReassignedBefore` gate `:1105-1106`) and `TryKeyPresenceProof` (def `:668`; three-arm guard switch `:680-686`) and `TryIndexBoundsProof` (def `:390`; three-arm guard switch `:428-434`) read `EffectiveGuard(obligation)` instead of the raw `obligation.Context switch` (EffectiveGuard ALSO repairs the missing ConstraintContext arm on the two three-arm sites). (4) `src/Precept/Pipeline/ProofEngine.Intervals.cs` — `BuildNarrowedIntervals` (`:519-529`, three-arm) reads `EffectiveGuard` (repairs ConstraintContext); `IntervalOfNarrowed` TypedConditional case (`:135-139`) refines per-branch by `ReduceFacts(cond.Condition, ±)` (via `NarrowByConstraint`/`NegateConstraintToInterval`) BEFORE the `.Union`. (5) WITNESS leg (under O-1 branch, see soundnessClass): an additive `(node, branch-polarity)` dominance/polarity provenance leg on the P3-built `GuardInPathWitness`/`FlowNarrowingWitness` AND the interval-containment witness (Decision 5) — ships WITH this design, NOT gated on O-1; the all-contributor capture rides O-1/O-E. (6) Tests: `test/Precept.Tests/ProofEngine/ConditionalTotalityTests.cs` (AC 1–12 + ReduceFacts unit tests), `ConditionalTotalityRegressionTests.cs` (AC-12 full-corpus empty-Γ diff), and the AC-13/13b witness-checker extension (in the soundness-arch test file, requires the P2 DU). NOT touched: `QualifierNarrowing.cs` (O-A scoped OUT); `TypedQuantifier` predicate (DEFERRED); `X is not set` IsNegated-postfix (DEFERRED forfeit-for-now, AC-6d).

**Decisions required before kickoff**: O-C (intra-expression relational narrowing depth) — must be DISPOSED in P6: lean (a) STAYS SINGLE-HOP, stated explicitly in P6, consistent with the existing depth-1 bound the relational `FlowNarrowing` tier already carries (`proof-engine.md` Strategy 4 / §0.6; per-field interval facts via `Intersect` are single-pass-cheap regardless of depth, but field-op-field relational facts do NOT chain past one hop). This is the one Open decision P6 owns. INHERITED (not re-litigated, disposed upstream): O-1 (from P2 — branches this phase's witness half, see dependencies); O-E (the inherited O-1 gate on the witness half per conditional-totality O-E); O-B (LOCKED — `AssumedConditions` is pre-reduced `ReducedFactSet`, never raw `TypedExpression`); O-A (DECIDED — qualifier/dimension narrowing scoped OUT); O-D (DECIDED — two-derivations-one-extractor `ReduceFacts`, checker-verified agreement). No NEW language-surface decision (owner-approved 2026-06-07 per Decisions-captured; proof-engine half locked-in-direction).

**Step-by-step execution**:

- **Step 0 — GATE CHECK (hard pre-step)**: confirm ALL THREE P3 arms P6 routes through are green (planted-false-witness rejects + 100% mutation-kill) and P5's short-circuit spec is committed to HEAD, BEFORE any discharge-to-`Proved` un-gating. The gate is necessary-AND-sufficient ONLY if it checks every arm P6's discharge surface routes through — not just `GuardInPath` (the `!=` then-branch route): P6 also discharges via the `FlowNarrowing` arm AND the interval-dominance/interval-containment arm (the `if Y > 0 then X/Y` dict route, exit (b)), so a config where `GuardInPath` is green but `FlowNarrowing` or the interval-dominance arm is absent/reverted would pass a `GuardInPath`-only grep yet let P6's interval-dict route discharge through an unbacked path. *Validate*: assert ALL of — `grep -n 'GuardInPathWitness' src/Precept/Pipeline/ProofChecker.cs` returns a live accepting arm AND its P3 mutation-kill artifact exists; `grep -n 'FlowNarrowingWitness' src/Precept/Pipeline/ProofChecker.cs` returns a live accepting arm (under O-1 (a)) OR the wired bare-bounds demote for its interval route (under O-1 (b)) AND its P3 mutation-kill artifact exists; the interval-containment-with-dominance arm (the **P3-built online `IntervalContainmentWitness` accepting arm** under O-1 (a) — assigned to P3 items-in-scope as the named Tier-A-leftover owning that arm, so this grep matches a live arm rather than vacuously blocking P6's interval-dict route; or the wired bare-bounds demote under O-1 (b)) is live AND mutation-killed — mirroring exit (g-i)'s "the P3 arms" (plural). Under O-1 (a) the grep target `IntervalContainmentWitness` resolves to the P3-owned arm (single named arm, single owning phase); under O-1 (b) the route demotes and step-0 instead confirms the P2-wired bare-bounds demote has a live caller. PLUS `grep -n 'short-circuit\|A and B ≡ if A then B else false' docs/language/precept-language-spec.md` (committed HEAD, not working tree — spec-first-against-HEAD discipline) returns the locked desugaring. If ANY of the three arms (or, under O-1 (b), its wired demote) is absent, OR the spec is absent, P6 ships PROVER-ONLY with discharge conservatively REJECTED (see exit (g)).
- **Step 1 — `ProofObligation.AssumedConditions` field** (`ProofLedger.cs:24-32`, mirroring the IsDefault-safe staleness-set pattern at `:34-82`). Define the `ReducedFactSet` type (disjunctive `ImmutableArray<ImmutableArray<GuardConstraint>>` + parallel relational array + FORFEIT `[[]]` sentinel). *Validate*: a unit test constructs a `ProofObligation` with default `AssumedConditions`, reads it back as the empty/FORFEIT sentinel (no `IsDefault` throw), and a full-corpus `precept_compile` over `samples/*.precept` shows zero disposition change (the field is inert until threaded) — confirms the carrier is additive-neutral.
- **Step 2 — net-new `ReduceFacts(condition, polarity)`** in `ProofEngine.Strategies.cs` alongside the existing extractors (`ExtractGuardBranchesCore` `:823`). Positive recursion: and→`⊕` (CombineAndBranches cross-product `:807-821`), or→`⊔` (branch-set concat, the Or arm `:829-833`), not→flip-polarity-recurse, leaf→`ExtractGuardLeafConstraints`/`ExtractFieldToFieldLeaf` verbatim. Negative recursion (de Morgan): and→`⊔`-then-FORFEIT-unless-all-disjuncts-constrain-the-target, or→`⊕`, not→flip, leaf→`NegateOp`/`InvertOp`; presence/`X is not set`/`contains`→∅ FORFEIT. *Validate*: a `ReduceFacts` unit-test matrix asserts `ReduceFacts((Y>0) and W, Negative)` returns the FORFEIT sentinel `[[]]` (AC-4d numeric + AC-7c relational), `ReduceFacts(Y>0 or Y<0, Positive)` returns the genuine two-branch `[[Y>0],[Y<0]]` (AC-9b, NOT a flat conjunction), `ReduceFacts(not(Y==0), Positive)` = `ReduceFacts(Y==0, Negative)` = `{Y NotEquals 0}` (AC-5a), `ReduceFacts(X is set, Negative)` = ∅ (AC-6b) — tested DIRECTLY, not only through discharge.
- **Step 3 — `WalkExpression` Γ-threading** (`ProofEngine.cs:275-365`). And/Or arms (`:305-310`): walk left under bare Γ, right under `Γ ⊕ ReduceFacts(left, Positive/Negative)`. Conditional arm (`:329-333`): condition under bare Γ, Then under `Γ ⊕ ReduceFacts(C,+)`, Else under `Γ ⊕ ReduceFacts(C,−)`. Stamp every obligation `AssumedConditions = Γ`. NON-gated positions (left operand, condition) keep bare Γ. *Validate*: `precept_compile` on `rule X / Y > 0 and Y != 0` (divisor on the LEFT) STILL emits PRE0083 / divisor `Unresolved` (AC-10, Falsifier 6 — the left operand is non-gated, must NOT narrow; verified Unresolved at baseline so this is a genuine must-still-reject tripwire). A snapshot meta-test asserts the obligation inside `A` of `A and B` carries empty `AssumedConditions`.
- **Step 4 — one `EffectiveGuard(obligation, semantics)` helper** in `ProofEngine.cs` = `CombineAndBranches`-cross-product the context-guard branch set with the `AssumedConditions` branch set, AFTER dropping each `AssumedConditions` fact whose field is in `obligation.ReassignedBefore` (the per-field staleness drop-filter, applied AT this new fold site, NOT inherited). The helper itself reads the four-arm ConstraintContext guard for every site. *Validate*: an action-chain fixture that does `set Y = 0` then narrows by an old `Y != 0` over the same field STILL rejects, and the test asserts the drop happened in `EffectiveGuard` (AC-11, Falsifier 7) — verifying the filter is at the fold site.
- **Step 5 — re-route the six guard-reading sites through `EffectiveGuard`** (each cited at its in-body guard switch; function defs are `TryGuardInPathProof:713` / `TryFlowNarrowingProof:1078` / `TryKeyPresenceProof:668` / `TryIndexBoundsProof:390`): `TryGuardInPathProof` (guard switch `Strategies.cs:723`), `TryFlowNarrowingProof` (`:1109`), `TryKeyPresenceProof` (`:680`), `TryIndexBoundsProof` (`:428`), `BuildNarrowedIntervals` (`Intervals.cs:519`); the presence arm of `TryGuardInPathProof` (`:777-781`) rides for free. Re-run `grep -n 'obligation.Context switch' ProofEngine.Strategies.cs ProofEngine.Intervals.cs` to confirm NO seventh site appears. *Validate*: `precept_compile` on the AC-9 two-fact index-bounds band `rule Items.at(A) > 0 when A >= 0 and A < Items.count` flips IndexBounds Unresolved→Proved (the ConstraintContext repair the three-arm `TryIndexBoundsProof` lacked), while floor-alone (`when A >= 0`) and ceiling-alone (`when A < Items.count`) each STAY Unresolved — confirming the AND-composition by Intersect is load-bearing, not single-fact discharge.
- **Step 6 — `IntervalOfNarrowed` per-branch refinement** (`Intervals.cs:135-139`): refine the per-branch `narrowed` dict by `ReduceFacts(cond.Condition, ±)` (translated via `NarrowByConstraint`/`NegateConstraintToInterval`) BEFORE the `.Union`. Note the `!=` then-branch is a dict fail-to-narrow (`NegateConstraintToInterval(Equals)=null` `:819`; `NarrowByConstraint(NotEquals)=current` `Satisfiability.cs:256`) — it discharges via the GuardConstraint path, not the dict. *Validate*: `precept_compile` on probe (b) `rule (if Y != 0 then X / Y else 1) > 0` flips divisor Unresolved→Proved with credited strategy GuardInPath (AC-2 — verified PRE0083 at baseline); AND the representable half-line `rule (if Y > 0 then X / Y else 1) > 0` flips via the dict route. The headline probe (a) `rule Y != 0 and X / Y > 0` flips Unresolved→Proved/GuardInPath (AC-1 — verified PRE0083 at baseline).
- **Step 7 — O-C disposition**: state in `proof-engine.md` + `soundness-and-coverage.md` that intra-expression RELATIONAL (field-op-field) narrowing stays SINGLE-HOP (consistent with the existing Strategy 4 depth-1 bound); per-field interval facts (`Intersect`) compose at any depth. *Validate*: a nested fixture `rule A >= B and B >= C and sqrt(A - C) >= 0` does NOT discharge the two-hop relation (stays Unresolved), pinning the single-hop bound; the single-hop case `rule A >= B and sqrt(A - B) >= 0` DOES discharge (AC-7a).
- **Step 8 — witness dominance/polarity leg** (Decision 5): `WalkExpression`/`IntervalOfNarrowed` POPULATE the ordered `(node, branch-polarity)` path + per-step facts into the dominance/polarity provenance slot the P3-built `GuardInPathWitness`/`FlowNarrowingWitness` (and the interval-containment witness) RESERVED empty/`None` at P3-build time (P3 items-in-scope). P6 EXTENDS the witness shape via that reserved slot — it does NOT mutate the P3 shape, so no P3 checker arm is silently re-opened. The checker re-walks the typed tree re-deriving each step via the SAME `ReduceFacts(stepCondition, polarity)`. This leg ships WITH P6 (NOT gated on O-1 — it needs no interval-contributor capture). **The dominance-re-walk code path P6 adds is net-new trusted-checker surface and is 100%-mutation-killed at exit (g-iv)** — it is not covered by P3's mutation-kill of the base arm (which ran before the leg existed). *Validate*: AC-13 — a planted witness citing a non-gated left operand (or facts⁺ on the else-branch) is REJECTED by the dominance re-walk on BOTH routes (a GuardConstraint-discharged divisor AND an interval-dict half-line then-branch); AC-13b — a planted witness whose stamped fact disagrees with `ReduceFacts(cond.Condition, polarity)` is REJECTED (same-extractor agreement). These require the P2 `ProofChecker`/`ProofWitness` DU (O-E).
- **Step 9 — full-corpus regression-neutrality.** *Validate*: `ConditionalTotalityRegressionTests` — a full-corpus proof-disposition diff over `samples/*.precept` ∪ proof-engine fixtures asserts every obligation OUTSIDE an `and`/`or`/`if` has empty `AssumedConditions` and IDENTICAL disposition before/after; NO obligation flips Proved→Unresolved (AC-12, Falsifier 8).

**Dependencies**: HARD content edges (canonical, per the P6 stub `:248-255` and the Soundness-sequencing argument `:283-291` — do NOT invent): (1) **P2** (DU + `ProofChecker` spine + wired bare-bounds demote backstop) — P6 is a `Proved`-widening capability and constraint-1 forbids it shipping ahead of its backstop. (2) **P3** (the GuardInPath/FlowNarrowing/interval-dominance DISCHARGE arms) — a CONTENT prerequisite for P6's discharge-to-`Proved`, NOT mere order-urgency: P6's `!=` then-branch `Proved` routes through the `GuardConstraint` path to a `GuardInPathWitness` (Decision 5), NOT the interval-dict path, and the P2 demote-to-bare-bounds backstop covers ONLY interval-containment (the bare numeric path) — it does NOT cover the guard-fact-derived `GuardInPath`/`FlowNarrowing` witness family. So a P6-before-P3 schedule does NOT demote the `!=` GuardInPath cases — it conservatively REJECTS them (P6's headline capability universally over-rejected until P3). (3) **P5** (the short-circuit spec §0.4/§2.1 + evaluator opcode lock + the locked `if/then/else` lowering) — a CONTENT prerequisite: P6's narrowing soundness is conditioned on the short-circuit semantics P5 locks; a scheduler treating P5 as "low-urgency / order-independent" must NOT silently defer it past P6 (P5 is order-independent only w.r.t. the checker-first constraint, per the P5 stub `:246` and Soundness-sequencing `:289`). O-1 BRANCHING (P2 disposes it; flows here via O-E): under O-1 **(a)** online certificate — P6's interval-dict route (`if Y > 0 then X/Y`) discharges through the **P3-built online `IntervalContainmentWitness` accepting arm** (strategy `IntervalContainment`, `ProofEngine.cs:1088`; P3 items-in-scope owns building it), and the relational FlowNarrowing route through the P3 `FlowNarrowingWitness` all-contributor arm — two distinct P3-owned arms, NOT one FlowNarrowing arm covering both; under O-1 **(b)** demote-to-bare-bounds — P6's narrowing WITNESS half ships DEMOTE-ONLY (no online certificate; narrowing-derived interval-containment Proved demotes to Unresolved on the P2-wired bare-bounds path, and the relational FlowNarrowing route conservatively rejects per P3 exit (e)(e')). BOTH branches: the `(node, polarity)` DOMINANCE leg (step 8) ships regardless — it is a hard precondition of THIS design, NOT gated on O-1 (O-1 gates only the all-contributor interval-contributor capture, per Decision 5 / O-E). The GuardConstraint-route `!=`/equality discharges are unaffected by the O-1 branch either way (they route GuardInPath, re-checked by the P3 arm + dominance re-walk).

**Exit criteria** (testable):
- (a) `precept_compile` on probe (a) `rule Y != 0 and X / Y > 0` → divisor **Proved/GuardInPath** (was Unresolved/PRE0083 — verified baseline) — disposition-level assertion (AC-1).
- (b) `precept_compile` on probe (b) `rule (if Y != 0 then X / Y else 1) > 0` → divisor **Proved/GuardInPath** (was PRE0083), credited strategy asserted GuardInPath not interval-dict (AC-2).
- (c) Cross-clause `rule X / Y > 0 when Y != 0` STAYS Proved/GuardInPath (the `AssumedConditions=∅` special case — verified baseline Proved, no regression, AC-3).
- (d) The full OR/AND/NOT polarity grid passes: or-reject `Y != 0 or X / Y > 0` STILL rejects (AC-4a); compound-left de-Morgan tripwires `(Y > 0 and W) or (X / Y > 0)` (AC-4d) and `(X >= Y and W) or sqrt(X - Y) >= 0` (AC-7c) STILL reject; positive-disjunctive `(Y > 0 or Y < 0) and X / Y > 0` → Proved (was Unresolved — verified baseline, AC-9b) while `(Y > 0 or W) and X / Y > 0` STILL rejects (AC-9c); presence/key positive-only (AC-6b/6c/6d/8b reject).
- (e) Left-operand-fault `rule X / Y > 0 and Y != 0` STILL rejects (AC-10 — verified baseline Unresolved).
- (f) Regression-neutrality: full-corpus diff shows every obligation outside an `and`/`or`/`if` has empty `AssumedConditions` + identical disposition, NO Proved→Unresolved flip (AC-12).
- (g) **BLOCKING SOUNDNESS GATE (Class C — the checker-first + demote-does-not-cover-guard-facts constraint):** P6's discharge-to-`Proved` is GATED on the P3 GuardInPath/FlowNarrowing/interval-dominance arms being green. A meta-test asserts: (g-i) the P6 discharge-un-gating diff is ordered strictly AFTER the P3 arms' planted-false-witness tests reject AND mutation-kill 100% (co-occur in the same or a later commit); (g-ii) WITHOUT P3 (P2+P5 present, P3 absent) P6 ships PROVER-ONLY — `ReduceFacts`/`EffectiveGuard`/`AssumedConditions` + the dominance leg land, but probe (a)/(b) discharge is conservatively REJECTED (Unresolved/PRE0083, friction-safe — NOT demoted, because the demote backstop does not cover the guard-fact witness family); a test asserts probe (a)/(b) stay Unresolved in that configuration; (g-iii) the AC-13/13b planted-false-witness tests REJECT on both discharge routes (dominance re-walk + same-extractor agreement) once the P2 DU is present. (g-iv) **the dominance/polarity leg P6 POPULATES on the P3-built `GuardInPathWitness`/`FlowNarrowingWitness` (and the interval-containment witness) is itself TRUSTED-CHECKER surface and is 100%-MUTATION-KILLED over its own code path via the O-10 mechanism** — mutating the dominance re-walk (drop a `(node, branch-polarity)` step, flip a step polarity, skip a `ReduceFacts(stepCondition, polarity)` re-derivation) makes ≥1 AC-13/13b test go red; a surviving mutant FAILS. Because P6 EXTENDS the P3 witness shapes via their reserved dominance/polarity slot (P3 items-in-scope) rather than mutating them, P6 does NOT silently re-open a P3 checker arm — but the leg P6 ADDS is net-new trusted surface, so this gate re-mutation-kills the dominance-re-walk path specifically (not just the P3 base arm, which P3 already mutation-killed). Under O-1 (b): an additional assertion records the measured count of narrowing-derived interval-containment Proved that demote to Unresolved (a figure, not an implied "fine").
- (h) **Author-visible flip GATED behind the executor** (Decision 1 option (a)): a test asserts NO author-facing program flips reject→accept (no clean compile of a previously-rejected program); only the proof-engine DISPOSITION flips (AC 1–12), and `precept_proofs`/hover MAY show the narrowed Proved while the compile still emits PRE0083 (the intended, documented interim divergence).
- (i) O-C disposed: nested two-hop relational fixture does NOT discharge (single-hop bound pinned); single-hop does.
- (j) **AC-8 DELTA RE-SIZING (P6's own trusted-base obligation — the P3 full-surface re-run precedes P6 and cannot have sized un-built-at-P3-time P6 code).** P6 adds net-new soundness-load-bearing trusted code (`ReduceFacts` — "the net-new soundness-load-bearing logic", per Effort — plus `EffectiveGuard` and the dominance/polarity re-walk leg). Record P6's `ReduceFacts` + `EffectiveGuard` + dominance-leg LOC against the AC-8 ~900-LOC trusted-base ceiling, with the SAME pre-decided overflow action P3 exit (g) carries (proceed within ceiling / owner-authorized raise-with-justification / cell-to-stated-non-closure) — Lock-blocking, not after-the-fact. This closes the sizing gap whereby P6's additions fall outside both the P3 re-run (which precedes P6) and any P6 obligation: the AC-8 ceiling cannot be silently exceeded by P6 with no gate catching it. *Validate*: the recorded P6 LOC figure is compared against the ceiling and the pre-decided action applied; a meta-assertion confirms the P6 trusted-base delta is enrolled into the soundness-case §4 audit (not double-counted against the P3 re-run, which did not include this code).

**Effort**: **L (6–9 days)**. The `ReduceFacts` de-Morgan reducer + forfeit-on-disjunction discipline + the FORFEIT-sentinel-observable plumbing is the net-new soundness-load-bearing logic and the cost driver (it is the only thing preventing a false `Proved` on the relational tier where `NegateOp` is total); the `AssumedConditions` field + Γ-threading + the single `EffectiveGuard` substitution + the two ConstraintContext repairs are mechanical-but-broad (six reader sites + every `WalkExpression` arm); the dominance/polarity witness leg + AC-13/13b on both routes is modest (a structural tree re-walk). Calibrated against the trusted-base ~750–900 LOC total: `ReduceFacts` + `EffectiveGuard` + the dominance-leg are net-new trusted code that P6 ENROLLS into the soundness-arch trusted base alongside `ExtractGuardLeafConstraints`/`NegateOp`. **These additions are P6's OWN AC-8 delta obligation (exit (j)) — the P3 full-surface AC-8 re-run precedes P6 and does not size un-built-at-P3-time P6 code, so P6 records its `ReduceFacts`/`EffectiveGuard`/dominance-leg LOC against the ceiling itself (the earlier "AC-8 re-run is P3's obligation, not P6's" framing was a sizing gap — P3's re-run covers the P3 surface; P6's net-new trusted code is P6's to size).** Band assumes P3/P5 already green (Step 0 gate); if P6 ships prover-only ahead of P3, the discharge-un-gating is a separate follow-on commit (not in this band).

**Doc-update obligations** (CLAUDE.md routing): CLAUDE.md routing + canonical promotion (Definition-of-done item 9, the standing per-phase obligation): (1) **`docs/compiler/soundness-and-coverage.md`** (the new canonical home, currently Design stub — verified exists, status `Design`/`Stub`) — PROMOTE P6's content into the witness/narrowing section: the `AssumedConditions`/`EffectiveGuard`/`ReduceFacts` mechanism as a narrowing SOURCE on the existing five-path StalenessClean family (NO sixth staleness carrier), the dominance/polarity witness leg on all three discharge routes, the O-1 (a)/(b) branch for the all-contributor capture; advance that section Design→Incremental. (2) **`docs/language/precept-language-spec.md` §0.4/§3A** — PROMOTE the conditional-totality pieces: the N-AND/N-OR/N-IF/N-NOT propagation rules, the gated-position-only refinement, the structural-de-Morgan `facts⁻` reduction + FORFEIT, the facts⁺/facts⁻ GuardConstraint-vs-interval-dict split, the presence/key positive-only rule incl the `X is not set` DEFERRED disposition, the O-C single-hop relational bound (per the P5/P6 promotion obligation in the plan's Definition-of-done item 9 and plan-update protocol). (3) **`docs/compiler/proof-engine.md`** — the intra-expression narrowing capability: `AssumedConditions` carries pre-reduced `ReducedFactSet` (O-B), the `ReduceFacts` reducer, the six-site `EffectiveGuard` unification incl the ConstraintContext repair on the three three-arm sites + the two-fact index-bounds AND-composition it enables, the quantifier-predicate DEFERRED disposition, the interval-dict purity-not-staleness soundness argument, the single-pass/no-cross-obligation-dependency discharge-phase termination invariant, the cross-clause-as-`AssumedConditions=∅`-special-case framing; the O-C single-hop relational restatement; cross-link the P1/O-9 GuardInPath-only self-narrow vs THIS intra-expression narrowing (must-not-conflate note). (4) **`docs/runtime/evaluator.md` + `precept-builder.md`** — confirm the P5-landed locked `if/then/else` lowering + BRANCH semantics are the named evaluator counterpart of Decision 4's then-branch narrowing (P6 consumes, P5 authors). (5) `docs/runtime/runtime-api.md` + `result-types.md` + `descriptor-types.md` + `docs/tooling/mcp.md` (`precept_proofs` DTO gains the assumed-condition provenance + the documented interim Proved-while-PRE0083 divergence note) + `docs/tooling/language-server.md` (hover reads the assumed-condition provenance). (6) `docs/Working/conditional-totality-short-circuit-narrowing-2026-06-07.md` — flip the witness-half status once the P2 DU lands; `docs/Working/soundness-case-2026-06-07.md` §2 — add the probe-a/probe-b Proved rows (BLOCKING per-phase soundness-case update) with the O-1 (b) demote-count figure; §4 — enroll the P6 AC-8 delta rows (`ReduceFacts` + `EffectiveGuard` + the dominance/polarity re-walk leg) with their LOC against the ceiling per exit (j), so the trusted-base audit reflects P6's net-new trusted code (not folded into the P3 re-run). NOTE: no transient refs (Decision-N, O-N, BUG-NNN, phase labels) in code comments — the spec/proof-engine.md/soundness-and-coverage.md citations are canonical-stable.

**Soundness class**: **Class C (Proved-widening).** P6 is the canonical new-`Proved` capability of the plan — it WIDENS what compiles to `Proved` (probe (a)/(b)'s divisor flips Unresolved→Proved). Per the SOUNDNESS SEQUENCING hard constraint, it lands AT-OR-AFTER its checker backstop. Its backstop is a TWO-PART, route-specific arrangement (verified against the witness shapes and the demote-coverage boundary): (1) the GuardConstraint route (the headline `!=`/equality discharges → `GuardInPathWitness`/`FlowNarrowingWitness`) is backstopped by the **P3 GuardInPath/FlowNarrowing checker arms** (built in P3, NOT P2 — P2 builds only Tier-A + the two P2-produced Tier-B arms) PLUS the **dominance/polarity re-walk arm this phase ships** (Decision 5, not gated on O-1); (2) the interval-dict route (representable half-line then-branches → interval-containment witness) is backstopped by the P3 interval-dominance arm under O-1 (a), or DEMOTED to bare-bounds under O-1 (b) via the **P2-wired demote target** (`TryIntervalContainmentProof`, `Intervals.cs:467`, dead today — P2 wires it). CRITICAL BOUNDARY: the P2 demote backstop covers interval-derived witnesses ONLY; it does NOT cover the guard-fact-derived `GuardInPath`/`FlowNarrowing` family — so a P6-before-P3 schedule conservatively REJECTS the headline capability (friction-safe), it does NOT demote. This is why P3 is a HARD CONTENT EDGE of P6's discharge, not an ordering preference. The dominance/polarity leg (built here) is what makes the widened `Proved` re-checkable WITHOUT O-1; the all-contributor interval capture rides O-1/O-E. P6 ships no unchecked `Proved`: discharge-to-`Proved` is gated on the P3 arms being green (exit (g)), and the author-visible flip is additionally gated behind the unbuilt executor (Decision 1 option (a)).

---

## Heavyweight phase block — P7: Empirical net (buildable subset)

**Goal**: Stand up the buildable subset of the soundness-case §5 standing empirical net — §5.4 adversarial false-witness suite (the trusted checker MUST reject every planted false witness, seeded from the conformance-suite fixtures), §5.3 100%-mutation-kill of the trusted checker via the O-10 mechanism, and §5.2 hand-built bounded-model-checking over Precept's finite reachable state space for the three coverage-residual cells — providing the *independent runtime evidence* (not just "the theorems are argued") that the §2 soundness theorem and §3 coverage theorem hold in the running system; soundness-class-neutral (P7 builds NO `Proved`-emitting prover capability — it is a verification harness over the already-checked trusted base, so it cannot itself manufacture a hollow `Proved`).

**Items in scope**: Four concrete artifacts, all under test projects (no `src/Precept/` prover code touched): (1) `test/Precept.Tests/ProofEngine/AdversarialFalseWitnessTests.cs` — the §5.4 standing false-witness corpus: one planted-false witness per `ProofWitnessKind` shape built-and-checked by P2/P3 (Tier-A under-approximating `IntervalContainmentWitness.Computed`; `LiteralWitness` whose `(v cmp t)` re-evaluates false; `CountContainmentWitness` with `hi > max`; the P2-produced `CompositionalConstraintWitness` citing a self-unsat contributor; the `SuppressedByStructureWitness.DeadEndToDeadEnd` with a real `FromState` fact but a fabricated/wrong `TargetState` fact; the Tier-B `GuardInPathWitness`/`FlowNarrowingWitness`/qualifier-family/`DeclarationAttribute`/`DimensionalProduct` false witnesses including a stale-guard witness on ALL FIVE staleness-carrying paths per invariant 1, and — once P4/O-6 lands — the cross-unit factor-drop witness whose `SourceUnitText` re-parses to a non-unit factor), each asserting `ProofChecker.CheckLedger` returns a non-empty rejection; seeded from `docs/Working/conformance-suite-design-2026-06-01.md` fixtures (A1 price×quantity, E4 counting-unit silent-cancel) once that suite's fixtures exist in-tree. (2) `test/Precept.Tests/ProofEngine/CheckerMutationKillTests.cs` — the §5.3 harness driving the O-10 mechanism (the P2-step-0-selected hand-rolled arm-mutation harness scoped to `src/Precept/Language/ProofWitness.cs` + `src/Precept/Pipeline/ProofChecker.cs` + the shared trusted leaves `UcumParser`/`ConstantFold`/`NarrowByConstraint`/`ExtractFieldInterval`/sign tables; Stryker.NET only if O-10 forced the fallback) to a 100%-kill assertion on the re-sized trusted base. (3) `test/Precept.Tests/ProofEngine/BoundedModelCheckTests.cs` — the §5.2 hand-built finite-state enumeration (NO in-repo BMC harness — verified `grep` empty) over the reachable configuration space for the three coverage-residual cells: interior-arithmetic-overflow completeness (probe a), the empty-`ProofRequirements` blind spot (Decision 5 Claim 3a), and the O-4 event-arg constraint-carrying seam (whose removal obligation is HOMED here per the O-4 residual-homing correction — not on the P8 convention-only stub) at `ProofEngine.cs:266/301/315/383`. (4) `test/Precept.Tests/ProofEngine/RuntimeDifferentialSkeleton.cs` — the §5.1 differential harness SKELETON-against-stub ONLY: the test scaffold + generated-input corpus + differential-compare wiring, fail-closed on `Runtime/Evaluator.cs`'s `NotImplementedException` (161-line / 5×NIE / no `ExecutionPlan`, verified). The LIVE §5.1 differential run is NOT in scope and NOT scheduled (evaluator-blocked dependency note only).

**Decisions required before kickoff**: Inherits O-10 (the C# mutation-testing MECHANISM) — NOT re-decided in P7; it is settled in P2 step 0 (the spike), and P7 CONSUMES the chosen mechanism. P7's 100%-kill exit is unrunnable until O-10 is recorded in Decisions captured with the chosen mechanism (hand-rolled harness vs Stryker.NET). Two P7-local thresholds to fix at kickoff (neither a language-surface decision; both Tier-1 mechanical): (i) the mutation-kill exit threshold = 100% per soundness-case F-3 (a checker arm resisting kill is provisionally-trusted and routes to over-rejection); (ii) the BMC enumeration bound (the finite-state cap — grounded in spec §0.4.1/0.4.3 finiteness: no loops, no reconverging flow, closed type vocabulary at spec `:158`/`:164`/`:166` — so the reachable configuration space IS finite and hand-enumerable; the bound is the per-cell input-domain cap, not a soundness decision). **The bound is NOT free-floating: it MUST be recorded as a committed artifact with its derivation (step 4 / exit (c) — interior-overflow bound = max corpus nesting depth + headroom; the others from the closed type vocabulary), and the non-vacuity assertion enforces the enumeration reaches the known fault-prone shapes — so "exhaustive to the bound" is a checkable figure, not an unspecified cap that could pass vacuously.** O-1's disposition (settled in P2) shapes the §5.4 corpus: under O-1 (a) the corpus MUST include a false all-contributor `FlowNarrowingWitness` (a planted online certificate that does not actually re-verify) that the online checker arm rejects; under O-1 (b) the `FlowNarrowing`/narrowing-derived-`IntervalContainment` cell ships demote-only, so the corpus instead asserts the demote-to-bare-bounds path holds (a narrowing witness with no re-verifiable provenance demotes to `Unresolved`, never survives as `Proved`) and there is NO online-certificate false-witness for that cell to plant — BOTH dispositions written below.

**Step-by-step execution**:

- **0. Confirm the O-10 mechanism is wired and recorded (hard pre-step, mirrors P2 step 0 for P7).** P7's §5.3 harness cannot run until the P2-step-0-selected mechanism is in Decisions captured. If O-10 is still PLAN-TIME PENDING when P7 lifts from stub, P7 is blocked on that recording (not on a P7 decision). *Validate*: the recorded-mechanism artifact exists and a single deliberately-mutated band-check (`<=`→`<`) on the chosen Tier-A arm in `ProofChecker.cs` already makes a `WitnessCheckerTests` case go red (the P2 step-0 acceptance test) — re-run it green/red to confirm the harness P7 extends is live.
- **1. Build the §5.4 adversarial false-witness corpus** in `AdversarialFalseWitnessTests.cs`, seeded from P2's `WitnessCheckerTests` planted-false fixtures (do NOT fork — extend the same fixtures). One `[Theory]` row per `ProofWitnessKind` shape that P2/P3 built-and-check: each row constructs a *plausible-but-wrong* witness and asserts `ProofChecker.CheckLedger(ledger, semanticIndex, catalogs)` returns a non-empty `ImmutableArray<CheckerRejection>`. Cover the soundness-case §5.4 named failure modes (an interval that under-approximates; a Tier-B named-contributor witness whose cited fact does not hold; a `SuppressedByStructureWitness.DeadEndToDeadEnd` with a fabricated `TargetState` leg — the exact two-leg-stopgap defense from soundness-arch invariant 5). Add the invariant-1 staleness row on ALL FIVE staleness-carrying paths (`GuardInPath`/`FlowNarrowing`/`QualifierCompatibility`/`AssignmentQualifier`/`DeclarationAttribute`-Dimension, per soundness-arch invariant 1 `:117-123`): a witness citing a guard whose field is in `ReassignedBefore` must be rejected on each path. *Validate*: every false-witness row produces ≥1 rejection (red if any survives); and a meta-assertion that the corpus has ≥1 row keyed to each P2/P3-checked `ProofWitnessKind` member (a missing shape fails — the corpus tracks the checker surface, not a hand-list).
- **2. Wire the conformance-fixture seam.** `docs/Working/conformance-suite-design-2026-06-01.md` (Locked 2026-06-01) defines fixtures (A1 price×quantity, E4 counting-unit silent-cancel) whose `.precept` forms ARE the BUG-030 anchor shapes; the soundness-arch Dependencies prose names the suite as the §5.4 seed (the suite is offline coverage, the checker is online soundness; the fixtures cross-feed). Add a corpus loader that, WHEN the conformance fixtures exist in-tree (verified NOT present today — `find` empty), enrolls each A1/E4 fixture as a false-witness source for the cross-unit factor-drop witness. *Validate*: the loader is fail-soft (skips with an explicit `Skip=` reason when fixtures absent, so P7 is not blocked on the conformance suite shipping) AND a meta-test asserts that once the cross-unit witness exists (post-P4/O-6), an A1-derived factor-drop witness whose `SourceUnitText` re-parses to a kg↔g ×1000 factor is rejected.
- **3. Build the §5.3 mutation-kill harness** in `CheckerMutationKillTests.cs` driving the O-10 mechanism over the re-sized trusted base (the AC-8 P3-re-run LOC surface: `ProofWitness.cs` + `ProofChecker.cs` + the shared trusted leaves). Each checker arm is mutated (comparison flip, band-bound drop, staleness-set-check deletion, named-contributor-check removal); the harness asserts ≥1 test in the §5.4 corpus + `WitnessCheckerTests` goes red for EVERY mutant. *Validate*: 100% kill on the full trusted base (soundness-case F-3 / §5.3 exit); a checker arm that survives any mutant is reported by name and routes to over-rejection (the arm is provisionally-trusted, NOT shipped as accepting) — a surviving mutant FAILS this exit, it does not silently pass.
- **4. Build the §5.2 hand-built BMC enumeration** in `BoundedModelCheckTests.cs` — a from-scratch finite-state enumerator (NOT a model-checker wrapper; none exists in-repo, verified). Grounded in spec §0.4.1/0.4.3 finiteness (no loops `:158`, no reconverging flow `:164`, closed type vocabulary `:166`), enumerate the reachable configuration space per residual cell and assert no faulting/un-obligated configuration is reachable. **COMMIT THE BMC BOUND AS AN ARTIFACT WITH ITS DERIVATION (mirrors P2's `pre-checker-worst-file-ms.txt` discipline — the bound must be a recorded figure, not an unstated cap, else "exhaustive to the bound" is only as strong as a possibly-trivial cap).** Record, per cell, the enumeration bound (e.g. `test/Precept.Tests/ProofEngine/__snapshots__/bmc-bound.txt`) AND its derivation from the finite state space — for the interior-overflow cell, the bound = max arithmetic nesting depth observed across the corpus + a stated headroom (so the enumeration provably reaches at least the deepest real shape plus margin), grounded in §0.4.1/0.4.3 finiteness; for the empty-`ProofRequirements` and event-arg cells, the per-cell input-domain cap derived from the closed type vocabulary. A too-small bound that passes vacuously (no counterexample found within a trivial cap) is forbidden — the bound + derivation are committed and the non-vacuity assertion (below) enforces that the enumeration actually reaches the known fault-prone shapes. (4a) **interior-arithmetic-overflow** (probe a) — enumerate nested arithmetic `(a*b)*c` shapes to the committed bound and confirm every interior multiply carries a `NumericOverflow` obligation (the `TypedExpressionShape` exhaustiveness is structurally blind to this — BMC is the backstop). **NON-VACUITY (mirrors P1 step-10a's "commit the trigger fixtures so the gate is non-vacuous"): assert the enumerated set INCLUDES the known fault-prone shapes — the interior-overflow `(a*b)*c` fixture is provably inside the enumerated range (depth ≤ bound), so a green result means "no counterexample among shapes that include the real fault-prone ones", not "no counterexample because the cap excluded them".** (4b) **empty-`ProofRequirements` blind spot** (Decision 5 Claim 3a) — enumerate ops declaring an empty `ProofRequirements` list and confirm none is a fault-prone shape silently "covered" (a found counterexample is a §1.5 hole). (4c) **the O-4 event-arg constraint-carrying seam** (`ProofEngine.cs:266/301/315/383` — `Events[].Args[].DefaultExpression` + the `AddPresenceObligation("Argument", …)` path) — enumerate event-arg-bearing reachable configs and surface any un-obligated faulting event arg as a found counterexample. **This is the live discharger of the O-4 removal obligation** (the O-4 residual-homing correction routes it HERE, not to the P8 convention-only stub which only RECORDS the seam). *Validate*: each cell's enumeration is exhaustive to the kickoff-fixed bound and reports any reachable faulting/un-obligated config as a failing counterexample (green = no counterexample found within the bound).
- **5. Build the §5.1 differential SKELETON-against-stub ONLY** in `RuntimeDifferentialSkeleton.cs` — the test scaffold + generated-input corpus + differential-compare wiring (for each `Proved` divisor-non-zero verdict, no generated input may make the divisor zero), fail-closed on `Runtime/Evaluator.cs`'s `NotImplementedException`. *Validate*: the skeleton compiles and is marked `[Fact(Skip="evaluator stub — Runtime/Evaluator.cs throws NotImplementedException; activates when the executor lands")]` (or asserts the NIE is thrown), so it activates the MOMENT the evaluator ships WITHOUT a P7 re-open; a meta-test confirms the skeleton wiring references the generated-input corpus and the proof-ledger `Proved` set (the scaffold is real, not a placeholder comment). The LIVE differential run is explicitly NOT scheduled — a dependency note only.

**Dependencies**: Hard upstream edges (cite the canonical graph, not invented): **P2** (the `ProofWitness` DU + `ProofChecker` spine + `WitnessCheckerTests` — the §5.4 corpus extends those fixtures and the §5.3 harness mutates that checker; per the P7 stub "seeded from P2's `WitnessCheckerTests`") and **the O-10 mechanism** wired in P2 step 0 (the §5.3 100%-kill exit is unrunnable without it — P7 inherits, does not re-decide). **P3** (the remaining 10 strategy arms + cross-cutting predicates — the §5.4 corpus needs a planted false witness per checker-built `ProofWitnessKind`, and the §5.3 trusted base must be STABLE to mutate; per the stub "TBD pending P2–P4/P6 — the full trusted base must be stable to mutate/falsify"). **P4/O-6** (the cross-unit `SourceUnitText` witness — the A1/E4 conformance-seeded factor-drop false-witness rows only become assertable post-O-6; pre-O-6 those rows are Skip-gated). **P6** (the conditional-totality narrowing witness — its false witnesses join the §5.4 corpus once it ships). O-1's P2 disposition (a)/(b) shapes the §5.4 narrowing-cell rows (a online-certificate false witness vs b demote-path assertion — both written in steps above). The §5.1 LIVE run depends on the evaluator (OUT of this plan's scope per the Discovered-during-planning evaluator-stub note — skeleton only). No phase depends ON P7 except the Definition-of-done items 6/7 (the 100%-kill result) and the O-4 removal obligation it discharges.

**Exit criteria** (testable):
- (a) §5.4: every false-witness row in `AdversarialFalseWitnessTests.cs` yields a non-empty `CheckerRejection` from `ProofChecker.CheckLedger` (a survived false witness fails the build); the corpus meta-test asserts ≥1 row per P2/P3-checked `ProofWitnessKind` member (a checker-built shape with no planted false witness fails).
- (b) §5.4 invariant-1 coverage: a stale-guard false witness on ALL FIVE staleness-carrying paths is rejected (one row per path; a missing path fails).
- (c) §5.2 BMC: the per-cell enumeration bound is COMMITTED as an artifact with its derivation from the finite state space (e.g. `bmc-bound.txt` — interior-overflow bound = max corpus nesting depth + headroom; the others' input-domain caps from the closed type vocabulary), and each of the three residual cells (interior-overflow, empty-`ProofRequirements`, event-arg seam) enumerates to that committed bound with zero reachable faulting/un-obligated counterexamples; a found counterexample fails with the offending config named. **NON-VACUITY (required): a meta-assertion confirms the enumerated set includes the known fault-prone shapes (the interior-overflow `(a*b)*c` fixture is inside the enumerated range, depth ≤ bound) — so a green result is "no counterexample among shapes that demonstrably include the fault-prone ones", not a vacuous pass under a trivial cap.**
- (d) §5.1 skeleton compiles and is Skip-gated on the evaluator stub with the wiring meta-test green (the scaffold references the generated-input corpus + the `Proved` ledger set).
- (e) **Doc-state gate (falsifiable):** a doc-lint asserts soundness-case §5.2/§5.3/§5.4 carry a "buildable now" status token and §5.1 carries an explicit evaluator-dependency token; and that the O-4 residual cell in soundness-case §1.5 / §3-residuals names "→ P7 BMC" as its discharger (the O-4 removal obligation home).
- (f) **BLOCKING SOUNDNESS GATE — 100% MUTATION KILL ON THE TRUSTED BASE (soundness-case F-3 / §5.3 exit, Definition-of-done items 6–7).** The §5.3 harness, driving the O-10 mechanism over the AC-8-re-sized trusted base (`ProofWitness.cs` + `ProofChecker.cs` + the shared trusted leaves), kills 100% of mutants — every comparison-flip / band-bound-drop / staleness-check-deletion / named-contributor-check-removal mutant is caught by ≥1 test (§5.4 corpus ∪ `WitnessCheckerTests`). A SURVIVING MUTANT FAILS THIS GATE: the surviving arm is reported by name and is provisionally-trusted (routes to over-rejection — it MUST NOT ship as an accepting arm). This is the load-bearing justification for trusting the checker the entire soundness theorem rests on: a checker whose bugs the tests do not catch is not yet trustworthy. The gate is unrunnable until the O-10 mechanism is recorded (step 0); "100% kill" is a runnable assertion against that mechanism, never "phase complete."

**Effort**: **L (6–9 days)** — the §5.4 corpus reuses P2/P3's `WitnessCheckerTests` fixtures (extend, ~1.5–2 d for the per-shape planted-false rows + the five staleness paths + the conformance seam loader); the §5.3 mutation harness is thin over the O-10 mechanism IF the recommended hand-rolled harness landed (~1.5–2 d to drive it across the re-sized base to 100% kill; +1 d if O-10 forced Stryker.NET and the mutator set still needs scoping to the trusted-base files); the §5.2 hand-built BMC is the cost driver (~2.5–3 d — a from-scratch finite-state enumerator over three cells, no tooling to lean on); the §5.1 skeleton is ~0.5 d (scaffold + Skip-gate). Band EXCLUDES the evaluator-blocked LIVE §5.1 differential run (out of scope). Within the soundness-case §4 trusted-base ~750–900 LOC ceiling discipline — P7 adds NO trusted code (it is harness/test code that mutates and falsifies the existing trusted base).

**Doc-update obligations** (CLAUDE.md routing): CLAUDE.md routing + the standing canonical-promotion obligation (Definition-of-done item 9): (1) **PROMOTE into `docs/compiler/soundness-and-coverage.md`** — inline P7's content into its **Empirical net** section (the §5.4 false-witness suite + §5.3 mutation-kill + §5.2 BMC + the §5.1 skeleton-against-stub + the evaluator dependency), advancing that section Design→Incremental; drop the compiler-README maturity `stub` once the section is Incremental. (2) `docs/Working/soundness-case-2026-06-07.md` (the companion canonical artifact, maintained not archived) — §5.2/§5.3/§5.4 marked buildable-now with the harness paths recorded (§5.2 ALSO records the committed BMC bound artifact + its derivation from the finite state space + the non-vacuity assertion, so the BMC's "exhaustive to the bound" is a recorded figure); §5.1 kept as the evaluator-dependency; §6 falsifier register F-3 tied to the §5.3 100%-kill RESULT (move F-3 from "exit criterion in the plan's trust-harness phase" to a discharged-with-result row); §1.5 / §3-residuals: the O-4 event-arg cell records "→ P7 BMC" as its live discharger (the removal-obligation home) — BLOCKING per-phase soundness-case update. (3) `docs/compiler/diagnostic-system.md` — no new diagnostic code (P7 is test/harness only); a cross-reference note that the witness-checker internal-error surface (P2) is now mutation-kill-validated. (4) `docs/compiler/README.md` — proof-engine stage cross-reference: the empirical-net harness added (Discovered-during-planning mechanical routing — the doc-status-changing row mandates updating cross-referencing tables). (5) `docs/Working/bugs.md` — if §5.2 BMC surfaces a counterexample in any residual cell, file a `BUG-NNN` and route per the plan-update protocol (emission hole → current phase; over-prove → behind the checker); BUG-030's conformance-seeded false-witness rows recorded against P4/O-6 status. (6) `docs/Working/conformance-suite-design-2026-06-01.md` — record the §5.4-seeds-from-conformance cross-feed seam (the suite's fixtures feed the false-witness corpus) so the two efforts stay reconciled. No `docs/language/precept-language-spec.md` / `proof-engine.md` / `evaluator.md` promotion from P7 (those are the P5/P6 conditional-totality promotion targets, not P7's — P7 ships no language surface and no `Proved`).

**Soundness class**: Soundness-class-NEUTRAL (builds NO new `Proved`-emitting prover capability — neither Class A emission, Class B checker, nor Class C `Proved`-widening). P7 is the *verification harness over* the trusted base, not a capability that emits verdicts: it falsifies (§5.4 plants false witnesses the checker must reject), mutates (§5.3 perturbs the checker arms and confirms tests catch them), and enumerates (§5.2 BMC over the finite state space) — so it cannot itself introduce a hollow `Proved`. Its BACKSTOP DEPENDENCY runs the other direction: P7 *validates the Class-B backstop that P2 built* (the `ProofChecker` spine) and the Class-B/C arms P3 replicated — the §5.3 100%-mutation-kill is the F-3 justification for trusting that checker, and §5.4 is the F-1 evidence that each strategy's witness is genuinely re-checkable. P7 ALSO serves as the live backstop discharging the O-4 event-arg removal obligation (the P8 convention-only stub merely records that seam; the P7 BMC is what keeps it sound-by-rejection until a type-checker checker ships). Earlier phases this depends on: P2 (the checker + DU + `WitnessCheckerTests` the corpus extends + the O-10 mechanism), P3 (the full strategy surface stabilized for mutation), P4/O-6 (the cross-unit witness for the conformance-seeded rows).

---

## Heavyweight phase block — P8: Generalization plumbing convention + §1.4/F-6 fault↔diagnostic correspondence cell + RelationalCertificateWitness reservation

**Goal**: Establish the phase-agnostic witness-plumbing convention (the `ProofForwardingFact` extend-not-fork DU pattern carried toward the graph-analyzer's reachability-path/dominance certificates and the type-checker's discarded typing derivation), reserve the `RelationalCertificateWitness` Karr slot, and RECORD — not discharge — the §1.4/F-6 fault↔diagnostic-correspondence boundary (every `FaultCode` preventing-diagnostic must fire at every fault-reachable site, not merely be registered), including the `WriteToOmittedField`/`WriteToReadOnlyField` map-gap forward-pointer, as stated soundness boundaries each carrying an empirical-net backstop pointer; the checkers themselves are stubbed/deferred and this phase ships NO new `Proved`-emitting capability.

**Items in scope**: CONVENTION/RECORD-ONLY (no graph or type checker built):
1. `src/Precept/Pipeline/StateGraph.cs:91-139` — confirm + DOCUMENT the `ProofForwardingFact` extend-not-fork convention (the `:130-133` comment already states it for `UnreachableRowFact`) as the canonical witness-plumbing pattern the graph/type generalization phases extend; add a convention-anchor doc-comment naming the two future certificate families the DU will carry without forking (reachability-path certificate via `ReachabilityFact.PathFromInitial`; cut-set/dominance certificate via `DominancePathFact.DominatedTerminals`).
2. The graph-analyzer reachability-path / cut-set-dominance certificate SHAPE FORK record (the Alethe-pole design fork) — a recorded decision-required, NOT a built checker: whether the future graph checker re-verifies a reachability witness via the cited `PathFromInitial` edge-walk (fine-grained, DRAT-pole) or via a dominator/cut-set certificate (`DominancePathFact`, coarser); produced by `GraphAnalyzer.cs:261-264` (path) and `:272`/`:509-547` (dominator computation) — both already exist as `ProofForwardingFact`s, so the reservation rides existing producers.
3. Type-checker typing-derivation retention record — the `TypedExpression` typing derivation (`ResultType` + the `ProofRequirements` carried only on `TypedBinaryOp`/`TypedFunctionCall`/`TypedMemberAccess`, `SemanticIndex.cs:17,64,82,110`) is currently discarded at the proof-engine boundary; record the convention that the future type-checker checker retains it as a witness rather than re-deriving (no retention built now).
4. `RelationalCertificateWitness` reserved slot — confirm it lands as a declared-but-unbuilt member of the P2 `ProofWitnessKind`/`ProofWitness` DU (Farkas/affine-basis Karr shape), so the exhaustiveness annotation stays total and the deferred Karr work extends rather than forks.
5. §1.4/F-6 CELL RECORD — a stated soundness boundary, NOT an analyzer: the `[StaticallyPreventable]` map on `FaultCode.cs:9-55` (15 members) + `FaultMapDerivationTests.cs` assert correspondence is REGISTERED; neither proves the preventing diagnostic FIRES at every fault-reachable site (the §1.4 sufficiency gap, distinct from the §1.5 obligation-stamping surface `Precept0030` covers). Record + forward-point; the `WriteToOmittedField`/`WriteToReadOnlyField` map gap (neither a `FaultCode` nor a `DiagnosticCode` member; only `Evaluator.cs:60-113` XML forward-pointers — verified) has no fault-side backstop and rests on §1.2 type-side verdicts in the interim.
6. The O-4 event-arg constraint-carrying seam record — rides the §1.4/F-6 deferred-cell record as a stated boundary with a forward-pointer; its live backstop is the P7 BMC (NOT this stub — P8 cannot discharge it, convention-only).
EXPLICITLY OUT: the graph-analyzer checker; the type-checker checker; the Karr/relational EXPRESSIVENESS extension; any §1.4 coverage analyzer that proves fire-at-every-site (deferred to the same generalization phase as the graph/type checkers).

**Decisions required before kickoff**: (1) The reachability-path-vs-cut-set-dominance witness REFORMULATION FORK (the one Alethe-pole fork the stub names): does the future graph checker key its reachability certificate on the fine-grained `ReachabilityFact.PathFromInitial` edge-walk (DRAT-pole, cheaply re-checkable per the soundness-arch granularity argument) or on the coarser `DominancePathFact` cut-set/dominator certificate (Alethe-pole, forces the checker to re-derive dominance)? This phase only RECORDS the fork as a decision-required for the deferred graph-checker phase — it does NOT settle it (settling is a future-phase `/design` owner consultation, since the graph-checker is out of scope). Lean: record the fine-grained `PathFromInitial` pole as the soundness-arch-consistent default (matches the DRAT-pole granularity choice the architecture already took for proof-engine witnesses), but do not lock. (2) Whether §1.4/F-6 gets its OWN coverage axis (a future `Precept0031`-style fault-reachability analyzer) OR rides this phase as a recorded boundary only — lean (b) RIDE-AS-RECORD: P8 is convention/record-scoped, the existing `Precept0027`/`FaultMapDerivationTests` cover the registered-correspondence floor, and the fire-at-every-site sufficiency analyzer belongs in the deferred generalization phase alongside the graph/type checkers (its construction is non-trivial and out of this stub's scope). The §1.4/F-6 backstop in the interim is the P7 BMC over the finite reachable state space (the O-4 removal obligation is homed there, per the O-4 Open-decisions entry), NOT a P8 analyzer. No new language surface; no checker-first `Proved`-widening decision (P8 ships no `Proved`).

**Step-by-step execution**:

1. **Confirm + anchor the `ProofForwardingFact` extend-not-fork convention** — `StateGraph.cs:91-139`. The DU + the `:130-133` convention comment ("extended rather than forked into a separate `ProofEngineFact` DU because consumption is uniform across subtypes") already exist (verified). Add a convention-anchor doc-comment on the `ProofForwardingFact` base (`:91`) naming it the canonical witness-plumbing pattern for the deferred graph/type generalization, and citing the two future certificate families it will carry without forking: the reachability-path certificate (`ReachabilityFact.PathFromInitial`, `:96`) and the cut-set/dominance certificate (`DominancePathFact.DominatedTerminals`, `:101`). Reference only canonical homes (`docs/compiler/soundness-and-coverage.md`), never the working-doc paths (transient-ref rule). *Validate*: `dotnet test test/Precept.Tests/` stays green (doc-comment only, no behavior change); a `grep -c 'ProofEngineFact' src/Precept/Pipeline/*.cs` returns 0 (the convention held — no forked DU introduced); the convention-anchor comment cites no `docs/Working/` path (grep the added comment for `Working/` → empty).
2. **Record the reachability-path-vs-cut-set-dominance fork (decision (1))** — add the fork to `docs/compiler/soundness-and-coverage.md` § Generalization (deferred phases) as a stated decision-required for the future graph-checker, with the four-leg framing (rationale: the soundness-arch granularity argument already chose the DRAT-pole for proof-engine witnesses; precedent: `GraphAnalyzer.cs:261-264` produces `PathFromInitial`, `:272`/`:509-547` produces dominators — both live `ProofForwardingFact`s, so either pole rides existing producers; alternative: the coarse `DominancePathFact` pole; tradeoff: the fine pole is cheaper-to-check but the path witness is larger data). Do NOT settle it. *Validate*: a doc-lint (the same falsifiable-doc-state pattern P1 exit (e) uses) asserts the § Generalization section names BOTH poles and marks the fork OPEN (un-settled) — a section that silently picks one pole fails the lint; `grep` the section for both `PathFromInitial` and `DominancePathFact` tokens → both present.
3. **Record the type-checker typing-derivation retention convention** — `SemanticIndex.cs:17` (`TypedExpression` base carries `ResultType`) + the three `ProofRequirements`-carriers (`:64,:82,:110`). Record (in the canonical doc, not code) that the future type-checker checker RETAINS the `TypedExpression` typing derivation as a witness past the proof-engine boundary rather than re-deriving it (the §1.2 type-side verdicts). No retention is built now — this is a convention record so the generalization phase extends the witness DU. *Validate*: `grep -n 'ProofRequirements' src/Precept/Pipeline/SemanticIndex.cs` confirms the three-carrier set is unchanged (the record describes the existing boundary, does not move it); the canonical doc's § Generalization names the retention convention; no `src/` edit beyond step 1's doc-comment (`git diff --stat src/` shows only `StateGraph.cs`).
4. **Confirm the `RelationalCertificateWitness` reserved slot is live-and-unbuilt** — it is declared in the P2 `ProofWitness.cs` DU + `ProofWitnessKind` enum (P2 step 2 reserves it). Verify, in P8, that it is a declared member with NO accepting checker arm and NO producer, and that `ProofChecker`'s `[HandlesCatalogExhaustively(typeof(ProofWitnessKind))]` arm for it conservatively rejects (it can never be produced, so the reject is unreachable-but-total). *Validate*: a `WitnessCheckerTests` assertion that `ProofWitnessKind.RelationalCertificate` has a `[HandlesCatalogMember]` conservative-reject arm (the exhaustiveness analyzer `Precept0019`-model would fail the build if it had none — so this is mechanically enforced, not asserted); a `grep` over `ProofEngine.*.cs` confirms zero producers construct a `RelationalCertificateWitness` (the slot is reserved, not wired).
5. **Record the §1.4/F-6 fault↔diagnostic-correspondence cell as a stated soundness boundary** — `FaultCode.cs:9-55` (15 members, each `[StaticallyPreventable(DiagnosticCode.X)]`) + `FaultMapDerivationTests.cs` assert correspondence is REGISTERED (the bijective-core reproduction), and `Precept0027DiagnosticEmissionCoverage` (keys on `DiagnosticCode` emission-site PRESENCE, `:16`) asserts each diagnostic has ≥1 site — but NEITHER proves the preventing diagnostic FIRES at every fault-reachable path (the §1.4 sufficiency gap, distinct from the §1.5 stamping surface `Precept0030` closes). **CREATE the `## Stated boundaries` section in `docs/compiler/soundness-and-coverage.md`** (verified absent today — the doc's sections are Overview / The two theorems / Witness format / The trusted checker / Catalog-driven coverage analyzer / Trusted base / Relationship to conditional totality / Empirical net / Generalization / Design source / Cross-references; there is NO `## Stated boundaries` yet, so P8 is the phase that first populates it, advancing it Design→Incremental from empty). Populate it with: (a) the gap statement (registered ≠ fires-at-every-site); (b) the higher-risk rows from soundness-case §1.4 (non-bijective `UnexpectedNull` shared backstop `FaultCode.cs:23-24`; the many-to-one collection-safety collapses `ProofEngine.Diagnostics.cs:484-490`, hand-coded policy); (c) the empirical-net backstop pointer (P7 BMC over the finite reachable state space). *Validate*: a doc-lint asserts the now-created `## Stated boundaries` section exists AND enumerates the §1.4/F-6 cell WITH an explicit empirical-net route token (e.g. "→ P7 BMC") — a cell recorded without a route fails the lint (mirrors P1 exit (e)'s §1.5-residual-route discipline).
6. **Record the `WriteToOmittedField`/`WriteToReadOnlyField` map-gap forward-pointer** — verified: neither is a `FaultCode` member (`FaultCode.cs` has 15, none named these) nor a `DiagnosticCode` member (`grep` over `DiagnosticCode.cs` → absent); both exist only as `Evaluator.cs:60-113` XML-comment forward-pointers ("to be added with `[StaticallyPreventable(...)]`"). Record in the canonical doc that these faults have NO fault-side backstop today — prevention rests entirely on the §1.2 type-side verdicts (the `OmittedFieldRead/Set`/`ComputedFieldNotWritable` access-mode codes, soundness-case §1.2) until the faults + their `[StaticallyPreventable]` links ship in the deferred generalization phase. *Validate*: a doc-lint cross-checks the recorded gap against source — assert `FaultCode.cs` does NOT yet contain `WriteToOmittedField`/`WriteToReadOnlyField` (if it does, the gap closed and the record must flip); the canonical doc carries the type-side-interim-backstop sentence. This is a SOURCE-GROUNDED record, not prose: the lint fails if the source state diverges from the recorded boundary.
7. **Record the O-4 event-arg seam as a boundary riding the §1.4/F-6 cell** — per the O-4 Open-decisions disposition, P8 RECORDS the event-arg constraint-carrying seam (a type-checker stamping-layer hole neither the emission-walk nor the catalog-carrier axis reaches) as a stated boundary with a forward-pointer; its LIVE backstop is the P7 BMC (the O-4 removal obligation is homed on P7, NOT on this convention-only stub — verified against the O-4 entry which explicitly DROPS the earlier "P8 discharges it" clause). *Validate*: a doc-lint asserts the recorded O-4 seam names P7 BMC as its discharger and explicitly states P8 does NOT discharge it (a record claiming P8 closes the seam fails the lint); cross-reference the P7 stub's BMC item (the O-4 removal obligation appears there, not here).
8. **Build-green + convention-non-fork regression** — `dotnet build` + `dotnet test test/Precept.Tests/ test/Precept.Analyzers.Tests/` green; `grep -rc 'ProofEngineFact\|GraphFact\|TypeCheckerFact' src/Precept` → 0 (no parallel witness DU forked); `git diff --stat src/` shows only the `StateGraph.cs` convention-anchor comment (this phase is record/convention-scoped — it must NOT add a checker, an analyzer, or a producer). *Validate*: the diff-stat assertion is the mechanical proof that P8 stayed convention-scoped; a non-empty `src/` diff beyond the doc-comment is a scope breach the review catches.

**Dependencies**: Hard dependency on P2–P7 per the canonical stub ("TBD pending P2–P7 (the proof-engine-first checker must be proven sound before generalizing)"). Specifically: P2 supplies the `ProofWitness`/`ProofWitnessKind` DU whose `RelationalCertificateWitness` reserved slot (step 4) P8 confirms, and the `[HandlesCatalogExhaustively(ProofWitnessKind)]` exhaustiveness that makes the reserved slot's conservative-reject arm mechanically required. P7 is the LIVE backstop for the two boundaries P8 only RECORDS: the O-4 event-arg seam removal obligation is homed on the P7 BMC (per the O-4 Open-decisions entry, which explicitly drops the earlier "P8 discharges it" clause — verified), and the §1.4/F-6 fire-at-every-site sufficiency cell routes to the same P7 BMC over the finite reachable state space. P8 does NOT depend on P1's `Precept0030` for the §1.4 cell — they cover DIFFERENT surfaces (`Precept0030` = §1.5 obligation-stamping completeness on the `ProofRequirements→emission→discharge` surface; §1.4/F-6 = `FaultCode→preventing-diagnostic` fire-at-every-site, which the soundness-arch §1.4/F-6 disposition note explicitly states is NOT subsumed by the coverage axes). No O-1 branch flows into P8 (P8 builds no narrowing arm and no narrowing witness half — the O-1 (a)/(b) disposition does not rescope this convention-only stub; the `RelationalCertificateWitness` slot it confirms is the Karr/relational EXPRESSIVENESS reservation, distinct from the O-1 `FlowNarrowingWitness` re-checkability question). The graph/type-checker checkers and the Karr expressiveness extension are explicitly OUT (their own future consultations), so no downstream phase depends on P8 beyond inheriting its recorded conventions.

**Exit criteria** (testable):
- (a) The `ProofForwardingFact` extend-not-fork convention is doc-anchored (`StateGraph.cs:91`) and no parallel witness DU exists — `grep -rc 'ProofEngineFact\|GraphFact\|TypeCheckerFact' src/Precept` → 0; the anchor comment cites the canonical doc, not any `docs/Working/` path (transient-ref lint).
- (b) The reachability-path-vs-cut-set-dominance fork (decision (1)) is RECORDED as un-settled in `docs/compiler/soundness-and-coverage.md` § Generalization, naming BOTH the `PathFromInitial` (DRAT) and `DominancePathFact` (Alethe) poles — doc-lint asserts both tokens present and the fork marked OPEN; a section that silently picks one pole fails.
- (c) The `RelationalCertificateWitness` reserved slot has a `[HandlesCatalogMember]` conservative-reject arm (mechanically required by `Precept0019`-model exhaustiveness — build fails without it) and ZERO producers (`grep` over `ProofEngine.*.cs` → no construction site).
- (d) The type-checker typing-derivation retention convention is recorded (canonical doc § Generalization) with no `src/` retention built — `git diff --stat src/` shows only the `StateGraph.cs` convention comment.
- (e) **BLOCKING SOUNDNESS GATE — the §1.4/F-6 + map-gap boundary record is SOURCE-GROUNDED and route-pointed, not prose.** Three legs, all required, all mechanically asserted: **(e-i)** a doc-lint asserts the `## Stated boundaries` section (CREATED by this phase per step 5 — verified absent in the canonical doc today) exists in `docs/compiler/soundness-and-coverage.md` and enumerates the §1.4/F-6 fault↔diagnostic-correspondence cell WITH an explicit empirical-net route token ("→ P7 BMC") — a missing section, or a cell recorded without a route, fails (mirrors P1 exit (e)); **(e-ii)** the recorded `WriteToOmittedField`/`WriteToReadOnlyField` map gap is cross-checked against source — assert `FaultCode.cs` does NOT contain these members AND `DiagnosticCode.cs` does NOT contain them (verified absent today); if either source state diverges (a member was added), the lint FAILS so the boundary record cannot silently rot into a false claim; **(e-iii)** the O-4 event-arg seam record names P7 BMC as its discharger and explicitly states P8 does NOT discharge it — a record claiming P8 closes the seam fails the lint. Together these make the "P8 records, does not discharge" scope a mechanically-enforced fact: the gate goes RED if P8 silently claims to close a cell it only records, if the recorded map gap diverges from source, or if any boundary lacks its empirical-net route. This is the constraint-1-analogue for a record-only phase — P8 ships NO `Proved` (so no checker-first violation is possible), and this gate proves it did not OVER-CLAIM closure of a deferred soundness cell.
- (f) Build + test green: `dotnet build`; `dotnet test test/Precept.Tests/ test/Precept.Analyzers.Tests/` (no new failures); the convention-scope diff-stat assertion (step 8) holds — `git diff --stat src/` limited to the `StateGraph.cs` doc-comment.

**Effort**: **M (3–5 days, stub-scoped)**. The bulk is the canonical-doc records + the source-grounded doc-lints (steps 5–7) and the convention-anchor comment (step 1); near-zero `src/` code (no checker, no analyzer, no producer — confirmed by the step-8 diff-stat gate). The `RelationalCertificateWitness` slot (step 4) is a verification of P2's reservation, not new construction. Calibrated against the trusted-base discipline: P8 adds ~0 LOC to the trusted base (it builds no trusted code), well under the ~750–900 LOC total ceiling — its cost is record-fidelity + lint authoring, not checker LOC. Lower end (3 d) if the doc-lints reuse the P1 exit-(e) doc-state-lint harness; upper (5 d) if the reachability-path-vs-cut-set fork (decision (1)) record requires a fuller four-leg write-up for the deferred graph-checker phase.

**Doc-update obligations** (CLAUDE.md routing): CLAUDE.md routing + canonical promotion (Definition-of-done item 9):
- **`docs/compiler/soundness-and-coverage.md`** (the canonical home, status Design stub — the standing per-phase promotion target) — PROMOTE P8's content (Design→Incremental) into its existing `## Generalization` (deferred phases) section: the `ProofForwardingFact` extend-not-fork convention, the reachability-path-vs-cut-set-dominance fork (recorded un-settled), the type-checker typing-derivation retention convention, and the `RelationalCertificateWitness` reservation; AND **CREATE the `## Stated boundaries` section** (verified absent in the canonical doc today — P8 is the phase that first populates it, advancing it Design→Incremental from empty) to hold: the §1.4/F-6 fault↔diagnostic-correspondence cell + the `WriteToOmittedField`/`WriteToReadOnlyField` map-gap + the O-4 event-arg seam, each with its empirical-net (P7 BMC) route token.
- `docs/compiler/proof-engine.md` — cross-reference the generalization convention (the witness-plumbing pattern the graph/type phases extend) + the §1.4/F-6 cell as a stated boundary; note the `RelationalCertificateWitness` reserved-but-unbuilt slot.
- `docs/compiler/diagnostic-system.md` — record the §1.4 sufficiency gap (registered-vs-fires-at-every-site) as a stated boundary, distinct from `Precept0027`'s emission-site-presence floor and `Precept0030`'s §1.5 stamping surface; no new diagnostic code (P8 builds no analyzer per decision (2)(b)).
- `docs/compiler/README.md` — proof-engine-stage cross-reference: the generalization-convention reservation recorded (the §1.4/F-6 boundary + map gap noted as deferred, not closed). *(Discovered-during-planning mechanical CLAUDE.md routing.)*
- `docs/runtime/evaluator.md` — cross-reference the `WriteToOmittedField`/`WriteToReadOnlyField` map gap the `Evaluator.cs:60-113` forward-pointers describe, as a stated boundary with no fault-side backstop (type-side-verdict-interim).
- `docs/Working/soundness-case-2026-06-07.md` — §1.4/F-6 + the §1.5 residual cells: record that the §1.4 fire-at-every-site sufficiency cell and the O-4 seam are routed to the P7 BMC (the empirical-net home), and the map gap rides this record; mark F-6 as recorded-with-forward-pointer, not closed (BLOCKING per-phase soundness-case update — the soundness-case is the companion canonical artifact maintained per Definition-of-done items 7/9).
NO `precept-language-spec.md §0.4/§3A` / `precept-builder.md` touch (those are the P5/P6 conditional-totality promotion targets; P8 touches no language surface and no short-circuit semantics).

**Soundness class**: RECORD/CONVENTION-ONLY — neither Class A, B, nor C; P8 ships NO `Proved`-emitting capability and NO new obligation, so the checker-first sequencing constraint is satisfied vacuously (there is nothing to backstop). It is NOT Class A (it adds no obligation — it does not even move a site from false-clean to `Unresolved`), NOT Class B (it builds no checker — the graph/type checkers it reserves convention for are explicitly OUT), and NOT Class C (it widens no `Proved`). Its soundness contribution is purely RECORD FIDELITY: it ensures two deferred soundness cells (§1.4/F-6 fire-at-every-site + the `WriteToOmittedField`/`WriteToReadOnlyField` map gap) and the O-4 event-arg seam are surfaced-not-hidden (Definition-of-done item 8), each carrying its live backstop pointer. BACKSTOP DEPENDENCY: the boundaries P8 records are kept sound-by-rejection in the interim by the **P7 BMC** (the live discharger for the O-4 seam removal obligation and the §1.4 fire-at-every-site cell over the finite reachable state space), and the `RelationalCertificateWitness` slot it confirms is made mechanically-total by the **P2** `[HandlesCatalogExhaustively(ProofWitnessKind)]` exhaustiveness. The §1.4/F-6 cell is explicitly NOT backstopped by P1's `Precept0030` (different surface — §1.4 fault-correspondence vs §1.5 obligation-stamping, per the soundness-arch §1.4/F-6 disposition note). The BLOCKING SOUNDNESS GATE (exit (e)) for this record-only phase is the over-claim guard: it goes RED if P8 silently asserts closure of a cell it only records, if the recorded map gap diverges from source (`FaultCode.cs`/`DiagnosticCode.cs`), or if any boundary lacks its empirical-net route — the record-phase analogue of the no-unchecked-`Proved` gate.
