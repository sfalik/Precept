agentId=a3c1d89a1174a08d2 ts=

Cleaning up the probe verdict is done — I have everything needed. Findings, severity-first.

---

**Adversarial review — cell validator (slice 2). 9 findings; 3 blocking.**

**1. BLOCKING — silent pass for a "defined" cell with no obligation, contract, witnesses, or suggestions.** `tools/Precept.MatrixTools/CellValidator.cs:495-497` — `CheckWpRecomputation` returns silently (no finding, no skip) when a defined cell has no `obligation` object; `CheckNearMissPairing` (:332-334) and `CheckDecisionProcedures` (:472-474) also silently return when `witnesses`/`dischargeContract` are absent. The matrix defines the cell (§ The cell): "A **defined cell** is: 1. Obligation schema … 2. Discharge contract … 3. Suggestion schema … 4. Witnesses — at least one concrete triple". The component's own header (:20-22) claims "Every cell the WP check cannot recompute yields a named skip record — … never a generic answer, never silence," and the owner directive (normal-form draft, open item 7) requires every refusal to name "the specific open decision or written exclusion it rests on." **Verified empirically**: a probe cell with `"status": "defined"`, coordinates, and a respellability block but nothing else produced **0 findings, 0 skips**. The README records "witness-triple minimum: *Pending*" and "contract coverage: *Pending*", but obligation-presence appears in no pending bullet, and the WP check — claimed **Built** — has a silence path inside its own domain. Failure scenario: a population-pass generator drops the `obligation` object from a defined cell; the validator is green and the cell heads to ratification with no proof condition at all.

**2. BLOCKING — a discharge that omits the `addition` property entirely evades near-miss pairing.** `tools/Precept.MatrixTools/CellValidator.cs:352-354` — `hasAddition` is `TryGetProperty("addition") && ValueKind == String`, so an *absent* property is treated as the schema's null-means-standing case. The schema (`cell.schema.json` § dischargeWitness) does **not** list `addition` as required and says "Null **only** for a standing discharge (no addition exists to weaken, so no near-miss is owed)"; the `kind` description ties standing to `default-constant-fold`. The matrix (§ The cell, item 4): "Near-miss, one per discharge addition"; "Both verdict polarities — must-accept and must-reject — are first-class" (§ Vocabulary). **Verified empirically**: a `kind: "guard"` discharge with the property omitted produced **0 findings** — schema-legal JSON, validator-silent, and the must-reject polarity for that discharge silently disappears. The validator also never checks that a null/absent addition carries `kind: "default-constant-fold"`. Fix: absent-or-null addition with kind other than `default-constant-fold` = error.

**3. BLOCKING (doc-sync/false-green) — nothing machine-executes `cell.schema.json`, and the gap is recorded nowhere.** No schema-evaluation package or code exists in either csproj or anywhere in MatrixTools/tests (grep verified). Yet `docs/Working/obligation-discharge-matrix-2026-07-19-cells/README.md:39` states: "`cell.schema.json` (JSON Schema, draft 2020-12) **enforces** field shapes, the per-case-shape coordinate arms, dated owner rulings, provenance completeness on live-verified claims, validity-argument names…, conflicted cells citing at least two sources, and the disposition rules". **Verified empirically**: a citation with invalid kind `"blog-post"`, an `owner-ruling` citation with no date, and an `open` cell with no citations all pass with **0 findings**. Matrix ratification layer 2 says "Machines verify every cell — … No cell ships unchecked"; today the schema-expressible half of the checking regime is enforced only by a human's editor. Cheapest honest fix inside this slice: add a pending bullet "schema conformance is not machine-checked" to the README (or evaluate the schema inside `ValidateFile`). Note interaction: running the schema would also catch finding 1's hollow cell and most of finding 4, but **not** finding 2 (`addition` isn't schema-required).

**4. Medium — "citation present per cell" quietly became "citation resolution".** Matrix § Storage: "A cell validator runs from day one …: disposition totality over the product, **citation present per cell**, axis values…". The validator only validates citations that exist (`WalkCitations`, `CellValidator.cs:243`); presence is checked nowhere, and README bullet :44 reframes the check as "**Citation resolution** — … *Built*" with no Pending record for presence. Empirically: a cell with no `citations` at all → 0 findings. This is the redefine-the-bar pattern; record presence as pending or check it.

**5. Low-medium — out-of-range cited line is a Warning, not an Error.** `CellValidator.cs:304-309` downgrades a `:line` past EOF to Warning ("the cited text has moved"). Matrix ratification layer 2 requires "citations present and **resolving**"; a line that doesn't exist does not resolve. `HasErrors` (the presumable ratification gate) ignores warnings. Deliberate and README-documented, but it deviates from the matrix's letter — surface to the owner rather than settle it in code.

**6. Low — heading scan counts `#` lines inside fenced code blocks.** `CellValidator.cs:314-317` treats any line matching `^#{1,6}\s` as a markdown heading. A cited file whose bash/code fences contain column-0 `# comment` lines (e.g. `CLAUDE.md`'s build block) can make a genuinely unique anchor spuriously "ambiguous" or resolve a bogus one. Latent; no current cell trips it.

**7. Low — WP key check is membership over the plan×spec cross-product, not correspondence.** `CellValidator.cs:601-603` accepts the stored key if it matches *any* recomputed WP of *any* (plan, spec) pair — including specs of unrelated rules in the base program. A stored key that actually belongs to a different obligation of the same program would pass. Exact for today's single-rule bases; weakens silently as bases grow.

**8. Low — misleading error text when the named event's rows are all write-free.** `RowPlans` (`CellValidator.cs:664,670`) filters out empty-action rows, so a matching no-write row yields "the base program has **no row** for event(s)…" (:581-583) — the row exists; it frame-preserves. Wrong fact in an error message that authors will act on.

**9. Info — verification of the builder's literal claims at the current tree.** `dotnet build tools/Precept.MatrixTools/Precept.MatrixTools.csproj` → "0 Warning(s), 0 Error(s)" (confirmed). `dotnet test test/Precept.MatrixTools.Tests/` **fails to compile at the current tree**: untracked `ProseGeneratorTests.cs` (a parallel component's test-first file) references `CellProseGenerator`, which exists nowhere (CS0103 ×4). The builder's "Passed: 108" is therefore not reproducible as-is; with that one file excluded (isolated scratchpad copy, symlinked repo root), the suite gives literally: `Passed! - Failed: 0, Passed: 152, Skipped: 0, Total: 152` — 152 because parallel components' tests landed after the builder's run. `CellValidatorTests.cs` carries exactly 28 `[Fact]`s (confirmed). Claims verified true: skip reasons are specific and named (multi-write skip carries the calculator's verbatim refusal naming write-plan decomposition, `WpCalculator.cs:212-216`; fault/editable-ingress arms at `CellValidator.cs:512-521`); axis vocabularies are read from the schema at runtime with no parallel copy (`SchemaVocabulary.Load`, :746-775); the real cells directory yields 0 findings and exactly 1 named skip; the three preservation cells recompute to their stored keys; `docs/language/business-domain-types.md` diff is a 1-line change untouched by this component. Fixtures live under `test/`, not the real cells directory. No transient-doc references in code comments.

**Verdict: FIX — blocking: 1, 2, 3.**

Key files: `/home/sfalik/source/repos/Precept/tools/Precept.MatrixTools/CellValidator.cs`, `/home/sfalik/source/repos/Precept/test/Precept.MatrixTools.Tests/CellValidatorTests.cs`, `/home/sfalik/source/repos/Precept/docs/Working/obligation-discharge-matrix-2026-07-19-cells/README.md`, probe fixtures demonstrating findings 1/2/3/4 at `/tmp/claude-1000/-home-sfalik-source-repos-Precept/eb473e68-b1f1-4b97-9b68-85f3ae5d44b1/scratchpad/probe/cells/probe-hollow.cells.json` and `.../probe2/cells/probe-citations.cells.json`.