agentId=a2d4b45442973c4b2 ts=

FILES WRITTEN (all absolute paths; no C# touched, no commits made):
- /home/sfalik/source/repos/Precept/docs/Working/obligation-discharge-matrix-2026-07-19-cells/cell.schema.json
- /home/sfalik/source/repos/Precept/docs/Working/obligation-discharge-matrix-2026-07-19-cells/README.md
- /home/sfalik/source/repos/Precept/docs/Working/obligation-discharge-matrix-2026-07-19-cells/family-1-numeric-single-field.cells.json

VERIFICATION (literal results):
- dotnet build tools/Precept.MatrixTools/Precept.MatrixTools.csproj: 0 Warnings, 0 Errors
- dotnet test test/Precept.MatrixTools.Tests/: Failed: 0, Passed: 80, Skipped: 0, Total: 80
- Data file validates against cell.schema.json with python jsonschema (Draft 2020-12): PASS, 4 cells
- Negative enforcement checks: 8/8 mutations correctly rejected (missing obligation on a defined cell, missing decision procedure, citation without ref, live-verified provenance without commit hash, missing corpus-measurement gate, out-of-vocabulary axis value, empty disposition without pruning derivation, owner ruling without date)

KEY DESIGN DECISIONS (each with the rejected alternative):

1. File = one family object with file-level respellability + citations + definitionVersion, cells nested inside. Rejected: the precedent's bare flat array (exhaustive-gap-analysis *.cells.json). The matrix rules that the respellability verdict is stated once per contract family and inherited; a flat array forces per-cell duplication of a family-level fact. Kept from the precedent: stable path-style IDs, per-record citations, expected-outcome fields.

2. Coordinates as a JSON-Schema oneOf discriminated on caseShape (rule-write / fault / editable-ingress / structural), each arm with its own required axis set. Rejected: one flat record with nullable axis fields — the matrix's case-shape table is explicitly a DU of axis sets, and nullable-fields-on-a-flat-record is the exact shape the project's own DU rule forbids.

3. Committed power vs built power as separate fields on discharge witnesses: modelStatus ("provable-under-model") vs builtStatus ("proven-today"/"unresolved-today") vs provenance (live-verified with tool+date+head, or model-derived). Rejected: one combined status string mirroring the doc's prose ("provable under want; Unresolved today ✓v") — the matrix explicitly says the two are never conflated, and a combined string can't be validator-checked.

4. Discharge-contract entries carry premiseClasses as an array (supports Base B's joint (d)+(b) discharge) and admit an empty array (the establishment constant-fold consumes no authored premise). Rejected: single premiseClass field (can't represent joint discharge) and minItems 1 (would force inventing a premise class for the defaults fold).

5. decisionProcedure and validityArgument are required on every contract entry. Rejected: optional — the matrix makes both ratification gates ("without one, the reject side cannot be tested even in principle"; "no cell ratifies whose derivation cites an argument-less rule"), so the schema enforces presence; adequacy stays a human review.

6. Near-misses pair by pairsWith → discharge id; standing discharges (addition: null, kind default-constant-fold) owe no near-miss. Rejected: embedding the near-miss inside its discharge (loses the doc's separate must-reject rows) and requiring a near-miss unconditionally (would force inventing one for the establishment cell — see ambiguity 1).

7. Disposition is a per-cell object with conditional requirements (empty→pruningDerivation; open/deferred/conflicted→citations; defined→full content, relaxed to the weaker witness-only shape for structural cells). Rejected: encoding disposition implicitly by which fields are present — the matrix requires pruned/open cells to be written, findable records.

8. Citations are typed ({kind, ref, date?, note?}, kind enum includes owner-ruling which requires a date). Rejected: the precedent's single specCite string — the matrix distinguishes want-doc lines, spec lines, and dated owner rulings, and the validator must be able to check "owner rulings carry dates" mechanically.

9. wp.canonicalKey carries the mechanized normal-form key (e.g. "(le (+ arg(Add.Amount1) arg(Add.Amount2)) #1000)") with canonicalKeySource naming the pinning test in WitnessFamily1Tests.cs. Rejected: omitting it — the Storage ruling names "WP recomputation where mechanized" as a validator check, and the key is the hook that makes it possible.

10. definitionVersion is a plain string ("obligation-discharge-matrix-2026-07-19 rev 6 (2026-07-20, draft)"). Rejected: a structured semver object — the amendment rules imply major/minor versioning eventually, but no version scheme is owner-ruled yet; a string records the fact without inventing the scheme.

AMBIGUITIES FOUND (recorded in cell/README notes, not papered over):

1. The standing establishment cell has no base/near-miss triple in the matrix, though "The cell" §4 requires "at least one concrete triple" per defined cell. I recorded it exactly as the doc states it (a standing discharge, no rejecting base) and read "one near-miss per discharge addition" as owing nothing when there is no addition. A rejecting-default base (e.g. default 2000) would be trivial to add but is not in the doc.

2. The write-site axis (axis 3) has no member for the establishment-over-defaults site. The cell uses a file-local value construction-defaults, flagged in the schema description, the cell notes, and the README as an open axis-vocabulary item.

3. Base B's applicable-class set is not enumerated. The doc states only "(d) is available"; its decision procedure names only the (d)+(b) shape, and the guard-match validity argument names "Bases A and C" only — yet the calculator's test suite shows a restated guard mechanically matches Base B's WP (BaseB_RestatedGuardMatchesWp). Whether class (c) is applicable in Base B is genuinely unstated. Recorded as stated, flagged in cell notes.

4. Base C's guard addition carries "provable under want" with no today-status and no ✓v mark (unlike Base A's guard addition, marked "Unresolved today ✓v"). Its guard is also multi-term, so the same "multi-term fact shape unbuilt" note plausibly applies — but the doc doesn't say it, so builtStatus is left absent with a note.

5. Base rows carry no explicit ✓v marks (the tables mark addition statuses only), though the family is titled "live-verified slice". Per the doc's own rule "Unmarked = model-derived", bases are recorded model-derived, with a note.

6. Base B's event declaration is not given in the matrix (only the row `on Subtract -> ...` and the addition `Amount as decimal nonnegative`); I reconstructed `event Subtract(Amount as decimal)` (unconstrained) as the base the addition modifies, noted in the cell. Note the test fixture's BaseB includes nonnegative in its base because it tests the discharged form.

7. Suggestion schemas are stated for Base A only ("The suggestion schema names both", with the Vocabulary's per-class schema texts). Bases B and C have none stated; recorded as absent with notes rather than derived.

8. Base rejections' named missing-class lists: solid for A ("naming both classes") and C ("no other class applies"); for B the doc doesn't state one — I recorded ["b"] (the class the stated addition supplies) with a note.

Skip-record discipline honored: no cell or schema field answers anything with a generic "not supported"; the N9-division/N14 ruled-but-unimplemented gap is carried as an explicit citation note on every cell's normalization reference, and the unimplemented-vocabulary gap (construction-defaults) is a named flagged item.