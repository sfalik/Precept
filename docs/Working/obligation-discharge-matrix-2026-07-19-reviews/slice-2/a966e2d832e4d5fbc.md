agentId=a966e2d832e4d5fbc ts=

All findings applied and verified. Final report:

FINDINGS APPLIED — all against `docs/Working/obligation-discharge-matrix-2026-07-19-cells/` (schema `cell.schema.json`, data `family-1-numeric-single-field.cells.json`, `README.md`):

1. (HIGH) `suggestions` added to the defined non-structural required list; Base B and Base C populated with the Vocabulary's own per-class schema texts (`<WP>` placeholder form, cited to § Vocabulary); README's "Recorded only where the matrix states it" sentence deleted and the bullet restated as a required component. **Deviation from the fix-as-written**: an unconditional `minItems: 1` would invalidate the establishment cell, whose applicable-class set is empty — no authorable premise exists, so "per applicable premise class" is vacuous there and inventing a suggestion would violate the no-invention rule. Implemented instead: `suggestions` always required on defined non-structural cells, plus a conditional clause enforcing `minItems: 1` exactly when `applicablePremiseClasses` is non-empty; the establishment cell carries `"suggestions": []` with a note stating why. Base B's suggestion addresses class (b) only, with a note recording that the contract's (d) half is the standing inductive hypothesis, not an authorable premise (consistent with the base's `missingPremiseClasses: ["b"]`).

2. (HIGH) README's validator-owned list expanded to twelve named checks: disposition totality, citation resolution (under the new pinned ref forms), axis-value catalog cross-check, witness-triple minimum (with the establishment base-less exception recorded as a flagged ambiguity), near-miss pairing, contract/suggestion coverage of the applicable-class set, cell-id uniqueness within and across files, validity-argument presence per cited rule, respellability resolvability + respelling-when-yes-and-bandMember, evidence/corpusMeasurement consistency, WP recomputation via MatrixTools, open-dependency resolution. Cheap schema tightenings added: defined non-structural cells now require `dischargeContract` minItems 1 and `witnesses.discharges` present with minItems 1; the `respelling` "required in spirit" description replaced with explicit validator ownership.

3. (MEDIUM) Provenance attestation scope stated twice: in the shared `provenance` description (attests status claims, never `expected`) and in a site-specific description on `dischargeWitness.provenance` (live-verified + unresolved-today = a run demonstrating the engine does NOT prove the addition; `expected` is committed power under the model). README witnesses bullet carries the same sentence.

4. (MEDIUM) Chose the "pin a machine-checkable convention" option (the lighter of the two offered): `ref` is now one of three forms — path, `path:line`, or `path § anchor` where the anchor must be a substring of exactly one markdown heading in the cited file; sub-locations go in `note`. All 16 data instances with parenthetical prose anchors swept (`§ Vocabulary (sound-but-unprovable band)` → `§ Vocabulary` + note, etc.); convention documented in schema description and README.

5. (MEDIUM) `validityArgument` → `validityArguments`: array, minItems 1, enum of the seven names from the matrix § Validity arguments (bold lead-ins before parentheticals). Establishment contract now carries both `["Literal constant-fold for defaults", "Establishment over defaults"]`. Intended catalog anchors noted on `derivation` (CertificateStepKind/CertificatePremise) and `strategy` (ProofStrategy, validator-cross-checked).

6. (MEDIUM) Optional `openDependencies` (pattern `^[QS][0-9]+[a-z]?$`, shared `$def`) added on both cells and contract entries; populated on Base B's contract entry as `["S4", "Q5"]` with a citation to § Validity arguments explaining the load-bearing dependency (computed-field transitivity rides under S4, per the matrix's ⧖ marking). Populated at the contract entry (the consuming locus) rather than duplicated at cell level — nested fields are still mechanically findable.

7. (LOW) `application` DU added (row-guard append keyed by event name; event-arg-declaration replacements keyed by event+arg+full declaration) on discharge and near-miss witnesses; populated on all six addition-bearing witnesses in Family 1.

8. (LOW) Conflicted disposition now requires `citations` minItems 2 (new conditional branch).

9. (LOW) Establishment `wp.statement` is now `"0.0 <= 1000"`; the constant-fold commentary moved to the cell's notes. (Verified independently: `CanonRender.Key(CanonBool(true))` renders `"true"`, `CanonRender.cs:16`, so the `canonicalKey` stands; the test-pins-value-not-string tightening is deferred to the validator per the finding's own parenthetical.)

VERIFICATION: both JSON files parse; the data file validates against the updated schema with 0 errors (jsonschema Draft 2020-12); 8 negative probes each fail as intended (missing suggestions, empty suggestions with non-empty classes, empty witnesses, empty contract, 1-citation conflicted, unknown validity-argument name, malformed open-dependency id) and 2-citation conflicted passes. `dotnet build tools/Precept.MatrixTools/Precept.MatrixTools.csproj`: Build succeeded, 0 Warning(s), 0 Error(s). `dotnet test test/Precept.MatrixTools.Tests/`: Failed: 0, Passed: 80, Skipped: 0, Total: 80. No code, samples, src/Precept, or research/ files touched; nothing committed.