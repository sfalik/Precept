agentId=a1105d91b5bf119d2 ts=

Review complete. All verification was against the live working tree: I rebuilt the tool, reran the converter and runner (outputs byte-identical to the on-disk artifacts), reran the tests, and probed the runner with adversarial manifests.

**Verification of builder claims first**
- Build: `dotnet build tools/Precept.MatrixTools/Precept.MatrixTools.csproj` — 0 errors.
- `dotnet test test/Precept.MatrixTools.Tests/` **does not compile at the current tree**: `ProseGeneratorTests.cs` (an untracked file from a different slice) references a nonexistent `CellProseGenerator` (CS0103 at lines 36, 193, 218, 248). Not this component's file, but the stated gate command fails as the tree stands. Excluding only that file in a shadow copy: **152 passed, 0 failed** overall; filtered to this component: **21 passed, 0 failed** (13 CellTestConverterTests + 8 ManifestRunnerTests — count claim verified; "written before the implementation" is unverifiable, nothing is committed).
- Regenerated `family-1-numeric-single-field.manifest.json` and `.manifest-results.json` via the CLI — both **byte-identical** to disk. Tally: 11 rows — Pass=8, ExpectedFailure=2, UnstatedObservation=1; 1 skip. Builder's per-row table is accurate.

**Findings (severity-first)**

1. **[BLOCKING] Misattributed skip reason for calculator-declined shapes — the named record makes a false claim.** `CellTestConverter.cs:374-400` (`MatchSpecByStoredKey`) silently discards every `WpNotSupported` result, and `CellTestConverter.cs:331-333` then reports: *"the stored canonical WP key '…' does not recompute from any obligation of the base program through the WpCalculator — the cell data and the base program disagree."* Demonstrated live: a two-write base (the standing `OpenAccount`-shaped case) produces exactly that skip, when the true cause is the calculator's multi-write refusal — the data and program do not disagree. Same path swallows the N9-division/N14 ruled-but-unimplemented gaps the task brief explicitly requires to surface as *named* skips: a cell whose stored key depends on those rules would skip with this false disagreement claim. The owner directive (quoted in `normal-form-draft-2026-07-19.md:66`): "every refusal names the specific open decision or written exclusion it rests on." A confidently wrong reason is worse than the banned generic one. The `specs.Length == 0` branch (`CellTestConverter.cs:319-325`) has the same defect for fault-family cells: "the base program carries no rules … no obligation to key on" is false for a fault cell whose obligation is catalog-minted, not rule-derived.

2. **[BLOCKING] Two discharge-contract elements dropped silently — no row field, no skip, no head-note.** Matrix line 290: "base → expected diagnostic naming the obligation **and all applicable missing-premise classes** …; base + each addition → clean compile via the expected strategy, **premise list recorded**." The cell data carries both (`expected.missingPremiseClasses: ["b","c"]` etc., `expected.premiseListRecorded: true`), and the matrix § Verification asymmetry sets the coverage bar ("part of the definition's verification machinery, not a convenience"). The converter reads neither (`CellTestConverter.cs:192-208`, `258-269`), the runner checks neither (`ManifestRunner.cs:119-145`), and the manifest carries no trace (grep confirms). Both are genuinely uncheckable at HEAD (`ProofObligation` at `src/Precept/Pipeline/ProofLedger.cs:24-31` carries no premise list; diagnostics carry no premise-class structure) — which is exactly what the component's own two-expectations pattern exists for: a per-row named "unverifiable at HEAD" record, like the head/expected split it correctly built for accept/reject. Instead the contract halves vanish; a manifest reader cannot see they went unverified.

3. **[BLOCKING] Runner false-greens on invalid witness programs.** Reject rows match on `HasErrors && unresolved` only (`ManifestRunner.cs:124-126`), and near-miss rows hardcode `RequireOnlyObligationDiagnostics: false` (`CellTestConverter.cs:307`). Demonstrated live: corrupting the guard-nf near-miss to `when Add.Amount1 <= "oops"` (a TypeMismatch, not a weakened premise — proof runs unconditionally in `Compiler.Compile`) verdicts `[Pass]` with `errorCodes=[NumericOverflow, TypeMismatch]`. A near-miss is by definition base + a weakened-but-well-formed addition; a converter application-regex bug in a future family would be masked as a passing rejection. The converter test `EveryEmittedProgram_HasNoPreProofErrors` covers only family-1's generated rows at conversion-test time — the runner, which the matrix names as the sole reject-side conformance machinery, has no such guard.

4. **[BLOCKING] "Same obligation" is matched coarser than the definition's obligation identity.** The matrix's minting rule keys obligations to "that (constraint, **site**) pair"; the near-miss contract is "still rejects, naming the **same obligation**." `ObligationKey` (field, bound kind, bound, context class) cannot distinguish write sites, and `Matches` (`ManifestRunner.cs:179-213`) accepts any `EventHandlerContext`/`TransitionRowContext` obligation on the same field+bound. Demonstrated live: a program whose target `Add` handler is **fully discharged** (250/750 bounds) but which carries a second unproven `Other` handler verdicts `[Pass]` as a near-miss rejection — the "same obligation" named is a different site's. Benign for family 1 (single-site bases), but the converter emits no guard (e.g. skip or single-write-plan check) that confines it there. Related: the IntervalContainment arm compares `DeclaredMax` only for an upper key, so a min-side breach of the same field's interval obligation also satisfies an upper-bound key.

5. **[Medium] Base minimality is checked weaker than defined.** Matrix cell item 4: the base "rejects *only* for the target obligation … with no other diagnostics." The runner's check (`ManifestRunner.cs:114-126`) is "no **non-proof-stage** errors" — a base with a second, unrelated unresolved proof obligation (e.g. another field's failing default) still passes, and warning-severity diagnostics are ignored entirely.

6. **[Medium] `contextClass` derivation has no reject arm — silent shape swallow for future write-site categories.** `CellTestConverter.cs:177-180` maps `construction-defaults` → FieldDefault and *everything else* → WriteSite, but the schema enum also carries `editable-edit`, `entry-hook`, `collection-action`, `computed-transitive`, whose ledger contexts (`StateHookContext`, etc., `ProofLedger.cs:86-92`) the WriteSite matcher arm (`ManifestRunner.cs:181-188`) can never match. Such a cell converts silently, then Fails with "obligation minted=False" — a wrong-cause failure instead of a named skip. Same directive family as finding 1.

7. **[Low] No negative-control tests on the structural matcher or runner verdict paths.** All 8 runner tests assert family-1 happy outcomes; no test pins `Matches` rejecting a wrong key (wrong field/bound/kind/context), and no test exercises `Fail`/`UnexpectedPass` on a real compilation. A matcher that ignored bound and kind (matching on field alone) would pass the entire suite. The CLI subcommands' exit codes are also untested.

8. **[Low] Doc-sync.** Matrix line 290 still ends "Not started; the definitional pass comes first" — the conversion has now started and run; stale in the same doc the component implements. The cells-folder `README.md` documents the validator but not the converter/runner or the two generated artifacts now sitting in that folder (`manifest.json`, `manifest-results.json`) and their regeneration commands.

9. **[Low] Results artifact carries no linkage.** `ManifestRunner.ToJson` (`ManifestRunner.cs:66-67`) emits a bare `RowResult` array — no familyId, no `definitionVersion` (which the Storage ruling says "rides in the data" for certificate coupling), no reference to the manifest that produced it. A results file cannot be tied to a definition revision after the fact.

**What held up under attack**: manifest and results reproduce byte-identically from the CLI (generated-not-hand-edited claim verified); the family-1 skip record is accurate for its actual cell; head expectations (`mustPass`/`expectedFailure`/`unstated`) faithfully track the cell data's `builtStatus` vocabulary (verified against `cell.schema.json:298`) and are never invented; verdicts read only ledger records and codes, never message text; obligation-key derivation does real recomputation work (a wrong stored key demonstrably skips); no transient-doc references in the new code's comments; no parallel catalog copies (desugaring stays catalog-derived via `ObligationEnumerator`).

**Verdict: FIX — blocking findings 1, 2, 3, 4.**