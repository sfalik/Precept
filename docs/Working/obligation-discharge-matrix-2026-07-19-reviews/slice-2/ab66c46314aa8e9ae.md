agentId=ab66c46314aa8e9ae ts=

Verification complete; restoring nothing further — the tree is byte-identical to how I found it (ProseGeneratorTests.cs checksums match; only build artifacts and scratchpad files were touched). Findings, severity-first.

## Verified claims (builder's report holds on the numbers)

- `dotnet build tools/Precept.MatrixTools/Precept.MatrixTools.csproj` → 0 Errors. Full test project (with the parallel session's uncompilable `ProseGeneratorTests.cs` temporarily parked): **Passed: 152, Failed: 0, Skipped: 0**; `CorpusMeasurementTests` alone: **Passed: 23, Failed: 0**. 23 `[Fact]`s counted in the file.
- Re-ran `measure-corpus samples`: 78 files, 59 with explicit rules, 1 error file, 89 licensed / 10 respell / 0 no-respelling / 2,410 out-of-scope, 5,919 frame pairs, and the per-reason splits (569/567/530/315/208/155/34/22/9/1, skip sub-split 490 accessor + 40 satisfaction-less) all reproduce exactly.
- Named-skip discipline holds where implemented: every out-of-scope row carries a specific reason constant; the two ruled-but-unimplemented normal-form rules surface as named skips in preservation (verified against `gap-division.precept`, `gap-negated-ordering.precept`); establishment's missing negated-ordering check is provably verdict-neutral (Canon.Not folds `not <bool>`, Canon.cs:70, so ground cases fold before the predicate matters).
- The builder's caveat about `-p:DefaultItemExcludes` is worse than stated: that property override suppresses the SDK's own obj/bin excludes on **every** transitively built project, so with stale `obj/` dirs the invocation fails on `Precept.Analyzers` duplicate-AssemblyInfo errors. Their number was honest but the recipe is unreliable.

## Findings

**1. FIX — Interval closure licenses derivations the matrix never states; 10 of the 14 preservation-licensed corpus rows rest on them.** `tools/Precept.MatrixTools/CorpusMeasurement.cs:522-526` (lower-bound branch of `TryIntervalClosure`) and `:625-632` (`TermBound` sourcing lower bounds from `DeclaredMin`/`Nonnegative`/`Positive`). The matrix's stated class-(b) decision procedure is upper-bound only: "Class (b): exact-decimal upper-bound sum over the args' declared `max` bounds; an arg with no upper bound, or a computed sum exceeding the rule bound, fails the class" (obligation-discharge-matrix-2026-07-19.md:201); the band ruling licenses guard conjuncts into "the **same** interval arithmetic that discharges arg-modifier bounds" — i.e. the same upper-bound sum. No generative sentence states a lower-bound mirror for preservation WPs. Yet in the real report, 10 of 11 "arg-bound interval sum" rows are lower-bound shapes (`0 <= ReduceLimit.NewLimit`, `0 <= AdvanceDay.DayNumber` ×9), and the single "guard bound-conjunct interval" row is also lower-bound (`0 <= SeatsReserved`). All are semantically sound — but the harness's own header promises "a derivation is applied only where its stated decision procedure applies" (CorpusMeasurement.cs:23-24), and the matrix's exact contract makes accepting unlicensed programs nonconforming (matrix:40). Failure scenario: the family respellability verdict ratifies against a licensed count inflated by an unstated rule; when the written definition is implemented as stated, these 11 programs reject and the ratified evidence is wrong. Fix direction: either the owner states the lower-bound rule in the matrix first, or these rows get their own named bucket (e.g. "closable only by an unstated lower-bound interval rule"), not "licensed as written".

**2. FIX — The `number` (IEEE double) lane is admitted into the measured family and gets "licensed" verdicts the matrix's validity arguments exclude.** `CorpusMeasurement.cs:319` (`ScopeReason` accepts `TypeKind.Number` fields) and `:618` (`TermBound` accepts Number args into interval bounds). The matrix: "the argument covers the exact lanes (`integer`, `decimal`) only: on the `number` lane runtime addition rounds (IEEE 754…), so extending the rule there requires an outward-rounding side condition on the computed bound, not yet stated" (matrix:144). Empirically demonstrated: a probe file (`field Ratio as number`; `event Assign(X as number max 500)`; `set Ratio = Assign.X`) classifies `preservation: on Assign` as **licensedAsWritten / arg-bound interval sum**. Zero corpus rows today (`samples/` has no `as number` fields), so the published headline is unaffected — but the instrument produces wrong-direction verdicts on that lane, the exact failure the header calls unsafe. Related mislabel: `IsUnfoldedLiteralDivision` (`:695`) would tag number-lane literal division "owner-ruled but not yet implemented" when N9 rules number division **out** permanently (normal-form-draft:28); `CanonBinary` carries no lane info, so the predicate cannot distinguish.

**3. FIX — Catalog-rule violation: hardcoded parallel copy of modifier bound semantics.** `CorpusMeasurement.cs:629-632` (Nonnegative → bound 0 inclusive, Positive → bound 0 exclusive) and `:683-689` (`HasDeclaredNonnegativity` re-encodes the same). The Modifiers catalog already declares exactly this as `ProofSatisfaction.Numeric(SelfValue, GreaterThanOrEqual/GreaterThan, Constant(0m))` (src/Precept/Language/Modifiers.cs:69-98), and `ObligationEnumerator.DesugarModifier` proves it is mechanically derivable. CLAUDE.md: "Never switch on `*Kind` enum identity to dispatch per-member behavior… That behavior belongs in catalog metadata." Failure scenario: a catalog change to a sign modifier's satisfaction (or a new sign-bounded modifier) silently diverges from the harness's private table, corrupting licensed/respell counts with no test tripping.

**4. Non-blocking — Test-coverage claim is overstated; 8 of 18 reason buckets and 1 of 7 derivations have no pinning test.** `test/Precept.MatrixTools.Tests/CorpusMeasurementTests.cs:11-14` claims "every bucket is pinned by a fixture"; the builder's report says "16 fixture files, one per classification bucket" and "17 named reason constants" (actual count: 18). Untested: `ComputedMention`, `NonPrimitiveNumeric`, `RuleShapeOutsideFamily`, `NonSetAction`, `EditDeclarationSite`, `EstablishmentNotGround`, `CanonicalSurface`, `ErrorResidue`, and the `PreservationLiteralFold` derivation. Several fire at volume in the real corpus (155 non-primitive-numeric, 22 computed-mention, 9 edit-declaration): a `ScopeReason` regression rerouting hundreds of rows would leave all 152 tests green — a genuine false-green surface in ratification machinery.

**5. Non-blocking — "rule-bearing" vocabulary collision with the matrix's term of art.** The report's `ruleBearing` field and console line (`CorpusMeasurement.cs:287`, Cli.cs measure output) report 78 under a broad private definition (any enumerable entry or ensure), while the matrix's ratification layer 4 defines the corpus as "the 59 rule-bearing files in `samples/`" (matrix:284) — which maps to the report's `filesWithExplicitRules`. A ratification reader matching doc text to report output hits 78 vs 59.

**6. Non-blocking — latent crash path instead of a named record.** WP computation is not exception-wrapped: `ClassifyEstablishment` (`CorpusMeasurement.cs:352`) and `ClassifyPreservation` (`:392`) call into `Canonicalizer` on every field default and the write RHS; a `NotSupportedException` there (unlike the rule condition at `:212-219` and the guard at `:398-405`, which are wrapped) aborts the entire directory measurement. Currently unreachable on the shipped surface (defaults are literal/typed-constant; all `OperatorKind` members have canonical forms — verified), so it is fail-loud-if-ever, not silent, but it is the crash-shaped sibling of the banned generic refusal.

**7. Note — mixed-closure derivation label can misattribute.** If some WP conjuncts close by guard-fact membership (`:506-510`, sets `usedGuard`) and the rest by declared arg bounds only, the row is labeled "guard bound-conjunct interval coverage" though no guard *bound conjunct* fed the interval. Evidence-row label imprecision only; both premises are real.

**8. Note — ground-false WPs are filed under the matrix's third ratification bucket.** `NoLicensedRespelling` (`:366-371`, `:414-420`) is the layer-4 bucket that "routes to the owner as a surface decision" for *sound* band members; a WP-false program is unsound (the fixture rightly carries compile errors). Zero corpus rows today; the row Detail is honest ("the fix is a data change, not a premise"), but the bucket name conflates the two populations.

## Verdict

**FIX — blocking: 1, 2, 3.** The counts, named-skip discipline, conservative pre-state-hypothesis narrowing, and frame-pair handling all check out, and every numeric claim in the builder's report reproduced exactly. But the harness licenses through two derivations the written definition does not state (findings 1-2 — 11 of the 89 headline licensed rows affected, plus a demonstrated wrong-lane verdict), which is precisely the evidence-integrity property this slice exists to guarantee, and finding 3 violates a project non-negotiable with a live drift path.