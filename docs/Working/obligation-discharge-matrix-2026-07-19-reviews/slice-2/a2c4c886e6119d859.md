agentId=a2c4c886e6119d859 ts=

All evidence is in. Findings, severity-first.

**Build/test evidence (literal):** `dotnet build test/Precept.MatrixTools.Tests/` — Build FAILED, 4 errors (CS0103 `CellProseGenerator` does not exist: ProseGeneratorTests.cs lines 36, 193, 218, 248). `dotnet test test/Precept.MatrixTools.Tests/` — 0 tests executed (compile failure); the failure blocks the entire MatrixTools test project, including the other slice components' tests (CellTestConverterTests, CellValidatorTests, CorpusMeasurementTests, ManifestRunnerTests) and all pre-existing MatrixTools tests.

**Findings**

1. **BLOCKER — The prose generator was never written; only its tests exist.** `test/Precept.MatrixTools.Tests/ProseGeneratorTests.cs:36` calls `CellProseGenerator.RenderFile(...)`, but no `CellProseGenerator` class exists anywhere in the repo (verified by repo-wide grep for the class name and for "Prose" under `tools/`; `tools/Precept.MatrixTools/` contains no such file, tracked or untracked). Matrix doc contract (`docs/Working/obligation-discharge-matrix-2026-07-19.md:263`): "**Human-readable cell tables are generated from the data** and never hand-edited (the `tmLanguage` discipline), drift-checked by a test." Neither half is delivered: no generator, and the drift check cannot run. Failure scenario: the slice ships as test-only vaporware and its compile error takes down every other test in the project — 0 of the project's tests can run until this is fixed.

2. **BLOCKER — The `render-cells` CLI subcommand does not exist.** `ProseGeneratorTests.cs:242` invokes `Cli.Run(["render-cells", dir])` and expects exit 0, but `tools/Precept.MatrixTools/Cli.cs:17-22` dispatches only `convert-cells`, `run-manifest`, `measure-corpus`; `render-cells` falls through to the usage error (`args.Length != 1` → exit 2). The drift test's own failure message (`ProseGeneratorTests.cs:48-49`) instructs regenerating via `render-cells` — an instruction that cannot be executed.

3. **BLOCKER — The committed generated artifact is missing.** `docs/Working/obligation-discharge-matrix-2026-07-19-cells/generated/` does not exist (verified by `ls`), so the drift test's target `generated/family-1-numeric-single-field.md` (`ProseGeneratorTests.cs:43-44`) is absent. The doc:263 contract requires a committed generated table that regeneration must reproduce; there is nothing committed to drift-check against.

4. **MAJOR — False-green: the unparseable-file test passes even with no implementation.** `ProseGeneratorTests.cs:258-273` asserts only `exitCode.Should().NotBe(0)` for `Cli.Run(["render-cells", dir])` on broken JSON. Since an unrecognized subcommand already returns 2 from the usage path (`Cli.cs:27-33`), this test would pass today if the compile error were resolved without ever implementing `render-cells` — it cannot distinguish "generator rejected bad JSON" from "subcommand doesn't exist." It should assert a specific exit code distinct from the usage code, or assert the error output names the broken file (which its own name claims: "ReportsTheFileAndFails" — the file-naming half is never asserted).

5. **MEDIUM — False-green: schema-provenance claims are asserted only by substring presence.** `ProseGeneratorTests.cs:169-177` comments "The legend is read from cell.schema.json's premiseClass description — the schema stays the single owner of the class vocabulary," but the test only checks `Contain("field modifiers")` on the default render. A generator that hardcodes the legend text (a hand-maintained parallel copy of schema vocabulary — the exact catalog-rule violation class CLAUDE.md bans) passes identically. Same pattern at lines 94-105 ("the schema owns the vocabulary — the generator never rewrites it," verified only by substring). To actually pin provenance, mutate the schema description in a scratch copy and assert the rendered legend follows the mutation.

6. **MINOR — Doc drift inside the slice diff:** the `Cli` class XML summary (`Cli.cs:7-14`) enumerates subcommands `convert-cells` and `run-manifest` but omits the implemented `measure-corpus` (and will need `render-cells` when finding 2 is fixed).

**Positive notes (verified, non-trivial):** the drift test itself is well-shaped — it fails loudly when the committed file is missing (`File.Exists` assert at line 46) rather than skipping; the unknown-property test (line 182) enforces the no-silent-drop rule; line-ending normalization on both sides avoids a CRLF false-red.

VERDICT: FIX (blocking: 1, 2, 3)