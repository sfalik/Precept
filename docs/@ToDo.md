# General ToDo list
- change preview to use one window for every file -- display active, like markdown preview
- CLI (is this needed if we have MCP?)
- integration test for samples -- not hardcoded, use AI to drive CLI?  or include test plan in comments at bottom of sample?
- passing one precept as an event argument to another !!!!!!!!!!!!