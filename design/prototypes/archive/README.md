# Archived Prototypes

These artifacts were moved out of the `design/prototypes/` root to keep that folder reserved for active, top-level prototype work.

Treat the files here as preserved reference artifacts.

They are not the live design-system authority and they are not the active prototype backlog. Canonical reusable guidance stays in `design/system/`; only preserved explorations belong here.

If an archived prototype becomes active again, promote the whole coupled artifact set back into an active location deliberately rather than letting active and archived material mix in place.

If any archived prototype is moved again, keep its paired sample files together and re-check any relative links to shared assets in `design/system/`.