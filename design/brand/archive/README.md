# Brand Archive

This folder preserves superseded brand artifacts that are still worth keeping as historical references.

- `2026-04-06-brand-spec-legacy.html` is the archived pre-split brand specification that previously combined brand identity guidance with surface-level visual system detail.

The current split outputs are being derived from that archived combined spec rather than rewritten from scratch. Use the archive as historical source material only. Do not treat it as a live spec.

Current canonical sources:

- Brand identity meaning lives in `design/brand/brand-spec.html`.
- Reusable visual-system guidance lives in `design/system/foundations/semantic-visual-system.html`.

Ownership boundary:

- Peterman owns brand meaning.
- Elaine owns reusable system guidance.
- Frank arbitrates boundary questions.