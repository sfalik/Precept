# Foundations

This folder is for shared design-system guidance that multiple product surfaces inherit.

The current authority for reusable visual-system guidance is `semantic-visual-system.html`. The archived combined spec in `../../brand/archive/2026-04-06-brand-spec-legacy.html` is historical source material only.

Examples of future content:

- semantic visual foundations
- shared typography roles for product surfaces
- shared token guidance once those tokens are stabilized
- cross-surface invariants that belong outside any single surface spec

Keep this folder focused on reusable product guidance. Do not duplicate brand narrative or identity rationale from `design/brand/`.

Ownership boundary:

- Peterman owns brand meaning in `../../brand/brand-spec.html`.
- Elaine owns reusable system guidance in `semantic-visual-system.html`.
- Frank arbitrates boundary questions.